This file is a merged representation of a subset of the codebase, containing specifically included files and files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: **/*.gd, project.godot, file_list.txt
- Files matching these patterns are excluded: addons/**, .godot/**
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
autoload/ConversationEventManager.gd
autoload/dialogue_history.gd
autoload/events.gd
autoload/FallbackManager.gd
autoload/flags.gd
autoload/GameManager.gd
autoload/inventory.gd
autoload/NotificationManager.gd
autoload/settings.gd
autoload/SoundManager.gd
Boot.gd
characters/aida.gd
characters/CharacterProfile.gd
characters/dread_entity.gd
characters/hope_entity.gd
characters/mcbucket.gd
characters/npc.gd
characters/player.gd
characters/sergey.gd
characters/WaypointMovement.gd
conversation/AdvancedConversationOverlay.gd
conversation/balloon.gd
conversation/character_conversation_overlay.gd
conversation/conversationballoon.gd
conversation/MedicineCabinetZoom.gd
conversation/MemoryBoxOverlay.gd
conversation/object_zoom_overlay.gd
Cutscenes/Cutscene.gd
Cutscenes/ToiletClogCutscene.gd
data/explanation_data.gd
data/InteractionResponse.gd
data/ItemData.gd
data/JournalPageData.gd
data/MemoryChapterData.gd
data/MemoryGroupData.gd
data/VerbData.gd
Dev/alyssa_exporter.gd
Dev/MarketingMockup.gd
file_list.txt
Interactables/Game World/Hospital Bathroom/hospital_toilet.gd
Interactables/Game World/Hospital Room/medicine_dispenser.gd
Interactables/Game World/Hospital Room/tv_controller.gd
interactions/action.gd
interactions/actions/AddItemAction.gd
interactions/actions/CallMethodAction.gd
interactions/actions/DestroyAction.gd
interactions/actions/OpenMemoryBoxAction.gd
interactions/actions/OpenZoomViewAction.gd
interactions/actions/PlayerFlashAction.gd
interactions/actions/PlaySoundAction.gd
interactions/actions/RemoveItemAction.gd
interactions/actions/SayLineAction.gd
interactions/actions/SetLevelFlagAction.gd
interactions/actions/ShowCustomDialogueAction.gd
interactions/actions/ShowDialogueActiont.gd
interactions/actions/StartConversationAction.gd
interactions/actions/TeleportAction.gd
interactions/actions/TriggerCutsceneAction.gd
interactions/actions/UniversalFlashAction.gd
interactions/interactable.gd
LevelHintManager.gd
LevelStateManager.gd
Main.gd
Non Interactables/camera_2d.gd
Non Interactables/camera_region.gd
project.godot
testvideoplayer.gd
ui_interactable.gd
ui/black_screen_overlay.gd
ui/ChapterButton.gd
ui/controls_menu.gd
ui/credits_menu.gd
ui/custom_cursor_control.gd
ui/custom_cursor.gd
ui/DateInputWidget.gd
ui/dialogue_history_ui.gd
ui/difficulty_select_screen.gd
ui/DigitSpinner.gd
ui/explanation_layer.gd
ui/game_over.gd
ui/input_blocker.gd
ui/insurance_form_button_ui.gd
ui/insurance_form.gd
ui/InventoryUI.gd
ui/journal_button_ui.gd
ui/journal_overlay.gd
ui/loading_screen.gd
ui/LocationRow.gd
ui/logo_cursor.gd
ui/LogoSplash.gd
ui/MainMenu.gd
ui/Notification.gd
ui/patreon_world_button.gd
ui/pause_menu_ui.gd
ui/settings_menu.gd
ui/TransitionLayer.gd
ui/VerbUI.gd
ui/walk_indicator.gd
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="file_list.txt">
.claude/settings.local.json
.gitattributes
.gitignore
1080test1.jpg
1080test1.jpg.import
Animations/AIda/Idle/AIda_idle_0001.png
Animations/AIda/Idle/AIda_idle_0001.png.import
Animations/AIda/Idle/AIda_idle_0002.png
Animations/AIda/Idle/AIda_idle_0002.png.import
Animations/AIda/Idle/AIda_idle_0003.png
Animations/AIda/Idle/AIda_idle_0003.png.import
Animations/AIda/Idle/AIda_idle_0004.png
Animations/AIda/Idle/AIda_idle_0004.png.import
Animations/AIda/Idle/AIda_idle_0005.png
Animations/AIda/Idle/AIda_idle_0005.png.import
Animations/AIda/Idle/AIda_idle_0006.png
Animations/AIda/Idle/AIda_idle_0006.png.import
Animations/AIda/Idle/AIda_idle_0007.png
Animations/AIda/Idle/AIda_idle_0007.png.import
Animations/AIda/Idle/AIda_idle_0008.png
Animations/AIda/Idle/AIda_idle_0008.png.import
Animations/AIda/Idle/AIda_idle_0009.png
Animations/AIda/Idle/AIda_idle_0009.png.import
Animations/AIda/Idle/AIda_idle_0010.png
Animations/AIda/Idle/AIda_idle_0010.png.import
Animations/AIda/Idle/AIda_idle_0011.png
Animations/AIda/Idle/AIda_idle_0011.png.import
Animations/AIda/Idle/AIda_idle_0012.png
Animations/AIda/Idle/AIda_idle_0012.png.import
Animations/AIda/Idle/AIda_idle_0013.png
Animations/AIda/Idle/AIda_idle_0013.png.import
Animations/AIda/Idle/AIda_idle_0014.png
Animations/AIda/Idle/AIda_idle_0014.png.import
Animations/AIda/Idle/AIda_idle_0015.png
Animations/AIda/Idle/AIda_idle_0015.png.import
Animations/AIda/Idle/AIda_idle_0016.png
Animations/AIda/Idle/AIda_idle_0016.png.import
Animations/AIda/Idle/AIda_idle_0017.png
Animations/AIda/Idle/AIda_idle_0017.png.import
Animations/AIda/Idle/AIda_idle_0018.png
Animations/AIda/Idle/AIda_idle_0018.png.import
Animations/AIda/Idle/AIda_idle_0019.png
Animations/AIda/Idle/AIda_idle_0019.png.import
Animations/AIda/Idle/AIda_idle_0020.png
Animations/AIda/Idle/AIda_idle_0020.png.import
Animations/AIda/Idle/AIda_idle_0021.png
Animations/AIda/Idle/AIda_idle_0021.png.import
Animations/AIda/Idle/AIda_idle_0022.png
Animations/AIda/Idle/AIda_idle_0022.png.import
Animations/AIda/Idle/AIda_idle_0023.png
Animations/AIda/Idle/AIda_idle_0023.png.import
Animations/AIda/Idle/AIda_idle_0024.png
Animations/AIda/Idle/AIda_idle_0024.png.import
Animations/AIda/Walk/Aida_walking_0001.png
Animations/AIda/Walk/Aida_walking_0001.png.import
Animations/AIda/Walk/Aida_walking_0002.png
Animations/AIda/Walk/Aida_walking_0002.png.import
Animations/AIda/Walk/Aida_walking_0003.png
Animations/AIda/Walk/Aida_walking_0003.png.import
Animations/AIda/Walk/Aida_walking_0004.png
Animations/AIda/Walk/Aida_walking_0004.png.import
Animations/AIda/Walk/Aida_walking_0005.png
Animations/AIda/Walk/Aida_walking_0005.png.import
Animations/AIda/Walk/Aida_walking_0006.png
Animations/AIda/Walk/Aida_walking_0006.png.import
Animations/AIda/Walk/Aida_walking_0007.png
Animations/AIda/Walk/Aida_walking_0007.png.import
Animations/AIda/Walk/Aida_walking_0008.png
Animations/AIda/Walk/Aida_walking_0008.png.import
Animations/AIda/walk_new/aida_walk_0001.png
Animations/AIda/walk_new/aida_walk_0001.png.import
Animations/AIda/walk_new/aida_walk_0002.png
Animations/AIda/walk_new/aida_walk_0002.png.import
Animations/AIda/walk_new/aida_walk_0003.png
Animations/AIda/walk_new/aida_walk_0003.png.import
Animations/AIda/walk_new/aida_walk_0004.png
Animations/AIda/walk_new/aida_walk_0004.png.import
Animations/AIda/walk_new/aida_walk_0005.png
Animations/AIda/walk_new/aida_walk_0005.png.import
Animations/AIda/walk_new/aida_walk_0006.png
Animations/AIda/walk_new/aida_walk_0006.png.import
Animations/AIda/walk_new/aida_walk_0007.png
Animations/AIda/walk_new/aida_walk_0007.png.import
Animations/AIda/walk_new/aida_walk_0008.png
Animations/AIda/walk_new/aida_walk_0008.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Glow/Medicine Dispenser Cyan Glow.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Glow/Medicine Dispenser Cyan Glow.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0001.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0001.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0002.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0002.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0003.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0003.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0004.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0004.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0005.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0005.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0006.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Cyan Sprites/Medicine dispenser cyan_0006.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Glow/Medicine Dispenser Yellow Glow.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Glow/Medicine Dispenser Yellow Glow.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0001.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0001.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0002.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0002.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0003.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0003.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0004.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0004.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0005.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0005.png.import
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0006.png
Animations/Game Objects/Hospital Room/Medicine Dispenser/Base Yellow Sprites/Medicine dispenser yellow_0006.png.import
Animations/Hj frames new/Hj-frames-new_0001.png
Animations/Hj frames new/Hj-frames-new_0001.png.import
Animations/Hj frames new/Hj-frames-new_0002.png
Animations/Hj frames new/Hj-frames-new_0002.png.import
Animations/Hj frames new/Hj-frames-new_0003.png
Animations/Hj frames new/Hj-frames-new_0003.png.import
Animations/Hj frames new/Hj-frames-new_0004.png
Animations/Hj frames new/Hj-frames-new_0004.png.import
Animations/Hj frames new/Hj-frames-new_0005.png
Animations/Hj frames new/Hj-frames-new_0005.png.import
Animations/Hj frames new/Hj-frames-new_0006.png
Animations/Hj frames new/Hj-frames-new_0006.png.import
Animations/Hj frames new/Hj-frames-new_0007.png
Animations/Hj frames new/Hj-frames-new_0007.png.import
Animations/Hj frames new/Hj-frames-new_0008.png
Animations/Hj frames new/Hj-frames-new_0008.png.import
Animations/Hj frames new/Hj-frames-new_0009.png
Animations/Hj frames new/Hj-frames-new_0009.png.import
Animations/Hj frames new/Hj-frames-new_0010.png
Animations/Hj frames new/Hj-frames-new_0010.png.import
Animations/Hj frames new/Hj-frames-new_0011.png
Animations/Hj frames new/Hj-frames-new_0011.png.import
Animations/Hj frames new/Hj-frames-new_0012.png
Animations/Hj frames new/Hj-frames-new_0012.png.import
Animations/Hj frames new/Hj-frames-new_0013.png
Animations/Hj frames new/Hj-frames-new_0013.png.import
Animations/Hj frames new/Hj-frames-new_0014.png
Animations/Hj frames new/Hj-frames-new_0014.png.import
Animations/Hj frames new/Hj-frames-new_0015.png
Animations/Hj frames new/Hj-frames-new_0015.png.import
Animations/Hj frames new/Hj-frames-new_0016.png
Animations/Hj frames new/Hj-frames-new_0016.png.import
Animations/Hj frames new/Hj-frames-new_0017.png
Animations/Hj frames new/Hj-frames-new_0017.png.import
Animations/Hj frames new/Hj-frames-new_0018.png
Animations/Hj frames new/Hj-frames-new_0018.png.import
Animations/Hj frames new/Hj-frames-new_0019.png
Animations/Hj frames new/Hj-frames-new_0019.png.import
Animations/Hj frames new/Hj-frames-new_0020.png
Animations/Hj frames new/Hj-frames-new_0020.png.import
Animations/Mcbucket/High/Mcbucket_high_0001.png
Animations/Mcbucket/High/Mcbucket_high_0001.png.import
Animations/Mcbucket/High/Mcbucket_high_0002.png
Animations/Mcbucket/High/Mcbucket_high_0002.png.import
Animations/Mcbucket/High/Mcbucket_high_0003.png
Animations/Mcbucket/High/Mcbucket_high_0003.png.import
Animations/Mcbucket/High/Mcbucket_high_0004.png
Animations/Mcbucket/High/Mcbucket_high_0004.png.import
Animations/Mcbucket/High/Mcbucket_high_0005.png
Animations/Mcbucket/High/Mcbucket_high_0005.png.import
Animations/Mcbucket/High/Mcbucket_high_0006.png
Animations/Mcbucket/High/Mcbucket_high_0006.png.import
Animations/Mcbucket/High/Mcbucket_high_0007.png
Animations/Mcbucket/High/Mcbucket_high_0007.png.import
Animations/Mcbucket/High/Mcbucket_high_0008.png
Animations/Mcbucket/High/Mcbucket_high_0008.png.import
Animations/Mcbucket/High/Mcbucket_high_0009.png
Animations/Mcbucket/High/Mcbucket_high_0009.png.import
Animations/Mcbucket/High/Mcbucket_high_0010.png
Animations/Mcbucket/High/Mcbucket_high_0010.png.import
Animations/Mcbucket/High/Mcbucket_high_0011.png
Animations/Mcbucket/High/Mcbucket_high_0011.png.import
Animations/Mcbucket/High/Mcbucket_high_0012.png
Animations/Mcbucket/High/Mcbucket_high_0012.png.import
Animations/Mcbucket/High/Mcbucket_high_0013.png
Animations/Mcbucket/High/Mcbucket_high_0013.png.import
Animations/Mcbucket/High/Mcbucket_high_0014.png
Animations/Mcbucket/High/Mcbucket_high_0014.png.import
Animations/Mcbucket/High/Mcbucket_high_0015.png
Animations/Mcbucket/High/Mcbucket_high_0015.png.import
Animations/Mcbucket/High/Mcbucket_high_0016.png
Animations/Mcbucket/High/Mcbucket_high_0016.png.import
Animations/Mcbucket/High/Mcbucket_high_0017.png
Animations/Mcbucket/High/Mcbucket_high_0017.png.import
Animations/Mcbucket/High/Mcbucket_high_0018.png
Animations/Mcbucket/High/Mcbucket_high_0018.png.import
Animations/Mcbucket/High/Mcbucket_high_0019.png
Animations/Mcbucket/High/Mcbucket_high_0019.png.import
Animations/Mcbucket/High/Mcbucket_high_0020.png
Animations/Mcbucket/High/Mcbucket_high_0020.png.import
Animations/Mcbucket/High/Mcbucket_high_0021.png
Animations/Mcbucket/High/Mcbucket_high_0021.png.import
Animations/Mcbucket/High/Mcbucket_high_0022.png
Animations/Mcbucket/High/Mcbucket_high_0022.png.import
Animations/Mcbucket/High/Mcbucket_high_0023.png
Animations/Mcbucket/High/Mcbucket_high_0023.png.import
Animations/Mcbucket/High/Mcbucket_high_0024.png
Animations/Mcbucket/High/Mcbucket_high_0024.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0001.png
Animations/Mcbucket/Idle/Mcbucket_idle_0001.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0002.png
Animations/Mcbucket/Idle/Mcbucket_idle_0002.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0003.png
Animations/Mcbucket/Idle/Mcbucket_idle_0003.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0004.png
Animations/Mcbucket/Idle/Mcbucket_idle_0004.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0005.png
Animations/Mcbucket/Idle/Mcbucket_idle_0005.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0006.png
Animations/Mcbucket/Idle/Mcbucket_idle_0006.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0007.png
Animations/Mcbucket/Idle/Mcbucket_idle_0007.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0008.png
Animations/Mcbucket/Idle/Mcbucket_idle_0008.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0009.png
Animations/Mcbucket/Idle/Mcbucket_idle_0009.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0010.png
Animations/Mcbucket/Idle/Mcbucket_idle_0010.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0011.png
Animations/Mcbucket/Idle/Mcbucket_idle_0011.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0012.png
Animations/Mcbucket/Idle/Mcbucket_idle_0012.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0013.png
Animations/Mcbucket/Idle/Mcbucket_idle_0013.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0014.png
Animations/Mcbucket/Idle/Mcbucket_idle_0014.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0015.png
Animations/Mcbucket/Idle/Mcbucket_idle_0015.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0016.png
Animations/Mcbucket/Idle/Mcbucket_idle_0016.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0017.png
Animations/Mcbucket/Idle/Mcbucket_idle_0017.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0018.png
Animations/Mcbucket/Idle/Mcbucket_idle_0018.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0019.png
Animations/Mcbucket/Idle/Mcbucket_idle_0019.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0020.png
Animations/Mcbucket/Idle/Mcbucket_idle_0020.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0021.png
Animations/Mcbucket/Idle/Mcbucket_idle_0021.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0022.png
Animations/Mcbucket/Idle/Mcbucket_idle_0022.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0023.png
Animations/Mcbucket/Idle/Mcbucket_idle_0023.png.import
Animations/Mcbucket/Idle/Mcbucket_idle_0024.png
Animations/Mcbucket/Idle/Mcbucket_idle_0024.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0001.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0001.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0002.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0002.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0003.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0003.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0004.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0004.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0005.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0005.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0006.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0006.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0007.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0007.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0008.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0008.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0009.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0009.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0010.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0010.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0011.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0011.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0012.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0012.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0013.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0013.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0014.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0014.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0015.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0015.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0016.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0016.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0017.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0017.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0018.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0018.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0019.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0019.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0020.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0020.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0021.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0021.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0022.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0022.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0023.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0023.png.import
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0024.png
Animations/Mcbucket/Invigirol/Mcbucket_invigirol_0024.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0001.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0001.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0002.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0002.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0003.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0003.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0004.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0004.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0005.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0005.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0006.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0006.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0007.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0007.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0008.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0008.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0009.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0009.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0010.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0010.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0011.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0011.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0012.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0012.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0013.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0013.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0014.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0014.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0015.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0015.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0016.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0016.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0017.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0017.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0018.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0018.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0019.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0019.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0020.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0020.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0021.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0021.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0022.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0022.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0023.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0023.png.import
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0024.png
Animations/Mcbucket/Sleeping/Mcbucket_sleeping_0024.png.import
Animations/Protag/Flash/Flash_0001.png
Animations/Protag/Flash/Flash_0001.png.import
Animations/Protag/Flash/Flash_0002.png
Animations/Protag/Flash/Flash_0002.png.import
Animations/Protag/Flash/Flash_0003.png
Animations/Protag/Flash/Flash_0003.png.import
Animations/Protag/Flash/Flash_0004.png
Animations/Protag/Flash/Flash_0004.png.import
Animations/Protag/Flash/Flash_0005.png
Animations/Protag/Flash/Flash_0005.png.import
Animations/Protag/Flash/Flash_0006.png
Animations/Protag/Flash/Flash_0006.png.import
Animations/Protag/Flash/Flash_0007.png
Animations/Protag/Flash/Flash_0007.png.import
Animations/Protag/Flash/Flash_0008.png
Animations/Protag/Flash/Flash_0008.png.import
Animations/Protag/Flash/Flash_0009.png
Animations/Protag/Flash/Flash_0009.png.import
Animations/Protag/Flash/Flash_0010.png
Animations/Protag/Flash/Flash_0010.png.import
Animations/Protag/Flash/Flash_0011.png
Animations/Protag/Flash/Flash_0011.png.import
Animations/Protag/Flash/Flash_0012.png
Animations/Protag/Flash/Flash_0012.png.import
Animations/Protag/Idle New/Idle_new_resized_0001.png
Animations/Protag/Idle New/Idle_new_resized_0001.png.import
Animations/Protag/Idle New/Idle_new_resized_0002.png
Animations/Protag/Idle New/Idle_new_resized_0002.png.import
Animations/Protag/Idle New/Idle_new_resized_0003.png
Animations/Protag/Idle New/Idle_new_resized_0003.png.import
Animations/Protag/Idle New/Idle_new_resized_0004.png
Animations/Protag/Idle New/Idle_new_resized_0004.png.import
Animations/Protag/Idle New/Idle_new_resized_0005.png
Animations/Protag/Idle New/Idle_new_resized_0005.png.import
Animations/Protag/Idle New/Idle_new_resized_0006.png
Animations/Protag/Idle New/Idle_new_resized_0006.png.import
Animations/Protag/Idle New/Idle_new_resized_0007.png
Animations/Protag/Idle New/Idle_new_resized_0007.png.import
Animations/Protag/Idle New/Idle_new_resized_0008.png
Animations/Protag/Idle New/Idle_new_resized_0008.png.import
Animations/Protag/Idle New/Idle_new_resized_0009.png
Animations/Protag/Idle New/Idle_new_resized_0009.png.import
Animations/Protag/Idle New/Idle_new_resized_0010.png
Animations/Protag/Idle New/Idle_new_resized_0010.png.import
Animations/Protag/Idle New/Idle_new_resized_0011.png
Animations/Protag/Idle New/Idle_new_resized_0011.png.import
Animations/Protag/Idle New/Idle_new_resized_0012.png
Animations/Protag/Idle New/Idle_new_resized_0012.png.import
Animations/Protag/Idle New/Idle_new_resized_0013.png
Animations/Protag/Idle New/Idle_new_resized_0013.png.import
Animations/Protag/Idle New/Idle_new_resized_0014.png
Animations/Protag/Idle New/Idle_new_resized_0014.png.import
Animations/Protag/Idle New/Idle_new_resized_0015.png
Animations/Protag/Idle New/Idle_new_resized_0015.png.import
Animations/Protag/Idle New/Idle_new_resized_0016.png
Animations/Protag/Idle New/Idle_new_resized_0016.png.import
Animations/Protag/Idle New/Idle_new_resized_0017.png
Animations/Protag/Idle New/Idle_new_resized_0017.png.import
Animations/Protag/Idle New/Idle_new_resized_0018.png
Animations/Protag/Idle New/Idle_new_resized_0018.png.import
Animations/Protag/Idle New/Idle_new_resized_0019.png
Animations/Protag/Idle New/Idle_new_resized_0019.png.import
Animations/Protag/Idle New/Idle_new_resized_0020.png
Animations/Protag/Idle New/Idle_new_resized_0020.png.import
Animations/Protag/Idle New/Idle_new_resized_0021.png
Animations/Protag/Idle New/Idle_new_resized_0021.png.import
Animations/Protag/Idle New/Idle_new_resized_0022.png
Animations/Protag/Idle New/Idle_new_resized_0022.png.import
Animations/Protag/Idle New/Idle_new_resized_0023.png
Animations/Protag/Idle New/Idle_new_resized_0023.png.import
Animations/Protag/Idle New/Idle_new_resized_0024.png
Animations/Protag/Idle New/Idle_new_resized_0024.png.import
Animations/Protag/Idle/Protag_idle_0001.png
Animations/Protag/Idle/Protag_idle_0001.png.import
Animations/Protag/Idle/Protag_idle_0002.png
Animations/Protag/Idle/Protag_idle_0002.png.import
Animations/Protag/Idle/Protag_idle_0003.png
Animations/Protag/Idle/Protag_idle_0003.png.import
Animations/Protag/Idle/Protag_idle_0004.png
Animations/Protag/Idle/Protag_idle_0004.png.import
Animations/Protag/Idle/Protag_idle_0005.png
Animations/Protag/Idle/Protag_idle_0005.png.import
Animations/Protag/Idle/Protag_idle_0006.png
Animations/Protag/Idle/Protag_idle_0006.png.import
Animations/Protag/Idle/Protag_idle_0007.png
Animations/Protag/Idle/Protag_idle_0007.png.import
Animations/Protag/Idle/Protag_idle_0008.png
Animations/Protag/Idle/Protag_idle_0008.png.import
Animations/Protag/Idle/Protag_idle_0009.png
Animations/Protag/Idle/Protag_idle_0009.png.import
Animations/Protag/Idle/Protag_idle_0010.png
Animations/Protag/Idle/Protag_idle_0010.png.import
Animations/Protag/Idle/Protag_idle_0011.png
Animations/Protag/Idle/Protag_idle_0011.png.import
Animations/Protag/Idle/Protag_idle_0012.png
Animations/Protag/Idle/Protag_idle_0012.png.import
Animations/Protag/Idle/Protag_idle_0013.png
Animations/Protag/Idle/Protag_idle_0013.png.import
Animations/Protag/Idle/Protag_idle_0014.png
Animations/Protag/Idle/Protag_idle_0014.png.import
Animations/Protag/Idle/Protag_idle_0015.png
Animations/Protag/Idle/Protag_idle_0015.png.import
Animations/Protag/Idle/Protag_idle_0016.png
Animations/Protag/Idle/Protag_idle_0016.png.import
Animations/Protag/Idle/Protag_idle_0017.png
Animations/Protag/Idle/Protag_idle_0017.png.import
Animations/Protag/Idle/Protag_idle_0018.png
Animations/Protag/Idle/Protag_idle_0018.png.import
Animations/Protag/Idle/Protag_idle_0019.png
Animations/Protag/Idle/Protag_idle_0019.png.import
Animations/Protag/Idle/Protag_idle_0020.png
Animations/Protag/Idle/Protag_idle_0020.png.import
Animations/Protag/Idle/Protag_idle_0021.png
Animations/Protag/Idle/Protag_idle_0021.png.import
Animations/Protag/Idle/Protag_idle_0022.png
Animations/Protag/Idle/Protag_idle_0022.png.import
Animations/Protag/Idle/Protag_idle_0023.png
Animations/Protag/Idle/Protag_idle_0023.png.import
Animations/Protag/Idle/Protag_idle_0024.png
Animations/Protag/Idle/Protag_idle_0024.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0001.png
Animations/Protag/Walk New Resized/walk_new_resized_0001.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0002.png
Animations/Protag/Walk New Resized/walk_new_resized_0002.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0003.png
Animations/Protag/Walk New Resized/walk_new_resized_0003.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0004.png
Animations/Protag/Walk New Resized/walk_new_resized_0004.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0005.png
Animations/Protag/Walk New Resized/walk_new_resized_0005.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0006.png
Animations/Protag/Walk New Resized/walk_new_resized_0006.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0007.png
Animations/Protag/Walk New Resized/walk_new_resized_0007.png.import
Animations/Protag/Walk New Resized/walk_new_resized_0008.png
Animations/Protag/Walk New Resized/walk_new_resized_0008.png.import
Animations/Protag/Walk/Protag_walk_0001.png
Animations/Protag/Walk/Protag_walk_0001.png.import
Animations/Protag/Walk/Protag_walk_0002.png
Animations/Protag/Walk/Protag_walk_0002.png.import
Animations/Protag/Walk/Protag_walk_0003.png
Animations/Protag/Walk/Protag_walk_0003.png.import
Animations/Protag/Walk/Protag_walk_0004.png
Animations/Protag/Walk/Protag_walk_0004.png.import
Animations/Protag/Walk/Protag_walk_0005.png
Animations/Protag/Walk/Protag_walk_0005.png.import
Animations/Protag/Walk/Protag_walk_0006.png
Animations/Protag/Walk/Protag_walk_0006.png.import
Animations/Protag/Walk/Protag_walk_0007.png
Animations/Protag/Walk/Protag_walk_0007.png.import
Animations/Protag/Walk/Protag_walk_0008.png
Animations/Protag/Walk/Protag_walk_0008.png.import
Animations/Protag/idle new resized/idle_new_resized_0001.png
Animations/Protag/idle new resized/idle_new_resized_0001.png.import
Animations/Protag/idle new resized/idle_new_resized_0002.png
Animations/Protag/idle new resized/idle_new_resized_0002.png.import
Animations/Protag/idle new resized/idle_new_resized_0003.png
Animations/Protag/idle new resized/idle_new_resized_0003.png.import
Animations/Protag/idle new resized/idle_new_resized_0004.png
Animations/Protag/idle new resized/idle_new_resized_0004.png.import
Animations/Protag/idle new resized/idle_new_resized_0005.png
Animations/Protag/idle new resized/idle_new_resized_0005.png.import
Animations/Protag/idle new resized/idle_new_resized_0006.png
Animations/Protag/idle new resized/idle_new_resized_0006.png.import
Animations/Protag/idle new resized/idle_new_resized_0007.png
Animations/Protag/idle new resized/idle_new_resized_0007.png.import
Animations/Protag/idle new resized/idle_new_resized_0008.png
Animations/Protag/idle new resized/idle_new_resized_0008.png.import
Animations/Protag/idle new resized/idle_new_resized_0009.png
Animations/Protag/idle new resized/idle_new_resized_0009.png.import
Animations/Protag/idle new resized/idle_new_resized_0010.png
Animations/Protag/idle new resized/idle_new_resized_0010.png.import
Animations/Protag/idle new resized/idle_new_resized_0011.png
Animations/Protag/idle new resized/idle_new_resized_0011.png.import
Animations/Protag/idle new resized/idle_new_resized_0012.png
Animations/Protag/idle new resized/idle_new_resized_0012.png.import
Animations/Protag/idle new resized/idle_new_resized_0013.png
Animations/Protag/idle new resized/idle_new_resized_0013.png.import
Animations/Protag/idle new resized/idle_new_resized_0014.png
Animations/Protag/idle new resized/idle_new_resized_0014.png.import
Animations/Protag/idle new resized/idle_new_resized_0015.png
Animations/Protag/idle new resized/idle_new_resized_0015.png.import
Animations/Protag/idle new resized/idle_new_resized_0016.png
Animations/Protag/idle new resized/idle_new_resized_0016.png.import
Animations/Protag/idle new resized/idle_new_resized_0017.png
Animations/Protag/idle new resized/idle_new_resized_0017.png.import
Animations/Protag/idle new resized/idle_new_resized_0018.png
Animations/Protag/idle new resized/idle_new_resized_0018.png.import
Animations/Protag/idle new resized/idle_new_resized_0019.png
Animations/Protag/idle new resized/idle_new_resized_0019.png.import
Animations/Protag/idle new resized/idle_new_resized_0020.png
Animations/Protag/idle new resized/idle_new_resized_0020.png.import
Animations/Protag/idle new resized/idle_new_resized_0021.png
Animations/Protag/idle new resized/idle_new_resized_0021.png.import
Animations/Protag/idle new resized/idle_new_resized_0022.png
Animations/Protag/idle new resized/idle_new_resized_0022.png.import
Animations/Protag/idle new resized/idle_new_resized_0023.png
Animations/Protag/idle new resized/idle_new_resized_0023.png.import
Animations/Protag/idle new resized/idle_new_resized_0024.png
Animations/Protag/idle new resized/idle_new_resized_0024.png.import
Animations/Protag/walk_new/protag_walk_0001.png
Animations/Protag/walk_new/protag_walk_0001.png.import
Animations/Protag/walk_new/protag_walk_0002.png
Animations/Protag/walk_new/protag_walk_0002.png.import
Animations/Protag/walk_new/protag_walk_0003.png
Animations/Protag/walk_new/protag_walk_0003.png.import
Animations/Protag/walk_new/protag_walk_0004.png
Animations/Protag/walk_new/protag_walk_0004.png.import
Animations/Protag/walk_new/protag_walk_0005.png
Animations/Protag/walk_new/protag_walk_0005.png.import
Animations/Protag/walk_new/protag_walk_0006.png
Animations/Protag/walk_new/protag_walk_0006.png.import
Animations/Protag/walk_new/protag_walk_0007.png
Animations/Protag/walk_new/protag_walk_0007.png.import
Animations/Protag/walk_new/protag_walk_0008.png
Animations/Protag/walk_new/protag_walk_0008.png.import
Animations/Sergey/Idle/Sergey_idle_0001.png
Animations/Sergey/Idle/Sergey_idle_0001.png.import
Animations/Sergey/Idle/Sergey_idle_0002.png
Animations/Sergey/Idle/Sergey_idle_0002.png.import
Animations/Sergey/Idle/Sergey_idle_0003.png
Animations/Sergey/Idle/Sergey_idle_0003.png.import
Animations/Sergey/Idle/Sergey_idle_0004.png
Animations/Sergey/Idle/Sergey_idle_0004.png.import
Animations/Sergey/Idle/Sergey_idle_0005.png
Animations/Sergey/Idle/Sergey_idle_0005.png.import
Animations/Sergey/Idle/Sergey_idle_0006.png
Animations/Sergey/Idle/Sergey_idle_0006.png.import
Animations/Sergey/Idle/Sergey_idle_0007.png
Animations/Sergey/Idle/Sergey_idle_0007.png.import
Animations/Sergey/Idle/Sergey_idle_0008.png
Animations/Sergey/Idle/Sergey_idle_0008.png.import
Animations/Sergey/Idle/Sergey_idle_0009.png
Animations/Sergey/Idle/Sergey_idle_0009.png.import
Animations/Sergey/Idle/Sergey_idle_0010.png
Animations/Sergey/Idle/Sergey_idle_0010.png.import
Animations/Sergey/Idle/Sergey_idle_0011.png
Animations/Sergey/Idle/Sergey_idle_0011.png.import
Animations/Sergey/Idle/Sergey_idle_0012.png
Animations/Sergey/Idle/Sergey_idle_0012.png.import
Animations/Sergey/Idle/Sergey_idle_0013.png
Animations/Sergey/Idle/Sergey_idle_0013.png.import
Animations/Sergey/Idle/Sergey_idle_0014.png
Animations/Sergey/Idle/Sergey_idle_0014.png.import
Animations/Sergey/Idle/Sergey_idle_0015.png
Animations/Sergey/Idle/Sergey_idle_0015.png.import
Animations/Sergey/Idle/Sergey_idle_0016.png
Animations/Sergey/Idle/Sergey_idle_0016.png.import
Animations/Sergey/Idle/Sergey_idle_0017.png
Animations/Sergey/Idle/Sergey_idle_0017.png.import
Animations/Sergey/Idle/Sergey_idle_0018.png
Animations/Sergey/Idle/Sergey_idle_0018.png.import
Animations/Sergey/Idle/Sergey_idle_0019.png
Animations/Sergey/Idle/Sergey_idle_0019.png.import
Animations/Sergey/Idle/Sergey_idle_0020.png
Animations/Sergey/Idle/Sergey_idle_0020.png.import
Animations/Sergey/Idle/Sergey_idle_0021.png
Animations/Sergey/Idle/Sergey_idle_0021.png.import
Animations/Sergey/Idle/Sergey_idle_0022.png
Animations/Sergey/Idle/Sergey_idle_0022.png.import
Animations/Sergey/Idle/Sergey_idle_0023.png
Animations/Sergey/Idle/Sergey_idle_0023.png.import
Animations/Sergey/Idle/Sergey_idle_0024.png
Animations/Sergey/Idle/Sergey_idle_0024.png.import
App Icons/IIRC.icns
App Icons/IIRC.ico
App Icons/android 192x192.png
App Icons/android 192x192.png.import
App Icons/android 432x432.png
App Icons/android 432x432.png.import
App Icons/android black background 432x432.png
App Icons/android black background 432x432.png.import
Backgrounds/Aida/aida_hospital_base.png
Backgrounds/Aida/aida_hospital_base.png.import
Backgrounds/Aida/aida_hospital_concerned_thinking.png
Backgrounds/Aida/aida_hospital_concerned_thinking.png.import
Backgrounds/Aida/aida_hospital_explaining_confident.png
Backgrounds/Aida/aida_hospital_explaining_confident.png.import
Backgrounds/Aida/aida_hospital_happy.png
Backgrounds/Aida/aida_hospital_happy.png.import
Backgrounds/Aida/aida_hospital_surprised.png
Backgrounds/Aida/aida_hospital_surprised.png.import
Backgrounds/Aida/aida_hospital_thinking.png
Backgrounds/Aida/aida_hospital_thinking.png.import
Backgrounds/CGs/memory_box_cg.png
Backgrounds/CGs/memory_box_cg.png.import
Backgrounds/Levels/aida_hospital_background.png
Backgrounds/Levels/aida_hospital_background.png.import
Backgrounds/Levels/layla shower bg.png
Backgrounds/Levels/layla shower bg.png.import
Backgrounds/Levels/sergey_background.png
Backgrounds/Levels/sergey_background.png.import
Backgrounds/Levels/static_hospital_background.PNG
Backgrounds/Levels/static_hospital_background.PNG.import
Backgrounds/Levels/static_hospital_background_ventless.png
Backgrounds/Levels/static_hospital_background_ventless.png.import
Backgrounds/Levels/static_hospital_bathroom_background.PNG
Backgrounds/Levels/static_hospital_bathroom_background.PNG.import
Backgrounds/Main Menu Background/main_menu_background_no_logo.png
Backgrounds/Main Menu Background/main_menu_background_no_logo.png.import
Backgrounds/McBucket/default/mcbucket_normal_base.png
Backgrounds/McBucket/default/mcbucket_normal_base.png.import
Backgrounds/McBucket/default/mcbucket_normal_ree.png
Backgrounds/McBucket/default/mcbucket_normal_ree.png.import
Backgrounds/McBucket/high/mcbucket_high_base.png
Backgrounds/McBucket/high/mcbucket_high_base.png.import
Backgrounds/McBucket/high/mcbucket_high_base_mouth_open.PNG
Backgrounds/McBucket/high/mcbucket_high_base_mouth_open.PNG.import
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_closed_mouth_open.png
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_closed_mouth_open.png.import
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_open_mouth_open (2).PNG
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_open_mouth_open (2).PNG.import
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_open_mouth_open.PNG
Backgrounds/McBucket/high/mcbucket_high_explaining_eyes_open_mouth_open.PNG.import
Backgrounds/McBucket/high/mcbucket_high_hand_up.png
Backgrounds/McBucket/high/mcbucket_high_hand_up.png.import
Backgrounds/McBucket/high/mcbucket_high_happy.png
Backgrounds/McBucket/high/mcbucket_high_happy.png.import
Backgrounds/McBucket/invigirol/mcbucket_ivigirol_base.png
Backgrounds/McBucket/invigirol/mcbucket_ivigirol_base.png.import
Backgrounds/Protagonist/protag_hospital_base_1.png
Backgrounds/Protagonist/protag_hospital_base_1.png.import
Backgrounds/Protagonist/protag_hospital_base_aida_green_eyes.png
Backgrounds/Protagonist/protag_hospital_base_aida_green_eyes.png.import
Backgrounds/Protagonist/protag_hospital_base_aida_red_eyes.png
Backgrounds/Protagonist/protag_hospital_base_aida_red_eyes.png.import
Backgrounds/Protagonist/protag_hospital_base_aida_red_eyes_gloves.png
Backgrounds/Protagonist/protag_hospital_base_aida_red_eyes_gloves.png.import
Backgrounds/Protagonist/xSergei/hj_climax_image.PNG
Backgrounds/Protagonist/xSergei/hj_climax_image.PNG.import
Backgrounds/Protagonist/xSergei/protag_brushing_hair_on_bed.PNG
Backgrounds/Protagonist/xSergei/protag_brushing_hair_on_bed.PNG.import
Backgrounds/Protagonist/xSergei/protag_closeup_curious.png
Backgrounds/Protagonist/xSergei/protag_closeup_curious.png.import
Backgrounds/Protagonist/xSergei/protag_closeup_domme.png
Backgrounds/Protagonist/xSergei/protag_closeup_domme.png.import
Backgrounds/Protagonist/xSergei/protag_closeup_speaking.png
Backgrounds/Protagonist/xSergei/protag_closeup_speaking.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_gown only.png
Backgrounds/Protagonist/xSergei/protag_sergey_gown only.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_gown_hand_slipped_in.png
Backgrounds/Protagonist/xSergei/protag_sergey_gown_hand_slipped_in.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_one.png
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_one.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_three.png
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_three.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_two.png
Backgrounds/Protagonist/xSergei/protag_sergey_hand_being_moved_two.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_hand_hovering.png
Backgrounds/Protagonist/xSergei/protag_sergey_hand_hovering.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_inhand.png
Backgrounds/Protagonist/xSergei/protag_sergey_inhand.png.import
Backgrounds/Protagonist/xSergei/protag_sergey_knees_touching.png
Backgrounds/Protagonist/xSergei/protag_sergey_knees_touching.png.import
Backgrounds/Protagonist/xSergei/protag_top_off.png
Backgrounds/Protagonist/xSergei/protag_top_off.png.import
Backgrounds/Protagonist/xSergei/protag_touching_sergey.png
Backgrounds/Protagonist/xSergei/protag_touching_sergey.png.import
Backgrounds/Protagonist/xSergei/sergey_handjob_frame_one.png
Backgrounds/Protagonist/xSergei/sergey_handjob_frame_one.png.import
Backgrounds/Protagonist/xSergei/sergey_part_reveal.PNG
Backgrounds/Protagonist/xSergei/sergey_part_reveal.PNG.import
Backgrounds/Sergey/sergei_closeup_laughing.png
Backgrounds/Sergey/sergei_closeup_laughing.png.import
Backgrounds/Sergey/sergei_closeup_lookaway_sad_mouth_open.png
Backgrounds/Sergey/sergei_closeup_lookaway_sad_mouth_open.png.import
Backgrounds/Sergey/sergei_closeup_neutral_speaking.png
Backgrounds/Sergey/sergei_closeup_neutral_speaking.png.import
Backgrounds/Sergey/sergei_closeup_surprised_blush.png
Backgrounds/Sergey/sergei_closeup_surprised_blush.png.import
Backgrounds/Sergey/sergei_closeup_surprised_no_blush.png
Backgrounds/Sergey/sergei_closeup_surprised_no_blush.png.import
Backgrounds/Sergey/sergei_front_confused.png
Backgrounds/Sergey/sergei_front_confused.png.import
Backgrounds/Sergey/sergey_closeup_hand_behind_head.png
Backgrounds/Sergey/sergey_closeup_hand_behind_head.png.import
Backgrounds/Sergey/sergey_closeup_lookaway_sad.png
Backgrounds/Sergey/sergey_closeup_lookaway_sad.png.import
Backgrounds/Sergey/sergey_closeup_surprised.png
Backgrounds/Sergey/sergey_closeup_surprised.png.import
Backgrounds/Sergey/sergey_eyes_closed.png
Backgrounds/Sergey/sergey_eyes_closed.png.import
Backgrounds/Sergey/sergey_eyes_only_eye_to_eye.png
Backgrounds/Sergey/sergey_eyes_only_eye_to_eye.png.import
Backgrounds/Sergey/sergey_eyes_only_eye_to_eye_intense.png
Backgrounds/Sergey/sergey_eyes_only_eye_to_eye_intense.png.import
Backgrounds/Sergey/sergey_eyes_only_looking_away.png
Backgrounds/Sergey/sergey_eyes_only_looking_away.png.import
Backgrounds/Sergey/sergey_eyes_only_pleasure.png
Backgrounds/Sergey/sergey_eyes_only_pleasure.png.import
Backgrounds/Sergey/sergey_flat_and_drained.png
Backgrounds/Sergey/sergey_flat_and_drained.png.import
Backgrounds/Sergey/sergey_flat_and_embarrassed.png
Backgrounds/Sergey/sergey_flat_and_embarrassed.png.import
Backgrounds/Sergey/sergey_front_hand_behind_head.png
Backgrounds/Sergey/sergey_front_hand_behind_head.png.import
Backgrounds/Sergey/sergey_front_neutral_mouth_closed.png
Backgrounds/Sergey/sergey_front_neutral_mouth_closed.png.import
Backgrounds/Sergey/sergey_front_neutral_mouth_open.png
Backgrounds/Sergey/sergey_front_neutral_mouth_open.png.import
Backgrounds/Sergey/sergey_frown.png
Backgrounds/Sergey/sergey_frown.png.import
Backgrounds/Sergey/sergey_frown_look_away.png
Backgrounds/Sergey/sergey_frown_look_away.png.import
Backgrounds/Sergey/sergey_hand_behind_head.png
Backgrounds/Sergey/sergey_hand_behind_head.png.import
Backgrounds/Sergey/sergey_hand_up.png
Backgrounds/Sergey/sergey_hand_up.png.import
Backgrounds/Sergey/sergey_neutral.png
Backgrounds/Sergey/sergey_neutral.png.import
Backgrounds/Sergey/sergey_neutral_mouth_open.png
Backgrounds/Sergey/sergey_neutral_mouth_open.png.import
Backgrounds/Sergey/sergey_shutted_up.PNG
Backgrounds/Sergey/sergey_shutted_up.PNG.import
Backgrounds/Sergey/sergey_techpass_inhand.png
Backgrounds/Sergey/sergey_techpass_inhand.png.import
Backgrounds/dread_crying_face.png
Backgrounds/dread_crying_face.png.import
Backgrounds/dread_evil_face.png
Backgrounds/dread_evil_face.png.import
Backgrounds/intro_silhouette.png
Backgrounds/intro_silhouette.png.import
Boot.gd
Boot.gd.uid
Boot.tscn
CherryBomb-Regular.ttf
CherryBomb-Regular.ttf.import
CourierPrime-Regular.ttf
CourierPrime-Regular.ttf.import
Cutscenes/Cutscene.gd
Cutscenes/Cutscene.gd.uid
Cutscenes/ToiletClogCutscene.gd
Cutscenes/ToiletClogCutscene.gd.uid
Dev/MarketingMockup.gd
Dev/MarketingMockup.gd.uid
Dev/MarketingMockup.tscn
Dev/alyssa_exporter.gd
Dev/alyssa_exporter.gd.uid
Dev/alyssa_exporter.tscn
Dev/damp_skin.gdshader
Dev/damp_skin.gdshader.uid
Dev/glass_water_drops.gdshader
Dev/glass_water_drops.gdshader.uid
Dev/sexy_scene_mockup.tscn
Dev/shower_stream.gdshader
Dev/shower_stream.gdshader.uid
Dev/steam_fog.gdshader
Dev/steam_fog.gdshader.uid
Dev/water_drip.gdshader
Dev/water_drip.gdshader.uid
Draft walk/Draft walk_0000.png
Draft walk/Draft walk_0000.png.import
Draft walk/Draft walk_0001.png
Draft walk/Draft walk_0001.png.import
Draft walk/Draft walk_0002.png
Draft walk/Draft walk_0002.png.import
Draft walk/Draft walk_0003.png
Draft walk/Draft walk_0003.png.import
Draft walk/Draft walk_0004.png
Draft walk/Draft walk_0004.png.import
Draft walk/Draft walk_0005.png
Draft walk/Draft walk_0005.png.import
Draft walk/Draft walk_0006.png
Draft walk/Draft walk_0006.png.import
Draft walk/Draft walk_0007.png
Draft walk/Draft walk_0007.png.import
Draft walk/Draft walk_0008.png
Draft walk/Draft walk_0008.png.import
Draft walk/Draft walk_0009.png
Draft walk/Draft walk_0009.png.import
Fonts/IBMPlexSans-SemiBold.ttf
Fonts/IBMPlexSans-SemiBold.ttf.import
Fonts/Lato-Black.ttf
Fonts/Lato-Black.ttf.import
Fonts/Lato-Bold.ttf
Fonts/Lato-Bold.ttf.import
Fonts/Lato-Italic.ttf
Fonts/Lato-Italic.ttf.import
Fonts/Lato-Regular.ttf
Fonts/Lato-Regular.ttf.import
Fonts/Orbitron-SemiBold.ttf
Fonts/Orbitron-SemiBold.ttf.import
Fonts/PlayfairDisplay-Italic.ttf
Fonts/PlayfairDisplay-Italic.ttf.import
Fonts/PlayfairDisplay-Regular.ttf
Fonts/PlayfairDisplay-Regular.ttf.import
Fonts/PlayfairDisplay-SemiBold.ttf
Fonts/PlayfairDisplay-SemiBold.ttf.import
Fonts/PlayfairDisplay-SemiBoldItalic.ttf
Fonts/PlayfairDisplay-SemiBoldItalic.ttf.import
Fonts/RubikGlitch-Regular.ttf
Fonts/RubikGlitch-Regular.ttf.import
Fonts/VarelaRound-Regular.ttf
Fonts/VarelaRound-Regular.ttf.import
Hj animation.webm
IIRC_Master_Dialogue_Document.md
IIRC_Master_Dialogue_Document.pdf
Icons/back.png
Icons/back.png.import
Icons/cancel.png
Icons/cancel.png.import
Icons/dialogue_icon.png
Icons/dialogue_icon.png.import
Icons/insurance_form_icon.png
Icons/insurance_form_icon.png.import
Icons/journal_icon.png
Icons/journal_icon.png.import
Icons/leave.png
Icons/leave.png.import
Icons/logo_glass_icon.png
Icons/logo_glass_icon.png.import
Icons/magnifying-glass.png
Icons/magnifying-glass.png.import
Icons/patreon_logo.png
Icons/patreon_logo.png.import
Icons/proceed.png
Icons/proceed.png.import
Icons/title_glitch.gdshader
Icons/title_glitch.gdshader.uid
Icons/up_arrow.png
Icons/up_arrow.png.import
Interactables/Game World/Hospital Bathroom/hospital_bathroom_mirror.tscn
Interactables/Game World/Hospital Bathroom/hospital_bathroom_toilet_flush.tscn
Interactables/Game World/Hospital Bathroom/hospital_bathroom_toilet_paper.tscn
Interactables/Game World/Hospital Bathroom/hospital_toilet.gd
Interactables/Game World/Hospital Bathroom/hospital_toilet.gd.uid
Interactables/Game World/Hospital Bathroom/hospital_toilet.tscn
Interactables/Game World/Hospital Room/Invigirol.tscn
Interactables/Game World/Hospital Room/PulseOverlay.gdshader
Interactables/Game World/Hospital Room/PulseOverlay.gdshader.uid
Interactables/Game World/Hospital Room/Zanopram.tscn
Interactables/Game World/Hospital Room/exit_door.tscn
Interactables/Game World/Hospital Room/mcbucket_med_dispenser.tscn
Interactables/Game World/Hospital Room/medicine_cabinet.tscn
Interactables/Game World/Hospital Room/medicine_dispenser.gd
Interactables/Game World/Hospital Room/medicine_dispenser.gd.uid
Interactables/Game World/Hospital Room/medicine_dispenser.tscn
Interactables/Game World/Hospital Room/tv.tscn
Interactables/Game World/Hospital Room/tv_controller.gd
Interactables/Game World/Hospital Room/tv_controller.gd.uid
Journal Notes/page_one.png
Journal Notes/page_one.png.import
Journal Notes/page_one.tres
Journal Notes/page_two.png
Journal Notes/page_two.png.import
Journal Notes/page_two.tres
LevelHintManager.gd
LevelHintManager.gd.uid
LevelStateManager.gd
LevelStateManager.gd.uid
Main.gd
Main.gd.uid
Mcbucket_idle_0019.png
Mcbucket_idle_0019.png.import
Medicine Cabinet.png
Medicine Cabinet.png.import
Memory Box Chapter Images/Chapters/Chapter 1 - University.png
Memory Box Chapter Images/Chapters/Chapter 1 - University.png.import
Memory Box Chapter Images/Locations/University.png
Memory Box Chapter Images/Locations/University.png.import
Non Interactables/camera_2d.gd
Non Interactables/camera_2d.gd.uid
Non Interactables/camera_region.gd
Non Interactables/camera_region.gd.uid
Non Interactables/hospital_bathroom_sink.tscn
Non Interactables/hospital_scrolling_wall_background.tscn
Non Interactables/static_bathroom_background.tscn
Non Interactables/static_wall_background.tscn
Non Interactables/static_wall_background_ventless.tscn
RobotoMono-VariableFont_wght.ttf
RobotoMono-VariableFont_wght.ttf.import
Sfx/Dialog/Dialog_sfx.wav
Sfx/Dialog/Dialog_sfx.wav.import
Sfx/Dialog/heavy_breathing.mp3
Sfx/Dialog/heavy_breathing.mp3.import
Sfx/Dialog/mcbucket_scream.wav
Sfx/Dialog/mcbucket_scream.wav.import
Sfx/Dialog/scan_effect.mp3
Sfx/Dialog/scan_effect.mp3.import
Sfx/Dialog/swish_effect.wav
Sfx/Dialog/swish_effect.wav.import
Sfx/Game World/ambience_distant_highway.mp3
Sfx/Game World/ambience_distant_highway.mp3.import
Sfx/Game World/ambience_fluoroscent_buzz.mp3
Sfx/Game World/ambience_fluoroscent_buzz.mp3.import
Sfx/Game World/ambience_spaceship.mp3
Sfx/Game World/ambience_spaceship.mp3.import
Sfx/Game World/bathroom_music.mp3
Sfx/Game World/bathroom_music.mp3.import
Sfx/Game World/hospital_toilet_flush.mp3
Sfx/Game World/hospital_toilet_flush.mp3.import
Sfx/Game World/monitor_pulse.mp3
Sfx/Game World/monitor_pulse.mp3.import
Sfx/Game World/notification_ping.wav
Sfx/Game World/notification_ping.wav.import
Sfx/Game World/oxygen_bubbles.mp3
Sfx/Game World/oxygen_bubbles.mp3.import
Sfx/Game World/scifi door close.mp3
Sfx/Game World/scifi door close.mp3.import
Sfx/Game World/scifi door open.mp3
Sfx/Game World/scifi door open.mp3.import
Sfx/Game World/ui_click.wav
Sfx/Game World/ui_click.wav.import
Sfx/Music/8. comeaux.mp3
Sfx/Music/8. comeaux.mp3.import
Sfx/Music/8. comeaux.wav
Sfx/Music/8. comeaux.wav.import
Sfx/Music/Unnatural City.ogg
Sfx/Music/Unnatural City.ogg.import
Sfx/Music/aida_corporate_theme.mp3
Sfx/Music/aida_corporate_theme.mp3.import
Sfx/Music/mcbucket_cannathink_theme.mp3
Sfx/Music/mcbucket_cannathink_theme.mp3.import
Sfx/Music/mcbucket_regular_theme.mp3
Sfx/Music/mcbucket_regular_theme.mp3.import
Sfx/Music/sergey_sad_theme.mp3
Sfx/Music/sergey_sad_theme.mp3.import
Sfx/Music/snake_city_run_boy_run.mp3
Sfx/Music/snake_city_run_boy_run.mp3.import
Sfx/Music/wii menu.mp3
Sfx/Music/wii menu.mp3.import
Sfx/Player/squeak_01.wav
Sfx/Player/squeak_01.wav.import
Sfx/Player/squeak_02.wav
Sfx/Player/squeak_02.wav.import
Sfx/Player/squeak_03.wav
Sfx/Player/squeak_03.wav.import
Sfx/form_correct_input.mp3
Sfx/form_correct_input.mp3.import
Sfx/form_incorrect_input.mp3
Sfx/form_incorrect_input.mp3.import
Sfx/start.wav
Sfx/start.wav.import
Shaders/grayscale_material.gdshader
Shaders/grayscale_material.gdshader.uid
Shaders/grayscale_material.tres
Shaders/white_icon_shader.gdshader
Shaders/white_icon_shader.gdshader.uid
Sprites/Cabinet Medicines/Cannathink.png
Sprites/Cabinet Medicines/Cannathink.png.import
Sprites/Cabinet Medicines/Cannathink_Mask.png
Sprites/Cabinet Medicines/Cannathink_Mask.png.import
Sprites/Cabinet Medicines/Invigirol.png
Sprites/Cabinet Medicines/Invigirol.png.import
Sprites/Cabinet Medicines/Invigirol_Mask.png
Sprites/Cabinet Medicines/Invigirol_Mask.png.import
Sprites/Cabinet Medicines/Zanopram.png
Sprites/Cabinet Medicines/Zanopram.png.import
Sprites/Cabinet Medicines/Zanopram_Mask.png
Sprites/Cabinet Medicines/Zanopram_Mask.png.import
Sprites/Hospital Bathroom/hospital_bathroom_commode.PNG
Sprites/Hospital Bathroom/hospital_bathroom_commode.PNG.import
Sprites/Hospital Bathroom/hospital_bathroom_commode_error.png
Sprites/Hospital Bathroom/hospital_bathroom_commode_error.png.import
Sprites/Hospital Bathroom/hospital_bathroom_commode_error_glow.png
Sprites/Hospital Bathroom/hospital_bathroom_commode_error_glow.png.import
Sprites/Hospital Bathroom/hospital_bathroom_commode_glow.PNG
Sprites/Hospital Bathroom/hospital_bathroom_commode_glow.PNG.import
Sprites/Hospital Bathroom/hospital_bathroom_door.PNG
Sprites/Hospital Bathroom/hospital_bathroom_door.PNG.import
Sprites/Hospital Bathroom/hospital_bathroom_mirror.png
Sprites/Hospital Bathroom/hospital_bathroom_mirror.png.import
Sprites/Hospital Bathroom/hospital_bathroom_sink.png
Sprites/Hospital Bathroom/hospital_bathroom_sink.png.import
Sprites/Hospital Bathroom/hospital_bathroom_sink_glow.png
Sprites/Hospital Bathroom/hospital_bathroom_sink_glow.png.import
Sprites/Hospital Bathroom/hospital_bathroom_toilet_flush.png
Sprites/Hospital Bathroom/hospital_bathroom_toilet_flush.png.import
Sprites/Hospital Bathroom/hospital_bathroom_toilet_paper.PNG
Sprites/Hospital Bathroom/hospital_bathroom_toilet_paper.PNG.import
Sprites/Hospital Room/Medicine dispenser yellow_0001.png
Sprites/Hospital Room/Medicine dispenser yellow_0001.png.import
Sprites/Hospital Room/hospital_room_door.png
Sprites/Hospital Room/hospital_room_door.png.import
Sprites/Hospital Room/mcbucket_bandaid.png
Sprites/Hospital Room/mcbucket_bandaid.png.import
Sprites/Hospital Room/medicine_cabinet.png
Sprites/Hospital Room/medicine_cabinet.png.import
Sprites/Hospital Room/medicine_cabinet_glow.png
Sprites/Hospital Room/medicine_cabinet_glow.png.import
Sprites/Hospital Room/memory_box.png
Sprites/Hospital Room/memory_box.png.import
Sprites/Hospital Room/washroom_sign.png
Sprites/Hospital Room/washroom_sign.png.import
Sprites/characters/aida/aida_base.png
Sprites/characters/aida/aida_base.png.import
Sprites/characters/aida/aida_concerned.png
Sprites/characters/aida/aida_concerned.png.import
Sprites/characters/aida/aida_pointing_up.png
Sprites/characters/aida/aida_pointing_up.png.import
Sprites/characters/aida/aida_smiling.png
Sprites/characters/aida/aida_smiling.png.import
Sprites/characters/aida/aida_surprised.png
Sprites/characters/aida/aida_surprised.png.import
Sprites/characters/aida/aida_thinking.png
Sprites/characters/aida/aida_thinking.png.import
Sprites/characters/layla/layla_angry_1.png
Sprites/characters/layla/layla_angry_1.png.import
Sprites/characters/layla/layla_angry_2.png
Sprites/characters/layla/layla_angry_2.png.import
Sprites/characters/layla/layla_base.png
Sprites/characters/layla/layla_base.png.import
Sprites/characters/layla/layla_pout.png
Sprites/characters/layla/layla_pout.png.import
Sprites/characters/layla/layla_proud.png
Sprites/characters/layla/layla_proud.png.import
Sprites/characters/layla/layla_victory.png
Sprites/characters/layla/layla_victory.png.import
Sprites/characters/lewgend/lewgend_arms_open.png
Sprites/characters/lewgend/lewgend_arms_open.png.import
Sprites/characters/lewgend/lewgend_determined.png
Sprites/characters/lewgend/lewgend_determined.png.import
Sprites/characters/lewgend/lewgend_embarrassed.png
Sprites/characters/lewgend/lewgend_embarrassed.png.import
Sprites/characters/lewgend/lewgend_explaining_1.png
Sprites/characters/lewgend/lewgend_explaining_1.png.import
Sprites/characters/lewgend/lewgend_explaining_2.png
Sprites/characters/lewgend/lewgend_explaining_2.png.import
Sprites/characters/lewgend/lewgend_explaining_3.png
Sprites/characters/lewgend/lewgend_explaining_3.png.import
Sprites/characters/lewgend/lewgend_hand_on_chest.png
Sprites/characters/lewgend/lewgend_hand_on_chest.png.import
Sprites/characters/lewgend/lewgend_waving.png
Sprites/characters/lewgend/lewgend_waving.png.import
Sprites/characters/malcolm/sm1_sensei_blush.png
Sprites/characters/malcolm/sm1_sensei_blush.png.import
Sprites/characters/mcbucket/mcbucket_default.png
Sprites/characters/mcbucket/mcbucket_default.png.import
Sprites/characters/mcbucket/mcbucket_high_base.png
Sprites/characters/mcbucket/mcbucket_high_base.png.import
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_closed_mouth_closed.png
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_closed_mouth_closed.png.import
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_closed_mouth_open.png
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_closed_mouth_open.png.import
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_open_mouth_closed.png
Sprites/characters/mcbucket/mcbucket_high_explaining_eyes_open_mouth_closed.png.import
Sprites/characters/mcbucket/mcbucket_high_hand_up.png
Sprites/characters/mcbucket/mcbucket_high_hand_up.png.import
Sprites/characters/mcbucket/mcbucket_high_happy.png
Sprites/characters/mcbucket/mcbucket_high_happy.png.import
Sprites/characters/mcbucket/mcbucket_invigirol_base.png
Sprites/characters/mcbucket/mcbucket_invigirol_base.png.import
Sprites/characters/mcbucket/mcbucket_ree.png
Sprites/characters/mcbucket/mcbucket_ree.png.import
Sprites/characters/sergey/sergey_base.png
Sprites/characters/sergey/sergey_base.png.import
Sprites/characters/sergey/sergey_eyes_closed.png
Sprites/characters/sergey/sergey_eyes_closed.png.import
Sprites/characters/sergey/sergey_frown.png
Sprites/characters/sergey/sergey_frown.png.import
Sprites/characters/sergey/sergey_frown_look_away.png
Sprites/characters/sergey/sergey_frown_look_away.png.import
Sprites/characters/sergey/sergey_hand_behind_head.png
Sprites/characters/sergey/sergey_hand_behind_head.png.import
Sprites/characters/sergey/sergey_hand_up.png
Sprites/characters/sergey/sergey_hand_up.png.import
Sprites/characters/sergey/sergey_neutral_mouth_open.png
Sprites/characters/sergey/sergey_neutral_mouth_open.png.import
Sprites/dialogue sprites/aida_dialogue_sprite.PNG
Sprites/dialogue sprites/aida_dialogue_sprite.PNG.import
Sprites/dialogue sprites/layla_dialogue_sprite.png
Sprites/dialogue sprites/layla_dialogue_sprite.png.import
Sprites/dialogue sprites/lewgend_dialogue_sprite.png
Sprites/dialogue sprites/lewgend_dialogue_sprite.png.import
Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG
Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG.import
Sprites/dialogue sprites/protag_dialogue_sprite.png
Sprites/dialogue sprites/protag_dialogue_sprite.png.import
Sprites/dialogue sprites/sergey_dialogue_sprite.png
Sprites/dialogue sprites/sergey_dialogue_sprite.png.import
Sprites/dialogue sprites/toilet_dialogue_sprite.png
Sprites/dialogue sprites/toilet_dialogue_sprite.png.import
Sprites/inventory_items/cannathink_icon.png
Sprites/inventory_items/cannathink_icon.png.import
Sprites/inventory_items/invigirol_icon.png
Sprites/inventory_items/invigirol_icon.png.import
Sprites/inventory_items/techpass_inventory_item.png
Sprites/inventory_items/techpass_inventory_item.png.import
Sprites/inventory_items/toilet_paper_inventory_sprite.png
Sprites/inventory_items/toilet_paper_inventory_sprite.png.import
Sprites/inventory_items/zanopram_icon.png
Sprites/inventory_items/zanopram_icon.png.import
Themes/Game_UI_Theme.tres
Themes/general_theme.tres
Themes/medfinance_theme.tres
Themes/verb_and_inventory_theme.tres
addons/dialogue_manager/DialogueManager.cs
addons/dialogue_manager/DialogueManager.cs.uid
addons/dialogue_manager/LICENSE
addons/dialogue_manager/assets/banner.png
addons/dialogue_manager/assets/banner.png.import
addons/dialogue_manager/assets/icon.svg
addons/dialogue_manager/assets/icon.svg.import
addons/dialogue_manager/assets/responses_menu.svg
addons/dialogue_manager/assets/responses_menu.svg.import
addons/dialogue_manager/assets/update.svg
addons/dialogue_manager/assets/update.svg.import
addons/dialogue_manager/compiler/compilation.gd
addons/dialogue_manager/compiler/compilation.gd.uid
addons/dialogue_manager/compiler/compiled_line.gd
addons/dialogue_manager/compiler/compiled_line.gd.uid
addons/dialogue_manager/compiler/compiler.gd
addons/dialogue_manager/compiler/compiler.gd.uid
addons/dialogue_manager/compiler/compiler_regex.gd
addons/dialogue_manager/compiler/compiler_regex.gd.uid
addons/dialogue_manager/compiler/compiler_result.gd
addons/dialogue_manager/compiler/compiler_result.gd.uid
addons/dialogue_manager/compiler/expression_parser.gd
addons/dialogue_manager/compiler/expression_parser.gd.uid
addons/dialogue_manager/compiler/resolved_goto_data.gd
addons/dialogue_manager/compiler/resolved_goto_data.gd.uid
addons/dialogue_manager/compiler/resolved_line_data.gd
addons/dialogue_manager/compiler/resolved_line_data.gd.uid
addons/dialogue_manager/compiler/resolved_tag_data.gd
addons/dialogue_manager/compiler/resolved_tag_data.gd.uid
addons/dialogue_manager/compiler/tree_line.gd
addons/dialogue_manager/compiler/tree_line.gd.uid
addons/dialogue_manager/components/code_edit.gd
addons/dialogue_manager/components/code_edit.gd.uid
addons/dialogue_manager/components/code_edit.tscn
addons/dialogue_manager/components/code_edit_syntax_highlighter.gd
addons/dialogue_manager/components/code_edit_syntax_highlighter.gd.uid
addons/dialogue_manager/components/download_update_panel.gd
addons/dialogue_manager/components/download_update_panel.gd.uid
addons/dialogue_manager/components/download_update_panel.tscn
addons/dialogue_manager/components/editor_property/editor_property.gd
addons/dialogue_manager/components/editor_property/editor_property.gd.uid
addons/dialogue_manager/components/editor_property/editor_property_control.gd
addons/dialogue_manager/components/editor_property/editor_property_control.gd.uid
addons/dialogue_manager/components/editor_property/editor_property_control.tscn
addons/dialogue_manager/components/editor_property/resource_button.gd
addons/dialogue_manager/components/editor_property/resource_button.gd.uid
addons/dialogue_manager/components/editor_property/resource_button.tscn
addons/dialogue_manager/components/errors_panel.gd
addons/dialogue_manager/components/errors_panel.gd.uid
addons/dialogue_manager/components/errors_panel.tscn
addons/dialogue_manager/components/files_list.gd
addons/dialogue_manager/components/files_list.gd.uid
addons/dialogue_manager/components/files_list.tscn
addons/dialogue_manager/components/search_and_replace.gd
addons/dialogue_manager/components/search_and_replace.gd.uid
addons/dialogue_manager/components/search_and_replace.tscn
addons/dialogue_manager/components/title_list.gd
addons/dialogue_manager/components/title_list.gd.uid
addons/dialogue_manager/components/title_list.tscn
addons/dialogue_manager/components/update_button.gd
addons/dialogue_manager/components/update_button.gd.uid
addons/dialogue_manager/components/update_button.tscn
addons/dialogue_manager/constants.gd
addons/dialogue_manager/constants.gd.uid
addons/dialogue_manager/dialogue_label.gd
addons/dialogue_manager/dialogue_label.gd.uid
addons/dialogue_manager/dialogue_label.tscn
addons/dialogue_manager/dialogue_line.gd
addons/dialogue_manager/dialogue_line.gd.uid
addons/dialogue_manager/dialogue_manager.gd
addons/dialogue_manager/dialogue_manager.gd.uid
addons/dialogue_manager/dialogue_processor.gd
addons/dialogue_manager/dialogue_processor.gd.uid
addons/dialogue_manager/dialogue_resource.gd
addons/dialogue_manager/dialogue_resource.gd.uid
addons/dialogue_manager/dialogue_response.gd
addons/dialogue_manager/dialogue_response.gd.uid
addons/dialogue_manager/dialogue_responses_menu.gd
addons/dialogue_manager/dialogue_responses_menu.gd.uid
addons/dialogue_manager/editor_translation_parser_plugin.gd
addons/dialogue_manager/editor_translation_parser_plugin.gd.uid
addons/dialogue_manager/example_balloon/ExampleBalloon.cs
addons/dialogue_manager/example_balloon/ExampleBalloon.cs.uid
addons/dialogue_manager/example_balloon/example_balloon.gd
addons/dialogue_manager/example_balloon/example_balloon.gd.uid
addons/dialogue_manager/example_balloon/example_balloon.tscn
addons/dialogue_manager/example_balloon/small_example_balloon.tscn
addons/dialogue_manager/export_plugin.gd
addons/dialogue_manager/export_plugin.gd.uid
addons/dialogue_manager/import_plugin.gd
addons/dialogue_manager/import_plugin.gd.uid
addons/dialogue_manager/inspector_plugin.gd
addons/dialogue_manager/inspector_plugin.gd.uid
addons/dialogue_manager/l10n/en.po
addons/dialogue_manager/l10n/es.po
addons/dialogue_manager/l10n/translations.pot
addons/dialogue_manager/l10n/uk.po
addons/dialogue_manager/l10n/zh.po
addons/dialogue_manager/l10n/zh_TW.po
addons/dialogue_manager/plugin.cfg
addons/dialogue_manager/plugin.cfg.uid
addons/dialogue_manager/plugin.gd
addons/dialogue_manager/plugin.gd.uid
addons/dialogue_manager/settings.gd
addons/dialogue_manager/settings.gd.uid
addons/dialogue_manager/test_scene.gd
addons/dialogue_manager/test_scene.gd.uid
addons/dialogue_manager/test_scene.tscn
addons/dialogue_manager/utilities/builtins.gd
addons/dialogue_manager/utilities/builtins.gd.uid
addons/dialogue_manager/utilities/dialogue_cache.gd
addons/dialogue_manager/utilities/dialogue_cache.gd.uid
addons/dialogue_manager/utilities/translations.gd
addons/dialogue_manager/utilities/translations.gd.uid
addons/dialogue_manager/utilities/waiter.gd
addons/dialogue_manager/utilities/waiter.gd.uid
addons/dialogue_manager/views/find_in_dialogue_view.gd
addons/dialogue_manager/views/find_in_dialogue_view.gd.uid
addons/dialogue_manager/views/find_in_dialogue_view.tscn
addons/dialogue_manager/views/main_view.gd
addons/dialogue_manager/views/main_view.gd.uid
addons/dialogue_manager/views/main_view.tscn
autoload/ConversationEventManager.gd
autoload/ConversationEventManager.gd.uid
autoload/FallbackManager.gd
autoload/FallbackManager.gd.uid
autoload/GameManager.gd
autoload/GameManager.gd.uid
autoload/GameManager.tscn
autoload/NotificationManager.gd
autoload/NotificationManager.gd.uid
autoload/NotificationManager.tscn
autoload/SoundManager.gd
autoload/SoundManager.gd.uid
autoload/dialogue_history.gd
autoload/dialogue_history.gd.uid
autoload/events.gd
autoload/events.gd.uid
autoload/flags.gd
autoload/flags.gd.uid
autoload/inventory.gd
autoload/inventory.gd.uid
autoload/settings.gd
autoload/settings.gd.uid
characters/CharacterProfile.gd
characters/CharacterProfile.gd.uid
characters/Sergey.tscn
characters/WaypointMovement.gd
characters/WaypointMovement.gd.uid
characters/aida.gd
characters/aida.gd.uid
characters/aida.tres
characters/aida.tscn
characters/dread_entity.gd
characters/dread_entity.gd.uid
characters/dread_entity.tscn
characters/hope_entity.gd
characters/hope_entity.gd.uid
characters/hope_entity.tscn
characters/layla_profile.tres
characters/lewgend_profile.tres
characters/malcolm.tres
characters/mcbucket.gd
characters/mcbucket.gd.uid
characters/mcbucket.tres
characters/mcbucket.tscn
characters/npc.gd
characters/npc.gd.uid
characters/npc.tscn
characters/player.gd
characters/player.gd.uid
characters/player.tscn
characters/sergey.gd
characters/sergey.gd.uid
characters/sergey.tres
conversation/AdvancedConversationOverlay.gd
conversation/AdvancedConversationOverlay.gd.uid
conversation/AdvancedConversationOverlay.tscn
conversation/CharacterConversationOverlay.tscn
conversation/MedCabinetzoomin.tscn
conversation/MedicineCabinetZoom.gd
conversation/MedicineCabinetZoom.gd.uid
conversation/MemoryBoxOverlay.gd
conversation/MemoryBoxOverlay.gd.uid
conversation/MemoryBoxOverlay.tscn
conversation/ObjectZoomOverlay.tscn
conversation/aida_advanced_overlay.tscn
conversation/balloon.gd
conversation/balloon.gd.uid
conversation/character_conversation_overlay.gd
conversation/character_conversation_overlay.gd.uid
conversation/character_conversation_scene.tscn
conversation/conversation_backgrounds.tres
conversation/conversationballoon.gd
conversation/conversationballoon.gd.uid
conversation/conversationballoon.tscn
conversation/mcbucket_advanced_overlay.tscn
conversation/mcbucket_thinking_conversation.tscn
conversation/memory_box.tscn
conversation/object_zoom_overlay.gd
conversation/object_zoom_overlay.gd.uid
conversation/sergey_advanced_overlay.tscn
conversation/sergey_conversation_1.tscn
conversation/sergey_conversation_2.tscn
data/ExplanationData.tres
data/InteractionResponse.gd
data/InteractionResponse.gd.uid
data/ItemData.gd
data/ItemData.gd.uid
data/JournalPageData.gd
data/JournalPageData.gd.uid
data/MemoryChapterData.gd
data/MemoryChapterData.gd.uid
data/MemoryGroupData.gd
data/MemoryGroupData.gd.uid
data/VerbData.gd
data/VerbData.gd.uid
data/cannathink.tres
data/chapter_university_1.tres
data/chapter_university_2.tres
data/chapter_university_3.tres
data/chapter_university_4.tres
data/chapter_university_5.tres
data/chapter_university_6.tres
data/examine_verb.tres
data/explanation_data.gd
data/explanation_data.gd.uid
data/flash_verb.tres
data/give_verb.tres
data/hospital_toilet_paper.tres
data/invigirol.tres
data/location_university.tres
data/memeoryboxleftandrightpanel.tres
data/memorybox.tres
data/memoryboxbackbutton.tres
data/pick_up_verb.tres
data/talk_to_verb.tres
data/techpass.tres
data/think_verb.tres
data/use_verb.tres
data/zanopram.tres
default_bus_layout.tres
dialogue/AIda.dialogue
dialogue/AIda.dialogue.import
dialogue/aida_dialogue_hub.dialogue
dialogue/aida_dialogue_hub.dialogue.import
dialogue/aida_toilet_assistance.dialogue
dialogue/aida_toilet_assistance.dialogue.import
dialogue/alyssa_marketing.dialogue
dialogue/alyssa_marketing.dialogue.import
dialogue/dev_cta.dialogue
dialogue/dev_cta.dialogue.import
dialogue/examinables.dialogue
dialogue/examinables.dialogue.import
dialogue/flash_dialogue.dialogue
dialogue/flash_dialogue.dialogue.import
dialogue/form_related_dialogue.dialogue
dialogue/form_related_dialogue.dialogue.import
dialogue/generic_lines.dialogue
dialogue/generic_lines.dialogue.import
dialogue/giveables.dialogue
dialogue/giveables.dialogue.import
dialogue/hints.dialogue
dialogue/hints.dialogue.import
dialogue/hints_adventure.dialogue
dialogue/hints_adventure.dialogue.import
dialogue/intro.dialogue
dialogue/intro.dialogue.import
dialogue/mcbucket_give_tp.dialogue
dialogue/mcbucket_give_tp.dialogue.import
dialogue/mcbucket_thinking_dialogue.dialogue
dialogue/mcbucket_thinking_dialogue.dialogue.import
dialogue/mcbucket_unified.dialogue
dialogue/mcbucket_unified.dialogue.import
dialogue/npcs/faye.dialogue
dialogue/npcs/faye.dialogue.import
dialogue/sergey_dialogue_1.dialogue
dialogue/sergey_dialogue_1.dialogue.import
dialogue/sergey_dialogue_2.dialogue
dialogue/sergey_dialogue_2.dialogue.import
dialogue/talkto.dialogue
dialogue/talkto.dialogue.import
dialogue/testdialogue.dialogue
dialogue/testdialogue.dialogue.import
dialogue/unflashables.dialogue
dialogue/unflashables.dialogue.import
dialogue/unpickables.dialogue
dialogue/unpickables.dialogue.import
dialogue/untalkables.dialogue
dialogue/untalkables.dialogue.import
dialogue/unusables.dialogue
dialogue/unusables.dialogue.import
dialogue/use_item_accompanying_dialogue.dialogue
dialogue/use_item_accompanying_dialogue.dialogue.import
exit_sign_visual.tscn
export_presets.cfg
exported_logo.png
exported_logo.png.import
generate_pdf.py
hiragino-kaku-gothic-std-w8.otf
hiragino-kaku-gothic-std-w8.otf.import
hospital bed.png
hospital bed.png.import
hospital door.png
hospital door.png.import
hospital_bathroom_door.tscn
hospital_door.tscn
icon.svg
icon.svg.import
image0(6).png
image0(6).png.import
insurance form bg.png
insurance form bg.png.import
interactions/action.gd
interactions/action.gd.uid
interactions/actions/AddItemAction.gd
interactions/actions/AddItemAction.gd.uid
interactions/actions/CallMethodAction.gd
interactions/actions/CallMethodAction.gd.uid
interactions/actions/DefaultExamineResponse.tres
interactions/actions/DestroyAction.gd
interactions/actions/DestroyAction.gd.uid
interactions/actions/OpenMemoryBoxAction.gd
interactions/actions/OpenMemoryBoxAction.gd.uid
interactions/actions/OpenMemoryBoxAction.tres
interactions/actions/OpenZoomViewAction.gd
interactions/actions/OpenZoomViewAction.gd.uid
interactions/actions/PlaySoundAction.gd
interactions/actions/PlaySoundAction.gd.uid
interactions/actions/PlayerFlashAction.gd
interactions/actions/PlayerFlashAction.gd.uid
interactions/actions/RemoveItemAction.gd
interactions/actions/RemoveItemAction.gd.uid
interactions/actions/SayLineAction.gd
interactions/actions/SayLineAction.gd.uid
interactions/actions/SetLevelFlagAction.gd
interactions/actions/SetLevelFlagAction.gd.uid
interactions/actions/SetLevelFlagAction.tres
interactions/actions/ShowCustomDialogueAction.gd
interactions/actions/ShowCustomDialogueAction.gd.uid
interactions/actions/ShowCustomDialogueAction.tres
interactions/actions/ShowDialogueActiont.gd
interactions/actions/ShowDialogueActiont.gd.uid
interactions/actions/StartConversationAction.gd
interactions/actions/StartConversationAction.gd.uid
interactions/actions/TeleportAction.gd
interactions/actions/TeleportAction.gd.uid
interactions/actions/TeleportAction.tres
interactions/actions/TriggerCutsceneAction.gd
interactions/actions/TriggerCutsceneAction.gd.uid
interactions/actions/TriggerCutsceneAction.tres
interactions/actions/UniversalFlashAction.gd
interactions/actions/UniversalFlashAction.gd.uid
interactions/actions/interactable_outline.gdshader
interactions/actions/interactable_outline.gdshader.uid
interactions/actions/interactable_outline.tres
interactions/actions/sneak_pills_action.tres
interactions/interactable.gd
interactions/interactable.gd.uid
interactions/interactable.tscn
invigirol.png
invigirol.png.import
iris_wipe.gdshader
iris_wipe.gdshader.uid
key.png
key.png.import
level_state_manager.tscn
lewgend_arms_open.png
lewgend_arms_open.png.import
logo_exporter.tscn
main.tscn
main.tscn5296345394.tmp
main.tscn8249773746.tmp
mcbucketRee.PNG
mcbucketRee.PNG.import
med dispenser.png
med dispenser.png.import
medcabinetzoomed.png
medcabinetzoomed.png.import
memorybox.png
memorybox.png.import
old_paper.jpg
old_paper.jpg.import
project.godot
repomix-output.xml
table.png
table.png.import
television_screen.gdshader
television_screen.gdshader.uid
test_action.tres
test_character_portrait.jpg
test_character_portrait.jpg.import
test_sprite_frames.tres
testvideoplayer.gd
testvideoplayer.gd.uid
tubelight.png
tubelight.png.import
tv_sprite.png
tv_sprite.png.import
ui/ChapterButton.gd
ui/ChapterButton.gd.uid
ui/ChapterButton.tscn
ui/DateInputWidget.gd
ui/DateInputWidget.gd.uid
ui/DateInputWidget.tscn
ui/DigitSpinner.gd
ui/DigitSpinner.gd.uid
ui/DigitSpinner.tscn
ui/InventoryUI.gd
ui/InventoryUI.gd.uid
ui/InventoryUI.tscn
ui/LocationRow.gd
ui/LocationRow.gd.uid
ui/LocationRow.tscn
ui/LogoSplash.gd
ui/LogoSplash.gd.uid
ui/MainMenu.gd
ui/MainMenu.gd.uid
ui/Notification.gd
ui/Notification.gd.uid
ui/Notification.tscn
ui/TransitionLayer.gd
ui/TransitionLayer.gd.uid
ui/TransitionLayer.tscn
ui/UI_Interactable.tscn
ui/UI_Invigirol.tscn
ui/UI_Zanopram.tscn
ui/UI_cannathink.tscn
ui/VerbUI.gd
ui/VerbUI.gd.uid
ui/VerbUI.tscn
ui/black_screen_overlay.gd
ui/black_screen_overlay.gd.uid
ui/black_screen_overlay.tscn
ui/chapter_button.tres
ui/controls_menu.gd
ui/controls_menu.gd.uid
ui/controls_menu.tscn
ui/credits_menu.gd
ui/credits_menu.gd.uid
ui/credits_menu.tscn
ui/custom_cursor.gd
ui/custom_cursor.gd.uid
ui/custom_cursor.tscn
ui/custom_cursor_control.gd
ui/custom_cursor_control.gd.uid
ui/dialogue_history_ui.gd
ui/dialogue_history_ui.gd.uid
ui/dialogue_history_ui.tscn
ui/difficulty_select_screen.gd
ui/difficulty_select_screen.gd.uid
ui/difficulty_select_screen.tscn
ui/explanation_layer.gd
ui/explanation_layer.gd.uid
ui/explanation_layer.gdshader
ui/explanation_layer.gdshader.uid
ui/explanation_layer.tscn
ui/game_over.gd
ui/game_over.gd.uid
ui/game_over.tscn
ui/input_blocker.gd
ui/input_blocker.gd.uid
ui/insurance_form.gd
ui/insurance_form.gd.uid
ui/insurance_form.tscn
ui/insurance_form_button_ui.gd
ui/insurance_form_button_ui.gd.uid
ui/insurance_form_button_ui.tscn
ui/insurance_form_field_font.tres
ui/journal_button_ui.gd
ui/journal_button_ui.gd.uid
ui/journal_button_ui.tscn
ui/journal_overlay.gd
ui/journal_overlay.gd.uid
ui/journal_overlay.tscn
ui/loading_screen.gd
ui/loading_screen.gd.uid
ui/loading_screen.tscn
ui/logo_cursor.gd
ui/logo_cursor.gd.uid
ui/logo_splash.tscn
ui/main_menu.tscn
ui/patreon_world_button.gd
ui/patreon_world_button.gd.uid
ui/pause_menu_ui.gd
ui/pause_menu_ui.gd.uid
ui/pause_menu_ui.tscn
ui/settings_menu.gd
ui/settings_menu.gd.uid
ui/settings_menu.tscn
ui/walk_indicator.gd
ui/walk_indicator.gd.uid
ui/walk_indicator.tscn
ui_interactable.gd
ui_interactable.gd.uid
void
washroom_sign_visual.tscn
zanopram_map.png
zanopram_map.png.import
</file>

<file path="autoload/ConversationEventManager.gd">
# ConversationEventManager.gd
extends Node


# Signal to request a background change in the active conversation overlay
# We pass the new texture path as an argument.
signal change_conversation_background(new_background_texture_path: String)

# You can add more conversation-related signals here later if needed,
# e.g., for character sprite changes, sound effects, etc.
var show_special_response: bool = false

#Sergey's variables
var has_heard_fresh_start_line: bool = false
var asked_sergey_duration: bool = false
var asked_sergey_identity: bool = false

func reset_run_state():
	show_special_response = false
	has_heard_fresh_start_line = false
	asked_sergey_duration = false
	asked_sergey_identity = false
</file>

<file path="autoload/dialogue_history.gd">
# DialogueHistory.gd (Autoload) — session log of dialogue lines, choices, and player actions.
extends Node

const MAX_ENTRIES = 100
var entries: Array[Dictionary] = []
var visited_responses: Dictionary = {}


func add_line(lookup_name: String, display_name: String, text: String):
	entries.append({
		"type": "line",
		"lookup_name": lookup_name,
		"display_name": display_name,
		"text": text
	})
	_trim()


func add_choice(lookup_name: String, display_name: String, options: Array[String], selected_index: int):
	entries.append({
		"type": "choice",
		"lookup_name": lookup_name,
		"display_name": display_name,
		"options": options,
		"selected_index": selected_index
	})
	_trim()


func add_action(verb_name: String, object_name: String, item_name: String = ""):
	entries.append({
		"type": "action",
		"verb": verb_name,
		"object": object_name,
		"item": item_name
	})
	_trim()


func _trim():
	if entries.size() > MAX_ENTRIES:
		entries.pop_front()


func reset():
	entries.clear()
	visited_responses.clear()
</file>

<file path="autoload/events.gd">
# Events.gd (Autoload) — global signal bus.
# Declare broadcast signals here. Anything may emit them; anything may connect.
# Rule of thumb: notifications ("X happened") go through Events.
# Commands ("do X") are direct calls on the service autoloads (Inventory, Flags, ...).
extends Node

# --- Game flow (values are GameManager.GameState / GameManager.InteractionState ints) ---
signal game_state_changed(new_state: int)
signal interaction_state_changed(new_state: int)

# --- Inventory ---
signal item_added(item_id: String)
signal item_removed(item_id: String)

# --- UI ---
signal notification_requested(message: String)
</file>

<file path="autoload/FallbackManager.gd">
# res://scripts/FallbackManager.gd
extends Node

const DEFAULT_CHECKPOINT_NAME = "start"

func trigger_fallback(verb_data: VerbData, object_id: String, item_id: String):
	if not is_instance_valid(verb_data):
		push_warning("FallbackManager: trigger_fallback was called with invalid VerbData.")
		return

	var fallback_resource = verb_data.fallback_dialogue_file
	if not is_instance_valid(fallback_resource):
		print_rich("[color=orange]FallbackManager: Verb '%s' has no fallback file assigned. No action taken.[/color]" % verb_data.verb_id)
		GameManager.interaction_complete.emit()
		return

	var checkpoint_to_use: String = _find_valid_checkpoint(fallback_resource, object_id, item_id)

	DialogueManager.dialogue_ended.connect(
		GameManager.restore_world_after_object_dialogue,
		CONNECT_ONE_SHOT
	)

	print_rich("[color=cyan]FallbackManager: Triggering fallback dialogue from '%s' with checkpoint '%s'.[/color]" % [fallback_resource.resource_path, checkpoint_to_use])
	DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", fallback_resource, checkpoint_to_use)


# This function now uses the correct method to check for checkpoints.
func _find_valid_checkpoint(resource: DialogueResource, object_id: String, item_id: String) -> String:
	# Level 1: Item + Object Combination
	if not item_id.is_empty():
		var item_specific_checkpoint = "%s_item_%s" % [object_id, item_id]
		# Use the confirmed method: check the 'titles' dictionary directly.
		if resource.titles.has(item_specific_checkpoint):
			print_rich("[color=green]FallbackManager: Found item-specific checkpoint: '%s'[/color]" % item_specific_checkpoint)
			return item_specific_checkpoint

	# Level 2: Object-Only (Only run this if NO item was used)
	if item_id.is_empty() and not object_id.is_empty():
		var object_specific_checkpoint = object_id
		if resource.titles.has(object_specific_checkpoint):
			print_rich("[color=green]FallbackManager: Found object-specific checkpoint: '%s'[/color]" % object_specific_checkpoint)
			return object_specific_checkpoint

	# Level 3: Generic Default
	print_rich("[color=yellow]FallbackManager: No specific checkpoint found. Using default: '%s'[/color]" % DEFAULT_CHECKPOINT_NAME)
	return DEFAULT_CHECKPOINT_NAME
</file>

<file path="autoload/flags.gd">
# Flags.gd (Autoload) — global (cross-level) flags + routing to the current level's flags.
extends Node

var game_flags: Dictionary = {}
var current_level_state_manager: LevelStateManager = null


# --- Global flags ---
func set_game_flag(flag_name: String, value: bool):
	if game_flags.get(flag_name, !value) == value:
		return
	game_flags[flag_name] = value


func get_game_flag(flag_name: String) -> bool:
	return game_flags.get(flag_name, false)


# --- Current level's flags ---
func register_level_state_manager(lsm: LevelStateManager):
	current_level_state_manager = lsm
	if is_instance_valid(lsm) and lsm.has_method("print_initial_flags"):
		lsm.print_initial_flags()


func set_level_flag(flag_name: String, value: bool):
	if is_instance_valid(current_level_state_manager):
		current_level_state_manager.set_level_flag(flag_name, value)


func get_level_flag(flag_name: String) -> bool:
	if is_instance_valid(current_level_state_manager):
		return current_level_state_manager.get_level_flag(flag_name)
	return false


func reset_run_state():
	game_flags.clear()
	current_level_state_manager = null
</file>

<file path="autoload/GameManager.gd">
# GameManager.gd
extends Node

const MAIN_GAME_SCENE_PATH = "res://main.tscn"
const INSURANCE_FORM_SCENE = preload("res://insurance_form.tscn")
const JOURNAL_OVERLAY_SCENE = preload("res://journal_overlay.tscn")
const MAIN_MENU_SCENE_PATH = "res://main_menu.tscn"
const INTRO_OVERLAY_SCENE_PATH = "res://AdvancedConversationOverlay.tscn"
const INTRO_BACKGROUND_ANIMATIONS_PATH = "res://conversation_backgrounds.tres"
const INTRO_INITIAL_ANIMATION_NAME = "float_loop"
const INTRO_DIALOGUE = preload("res://dialogue/intro.dialogue")
const GAME_OVER_SCENE = preload("res://game_over.tscn")
const DIFFICULTY_SELECT_SCENE = preload("res://difficulty_select_screen.tscn")
const CONVERSATION_BALLOON_SCENE = preload("res://conversationballoon.tscn")

var _insurance_form_instance: CanvasLayer = null
var _journal_overlay_instance: CanvasLayer = null

# --- Cached Scenes ---
var cached_main_menu_scene: PackedScene = null
var cached_intro_overlay_scene: PackedScene = null
var cached_main_game_scene: PackedScene = null
var _intro_overlay_instance: Node = null

# --- Signals ---
signal verb_changed(new_verb_id: String)
signal sentence_line_updated(text: String)
signal interaction_complete # For VerbUI to reset its state
signal available_verbs_changed(available_verb_data_array: Array[VerbData])
signal new_hint_available(is_available: bool)
signal verb_lock_changed(is_active: bool)

# character conversation ended signal
signal character_conversation_ended(dialogue_resource: DialogueResource)

signal selected_inventory_item_changed(selected_item_data: ItemData)

# --- High-Level Game State Management ---
enum GameState {
	BOOTING,
	LOGO_SPLASH,
	MAIN_MENU,
	DIFFICULTY_SELECT,
	INTRO_CONVERSATION,
	IN_GAME_PLAY,
	PAUSED,
	EXPLANATION,
	CUTSCENE,
	GAME_OVER
}

# --- Interaction Context Management ---
enum InteractionState {
	WORLD,
	CONVERSATION,
	ZOOM_VIEW
}
var current_interaction_state: InteractionState = InteractionState.WORLD

var verb_ui: CanvasLayer = null
var journal_button_ui: CanvasLayer = null
var inventory_ui: CanvasLayer = null
var insurance_form_button_ui: CanvasLayer = null
var explanation_layer: CanvasLayer = null
var transition_layer: CanvasLayer = null

var current_game_state: GameState = GameState.BOOTING
var main_game_scene_instance: Node = null
var main_menu_scene_instance: CanvasLayer = null
var pause_menu_ui: CanvasLayer = null
var input_blocker_layer: CanvasLayer = null
var custom_cursor_instance: CanvasLayer = null
var walk_indicator_instance: Node2D = null
var patreon_world_ui: CanvasLayer = null
var is_mouse_held_for_walk: bool = false
var is_transitioning: bool = false
var is_verb_lock_active: bool = false
var _scan_active: bool = false
var _scan_tween: Tween
var _is_game_over_triggering: bool = false
const ICON_SCAN = preload("res://Icons/magnifying-glass.png")
const ICON_CANCEL = preload("res://Icons/cancel.png")

# --- State Variables ---
var current_verb_id: String = ""
var persisting_verb_id: String = ""
var current_selected_item_data: ItemData = null # "In Hand" / "Selected" item
var hovered_interactable: Interactable = null
var hovered_interactables: Array[Interactable] = []
var player_node: CharacterBody2D

var _is_player_walking: bool = false
var _current_character_conversation_overlay_instance: Node = null

var _mouse_held_timer: float = 0.0
var _potential_hold_walk: bool = false
var _signals_connected_to_interactable: Interactable = null

var current_hint_manager: LevelHintManager = null

# --- Verb Management ---
@export var all_verb_data_resources: Array[VerbData] = []
var unlocked_verb_ids: Array[String] = []
var active_scene_verb_ids: Array[String] = []

# --- Inventory Management ---
@export var all_item_data_resources: Array[ItemData] = []

var current_unread_hint: String = ""
var last_read_hint: String = ""

func _ready():
	print("If I Remember Correctly — v%s" % str(ProjectSettings.get_setting("application/config/version", "unset")))
	Input.set_mouse_mode(Input.MOUSE_MODE_HIDDEN)

	var cursor_scene = load("res://custom_cursor.tscn")
	if cursor_scene and not OS.has_feature("mobile"):
		custom_cursor_instance = cursor_scene.instantiate()
		add_child(custom_cursor_instance)

	var indicator_scene = load("res://walk_indicator.tscn")
	if indicator_scene:
		walk_indicator_instance = indicator_scene.instantiate()
		add_child(walk_indicator_instance)
		walk_indicator_instance.set_physics_interpolation_mode(Node.PHYSICS_INTERPOLATION_MODE_OFF)

	# Spawn our Global Transition Layer immediately
	var transition_scene = preload("res://TransitionLayer.tscn")
	transition_layer = transition_scene.instantiate()
	add_child(transition_layer)

	# Spawn our Global Pause Menu (lives on GameManager so it works in all states)
	var pause_scene = preload("res://pause_menu_ui.tscn")
	pause_menu_ui = pause_scene.instantiate()
	add_child(pause_menu_ui)

	if DialogueManager:
		DialogueManager.dialogue_started.connect(_on_dialogue_started)

	# Initialize Verbs
	for verb_data_res in all_verb_data_resources:
		if verb_data_res and verb_data_res.unlocked_by_default and not verb_data_res.verb_id in unlocked_verb_ids:
			unlocked_verb_ids.append(verb_data_res.verb_id)
	active_scene_verb_ids = unlocked_verb_ids.duplicate()
	_emit_available_verbs_changed_update()

	Inventory.setup(all_item_data_resources)
	Events.item_removed.connect(_on_inventory_item_removed)

	patreon_world_ui = PatreonWorldButton.new()
	add_child(patreon_world_ui)

	# --- DIRECT SCENE RUN CHECK ---
	if current_game_state == GameState.BOOTING:
		var potential_player = get_tree().get_first_node_in_group("player")

		if is_instance_valid(potential_player):

			# 1. Manually assign the player node
			player_node = potential_player

			# 2. Assign the main scene instance (assuming player is a child of the main scene)
			main_game_scene_instance = player_node.get_owner()
			if not is_instance_valid(main_game_scene_instance):
				# Fallback if owner is not set correctly
				main_game_scene_instance = get_tree().get_root().get_child(-1)

			# Find and assign the UI nodes now that the main scene is confirmed to exist.
			_find_and_assign_ui_nodes()

			# 3. Manually set the state.
			current_game_state = GameState.IN_GAME_PLAY

			# 4. Ensure the player can move
			if player_node.has_method("set_can_move"):
				player_node.set_can_move(true)

			if is_instance_valid(pause_menu_ui):
				pause_menu_ui.menu_panel.show()

			await get_tree().process_frame
			SoundManager.play_ambience("room_tone_air", -5.0)
			SoundManager.play_ambience("room_tone_electric", -15.0)

func _process(delta):
	# --- FIX 1: Cancel verb lock if player tries to move manually with A/D ---
	if is_verb_lock_active and Input.get_axis("ui_left", "ui_right") != 0:
		cancel_current_action(false)

	# --- FIX 2: Mobile Hold-to-Walk overriding interactable clicks ---
	if _potential_hold_walk and Input.is_mouse_button_pressed(MOUSE_BUTTON_LEFT):
		if not is_mouse_held_for_walk and current_interaction_state == InteractionState.WORLD and current_game_state == GameState.IN_GAME_PLAY:
			_mouse_held_timer += delta
			if _mouse_held_timer > 0.25:
				is_mouse_held_for_walk = true

				if is_instance_valid(player_node) and player_node.get("_interactable_after_walk") != null:
					player_node._interactable_after_walk = null
					player_node._verb_for_interaction = ""
					player_node._item_for_interaction = null
	else:
		_mouse_held_timer = 0.0
		if not Input.is_mouse_button_pressed(MOUSE_BUTTON_LEFT):
			_potential_hold_walk = false

	# --- (Original Logic Continues Below) ---
	if current_game_state == GameState.IN_GAME_PLAY and is_instance_valid(current_hint_manager):
		var evaluated_hint = current_hint_manager.evaluate_hint()
		if evaluated_hint != "" and evaluated_hint != current_unread_hint and evaluated_hint != last_read_hint:
			current_unread_hint = evaluated_hint
			if Settings.assisted_mode:
				new_hint_available.emit(true)

	if is_mouse_held_for_walk:
		if not Input.is_mouse_button_pressed(MOUSE_BUTTON_LEFT):
			is_mouse_held_for_walk = false
			if is_instance_valid(walk_indicator_instance):
				walk_indicator_instance.hide()

			# Restore the cursor hover state if we stopped walking while currently over an object
			if is_instance_valid(custom_cursor_instance):
				custom_cursor_instance.set_hover_state(hovered_interactable != null)

			# Restore UI text instantly upon release
			update_sentence_line_ui()
		else:
			if current_game_state == GameState.IN_GAME_PLAY and is_instance_valid(player_node):
				var mouse_world_pos = player_node.get_global_mouse_position()

				# Mirror the Hysteresis logic from player.gd
				var is_player_manually_walking = player_node.get("_is_manual_walking")
				var active_deadzone = 20.0 if is_player_manually_walking else 150.0

				if abs(player_node.global_position.x - mouse_world_pos.x) > active_deadzone:
					if is_instance_valid(walk_indicator_instance):
						walk_indicator_instance.global_position = mouse_world_pos
						walk_indicator_instance.show()
				else:
					if is_instance_valid(walk_indicator_instance):
						walk_indicator_instance.hide()

func _unhandled_input(event: InputEvent):
	if event is InputEventMouseButton and event.is_pressed():

		# --- RIGHT CLICK: Cancel Current Action ---
		if event.button_index == MOUSE_BUTTON_RIGHT:
			if current_verb_id != "" or current_selected_item_data != null:
				cancel_current_action()
				get_viewport().set_input_as_handled()
			return

		# --- LEFT CLICK: Walk to Point / Hold to Walk ---
		if event.button_index == MOUSE_BUTTON_LEFT:
			if current_game_state != GameState.IN_GAME_PLAY or current_interaction_state != InteractionState.WORLD:
				return
			if is_transitioning:
				return

			# --- FIX: Register that the click landed in the Game World! ---
			_potential_hold_walk = true

			var mouse_world_pos = player_node.get_global_mouse_position()

			# To fix touch/mouse sync issues, we cast a point exactly where the player clicked
			# to see if an Interactable is underneath. This completely bypasses the hover delay.
			var clicked_interactable = false
			if is_instance_valid(player_node):
				var space_state = player_node.get_world_2d().direct_space_state
				var query = PhysicsPointQueryParameters2D.new()
				query.position = mouse_world_pos
				query.collide_with_areas = true
				query.collide_with_bodies = false
				var intersections = space_state.intersect_point(query)
				for hit in intersections:
					if hit.collider is Interactable:
						clicked_interactable = true
						break

			if clicked_interactable:
				# We clicked an object! DO NOT cancel the verb. DO NOT walk immediately.
				# Just return, and let the Area2D's _input_event catch this click and process it.
				# (If the finger stays down, _potential_hold_walk will upgrade it to continuous movement!)
				return

			# We clicked empty space / the floor.
			# Cancel the verb and drop the item (if any).
			if current_verb_id != "" or current_selected_item_data != null:
				cancel_current_action()

			# Start walking to that point
			if is_instance_valid(player_node) and player_node.has_method("walk_to_point"):
				if _scan_active: _cancel_scan()
				_is_player_walking = true
				is_mouse_held_for_walk = true
				player_node.walk_to_point(mouse_world_pos)
				get_viewport().set_input_as_handled()
				
				
func change_game_state(new_state: GameState):
	if new_state == current_game_state:
		return

	# Wait for the end of the current frame so UI interactions clear
	await get_tree().process_frame

	# =========================================================
	# 1. LEAVING THE OLD STATE (Cleanup)
	# =========================================================
	match current_game_state:
		GameState.MAIN_MENU:
			_cleanup_all_overlays()
			if is_instance_valid(main_menu_scene_instance):
				main_menu_scene_instance.queue_free()
				main_menu_scene_instance = null
		GameState.INTRO_CONVERSATION:
			if new_state == GameState.MAIN_MENU:
				# Destroy the intro overlay if we are aborting to the main menu
				if is_instance_valid(_intro_overlay_instance):
					_intro_overlay_instance.queue_free()
					_intro_overlay_instance = null
		GameState.IN_GAME_PLAY, GameState.PAUSED:
			# When LEAVING the game (e.g. to Menu), stop music and ambience.
			if new_state != GameState.CUTSCENE and new_state != GameState.EXPLANATION and new_state != GameState.PAUSED and new_state != GameState.IN_GAME_PLAY and new_state != GameState.INTRO_CONVERSATION:
				SoundManager.stop_music()
				SoundManager.stop_all_ambience()

			if new_state == GameState.MAIN_MENU:
				_cleanup_all_overlays()
				if is_instance_valid(main_game_scene_instance):
					main_game_scene_instance.queue_free()
					main_game_scene_instance = null

				# Aggressively clear UI references so they don't persist into the Main Menu
				verb_ui = null
				inventory_ui = null
				insurance_form_button_ui = null
				journal_button_ui = null
				# pause_menu_ui is global, don't null it
				explanation_layer = null
				input_blocker_layer = null

	current_game_state = new_state
	Events.game_state_changed.emit(current_game_state)

	# =========================================================
	# 2. ENTERING THE NEW STATE (Setup)
	# =========================================================
	match current_game_state:
		GameState.MAIN_MENU:
			_is_game_over_triggering = false
			if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.hide()
			if is_instance_valid(main_menu_scene_instance):
				return

			var menu_packed_scene = cached_main_menu_scene
			if not menu_packed_scene:
				# Fallback if testing the scene directly without the Boot loader
				menu_packed_scene = load(MAIN_MENU_SCENE_PATH)
				if not menu_packed_scene:
					return

			main_menu_scene_instance = menu_packed_scene.instantiate()
			main_menu_scene_instance.new_game_requested.connect(_on_main_menu_new_game_requested)
			main_menu_scene_instance.quit_game_requested.connect(_on_main_menu_quit_requested)

			get_tree().root.add_child(main_menu_scene_instance)

		GameState.DIFFICULTY_SELECT:
			var difficulty_screen = DIFFICULTY_SELECT_SCENE.instantiate()
			difficulty_screen.difficulty_chosen.connect(_on_difficulty_chosen)
			get_tree().root.add_child(difficulty_screen)

		GameState.EXPLANATION:
			pass

		GameState.INTRO_CONVERSATION:
			if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.hide()

			if not is_instance_valid(_intro_overlay_instance):
				_start_intro_conversation()

		GameState.IN_GAME_PLAY:
			# --- A. LOADING PHASE ---
			# Only load the scene if it doesn't exist (e.g. fresh boot)
			if not is_instance_valid(main_game_scene_instance):
				var main_packed_scene = cached_main_game_scene
				if not main_packed_scene:
					# Fallback: cache is null (preload failed/skipped, or scene run directly)
					main_packed_scene = load(MAIN_GAME_SCENE_PATH)
					if not main_packed_scene:
						print_rich("[color=red]GameManager Error: Failed to load Main Game Scene.[/color]")
						return

				main_game_scene_instance = main_packed_scene.instantiate()
				get_tree().root.add_child(main_game_scene_instance)
				_find_and_assign_ui_nodes()
				
				# Play music if we just loaded the game
				# SoundManager.play_music() 
				
				# --- START AMBIENCE FOR MAIN GAME ---
				SoundManager.play_ambience("room_tone_air", -5.0) 
				SoundManager.play_ambience("room_tone_electric", -15.0)

			# --- B. RESTORATION PHASE (Run this EVERY time we enter IN_GAME_PLAY) ---
			
			# 1. Ensure UI nodes are found (in case of re-linking)
			if not is_instance_valid(verb_ui): _find_and_assign_ui_nodes()

			# 2. Find Player if missing
			if not is_instance_valid(player_node):
				player_node = get_tree().get_first_node_in_group("player")

			# 3. Restore Main UI Visibility
			if current_interaction_state == InteractionState.WORLD:
				_set_gameplay_ui_visible(true)
				if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.show()
			else:
				_set_gameplay_ui_visible(false)
				if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.hide()

			# 5. Unblock Input (Remove the gray blocker)
			if is_instance_valid(input_blocker_layer):
				input_blocker_layer.visible = false

			# 6. Unlock Player Movement
			if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
				player_node.set_can_move(true)

			# Force the sentence line to refresh in case we had a verb or item selected before pausing
			update_sentence_line_ui()

		GameState.PAUSED:
			update_sentence_line_ui()

		GameState.BOOTING:
			pass
			
		GameState.CUTSCENE:
			# 1. Hide the UI
			_set_gameplay_ui_visible(false)
			if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.hide()
			
			# 2. Block Input (Clicking on things in the world)
			if is_instance_valid(input_blocker_layer):
				input_blocker_layer.visible = true
				
			# 3. Stop the Player from moving
			if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
				player_node.set_can_move(false)
				

func select_verb(verb_id_to_select: String):
	# If the player manually selects or toggles a verb, clear any forced sticky state
	persisting_verb_id = ""

	var previously_selected_verb_id = current_verb_id
	var new_verb_id = ""

	if current_verb_id == verb_id_to_select:
		new_verb_id = ""
	else:
		var is_selectable = false
		for verb_data in get_currently_displayable_verbs():
			if verb_data.verb_id == verb_id_to_select:
				is_selectable = true; break
		if verb_id_to_select == "" or is_selectable:
			new_verb_id = verb_id_to_select
		else:
			new_verb_id = previously_selected_verb_id

	if current_verb_id != new_verb_id:
		current_verb_id = new_verb_id
		verb_changed.emit(current_verb_id)

		# --- Think Verb Interception ---
		if current_verb_id == "think":
			current_verb_id = ""
			verb_changed.emit("")

			if is_instance_valid(current_hint_manager):
				var hint_res = current_hint_manager.hints_dialogue if Settings.assisted_mode else current_hint_manager.hints_adventure_dialogue
				if is_instance_valid(hint_res):
					last_read_hint = current_hint_manager.evaluate_hint()
					new_hint_available.emit(false)

					if not DialogueManager.dialogue_ended.is_connected(restore_world_after_object_dialogue):
						DialogueManager.dialogue_ended.connect(restore_world_after_object_dialogue, CONNECT_ONE_SHOT)

					DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, hint_res, last_read_hint)
			return
		# ------------------------------------------

		# --- QOL FIX: Empty Inventory Give Check ---
		if current_verb_id == "give" and Inventory.is_empty():
			current_verb_id = ""
			verb_changed.emit("")

			if not DialogueManager.dialogue_ended.is_connected(restore_world_after_object_dialogue):
				DialogueManager.dialogue_ended.connect(restore_world_after_object_dialogue, CONNECT_ONE_SHOT)

			var generic_lines = preload("res://generic_lines.dialogue")
			DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, generic_lines, "give_empty_inventory")
			return
		# -------------------------------------------

		# If we select a verb that DOES NOT use items, or if we toggle the verb OFF, drop the in-hand item
		if (current_verb_id == "" or (current_verb_id != "use" and current_verb_id != "give")) and current_selected_item_data != null:
			current_selected_item_data = null
			selected_inventory_item_changed.emit(null)

		var verb_data = get_verb_data_by_id(current_verb_id)
		if verb_data and verb_data.requires_target_object and current_verb_id != "walk_to" and current_verb_id != "think":
			_activate_verb_lock(true)
		else:
			_activate_verb_lock(false)

	update_sentence_line_ui()

func select_inventory_item(item_data_to_select: ItemData):
	var previous_verb_id_was_item_compatible = (current_verb_id == "use" or current_verb_id == "give")

	if not item_data_to_select:
		if current_selected_item_data != null:
			current_selected_item_data = null
			selected_inventory_item_changed.emit(null)
		update_sentence_line_ui()
		return

	if current_selected_item_data == item_data_to_select:
		current_selected_item_data = null
		selected_inventory_item_changed.emit(null)
	else:
		current_selected_item_data = item_data_to_select
		selected_inventory_item_changed.emit(current_selected_item_data)

		# If we select an item, but the current verb isn't item-compatible, drop the verb.
		if current_verb_id != "" and not previous_verb_id_was_item_compatible:
			current_verb_id = ""
			verb_changed.emit("")

		# If no verb is selected, default to "use" when picking up an item
		if current_verb_id == "":
			current_verb_id = "use"
			verb_changed.emit("use")

		# --- FIX: Turn on the Verb Lock so the thought bubble and highlights appear! ---
		_activate_verb_lock(true)

	update_sentence_line_ui()

func cancel_current_action(play_sound: bool = true):
	var did_cancel = false

	# If the player right-clicks to cancel, drop the sticky state
	persisting_verb_id = ""

	if current_verb_id != "":
		current_verb_id = ""
		verb_changed.emit("")
		did_cancel = true

	if current_selected_item_data != null:
		current_selected_item_data = null
		selected_inventory_item_changed.emit(null)
		did_cancel = true

	if did_cancel:
		_activate_verb_lock(false)
		update_sentence_line_ui()
		# Play a slightly lower pitched click sound to indicate cancellation
		if play_sound and SoundManager: SoundManager.play_sfx("ui_click")

# --- UI and Interaction Flow ---
func set_hovered_object(interactable: Interactable):
	if not hovered_interactables.has(interactable):
		hovered_interactables.append(interactable)
	_update_top_hovered_object()

func clear_hovered_object(interactable: Interactable = null):
	if interactable and hovered_interactables.has(interactable):
		hovered_interactables.erase(interactable)
	elif interactable == null:
		hovered_interactables.clear()
	_update_top_hovered_object()

func refresh_hovered_object():
	_update_top_hovered_object()

func _update_top_hovered_object():
	# Clean out invalid instances just in case an object was destroyed while hovered
	for i in range(hovered_interactables.size() - 1, -1, -1):
		if not is_instance_valid(hovered_interactables[i]):
			hovered_interactables.remove_at(i)

	# --- TRANSITION OVERRIDE ---
	if hovered_interactables.is_empty() or is_transitioning:
		hovered_interactable = null
		if is_instance_valid(custom_cursor_instance):
			custom_cursor_instance.set_hover_state(false)
		update_sentence_line_ui()
		return

	# Find the top-most interactable
	var top_interactable = hovered_interactables[0]
	for i in range(1, hovered_interactables.size()):
		var candidate = hovered_interactables[i]

		# Safely get effective Z-index (checking parent if needed)
		var top_z = top_interactable.z_index
		if top_z == 0 and top_interactable.get_parent() is Node2D:
			top_z = top_interactable.get_parent().z_index

		var cand_z = candidate.z_index
		if cand_z == 0 and candidate.get_parent() is Node2D:
			cand_z = candidate.get_parent().z_index

		if cand_z > top_z:
			top_interactable = candidate
		elif cand_z == top_z:
			# If Z-index is tied, lower on the screen (higher Y) is in front
			if candidate.global_position.y > top_interactable.global_position.y:
				top_interactable = candidate

	hovered_interactable = top_interactable

	update_sentence_line_ui()

	# --- QOL FIX: Cursor state for incomplete Give ---
	var should_hover_cursor = true
	if current_verb_id == "give" and current_selected_item_data == null:
		should_hover_cursor = false

	if is_instance_valid(custom_cursor_instance) and not is_mouse_held_for_walk:
		custom_cursor_instance.set_hover_state(should_hover_cursor)

func update_sentence_line_ui():
	if is_mouse_held_for_walk or _is_player_walking or current_game_state == GameState.PAUSED or is_transitioning:
		sentence_line_updated.emit("")
		if is_instance_valid(player_node): player_node.hide_thought_bubble()
		return

	var line_text = ""
	var bubble_text = ""

	if current_verb_id != "":
		var verb_data = get_verb_data_by_id(current_verb_id)
		var verb_text = verb_data.display_text if verb_data else current_verb_id

		if current_selected_item_data != null:
			var item_colored = "[color=#33d9ff]%s[/color]" % current_selected_item_data.display_name
			line_text = "%s %s" % [verb_text, item_colored]
			bubble_text = "%s %s on what?" % [verb_text, item_colored]

			if hovered_interactable:
				var preposition = " to " if current_verb_id == "give" else " on "
				var obj_colored = "[color=#33d9ff]%s[/color]" % hovered_interactable.object_display_name
				line_text += preposition + obj_colored
			else:
				var preposition = " to..." if current_verb_id == "give" else " on..."
				line_text += preposition
		else:
			if current_verb_id == "give":
				if hovered_interactable:
					var obj_colored = "[color=#33d9ff]%s[/color]" % hovered_interactable.object_display_name
					line_text = "Give what to %s?" % obj_colored
				else:
					line_text = "Give what? (Select an item)"
				bubble_text = "Give what? (Select an item)"
			else:
				line_text = verb_text
				bubble_text = verb_text + " what?"
				if hovered_interactable:
					var obj_colored = "[color=#33d9ff]%s[/color]" % hovered_interactable.object_display_name
					line_text += " " + obj_colored

	elif hovered_interactable and hovered_interactable.interaction_location == Interactable.InteractionLocation.WORLD:
		var obj_colored = "[color=#33d9ff]%s[/color]" % hovered_interactable.object_display_name
		line_text = "Walk to " + obj_colored

	sentence_line_updated.emit(line_text)

	if is_verb_lock_active and is_instance_valid(player_node) and bubble_text != "" and OS.has_feature("mobile"):
		player_node.show_thought_bubble(bubble_text)
	elif is_instance_valid(player_node):
		player_node.hide_thought_bubble()

func process_interaction_click(interactable_node: Interactable):
	if is_transitioning: return
	if not is_instance_valid(interactable_node): return

	if current_verb_id != "" and current_selected_item_data != null:
		_initiate_interaction_flow(interactable_node, current_verb_id, current_selected_item_data)
	elif current_verb_id != "":
		_initiate_interaction_flow(interactable_node, current_verb_id, null)
	else:
		if interactable_node.interaction_location == Interactable.InteractionLocation.WORLD:
			_initiate_interaction_flow(interactable_node, "walk_to", null)

func _initiate_interaction_flow(interactable_node: Interactable, verb_to_use_id: String, item_data_to_use: ItemData):
	if not is_instance_valid(interactable_node):
		_complete_interaction_cycle(); return

	# --- QOL FIX: Prevent "Give" without an item ---
	if verb_to_use_id == "give" and item_data_to_use == null:
		cancel_current_action(false)

		if not DialogueManager.dialogue_ended.is_connected(restore_world_after_object_dialogue):
			DialogueManager.dialogue_ended.connect(restore_world_after_object_dialogue, CONNECT_ONE_SHOT)

		var generic_lines = preload("res://generic_lines.dialogue")
		DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, generic_lines, "give_no_item_selected")
		return
	# -----------------------------------------------

	if interactable_node.has_method("notify_interaction_pending"):
		interactable_node.notify_interaction_pending()

	if is_verb_lock_active:
		_activate_verb_lock(false)

	var walk_needed = true

	if interactable_node.interaction_location == Interactable.InteractionLocation.UI_OVERLAY:
		walk_needed = false
	else:
		if interactable_node.has_method("does_verb_require_walk"):
			walk_needed = interactable_node.does_verb_require_walk(verb_to_use_id, item_data_to_use)
		else:
			walk_needed = true

	var item_name_for_log = "None"
	if item_data_to_use: item_name_for_log = item_data_to_use.display_name

	if walk_needed:
		_is_player_walking = true

		# Force the UI to instantly suppress the action label upon clicking
		update_sentence_line_ui()

		if not is_instance_valid(player_node):
			_perform_actual_interaction(interactable_node, verb_to_use_id, item_data_to_use)
			return
		if player_node.has_method("walk_to_and_interact"):
			var walk_target_pos = interactable_node.get_walk_to_position()
			player_node.walk_to_and_interact(walk_target_pos, interactable_node, verb_to_use_id, item_data_to_use)
		else:
			_perform_actual_interaction(interactable_node, verb_to_use_id, item_data_to_use)
	else:
		if is_instance_valid(player_node) and player_node.has_method("face_target"):
			if interactable_node.interaction_location == Interactable.InteractionLocation.WORLD:
				player_node.face_target(interactable_node.global_position)
		_perform_actual_interaction(interactable_node, verb_to_use_id, item_data_to_use)

func player_has_finished_walk_command():
	_is_player_walking = false
	update_sentence_line_ui()

func player_reached_interaction_target(interactable_node: Interactable, verb_to_use_id: String, item_data_to_use: ItemData):
	if not is_instance_valid(interactable_node):
		_complete_interaction_cycle(); return
	_perform_actual_interaction(interactable_node, verb_to_use_id, item_data_to_use)

func _perform_actual_interaction(interactable_node: Interactable, verb_to_use_id: String, item_in_hand_data: ItemData = null):
	if not is_instance_valid(interactable_node):
		_complete_interaction_cycle(); return

	var item_name_for_log = "None"
	var item_id_for_interaction = ""
	if item_in_hand_data:
		item_name_for_log = item_in_hand_data.display_name
		item_id_for_interaction = item_in_hand_data.item_id

	# --- RECORD ACTION IN HISTORY LOG ---
	var verb_data = get_verb_data_by_id(verb_to_use_id)
	var verb_name = verb_data.display_text if verb_data else verb_to_use_id
	var obj_name = interactable_node.object_display_name
	var itm_name = item_in_hand_data.display_name if item_in_hand_data else ""
	DialogueHistory.add_action(verb_name, obj_name, itm_name)
	# ------------------------------------

	_disconnect_interactable_request_signals()

	_signals_connected_to_interactable = interactable_node

	if not interactable_node.interaction_processed.is_connected(_on_interactable_action_finished):
		interactable_node.interaction_processed.connect(_on_interactable_action_finished)

	interactable_node.attempt_interaction(verb_to_use_id, item_id_for_interaction)

# --- DialogueManager Signal Handlers (Global) ---
func _on_dialogue_started(_resource: Resource):
	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(false)

	# Hide the main UI whenever any dialogue line appears.
	# This handles both in-world dialogue and character conversations.
	_set_gameplay_ui_visible(false)

func restore_world_after_object_dialogue(_resource: Resource):
	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		if current_interaction_state == InteractionState.WORLD and current_game_state == GameState.IN_GAME_PLAY:
			player_node.set_can_move(true)

	# Restore the main UI as long as we are NOT in a full-screen conversation.
	# This now correctly handles both the WORLD and the ZOOM_VIEW states.
	if current_interaction_state != InteractionState.CONVERSATION and current_game_state == GameState.IN_GAME_PLAY:
		_set_gameplay_ui_visible(true)

	_complete_interaction_cycle()

func register_character_conversation(overlay: Node):
	_current_character_conversation_overlay_instance = overlay
	if overlay.has_signal("conversation_finished") and not overlay.conversation_finished.is_connected(_on_character_conversation_finished):
		overlay.conversation_finished.connect(_on_character_conversation_finished, CONNECT_ONE_SHOT)

func _on_character_conversation_finished(resource: DialogueResource):
	exit_to_world_state()

	# Do NOT queue_free the overlay here — it cleans itself up
	# via _destroy_and_clear_cache / _cleanup_and_queue_free.
	# Calling queue_free here races with the overlay's own cleanup
	# and prevents texture references from being properly nulled,
	# causing large textures to linger in VRAM until GC collects them.
	_current_character_conversation_overlay_instance = null

	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(true)

	_complete_interaction_cycle()

	character_conversation_ended.emit(resource)

# --- Interactable Signal Handlers ---
func _on_interactable_action_finished():
	_complete_interaction_cycle()

func _on_inventory_item_removed(item_id: String):
	if current_selected_item_data and current_selected_item_data.item_id == item_id:
		current_selected_item_data = null
		selected_inventory_item_changed.emit(null)

func _disconnect_interactable_request_signals():
	if is_instance_valid(_signals_connected_to_interactable):
		var node_to_disconnect_from = _signals_connected_to_interactable

		if node_to_disconnect_from.interaction_processed.is_connected(_on_interactable_action_finished):
			node_to_disconnect_from.interaction_processed.disconnect(_on_interactable_action_finished)
	_signals_connected_to_interactable = null

func _complete_interaction_cycle():
	_disconnect_interactable_request_signals()
	interaction_complete.emit()
	_activate_verb_lock(false)

	# --- STICKY VERB FIX ---
	if persisting_verb_id != "":
		current_verb_id = persisting_verb_id
		verb_changed.emit(current_verb_id)
	else:
		if current_verb_id != "":
			current_verb_id = ""
			verb_changed.emit("")
		else:
			current_verb_id = ""
	# -----------------------

	if current_selected_item_data:
		current_selected_item_data = null
		selected_inventory_item_changed.emit(null)

	# --- FIX START: Restore Player Control and UI ---
	# Since we removed the unfreeze logic from the individual Dialogue actions,
	# we must ensure the player is un-frozen here, at the absolute end of the chain.
	
	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		# Only unfreeze if we are in the normal gameplay state (not a full cutscene/zoom)
		if current_interaction_state == InteractionState.WORLD and current_game_state == GameState.IN_GAME_PLAY:
			player_node.set_can_move(true)

	# Restore UI visibility
	if current_interaction_state != InteractionState.CONVERSATION and current_game_state == GameState.IN_GAME_PLAY:
		_set_gameplay_ui_visible(true)

	update_sentence_line_ui()

func _set_gameplay_ui_visible(show: bool):
	if is_instance_valid(verb_ui): verb_ui.visible = show
	if is_instance_valid(inventory_ui): inventory_ui.visible = show
	if is_instance_valid(journal_button_ui): journal_button_ui.visible = show
	if is_instance_valid(insurance_form_button_ui):
		insurance_form_button_ui.visible = Flags.get_level_flag("insurance_button_unlocked") if show else false
	if is_instance_valid(patreon_world_ui):
		patreon_world_ui.visible = Flags.get_level_flag("dev_cta_completed") if show else false

func _activate_verb_lock(active: bool):
	is_verb_lock_active = active
	verb_lock_changed.emit(active)
	if active:
		if _scan_tween: _scan_tween.kill()
		_scan_active = false
		if is_instance_valid(player_node):
			player_node.set_can_move(false)
		if OS.has_feature("mobile"):
			for interactable in get_tree().get_nodes_in_group("interactables"):
				if is_instance_valid(interactable): interactable.force_highlight(true)
		if is_instance_valid(pause_menu_ui):
			pause_menu_ui.set_cancel_mode(true)
	else:
		if is_instance_valid(player_node):
			player_node.hide_thought_bubble()
			player_node.set_can_move(true)
		if OS.has_feature("mobile"):
			for interactable in get_tree().get_nodes_in_group("interactables"):
				if is_instance_valid(interactable): interactable.force_highlight(false)
		if is_instance_valid(pause_menu_ui):
			pause_menu_ui.set_cancel_mode(false)
			pause_menu_ui.set_scan_highlight(false)

func _on_scan_cancel_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	if is_verb_lock_active:
		cancel_current_action(false)
	else:
		if _scan_active:
			_cancel_scan()
		else:
			_scan_active = true
			if is_instance_valid(pause_menu_ui):
				pause_menu_ui.set_scan_highlight(true)

			for interactable in get_tree().get_nodes_in_group("interactables"):
				if is_instance_valid(interactable): interactable.force_highlight(true)

			if _scan_tween: _scan_tween.kill()
			_scan_tween = create_tween()
			_scan_tween.tween_interval(2.0)
			_scan_tween.tween_callback(_cancel_scan)

func is_scan_active() -> bool:
	return _scan_active

func _cancel_scan():
	_scan_active = false
	if _scan_tween: _scan_tween.kill()
	if is_instance_valid(pause_menu_ui):
		pause_menu_ui.set_scan_highlight(false)

	if not (is_verb_lock_active and OS.has_feature("mobile")):
		for interactable in get_tree().get_nodes_in_group("interactables"):
			if is_instance_valid(interactable): interactable.force_highlight(false)

# --- Verb Data and Availability ---

func get_verb_data_by_id(verb_id_to_find: String) -> VerbData:
	for verb_data_res in all_verb_data_resources:
		if verb_data_res and verb_data_res.verb_id == verb_id_to_find:
			return verb_data_res
	return null

func get_currently_displayable_verbs() -> Array[VerbData]:
	var displayable_verbs: Array[VerbData] = []
	for verb_data_res in all_verb_data_resources:
		if verb_data_res and verb_data_res.verb_id in unlocked_verb_ids:
			if active_scene_verb_ids.is_empty() or verb_data_res.verb_id in active_scene_verb_ids:
				displayable_verbs.append(verb_data_res)
	return displayable_verbs

func _emit_available_verbs_changed_update():
	available_verbs_changed.emit(get_currently_displayable_verbs())

func set_active_scene_verbs(verb_ids_for_scene: Array[String]):
	active_scene_verb_ids = verb_ids_for_scene.duplicate()
	_emit_available_verbs_changed_update()
	if current_verb_id != "" and not is_verb_id_currently_active(current_verb_id):
		select_verb("")

func unlock_verb(verb_id_to_unlock: String):
	var verb_data = get_verb_data_by_id(verb_id_to_unlock)
	if verb_data and not verb_id_to_unlock in unlocked_verb_ids:
		unlocked_verb_ids.append(verb_id_to_unlock)

		if not active_scene_verb_ids.is_empty() and not verb_id_to_unlock in active_scene_verb_ids:
			active_scene_verb_ids.append(verb_id_to_unlock)

		_emit_available_verbs_changed_update()
	elif not verb_data: print_rich("[color=red]GM: Tried to unlock non-existent verb: '%s'[/color]" % verb_id_to_unlock)
	elif verb_id_to_unlock in unlocked_verb_ids: print_rich("[color=yellow]GM: Verb '%s' already unlocked.[/color]" % verb_id_to_unlock)

func lock_verb(verb_id_to_lock: String):
	if verb_id_to_lock in unlocked_verb_ids:
		unlocked_verb_ids.erase(verb_id_to_lock)

		if verb_id_to_lock in active_scene_verb_ids:
			active_scene_verb_ids.erase(verb_id_to_lock)

		_emit_available_verbs_changed_update()
		if current_verb_id == verb_id_to_lock:
			select_verb("")
	else: print_rich("[color=orange]GM: Tried to lock verb '%s' that was not unlocked or doesn't exist.[/color]" % verb_id_to_lock)

func is_verb_id_currently_active(verb_id_to_check: String) -> bool:
	if not verb_id_to_check in unlocked_verb_ids: return false
	if active_scene_verb_ids.is_empty(): return true
	return active_scene_verb_ids.has(verb_id_to_check)

func force_clear_all_hovered_interactables():
	var interactables_to_clear = hovered_interactables.duplicate()
	hovered_interactables.clear()

	for interactable in interactables_to_clear:
		if is_instance_valid(interactable):
			interactable._on_mouse_exited()

	hovered_interactable = null
	if is_instance_valid(custom_cursor_instance):
		custom_cursor_instance.set_hover_state(false)
	update_sentence_line_ui()

func enter_conversation_state():
	if current_interaction_state == InteractionState.CONVERSATION: return
	current_interaction_state = InteractionState.CONVERSATION

	force_clear_all_hovered_interactables()

	# Show the blocker on layer 1 to stop clicks to the world (layer 0)
	if is_instance_valid(input_blocker_layer):
		input_blocker_layer.visible = true

	# Hide the game UI
	_set_gameplay_ui_visible(false)
	if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.hide()
	Events.interaction_state_changed.emit(current_interaction_state)

func enter_zoom_view_state():
	if current_interaction_state == InteractionState.ZOOM_VIEW: return
	current_interaction_state = InteractionState.ZOOM_VIEW

	force_clear_all_hovered_interactables()

	if is_instance_valid(input_blocker_layer):
		input_blocker_layer.visible = true
	if is_instance_valid(verb_ui):
		verb_ui.layer = 3
		verb_ui.visible = true
	if is_instance_valid(inventory_ui):
		inventory_ui.layer = 3
		inventory_ui.visible = true
	if is_instance_valid(patreon_world_ui):
		patreon_world_ui.visible = false

	if is_instance_valid(player_node):
		player_node.set_can_move(false)

	get_tree().paused = true
	Events.interaction_state_changed.emit(current_interaction_state)

func exit_to_world_state():
	current_interaction_state = InteractionState.WORLD

	if is_instance_valid(input_blocker_layer):
		input_blocker_layer.visible = false
	if is_instance_valid(verb_ui): verb_ui.layer = 1
	if is_instance_valid(inventory_ui): inventory_ui.layer = 1
	_set_gameplay_ui_visible(true)
	if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.show()

	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(true)

	get_tree().paused = false
	Events.interaction_state_changed.emit(current_interaction_state)

func _find_and_assign_ui_nodes():
	# Check if we even have a main scene to search in.
	if not is_instance_valid(main_game_scene_instance):
		return

	# Tell Godot to look INSIDE the main scene for these nodes using their Unique Scene Names.
	verb_ui = main_game_scene_instance.get_node_or_null("%VerbUI_CanvasLayer")
	inventory_ui = main_game_scene_instance.get_node_or_null("%InventoryUI_CanvasLayer")
	insurance_form_button_ui = main_game_scene_instance.get_node_or_null("%InsuranceFormButtonUI")
	journal_button_ui = main_game_scene_instance.get_node_or_null("%JournalButtonUI")
	input_blocker_layer = main_game_scene_instance.get_node_or_null("%InputBlockerLayer")
	explanation_layer = main_game_scene_instance.get_node_or_null("%ExplanationLayer")
	# pause_menu_ui is now spawned globally in GameManager._ready()

	if is_instance_valid(pause_menu_ui):
		if not pause_menu_ui.scan_cancel_requested.is_connected(_on_scan_cancel_pressed):
			pause_menu_ui.scan_cancel_requested.connect(_on_scan_cancel_pressed)

	if is_instance_valid(insurance_form_button_ui):
		if not insurance_form_button_ui.form_button_pressed.is_connected(_on_insurance_form_button_pressed):
			insurance_form_button_ui.form_button_pressed.connect(_on_insurance_form_button_pressed)

	if is_instance_valid(journal_button_ui):
		if not journal_button_ui.journal_button_pressed.is_connected(_on_journal_button_pressed):
			journal_button_ui.journal_button_pressed.connect(_on_journal_button_pressed)

	if is_instance_valid(explanation_layer):
		if not explanation_layer.explanation_finished.is_connected(exit_explanation_state):
			explanation_layer.explanation_finished.connect(exit_explanation_state)
		
# This function is called ONLY when the "Close Form" button is pressed.
func _on_insurance_form_closed():

	# Clean up our reference to the form instance. This is important.
	_insurance_form_instance = null

	# Return control to the player and un-pause the game.
	exit_to_world_state()

func start_explanation(data: ExplanationData, root_node_to_search: Node):
	if current_game_state == GameState.EXPLANATION or not is_instance_valid(explanation_layer):
		return

	change_game_state(GameState.EXPLANATION)

	var nodes_to_keep_visible = []
	if "exceptions_to_hide" in data:
		for node_path in data.exceptions_to_hide:
			var node = root_node_to_search.get_node_or_null(node_path)
			if is_instance_valid(node):
				nodes_to_keep_visible.append(node)

	if is_instance_valid(verb_ui) and not verb_ui in nodes_to_keep_visible:
		verb_ui.hide()

	if is_instance_valid(inventory_ui) and not inventory_ui in nodes_to_keep_visible:
		inventory_ui.hide()
		
	if is_instance_valid(journal_button_ui) and not journal_button_ui in nodes_to_keep_visible:
		journal_button_ui.hide()
	if is_instance_valid(pause_menu_ui) and not pause_menu_ui in nodes_to_keep_visible:
		pause_menu_ui.menu_panel.hide()

	if is_instance_valid(insurance_form_button_ui):
		if insurance_form_button_ui in nodes_to_keep_visible:
			insurance_form_button_ui.show()
		else:
			insurance_form_button_ui.hide()

	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(false)

	get_tree().paused = true
	explanation_layer.show_explanation(data, root_node_to_search)

func exit_explanation_state():
	if current_game_state != GameState.EXPLANATION:
		return

	get_tree().paused = false

	# Show the main game UI
	_set_gameplay_ui_visible(true)
	if is_instance_valid(pause_menu_ui): pause_menu_ui.menu_panel.show()

	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(true)

	change_game_state(GameState.IN_GAME_PLAY)

func _on_insurance_form_button_pressed():
	if is_instance_valid(_insurance_form_instance):
		return

	_insurance_form_instance = INSURANCE_FORM_SCENE.instantiate()

	_insurance_form_instance.form_closed.connect(_on_insurance_form_closed)

	get_tree().root.add_child(_insurance_form_instance)
	_insurance_form_instance.show()
	
	enter_conversation_state()
	get_tree().paused = true

# =========================================================
# RUN-STATE RESET
# Called once per New Game (from _on_main_menu_new_game_requested)
# so a second playthrough in the same session starts clean.
# Intentionally does NOT touch user preferences (text_speed,
# instant_text, dialogue_text_scale, is_auto_playing), the
# cached_* PackedScenes, or is_transitioning (TransitionLayer owns it).
# =========================================================
func reset_run_state():
	# --- Interaction / input transients ---
	_disconnect_interactable_request_signals()
	hovered_interactables.clear()
	hovered_interactable = null
	is_mouse_held_for_walk = false
	_potential_hold_walk = false
	_mouse_held_timer = 0.0
	_is_player_walking = false
	_scan_active = false
	if _scan_tween: _scan_tween.kill()
	current_interaction_state = InteractionState.WORLD
	_is_game_over_triggering = false
	_activate_verb_lock(false)

	# --- Verb & item selection ---
	persisting_verb_id = ""
	if current_verb_id != "":
		current_verb_id = ""
		verb_changed.emit("")
	if current_selected_item_data != null:
		current_selected_item_data = null
		selected_inventory_item_changed.emit(null)

	# --- Inventory ---
	Inventory.reset()

	# --- Global flags & dialogue memory ---
	Flags.reset_run_state()
	DialogueHistory.reset()

	# --- Hints & difficulty (difficulty select re-establishes these) ---
	Settings.assisted_mode = false
	current_unread_hint = ""
	last_read_hint = ""
	new_hint_available.emit(false)

	# --- Verbs: re-derive defaults exactly like _ready() does ---
	unlocked_verb_ids.clear()
	for verb_data_res in all_verb_data_resources:
		if verb_data_res and verb_data_res.unlocked_by_default and not verb_data_res.verb_id in unlocked_verb_ids:
			unlocked_verb_ids.append(verb_data_res.verb_id)
	active_scene_verb_ids = unlocked_verb_ids.duplicate()
	_emit_available_verbs_changed_update()

	# --- Sibling autoload run-state (Sergey conversation memory etc.) ---
	if ConversationEventManager and ConversationEventManager.has_method("reset_run_state"):
		ConversationEventManager.reset_run_state()

	# --- Stale references (their owning scenes were freed on quit) ---
	current_hint_manager = null
	_insurance_form_instance = null
	_journal_overlay_instance = null
	_current_character_conversation_overlay_instance = null

func _on_main_menu_new_game_requested():
	if is_instance_valid(transition_layer) and transition_layer.has_method("global_fade_to_black"):
		await transition_layer.global_fade_to_black(0.5)

	reset_run_state()

	change_game_state(GameState.DIFFICULTY_SELECT)

	if is_instance_valid(transition_layer) and transition_layer.has_method("global_fade_from_black"):
		transition_layer.global_fade_from_black(0.5)

func _on_difficulty_chosen(is_assisted: bool):
	Settings.assisted_mode = is_assisted
	Settings.save_settings()
	print_rich("[color=green]GM: Difficulty selected. Assisted Mode = %s[/color]" % str(Settings.assisted_mode))

	unlock_verb("think")

	if is_instance_valid(transition_layer):
		await transition_layer.play_iris_close()

	var difficulty_screen = get_tree().root.get_node_or_null("DifficultySelectScreen")
	if is_instance_valid(difficulty_screen):
		difficulty_screen.queue_free()

	change_game_state(GameState.INTRO_CONVERSATION)

	if is_instance_valid(transition_layer):
		await get_tree().create_timer(0.5).timeout
		transition_layer.play_iris_open()

func _on_main_menu_quit_requested():
	get_tree().quit()

func _start_intro_conversation():

	var intro_overlay_packed_scene = cached_intro_overlay_scene
	if not intro_overlay_packed_scene:
		# Fallback to loading from disk if the cache was somehow cleared
		intro_overlay_packed_scene = load(INTRO_OVERLAY_SCENE_PATH)
		if not intro_overlay_packed_scene:
			print_rich("[color=red]GM Error: Failed to load Intro Overlay Scene at path: %s[/color]" % INTRO_OVERLAY_SCENE_PATH)
			return

	var intro_overlay = intro_overlay_packed_scene.instantiate()
	intro_overlay.is_intro_sequence = true

	# Configure its exported variables from code.
	intro_overlay.dialogue_resource = INTRO_DIALOGUE

	# Connect to its 'conversation_finished' signal.
	intro_overlay.conversation_finished.connect(_on_intro_conversation_finished, CONNECT_ONE_SHOT)

	# Add it to the scene tree so it becomes visible and starts running.
	get_tree().root.add_child(intro_overlay)
	_intro_overlay_instance = intro_overlay

func _on_intro_conversation_finished(_dialogue_resource):
	if is_instance_valid(transition_layer):
		await transition_layer.play_iris_close()

	# AWAIT the state change so the scene actually loads before we try to lock things!
	await change_game_state(GameState.IN_GAME_PLAY)
	_intro_overlay_instance = null

	# --- PREVENT MOVEMENT & UI DURING DARKNESS ---
	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(false)
	_set_gameplay_ui_visible(false)

	if is_instance_valid(transition_layer):
		# Wait 3 full seconds in the dark to let the player hear the environment
		await get_tree().create_timer(3.0).timeout

		# Now open the eyes
		await transition_layer.play_iris_open()

	# --- RESTORE MOVEMENT & UI ---
	if is_instance_valid(player_node) and player_node.has_method("set_can_move"):
		player_node.set_can_move(true)
	_set_gameplay_ui_visible(true)

# --- JOURNAL FUNCTIONS ---
func _on_journal_button_pressed():
	if is_instance_valid(_journal_overlay_instance):
		return

	_journal_overlay_instance = JOURNAL_OVERLAY_SCENE.instantiate()
	_journal_overlay_instance.journal_closed.connect(_on_journal_closed)

	get_tree().root.add_child(_journal_overlay_instance)

	# Hide the main UI using the conversation state
	enter_conversation_state()
	get_tree().paused = true

func _on_journal_closed():
	if is_instance_valid(_journal_overlay_instance):
		_journal_overlay_instance.queue_free()
		_journal_overlay_instance = null
		
	# Restore UI and unpause
	exit_to_world_state()

func trigger_game_over(fade_duration: float = 1.5):
	if _is_game_over_triggering:
		return
	_is_game_over_triggering = true

	print_rich("[color=red]GM: Game Over Triggered![/color]")

	# Instantly hide the current dialogue balloon so the player knows their click registered!
	if is_instance_valid(_intro_overlay_instance) and "current_balloon" in _intro_overlay_instance and is_instance_valid(_intro_overlay_instance.current_balloon):
		_intro_overlay_instance.current_balloon.hide()

	# 1. Trigger the GLOBAL fade so it isn't destroyed during cleanup
	if is_instance_valid(transition_layer) and transition_layer.has_method("global_fade_to_black"):
		await transition_layer.global_fade_to_black(fade_duration)
	else:
		await get_tree().create_timer(fade_duration).timeout

	# 2. Change state to stop player input
	change_game_state(GameState.GAME_OVER)

	# 3. Stop all audio aggressively
	if SoundManager:
		if SoundManager.has_method("stop_music"): SoundManager.stop_music()
		if SoundManager.has_method("stop_all_ambience"): SoundManager.stop_all_ambience()

	# 4. Clean up the main game scene if it exists
	if is_instance_valid(main_game_scene_instance):
		main_game_scene_instance.queue_free()
		main_game_scene_instance = null

	# 5. Aggressively hunt down Overlays AND Dialogue Balloons to prevent crashes
	_cleanup_all_overlays()

	# 6. Spawn the Game Over Scene
	var game_over_instance = GAME_OVER_SCENE.instantiate()
	get_tree().root.add_child(game_over_instance)

func quit_to_main_menu_smooth():
	if SoundManager:
		SoundManager.stop_all_audio()
	_cleanup_all_overlays()

	if is_instance_valid(transition_layer) and transition_layer.has_method("global_fade_to_black"):
		await transition_layer.global_fade_to_black(1.0)

	current_interaction_state = InteractionState.WORLD

	change_game_state(GameState.MAIN_MENU)

	if is_instance_valid(transition_layer) and transition_layer.has_method("global_fade_from_black"):
		transition_layer.global_fade_from_black(1.0)

func clear_active_dialogue_balloons(node: Node = null):
	if node == null:
		node = get_tree().root

	for child in node.get_children():
		if "Balloon" in child.name or "conversationballoon" in child.name.to_lower():
			child.queue_free()
		else:
			clear_active_dialogue_balloons(child)

func _cleanup_all_overlays(node: Node = null):
	if node == null:
		node = get_tree().root

	for child in node.get_children():
		if child is CharacterConversationOverlay or child is AdvancedConversationOverlay:
			if "current_balloon" in child and is_instance_valid(child.current_balloon):
				child.current_balloon.queue_free()
			child.queue_free()
		elif child is ObjectZoomOverlay:
			child.queue_free()
		elif "MemoryBoxOverlay" in child.name:
			child.queue_free()
		elif "Balloon" in child.name or "conversationballoon" in child.name.to_lower():
			child.queue_free()
		elif "DialogueHistory" in child.name and child != DialogueHistory:
			child.queue_free()
		else:
			_cleanup_all_overlays(child)

func has_unread_hint() -> bool:
	return current_unread_hint != "" and current_unread_hint != last_read_hint

func refresh_hint_system():
	current_unread_hint = ""
	last_read_hint = ""

	if is_instance_valid(current_hint_manager):
		var evaluated_hint = current_hint_manager.evaluate_hint()
		if evaluated_hint != "":
			current_unread_hint = evaluated_hint
			if Settings.assisted_mode:
				new_hint_available.emit(true)
			else:
				new_hint_available.emit(false)
</file>

<file path="autoload/inventory.gd">
# Inventory.gd (Autoload) — the player's items + master item-data registry.
extends Node

signal inventory_updated(items: Array[ItemData])

var items: Array[ItemData] = []
var _item_data_map: Dictionary = {}


func setup(all_item_data_resources: Array[ItemData]):
	_item_data_map.clear()
	for item_data_res in all_item_data_resources:
		if item_data_res and item_data_res.item_id != "" and not _item_data_map.has(item_data_res.item_id):
			_item_data_map[item_data_res.item_id] = item_data_res


func add_item(item_id_to_add: String):
	var item_data = get_item_data_by_id(item_id_to_add)
	if not item_data:
		return
	if not item_data.is_stackable and has_item(item_id_to_add):
		return
	items.append(item_data)
	inventory_updated.emit(items.duplicate())
	Events.item_added.emit(item_id_to_add)
	Events.notification_requested.emit("Picked up: " + item_data.display_name)


func remove_item(item_id_to_remove: String):
	var item_data_ref = get_item_data_by_id(item_id_to_remove)
	if not item_data_ref:
		return
	for i in range(items.size() - 1, -1, -1):
		if items[i].item_id == item_id_to_remove:
			items.remove_at(i)
			inventory_updated.emit(items.duplicate())
			Events.item_removed.emit(item_id_to_remove)
			if not item_data_ref.is_stackable:
				break


func has_item(item_id_to_check: String) -> bool:
	for item_data_in_inv in items:
		if item_data_in_inv.item_id == item_id_to_check:
			return true
	return false


func get_item_data_by_id(item_id_to_find: String) -> ItemData:
	if _item_data_map.has(item_id_to_find):
		return _item_data_map[item_id_to_find]
	return null


func get_items() -> Array[ItemData]:
	return items.duplicate()


func is_empty() -> bool:
	return items.is_empty()


func reset():
	items.clear()
	inventory_updated.emit(items.duplicate())
</file>

<file path="autoload/NotificationManager.gd">
# NotificationManager.gd
extends CanvasLayer

const NOTIFICATION_SCENE = preload("res://Notification.tscn")

@onready var notification_container = $NotificationContainer
var active_notification: Node = null

func _ready():
	Events.notification_requested.connect(add_notification)

func add_notification(message: String):
	# Play the sound effect as soon as a notification is requested.
	SoundManager.play_sfx("notification_ping")

	# Check if we already have a notification on screen
	if is_instance_valid(active_notification):
		# Tell the active one to bump and change text
		active_notification.update_message(message)
		return

	# If no active notification exists, create a new one
	active_notification = NOTIFICATION_SCENE.instantiate()
	notification_container.add_child(active_notification)
	active_notification.show_message(message)
</file>

<file path="autoload/settings.gd">
# Settings.gd (Autoload) — user preferences + persistence (user://settings.cfg).
extends Node

signal auto_forward_toggled(is_on: bool)

const SETTINGS_FILE_PATH = "user://settings.cfg"

# --- Dialogue text ---
var text_speed: float = 0.02
var instant_text: bool = false
var dialogue_text_scale: float = 1.0
var auto_time_delay: float = 0.486
var is_auto_playing: bool = false:
	set(val):
		is_auto_playing = val
		auto_forward_toggled.emit(val)

# --- Gameplay ---
var assisted_mode: bool = false


func _ready():
	load_settings()


func set_bus_volume(bus_name: String, linear_val: float):
	var bus_idx = AudioServer.get_bus_index(bus_name)
	if bus_idx != -1:
		AudioServer.set_bus_volume_db(bus_idx, linear_to_db(linear_val))


func get_bus_volume(bus_name: String) -> float:
	var bus_idx = AudioServer.get_bus_index(bus_name)
	if bus_idx != -1:
		return db_to_linear(AudioServer.get_bus_volume_db(bus_idx))
	return 1.0


func save_settings():
	var cfg = ConfigFile.new()
	cfg.set_value("audio", "master", get_bus_volume("Master"))
	cfg.set_value("audio", "music", get_bus_volume("Music"))
	cfg.set_value("audio", "sfx", get_bus_volume("SFX"))
	cfg.set_value("dialogue", "text_speed", text_speed)
	cfg.set_value("dialogue", "instant_text", instant_text)
	cfg.set_value("dialogue", "text_scale", dialogue_text_scale)
	cfg.set_value("dialogue", "auto_forward", is_auto_playing)
	cfg.set_value("dialogue", "auto_delay", auto_time_delay)
	cfg.set_value("gameplay", "assisted_mode", assisted_mode)
	var err = cfg.save(SETTINGS_FILE_PATH)
	if err != OK:
		push_warning("Settings: Could not save settings to %s (error %d)" % [SETTINGS_FILE_PATH, err])


func load_settings():
	var cfg = ConfigFile.new()
	if cfg.load(SETTINGS_FILE_PATH) != OK:
		return
	set_bus_volume("Master", clampf(float(cfg.get_value("audio", "master", get_bus_volume("Master"))), 0.0, 1.0))
	set_bus_volume("Music", clampf(float(cfg.get_value("audio", "music", get_bus_volume("Music"))), 0.0, 1.0))
	set_bus_volume("SFX", clampf(float(cfg.get_value("audio", "sfx", get_bus_volume("SFX"))), 0.0, 1.0))
	text_speed = clampf(float(cfg.get_value("dialogue", "text_speed", text_speed)), 0.005, 0.05)
	instant_text = bool(cfg.get_value("dialogue", "instant_text", instant_text))
	dialogue_text_scale = clampf(float(cfg.get_value("dialogue", "text_scale", dialogue_text_scale)), 0.5, 1.5)
	is_auto_playing = bool(cfg.get_value("dialogue", "auto_forward", is_auto_playing))
	auto_time_delay = clampf(float(cfg.get_value("dialogue", "auto_delay", auto_time_delay)), 0.125, 1.75)
	assisted_mode = bool(cfg.get_value("gameplay", "assisted_mode", assisted_mode))
</file>

<file path="autoload/SoundManager.gd">
# SoundManager.gd
extends Node

# --- Sound Effect Library ---
var sfx_library: Dictionary = {
	# UI & Misc
	"ui_click": preload("res://Sfx/Game World/ui_click.wav"),
	"start_game": preload("res://Sfx/start.wav"),
	"notification_ping": preload("res://Sfx/Game World/notification_ping.wav"),
	"swish": preload("res://Sfx/Dialog/swish_effect.wav"),
	"form_correct_input": preload("res://Sfx/form_correct_input.mp3"),
	"form_incorrect_input": preload("res://Sfx/form_incorrect_input.mp3"),
	"mcbucket_scream": preload("res://Sfx/Dialog/mcbucket_scream.wav"),
	
	# Game World
	"door_close": preload("res://Sfx/Game World/scifi door close.mp3"),
	"door_open": preload("res://Sfx/Game World/scifi door open.mp3"),
	"hospital_toilet_flush": preload("res://Sfx/Game World/hospital_toilet_flush.mp3"),
	
	# Footsteps
	"step_1": preload("res://Sfx/Player/squeak_01.wav"), 
	"step_2": preload("res://Sfx/Player/squeak_02.wav"),
	"step_3": preload("res://Sfx/Player/squeak_03.wav"),

	# Ambience
	"room_tone_air": preload("res://Sfx/Game World/ambience_spaceship.mp3"),
	"room_tone_electric": preload("res://Sfx/Game World/ambience_fluoroscent_buzz.mp3"),
	"room_tone_traffic": preload("res://Sfx/Game World/ambience_distant_highway.mp3"),
	
	# Looping SFX
	"heavy_breathing": preload("res://Sfx/Dialog/heavy_breathing.mp3"),
	"scan_effect": preload("res://Sfx/Dialog/scan_effect.mp3")
}

# --- Music Library ---
var music_library: Dictionary = {
	"aida_theme": preload("res://Sfx/Music/aida_corporate_theme.mp3"),
	"unnatural_city": preload("res://Sfx/Music/Unnatural City.ogg"),
	"sergey_sad_theme": preload("res://Sfx/Music/sergey_sad_theme.mp3"),
	"mcbucket_normal_theme": preload("res://Sfx/Music/mcbucket_regular_theme.mp3"),
	"mcbucket_cannathink_theme": preload("res://Sfx/Music/mcbucket_cannathink_theme.mp3"),
	"sergey_hj_music":preload("res://Sfx/Music/snake_city_run_boy_run.mp3")
}

# A dedicated, persistent player for background music
var _music_player: AudioStreamPlayer = null
var _music_tween: Tween = null

# --- NEW: Looping SFX Tracking ---
var _looping_sfx_players: Dictionary = {}
var _looping_sfx_tweens: Dictionary = {}


# Store active ambience players so we can stop them
var _active_ambience_players: Array[AudioStreamPlayer] = []

# Variable to remember the previous footstep (for non-repeating logic)
var _last_footstep_key: String = ""

# --- Audio Ducking Variables ---
var _ambience_bus_index: int = -1
var _base_ambience_volume: float = 0.0


# --- Initialization ---
func _ready():
	process_mode = Node.PROCESS_MODE_ALWAYS
	# Capture the Ambience bus index and its default volume so we can restore it later
	_ambience_bus_index = AudioServer.get_bus_index("Ambience")
	if _ambience_bus_index != -1:
		_base_ambience_volume = AudioServer.get_bus_volume_db(_ambience_bus_index)

	# --- MOBILE AUDIO ENHANCEMENT ---
	if OS.has_feature("mobile"):
		var master_bus_idx = AudioServer.get_bus_index("Master")
		if master_bus_idx != -1:
			var current_vol = AudioServer.get_bus_volume_db(master_bus_idx)
			AudioServer.set_bus_volume_db(master_bus_idx, current_vol + 4.0)

			var compressor = AudioEffectCompressor.new()
			compressor.threshold = -15.0
			compressor.ratio = 3.0
			compressor.release_ms = 250.0
			AudioServer.add_bus_effect(master_bus_idx, compressor)

func _initialize_music_player():
	if is_instance_valid(_music_player):
		return

	_music_player = AudioStreamPlayer.new()
	add_child(_music_player)
	_music_player.process_mode = Node.PROCESS_MODE_ALWAYS
	_music_player.bus = "Main Music"


# --- SFX Functions ---
func play_sfx(sound_name: String, pitch: float = 1.0, volume_db: float = 0.0, bus_name: String = "SFX") -> AudioStreamPlayer:
	if not sfx_library.has(sound_name):
		print_rich("[color=red]SoundManager Error: Tried to play non-existent SFX: '%s'[/color]" % sound_name)
		return null 

	var player = AudioStreamPlayer.new()
	player.process_mode = Node.PROCESS_MODE_ALWAYS
	add_child(player)
	
	player.stream = sfx_library[sound_name]

	# Globally soften the UI click (higher pitch = lighter sound, lower volume = less aggressive)
	if sound_name == "ui_click":
		if pitch == 1.0: pitch = 1.2
		if volume_db == 0.0: volume_db = -6.0

	player.pitch_scale = pitch
	player.volume_db = volume_db
	player.bus = bus_name 
	player.play()
	
	player.finished.connect(player.queue_free)
	return player

func play_random_footstep():
	var all_steps = ["step_1", "step_2", "step_3"]
	var valid_steps = all_steps.duplicate()
	
	if _last_footstep_key != "" and _last_footstep_key in valid_steps:
		valid_steps.erase(_last_footstep_key)
	
	var chosen_step = valid_steps.pick_random()
	_last_footstep_key = chosen_step
	
	play_sfx(chosen_step, randf_range(1.2, 1.4), -18.0, "Footsteps")


# --- Ambience Functions ---
func play_ambience(sound_name: String, volume_db: float = 0.0):
	if not sfx_library.has(sound_name):
		print("SoundManager: Ambience '%s' not found." % sound_name)
		return

	var player = AudioStreamPlayer.new()
	player.process_mode = Node.PROCESS_MODE_ALWAYS
	add_child(player)
	
	player.stream = sfx_library[sound_name]
	player.volume_db = volume_db
	player.bus = "Ambience" 
	player.play()
	
	_active_ambience_players.append(player)

func stop_all_ambience():
	for player in _active_ambience_players:
		if is_instance_valid(player):
			player.stop()
			player.queue_free()
	_active_ambience_players.clear()

# Helper function to let the Tween system change the bus volume
func set_ambience_volume(vol: float):
	if _ambience_bus_index != -1:
		AudioServer.set_bus_volume_db(_ambience_bus_index, vol)


# --- Music Functions ---

# Instantly stops the currently playing music and resets the ambience volume
func stop_music():
	fade_out_music(0.0)

# Called via dialogue: do SoundManager.play_music_track("aida_theme", 3.0)
func play_music_track(track_name: String, fade_duration: float = 1.0):
	_initialize_music_player()
	
	if not music_library.has(track_name):
		print_rich("[color=red]SoundManager: Music track '%s' not found![/color]" % track_name)
		return

	var stream = music_library[track_name]
	
	# Don't restart if it's already playing the same track
	if _music_player.stream == stream and _music_player.playing:
		return
		
	if track_name == "unnatural_city":
		_music_player.bus = "Intro Music"
	else:
		_music_player.bus = "Main Music"

	_music_player.stream = stream
	
	# Stop any current fade tweens
	if _music_tween and _music_tween.is_valid():
		_music_tween.kill()
		
	var current_ambience_vol = AudioServer.get_bus_volume_db(_ambience_bus_index)
		
	if fade_duration > 0.0:
		_music_player.volume_db = -80.0
		_music_player.play()
		
		# Set parallel to true so the music fades UP while the ambience fades DOWN
		_music_tween = create_tween().set_parallel(true)
		_music_tween.tween_property(_music_player, "volume_db", -5.0, fade_duration).set_trans(Tween.TRANS_SINE)
		_music_tween.tween_method(set_ambience_volume, current_ambience_vol, _base_ambience_volume - 30.0, fade_duration).set_trans(Tween.TRANS_SINE)
	else:
		_music_player.volume_db = -5.0
		_music_player.play()
		set_ambience_volume(_base_ambience_volume - 30.0)

# Called via dialogue: do SoundManager.fade_out_music(5.0)
func fade_out_music(fade_duration: float = 2.0):
	if not is_instance_valid(_music_player) or not _music_player.playing:
		return
		
	if _music_tween and _music_tween.is_valid():
		_music_tween.kill()

	var current_ambience_vol = AudioServer.get_bus_volume_db(_ambience_bus_index)

	if fade_duration > 0.0:
		# Set parallel to true so the music fades DOWN while the ambience fades UP
		_music_tween = create_tween().set_parallel(true)
		_music_tween.tween_property(_music_player, "volume_db", -80.0, fade_duration).set_trans(Tween.TRANS_SINE)
		_music_tween.tween_method(set_ambience_volume, current_ambience_vol, _base_ambience_volume, fade_duration).set_trans(Tween.TRANS_SINE)
		
		# Chain wait until both fades are done, then stop the player entirely
		_music_tween.chain().tween_callback(_music_player.stop)
	else:
		_music_player.stop()
		set_ambience_volume(_base_ambience_volume)

# --- Looping SFX Functions ---

# Called via dialogue: do SoundManager.play_looping_sfx("heavy_breathing", 2.0, 0.0)
func play_looping_sfx(sound_name: String, fade_duration: float = 1.0, target_volume_db: float = 0.0):
	if not sfx_library.has(sound_name):
		print_rich("[color=red]SoundManager: Looping SFX '%s' not found![/color]" % sound_name)
		return

	var player: AudioStreamPlayer
	
	# Check if it's already playing
	if _looping_sfx_players.has(sound_name) and is_instance_valid(_looping_sfx_players[sound_name]):
		player = _looping_sfx_players[sound_name]
		if player.playing: return # Already playing, do nothing
	else:
		# Create a new persistent player for this sound
		player = AudioStreamPlayer.new()
		player.process_mode = Node.PROCESS_MODE_ALWAYS
		player.bus = "looping sfx" # Ensure you create this bus in the Audio tab!
		add_child(player)
		player.stream = sfx_library[sound_name]
		_looping_sfx_players[sound_name] = player

	# Kill any existing fade tweens for this specific sound
	if _looping_sfx_tweens.has(sound_name) and _looping_sfx_tweens[sound_name].is_valid():
		_looping_sfx_tweens[sound_name].kill()

	var current_ambience_vol = AudioServer.get_bus_volume_db(_ambience_bus_index)

	# Start the fade in
	if fade_duration > 0.0:
		player.volume_db = -80.0
		player.play()
		
		# Set parallel to true so breathing fades UP while ambience fades DOWN
		var tween = create_tween().set_parallel(true)
		tween.tween_property(player, "volume_db", target_volume_db, fade_duration).set_trans(Tween.TRANS_SINE)
		tween.tween_method(set_ambience_volume, current_ambience_vol, _base_ambience_volume - 30.0, fade_duration).set_trans(Tween.TRANS_SINE)
		_looping_sfx_tweens[sound_name] = tween
	else:
		player.volume_db = target_volume_db
		player.play()
		set_ambience_volume(_base_ambience_volume - 30.0)

# Called via dialogue: do SoundManager.stop_looping_sfx("heavy_breathing", 3.0)
func stop_looping_sfx(sound_name: String, fade_duration: float = 1.0):
	if not _looping_sfx_players.has(sound_name) or not is_instance_valid(_looping_sfx_players[sound_name]):
		return

	var player = _looping_sfx_players[sound_name]

	if _looping_sfx_tweens.has(sound_name) and _looping_sfx_tweens[sound_name].is_valid():
		_looping_sfx_tweens[sound_name].kill()

	var current_ambience_vol = AudioServer.get_bus_volume_db(_ambience_bus_index)

	# Start the fade out
	if fade_duration > 0.0:
		# Set parallel to true so breathing fades DOWN while ambience fades UP
		var tween = create_tween().set_parallel(true)
		tween.tween_property(player, "volume_db", -80.0, fade_duration).set_trans(Tween.TRANS_SINE)
		tween.tween_method(set_ambience_volume, current_ambience_vol, _base_ambience_volume, fade_duration).set_trans(Tween.TRANS_SINE)
		
		# Clean up after fade finishes
		tween.chain().tween_callback(func(): _cleanup_looping_sfx(sound_name))
		_looping_sfx_tweens[sound_name] = tween
	else:
		_cleanup_looping_sfx(sound_name)
		set_ambience_volume(_base_ambience_volume)

# Internal helper to safely delete the audio player when it's done
func _cleanup_looping_sfx(sound_name: String):
	if _looping_sfx_players.has(sound_name):
		var player = _looping_sfx_players[sound_name]
		if is_instance_valid(player):
			player.stop()
			player.queue_free()
		_looping_sfx_players.erase(sound_name)
		_looping_sfx_tweens.erase(sound_name)

func stop_all_audio():
	stop_music()
	stop_all_ambience()
	for sound_name in _looping_sfx_players.keys():
		var player = _looping_sfx_players[sound_name]
		if is_instance_valid(player):
			player.stop()
			player.queue_free()
	_looping_sfx_players.clear()
	if _music_tween and _music_tween.is_valid():
		_music_tween.kill()
	for tween in _looping_sfx_tweens.values():
		if tween and tween.is_valid():
			tween.kill()
	_looping_sfx_tweens.clear()
	set_ambience_volume(_base_ambience_volume)
</file>

<file path="characters/aida.gd">
extends CharacterBody2D

const ROOM_THRESHOLD_Y: float = 1000.0 
var _was_in_main_room: bool = true 
var _is_interacting_with_me: bool = false

# --- References ---
@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var interactable_component: Interactable = $InteractionArea
@onready var movement_controller: WaypointMovement = $MovementController

# --- Timers ---
var _resume_walk_timer: Timer
var _patience_timer: Timer # NEW: Safety timer if player never arrives

func _process(_delta):
	# (Logic unchanged)
	var is_currently_in_main = global_position.y < ROOM_THRESHOLD_Y
	if is_currently_in_main != _was_in_main_room:
		_was_in_main_room = is_currently_in_main
		if GameManager:
			Flags.set_level_flag("aida_in_main_room", is_currently_in_main)

func _ready():
	# 1. Setup Resume Timer (Wait after talking)
	_resume_walk_timer = Timer.new()
	_resume_walk_timer.one_shot = true
	_resume_walk_timer.wait_time = 5.0
	add_child(_resume_walk_timer)
	_resume_walk_timer.timeout.connect(_on_resume_timer_timeout)

	# 2. NEW: Setup Patience Timer (Wait for player to arrive)
	_patience_timer = Timer.new()
	_patience_timer.one_shot = true
	_patience_timer.wait_time = 8.0 # Give player 8 seconds to walk to her
	add_child(_patience_timer)
	_patience_timer.timeout.connect(_on_patience_timeout)

	# 3. Connect signals
	if interactable_component:
		if not interactable_component.interaction_pending.is_connected(_on_interaction_pending):
			interactable_component.interaction_pending.connect(_on_interaction_pending)
		
		# NEW: Listen for actual start of interaction
		if not interactable_component.interaction_started.is_connected(_on_interaction_started):
			interactable_component.interaction_started.connect(_on_interaction_started)
			
		if not interactable_component.interaction_processed.is_connected(_on_interaction_finished):
			interactable_component.interaction_processed.connect(_on_interaction_finished)

	if GameManager:
		GameManager.interaction_complete.connect(_on_global_interaction_complete)
	
	if animation_player:
		animation_player.play("idle")

# --- SIGNAL HANDLERS ---

func _on_interaction_pending():
	print("Aida: Player clicked me. Pausing and waiting for arrival...")
	_is_interacting_with_me = true 
	
	# Stop Resume timer (we are busy now)
	_resume_walk_timer.stop()
	
	# Start Patience timer (If player doesn't arrive in 8s, we resume)
	_patience_timer.start()
	
	if movement_controller:
		movement_controller.pause_movement()
	
	if animation_player:
		animation_player.play("idle")

func _on_interaction_started():
	print("Aida: Player arrived! Interaction started. Stopping Patience timer.")
	# Player made it! We don't need to auto-resume anymore, the dialogue will handle it.
	_patience_timer.stop()

func _on_patience_timeout():
	# If this fires, the player clicked but never showed up.
	print("[color=orange]Aida: Player took too long to arrive. Resuming patrol.[/color]")
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_interaction_finished():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		print("Aida: Interaction processed, but in Conversation Overlay. Holding...")
		if not GameManager.character_conversation_ended.is_connected(_on_global_conversation_ended):
			GameManager.character_conversation_ended.connect(_on_global_conversation_ended, CONNECT_ONE_SHOT)
	else:
		_start_resume_countdown()

func _on_global_conversation_ended(_resource):
	_start_resume_countdown()

func _start_resume_countdown():
	visible = true
	modulate.a = 1.0
	print("Aida: Interaction done. Waiting 5 seconds before walking...")
	_resume_walk_timer.start()

func _on_resume_timer_timeout():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		return
		
	print("Aida: 5 seconds passed. Resuming patrol.")
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_global_interaction_complete():
	if _is_interacting_with_me:
		# Ensure patience timer is killed if we finished successfully
		_patience_timer.stop() 
		print("Aida: Global interaction complete. Starting resume countdown.")
		_is_interacting_with_me = false 
		_start_resume_countdown()
</file>

<file path="characters/CharacterProfile.gd">
extends Resource
class_name CharacterProfile

@export var actor_id: String = ""
@export var default_scale: float = 1.0
@export var expressions: Dictionary = {} # String (Emotion) -> String (res:// path)
</file>

<file path="characters/dread_entity.gd">
extends Control

var base_aura: TextureRect
var face_rect: TextureRect
var _base_pos: Vector2

var evil_face_tex: Texture2D
var crying_face_tex: Texture2D

func _ready():
	_base_pos = position

	# Load the textures (Paths can be adjusted in the inspector/editor if needed)
	evil_face_tex = load("res://Backgrounds/dread_evil_face.png")
	crying_face_tex = load("res://Backgrounds/dread_crying_face.png")

	# Procedurally generate a soft purple aura
	base_aura = TextureRect.new()
	var grad = Gradient.new()
	grad.offsets = PackedFloat32Array([0.0, 1.0])
	grad.colors = PackedColorArray([Color(0.5, 0.0, 0.8, 1.0), Color(0.5, 0.0, 0.8, 0.0)])
	var tex = GradientTexture2D.new()
	tex.gradient = grad
	tex.fill = GradientTexture2D.FILL_RADIAL
	tex.fill_from = Vector2(0.5, 0.5)
	tex.fill_to = Vector2(0.5, 0.0)
	tex.width = 300
	tex.height = 300
	base_aura.texture = tex
	base_aura.position = Vector2(-150, -150)
	add_child(base_aura)

	# Create the Face TextureRect
	face_rect = TextureRect.new()
	face_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	face_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	# Make the face smaller (200x200 instead of 400x400)
	face_rect.size = Vector2(200, 200)
	# Shift by exactly half the size (-100, -100) to keep it perfectly centered
	face_rect.position = Vector2(-100, -100)
	face_rect.modulate.a = 0.0
	add_child(face_rect)

	# Breathing tween
	var tween = create_tween().set_loops()
	tween.tween_property(self, "scale", Vector2(1.1, 1.1), 1.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	tween.tween_property(self, "scale", Vector2(0.9, 0.9), 1.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

func _process(_delta):
	# Glitch Movement
	if randf() < 0.08:
		position = _base_pos + Vector2(randf_range(-40, 40), randf_range(-20, 20))
		base_aura.modulate = Color(0.8, 0.2, 1.0, randf_range(0.4, 1.0))
	else:
		position = position.lerp(_base_pos, 0.2)
		base_aura.modulate = Color(0.6, 0.1, 0.8, 0.8)

	# Face Flash
	if randf() < 0.02 and face_rect.modulate.a <= 0.1:
		# Randomly pick which face to flash
		if randf() > 0.5:
			face_rect.texture = evil_face_tex
		else:
			face_rect.texture = crying_face_tex

		face_rect.modulate.a = randf_range(0.5, 1.0)
		var flash_tween = create_tween()
		flash_tween.tween_property(face_rect, "modulate:a", 0.0, 0.3)
</file>

<file path="characters/hope_entity.gd">
extends Control

var sun_sprite: TextureRect

func _ready():
	sun_sprite = TextureRect.new()
	var grad = Gradient.new()
	grad.offsets = PackedFloat32Array([0.0, 0.4, 1.0])
	grad.colors = PackedColorArray([Color(1.0, 0.9, 0.6, 1.0), Color(1.0, 0.6, 0.7, 0.8), Color(1.0, 0.4, 0.6, 0.0)])
	var tex = GradientTexture2D.new()
	tex.gradient = grad
	tex.fill = GradientTexture2D.FILL_RADIAL
	tex.fill_from = Vector2(0.5, 0.5)
	tex.fill_to = Vector2(0.5, 0.0)
	tex.width = 400
	tex.height = 400
	sun_sprite.texture = tex
	sun_sprite.position = Vector2(-200, -200)
	sun_sprite.pivot_offset = Vector2(200, 200)
	add_child(sun_sprite)

	# Gentle pulsing glow
	var pulse_tween = create_tween().set_loops()
	pulse_tween.tween_property(sun_sprite, "scale", Vector2(1.15, 1.15), 2.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	pulse_tween.tween_property(sun_sprite, "scale", Vector2(0.85, 0.85), 2.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
</file>

<file path="characters/mcbucket.gd">
extends CharacterBody2D

enum State { IDLE, HIGH, INVIGIROL, SLEEPING }

const MCBUCKET_ADVANCED_OVERLAY = "res://mcbucket_advanced_overlay.tscn"

@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var interactable_component = $InteractionArea
@onready var movement_controller: WaypointMovement = $MovementController

# --- Interaction State Variables ---
var _is_interacting_with_me: bool = false
var _resume_walk_timer: Timer
var _patience_timer: Timer 

func _ready():
	# 1. Setup Resume Timer (Wait after talking)
	_resume_walk_timer = Timer.new()
	_resume_walk_timer.one_shot = true
	_resume_walk_timer.wait_time = 5.0
	add_child(_resume_walk_timer)
	_resume_walk_timer.timeout.connect(_on_resume_timer_timeout)

	# 2. Setup Patience Timer (Wait for player to arrive)
	_patience_timer = Timer.new()
	_patience_timer.one_shot = true
	_patience_timer.wait_time = 8.0 
	add_child(_patience_timer)
	_patience_timer.timeout.connect(_on_patience_timeout)

	# 3. Connect signals
	if interactable_component:
		if not interactable_component.interaction_pending.is_connected(_on_interaction_pending):
			interactable_component.interaction_pending.connect(_on_interaction_pending)
		if not interactable_component.interaction_started.is_connected(_on_interaction_started):
			interactable_component.interaction_started.connect(_on_interaction_started)
		if not interactable_component.interaction_processed.is_connected(_on_interaction_finished):
			interactable_component.interaction_processed.connect(_on_interaction_finished)

	if GameManager:
		GameManager.interaction_complete.connect(_on_global_interaction_complete)

	# Programmatically add the "Give Toilet Paper" interaction
	if interactable_component:
		# 1. Response for when McBucket is SLEEPING
		var sleep_response = InteractionResponse.new()
		sleep_response.verb_id = "give"
		sleep_response.required_item_id = "hospital_toilet_paper"
		sleep_response.required_flag_id = "mcbucket_zanopram_used"
		sleep_response.required_flag_value = true

		var sleep_dialogue_action = ShowCustomDialogueAction.new()
		sleep_dialogue_action.dialogue_resource = load("res://mcbucket_give_tp.dialogue")
		sleep_dialogue_action.dialogue_checkpoint = "sleeping"
		sleep_response.actions_to_perform.append(sleep_dialogue_action)

		# 2. Response for when McBucket is AWAKE
		var give_tp_response = InteractionResponse.new()
		give_tp_response.verb_id = "give"
		give_tp_response.required_item_id = "hospital_toilet_paper"
		give_tp_response.required_flag_id = "mcbucket_zanopram_used"
		give_tp_response.required_flag_value = false

		var give_tp_action = ShowCustomDialogueAction.new()
		give_tp_action.dialogue_resource = load("res://mcbucket_give_tp.dialogue")
		give_tp_action.dialogue_checkpoint = "start"

		var remove_tp_action = RemoveItemAction.new()
		remove_tp_action.item_id_to_remove = "hospital_toilet_paper"

		# Append dialogue first, then remove item
		give_tp_response.actions_to_perform.append(give_tp_action)
		give_tp_response.actions_to_perform.append(remove_tp_action)

		# Add both to the interactable component
		interactable_component.interactions.append(sleep_response)
		interactable_component.interactions.append(give_tp_response)

	# 4. Check State
	await get_tree().process_frame
	if not GameManager: return

	if Flags.get_level_flag("mcbucket_cannathink_used"):
		change_state(State.HIGH)
	elif Flags.get_level_flag("mcbucket_invigirol_used"):
		change_state(State.INVIGIROL)
	elif Flags.get_level_flag("mcbucket_zanopram_used"):
		change_state(State.SLEEPING)
	else:
		change_state(State.IDLE)

func change_state(new_state: State):
	if not animation_player or not interactable_component:
		push_warning("McBucket script is missing node references!")
		return

	if GameManager:
		Flags.set_level_flag("mcbucket_cannathink_used", new_state == State.HIGH)
		Flags.set_level_flag("mcbucket_invigirol_used", new_state == State.INVIGIROL)
		Flags.set_level_flag("mcbucket_zanopram_used", new_state == State.SLEEPING)

	match new_state:
		State.IDLE:
			animation_player.play("idle")
			interactable_component.character_conversation_scene_path = MCBUCKET_ADVANCED_OVERLAY
		State.HIGH:
			animation_player.play("high")
			interactable_component.character_conversation_scene_path = MCBUCKET_ADVANCED_OVERLAY
		State.INVIGIROL:
			animation_player.play("invigirol", -1, 4.0)
			interactable_component.character_conversation_scene_path = MCBUCKET_ADVANCED_OVERLAY
		State.SLEEPING:
			animation_player.play("sleeping")
			interactable_component.character_conversation_scene_path = "" # Cannot talk to him


# --- SIGNAL HANDLERS ---

func _on_interaction_pending():
	_is_interacting_with_me = true 
	_resume_walk_timer.stop()
	_patience_timer.start()
	if movement_controller:
		movement_controller.pause_movement()

func _on_interaction_started():
	_patience_timer.stop()

func _on_patience_timeout():
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_interaction_finished():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		if not GameManager.character_conversation_ended.is_connected(_on_global_conversation_ended):
			GameManager.character_conversation_ended.connect(_on_global_conversation_ended, CONNECT_ONE_SHOT)
	else:
		_start_resume_countdown()

func _on_global_conversation_ended(_resource):
	_start_resume_countdown()

func _start_resume_countdown():
	visible = true
	modulate.a = 1.0
	_resume_walk_timer.start()

func _on_resume_timer_timeout():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		return
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_global_interaction_complete():
	if _is_interacting_with_me:
		_patience_timer.stop()
		_is_interacting_with_me = false
		_start_resume_countdown()


func react_to_tv(is_tv_off: bool):
	if not animation_player: return

	if GameManager:
		if Flags.get_level_flag("mcbucket_cannathink_used") or \
		   Flags.get_level_flag("mcbucket_invigirol_used") or \
		   Flags.get_level_flag("mcbucket_zanopram_used"):
			return

	if is_tv_off:
		animation_player.play("invigirol", -1, 4.0)
	else:
		animation_player.play("idle")
</file>

<file path="characters/npc.gd">
extends Area2D

var player_in_zone := false

func _ready():
	set_process(true)  # Enables the _process() function

func _on_body_entered(body):
	if body.is_in_group("player"):
		player_in_zone = true
		print("Player has entered NPC interaction zone!")

func _on_body_exited(body):
	if body.is_in_group("player"):
		player_in_zone = false
		print("Player has left NPC interaction zone.")

func _process(delta):
	if player_in_zone and Input.is_action_just_pressed("talk_to_npc"):
		var dlg = load("res://testdialogue.dialogue")
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", dlg, "start")
</file>

<file path="characters/player.gd">
extends CharacterBody2D

const SPEED = 700.0
const GRAVITY = 800.0
const WALK_TO_THRESHOLD_X = 5.0
const INTERACTION_OFFSET_X = 30.0

# Variable to hold the calculated safe stopping distance
var player_half_width: float = 65.0 

@onready var sprite_2d: Sprite2D = $Sprite
@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var collision_shape_2d: CollisionShape2D = $CollisionShape2D
@onready var thought_bubble_pivot = $ThoughtBubblePivot
@onready var thought_bubble_pos = $ThoughtBubblePos
@onready var prompt_label = $ThoughtBubblePivot/ThoughtBubble/MarginContainer/PromptLabel
var _thought_bounce_tween: Tween

var current_animation_state = "idle"

var _is_walking_to_target: bool = false
var _actual_walk_destination: Vector2
var _start_walk_position_x: float = 0.0
var _interactable_after_walk: Interactable = null
var _verb_for_interaction: String = ""
var _item_for_interaction: ItemData = null
var _can_move: bool = true
var _stuck_timer: float = 0.0 # To track how long we've been stuck
var _is_manual_walking: bool = false

func _ready():
	#if not sprite_2d: print_rich("[color=red]Player: Sprite2D node not found![/color]")
	#if not animation_player: print_rich("[color=red]Player: AnimationPlayer node not found![/color]")
	
	# --- AUTO-CALCULATE WIDTH ---
	# This makes the wall-stopping logic screen-size and scale independent.
	if collision_shape_2d and collision_shape_2d.shape is RectangleShape2D:
		var shape_w = collision_shape_2d.shape.size.x
		# Formula: (Shape Width / 2) * Object Scale + Buffer
		player_half_width = (shape_w * global_scale.x / 2.0) + 15.0
		#print("Player: Auto-calculated stopping distance: ", player_half_width)
	else:
		pass
		#print_rich("[color=yellow]Player: Could not calc width (Shape missing or not Rectangle). Using default 65.0[/color]")
	# ----------------------------
	
	play_animation("idle")

	thought_bubble_pivot.visible = false
	var bubble_style = StyleBoxFlat.new()
	bubble_style.bg_color = Color(0, 0, 0, 0.8)
	bubble_style.border_width_left = 3
	bubble_style.border_width_top = 3
	bubble_style.border_width_right = 3
	bubble_style.border_width_bottom = 3
	bubble_style.border_color = Color(1, 1, 1, 1)
	bubble_style.corner_radius_top_left = 20
	bubble_style.corner_radius_top_right = 20
	bubble_style.corner_radius_bottom_left = 20
	bubble_style.corner_radius_bottom_right = 20
	$ThoughtBubblePivot/ThoughtBubble.add_theme_stylebox_override("panel", bubble_style)
	prompt_label.add_theme_font_override("normal_font", preload("res://Fonts/VarelaRound-Regular.ttf"))
	prompt_label.add_theme_font_size_override("normal_font_size", 44)
	prompt_label.fit_content = true
	prompt_label.autowrap_mode = TextServer.AUTOWRAP_OFF

func _physics_process(delta: float):
	if not _can_move:
		if is_instance_valid(animation_player) and current_animation_state == "walk":
			set_animation_state("idle")
		velocity.x = 0
		if not is_on_floor():
			velocity.y += GRAVITY * delta
		move_and_slide()
		return

	# --- UNIFIED MANUAL MOVEMENT (Keyboard & Mouse Hold) ---
	var manual_direction = Input.get_axis("ui_left", "ui_right")

	if GameManager and GameManager.is_mouse_held_for_walk:
		var mouse_x = get_global_mouse_position().x
		var dist = abs(mouse_x - global_position.x)

		# HYSTERESIS:
		# If she is already walking, she won't stop until she gets very close to the cursor (20px).
		# If she is standing still, she won't start until the cursor is further away (150px).
		var active_deadzone = 20.0 if _is_manual_walking else 150.0

		if dist > active_deadzone:
			manual_direction = sign(mouse_x - global_position.x)

	if manual_direction != 0:
		if _is_walking_to_target:
			_stop_walking() # Override the point-and-click click

		_is_manual_walking = true
		velocity.x = manual_direction * SPEED

		if not is_on_floor():
			velocity.y += GRAVITY * delta

		if is_instance_valid(sprite_2d): sprite_2d.flip_h = (velocity.x < 0)

		move_and_slide()

		if abs(get_real_velocity().x) < 10.0:
			set_animation_state("idle")
		else:
			set_animation_state("walk")
		return

	elif _is_manual_walking:
		# She reached the 20px inner deadzone OR the player let go of the mouse. Smooth stop.
		_is_manual_walking = false
		velocity.x = 0
		set_animation_state("idle")

		if not is_on_floor():
			velocity.y += GRAVITY * delta

		move_and_slide()
		return
	# -------------------------------------------------------

	if _is_walking_to_target:
		var direction_to_destination = global_position.direction_to(_actual_walk_destination)
		var x_distance_to_destination = abs(global_position.x - _actual_walk_destination.x)

		# --- STUCK FAILSAFE ---
		if abs(get_real_velocity().x) < 10.0:
			_stuck_timer += delta
		else:
			_stuck_timer = 0.0

		if _stuck_timer > 0.2:
			_stop_walking()
			return
		# ----------------------

		# Calculate exactly how far we will move this frame
		var step_distance = SPEED * delta

		# If the distance to the target is larger than our step distance PLUS the threshold, keep walking
		if x_distance_to_destination > (step_distance + WALK_TO_THRESHOLD_X):
			velocity.x = sign(_actual_walk_destination.x - global_position.x) * SPEED

			if not is_on_floor():
				velocity.y += GRAVITY * delta

			if is_instance_valid(sprite_2d): sprite_2d.flip_h = (velocity.x < 0)
			set_animation_state("walk")

		else: # Reached destination (We are close enough that the next step would overshoot)
			_stop_walking()

			if is_instance_valid(_interactable_after_walk):
				face_target(_interactable_after_walk.global_position)
				var interactable_ref = _interactable_after_walk
				var verb_ref = _verb_for_interaction
				var item_ref = _item_for_interaction

				_interactable_after_walk = null
				_verb_for_interaction = ""
				_item_for_interaction = null

				if GameManager and GameManager.has_method("player_reached_interaction_target"):
					GameManager.player_reached_interaction_target(interactable_ref, verb_ref, item_ref)
		
		move_and_slide()
		return

	# Idle Physics
	if not is_on_floor():
		velocity.y += GRAVITY * delta

	velocity.x = 0

	if not _is_walking_to_target and current_animation_state == "walk":
		set_animation_state("idle")

	move_and_slide()

# Helper to cleanly stop walking
func _stop_walking():
	_is_walking_to_target = false
	_stuck_timer = 0.0
	velocity = Vector2.ZERO
	set_animation_state("idle")
	if GameManager and GameManager.has_method("player_has_finished_walk_command"):
		GameManager.player_has_finished_walk_command()

func walk_to_point(destination_pos: Vector2):
	if not _can_move: return

	_interactable_after_walk = null
	_verb_for_interaction = ""
	_item_for_interaction = null
	_stuck_timer = 0.0

	var space_state = get_world_2d().direct_space_state
	var query = PhysicsRayQueryParameters2D.create(global_position, destination_pos)
	query.exclude = [self.get_rid()]
	
	var result = space_state.intersect_ray(query)

	if result:
		#print_rich("[color=green]Hit Wall at %s. Adjusting destination.[/color]" % result.position)
		var direction_back = (global_position - result.position).normalized()
		
		# --- DYNAMIC STOPPING DISTANCE ---
		_actual_walk_destination = result.position + (direction_back * player_half_width)
		_actual_walk_destination.y = global_position.y
	else:
		_actual_walk_destination.x = destination_pos.x
		_actual_walk_destination.y = global_position.y 

	_start_walk_position_x = global_position.x
	_is_walking_to_target = true

func walk_to_and_interact(interactable_walk_to_point_pos: Vector2, interactable_node: Interactable, verb_id: String, item_data: ItemData):
	if not _can_move: return

	_interactable_after_walk = interactable_node
	_verb_for_interaction = verb_id
	_item_for_interaction = item_data
	_stuck_timer = 0.0

	var target_x = interactable_walk_to_point_pos.x

	# --- CUSTOM OFFSET LOGIC ---
	var current_offset = INTERACTION_OFFSET_X
	if verb_id == "flash":
		current_offset = 270.0
	# ---------------------------

	# --- APPROACH SIDE OVERRIDE ---
	var forced_side = 0
	if is_instance_valid(interactable_node) and "approach_side" in interactable_node:
		forced_side = interactable_node.approach_side

	if forced_side == 1:
		_actual_walk_destination.x = target_x - current_offset
	elif forced_side == 2:
		_actual_walk_destination.x = target_x + current_offset
	else:
		if global_position.x < target_x:
			_actual_walk_destination.x = target_x - current_offset
		else:
			_actual_walk_destination.x = target_x + current_offset
	# ------------------------------

	_actual_walk_destination.y = global_position.y

	_start_walk_position_x = global_position.x
	_is_walking_to_target = true

func set_can_move(value: bool):
	_can_move = value
	if not _can_move:
		_stop_walking()

func face_target(target_global_position: Vector2):
	if not is_instance_valid(sprite_2d): return
	if target_global_position.x > global_position.x + 1.0:
		sprite_2d.flip_h = false 
	elif target_global_position.x < global_position.x - 1.0: 
		sprite_2d.flip_h = true

func set_animation_state(new_state: String):
	if not is_instance_valid(animation_player): return
	if current_animation_state == new_state:
		if not animation_player.is_playing() and animation_player.has_animation(new_state):
			animation_player.play(new_state)
		return
	current_animation_state = new_state
	play_animation(new_state)

func play_animation(anim_name: String):
	if not is_instance_valid(animation_player): return
	if animation_player.has_animation(anim_name):
		animation_player.play(anim_name)
		
func show_thought_bubble(text: String):
	prompt_label.text = "[center]" + text + "[/center]"
	thought_bubble_pivot.visible = true

	if is_instance_valid(thought_bubble_pos):
		var target_x = thought_bubble_pos.position.x
		if sprite_2d and sprite_2d.flip_h:
			# Apply a manual offset to compensate for the character's visual center shifting when the sprite is flipped
			target_x = -target_x - (-10.0)

		thought_bubble_pivot.position = Vector2(target_x, thought_bubble_pos.position.y)

		if _thought_bounce_tween: _thought_bounce_tween.kill()
		_thought_bounce_tween = create_tween().set_loops()
		_thought_bounce_tween.tween_property(thought_bubble_pivot, "position:y", thought_bubble_pos.position.y - 10.0, 1.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
		_thought_bounce_tween.tween_property(thought_bubble_pivot, "position:y", thought_bubble_pos.position.y, 1.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

func hide_thought_bubble():
	thought_bubble_pivot.visible = false
	if _thought_bounce_tween: _thought_bounce_tween.kill()

var _last_step_time: int = 0
const STEP_COOLDOWN_MSEC: int = 350 # Time in milliseconds (increase to slow down sounds)

func on_footstep_frame():
	# 1. Check if we are moving
	if velocity.length() < 1.0:
		return

	# 2. Check Time Cooldown
	var current_time = Time.get_ticks_msec()
	if current_time - _last_step_time < STEP_COOLDOWN_MSEC:
		return # Too soon! Skip this sound.

	# 3. Play Sound & Reset Timer
	SoundManager.play_random_footstep()
	_last_step_time = current_time
</file>

<file path="characters/sergey.gd">
extends CharacterBody2D

@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var interactable_component = $InteractionArea
@onready var movement_controller: WaypointMovement = $MovementController

# --- Interaction State Variables ---
var _is_interacting_with_me: bool = false
var _resume_walk_timer: Timer
var _patience_timer: Timer

func _ready():
	animation_player.play("idle")

	# 1. Setup Resume Timer (Wait after talking)
	_resume_walk_timer = Timer.new()
	_resume_walk_timer.one_shot = true
	_resume_walk_timer.wait_time = 5.0
	add_child(_resume_walk_timer)
	_resume_walk_timer.timeout.connect(_on_resume_timer_timeout)

	# 2. Setup Patience Timer (Wait for player to arrive)
	_patience_timer = Timer.new()
	_patience_timer.one_shot = true
	_patience_timer.wait_time = 8.0 
	add_child(_patience_timer)
	_patience_timer.timeout.connect(_on_patience_timeout)

	# 3. Connect signals
	if interactable_component:
		if not interactable_component.interaction_pending.is_connected(_on_interaction_pending):
			interactable_component.interaction_pending.connect(_on_interaction_pending)
		if not interactable_component.interaction_started.is_connected(_on_interaction_started):
			interactable_component.interaction_started.connect(_on_interaction_started)
		if not interactable_component.interaction_processed.is_connected(_on_interaction_finished):
			interactable_component.interaction_processed.connect(_on_interaction_finished)

	if GameManager:
		GameManager.interaction_complete.connect(_on_global_interaction_complete)

# --- SIGNAL HANDLERS ---

func _on_interaction_pending():
	_is_interacting_with_me = true 
	_resume_walk_timer.stop()
	_patience_timer.start()
	if movement_controller:
		movement_controller.pause_movement()

func _on_interaction_started():
	_patience_timer.stop()

func _on_patience_timeout():
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_interaction_finished():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		if not GameManager.character_conversation_ended.is_connected(_on_global_conversation_ended):
			GameManager.character_conversation_ended.connect(_on_global_conversation_ended, CONNECT_ONE_SHOT)
	else:
		_start_resume_countdown()

func _on_global_conversation_ended(_resource):
	_start_resume_countdown()

func _start_resume_countdown():
	visible = true
	modulate.a = 1.0
	_resume_walk_timer.start()

func _on_resume_timer_timeout():
	if GameManager.current_interaction_state == GameManager.InteractionState.CONVERSATION:
		return
	_is_interacting_with_me = false
	if movement_controller:
		movement_controller.resume_movement()

func _on_global_interaction_complete():
	if _is_interacting_with_me:
		_patience_timer.stop() 
		_is_interacting_with_me = false 
		_start_resume_countdown()
</file>

<file path="characters/WaypointMovement.gd">
# WaypointMovement.gd
class_name WaypointMovement
extends Node

# --- Configuration ---
@export var enabled: bool = true
@export var movement_speed: float = 50.0
@export var wait_duration: float = 2.0 

@export var waypoints: Array[NodePath]

enum LoopType { ONE_SHOT, LOOP, PING_PONG }
@export var loop_type: LoopType = LoopType.LOOP

# --- Internal State ---
var _target_node: CharacterBody2D
var _animation_player: AnimationPlayer
var _sprite_2d: Sprite2D 
var _current_waypoint_index: int = 0
var _is_waiting: bool = false
var _ping_pong_direction: int = 1

var _wait_timer: Timer

func _ready():
	if not enabled or get_parent() == null or waypoints.is_empty():
		set_physics_process(false)
		return

	_target_node = get_parent()
	if not _target_node is CharacterBody2D:
		set_physics_process(false)
		return

	_animation_player = _target_node.get_node_or_null("AnimationPlayer")
	
	# Try to find the sprite
	_sprite_2d = _target_node.get_node_or_null("Sprite")
	if not _sprite_2d:
		_sprite_2d = _target_node.get_node_or_null("ObjectSprite")

	_wait_timer = Timer.new()
	_wait_timer.wait_time = wait_duration
	_wait_timer.one_shot = true
	add_child(_wait_timer)
	_wait_timer.timeout.connect(_on_wait_timer_timeout)

func _physics_process(delta: float):
	if _is_waiting:
		return

	var target_marker: Marker2D = get_node_or_null(waypoints[_current_waypoint_index])
	if not target_marker:
		set_physics_process(false)
		return

	var target_position = target_marker.global_position

	if _target_node.global_position.distance_to(target_position) < 5.0:
		_handle_arrival()
	else:
		_move_towards(target_position)

func _handle_arrival():
	_target_node.velocity = Vector2.ZERO 
	
	if _animation_player: 
		_animation_player.play("idle")

	_is_waiting = true
	_wait_timer.start()

	if loop_type == LoopType.PING_PONG:
		_current_waypoint_index += _ping_pong_direction
		if _current_waypoint_index >= waypoints.size() or _current_waypoint_index < 0:
			_ping_pong_direction *= -1
			_current_waypoint_index += _ping_pong_direction * 2
	else:
		_current_waypoint_index += 1

	if _current_waypoint_index >= waypoints.size():
		if loop_type == LoopType.ONE_SHOT:
			enabled = false
			set_physics_process(false)
			return
		elif loop_type == LoopType.LOOP:
			_current_waypoint_index = 0

func _move_towards(target_position: Vector2):
	var direction = _target_node.global_position.direction_to(target_position)
	_target_node.velocity = direction * movement_speed
	_target_node.move_and_slide()

	if _animation_player and _animation_player.current_animation != "walk":
		_animation_player.play("walk")

	if _sprite_2d:
		if _target_node.velocity.x > 0.1:
			_sprite_2d.flip_h = false 
		elif _target_node.velocity.x < -0.1:
			_sprite_2d.flip_h = true

func _on_wait_timer_timeout():
	_is_waiting = false 

# --- NEW PUBLIC FUNCTIONS FOR AIDA TO CONTROL ---
func pause_movement():
	set_physics_process(false) # Stops the _physics_process loop
	
	# Only pause the timer if it actually got created!
	if is_instance_valid(_wait_timer):
		_wait_timer.paused = true  
		
	if is_instance_valid(_target_node):
		_target_node.velocity = Vector2.ZERO
	if is_instance_valid(_animation_player):
		_animation_player.play("idle")

func resume_movement():
	# Only unpause the timer if it actually got created!
	if is_instance_valid(_wait_timer):
		_wait_timer.paused = false 
		
	# Only resume the physics loop if they actually have waypoints to walk to!
	if enabled and not waypoints.is_empty():
		set_physics_process(true)
	
# --- CUTSCENE CONTROL ---

# This function is a "Coroutine". We can 'await' it!
func move_to_position_async(target_pos: Vector2, stop_distance: float = 5.0, timeout: float = 8.0) -> void:
	pause_movement()
	
	#print_rich("[color=orange]AidaMove: START. From %s to %s. Timeout: %ss[/color]" % [_target_node.global_position, target_pos, timeout])
	
	var start_time = Time.get_ticks_msec()
	var arrived = false
	
	while not arrived:
		if not is_instance_valid(_target_node): return

		# --- DEBUGGING EVERY SECOND ---
		# We use modulo to print only occasionally, otherwise console floods
		if Time.get_ticks_msec() % 1000 < 20: 
			var dist = _target_node.global_position.distance_to(target_pos)
			#print("AidaMove: Dist: %.2f | Velocity: %s | TargetY: %.2f vs MyY: %.2f" % [dist, _target_node.velocity, target_pos.y, _target_node.global_position.y])

		# 1. CHECK TIMEOUT (The Safety Net)
		var elapsed = (Time.get_ticks_msec() - start_time) / 1000.0
		if elapsed > timeout:
			#print_rich("[color=red]AidaMove: TIMEOUT! Force teleporting.[/color]")
			_target_node.global_position = target_pos
			arrived = true
			break

		# 2. CALCULATE HORIZONTAL DISTANCE ONLY (The Logic Fix)
		# We only care about X distance, ignoring Y height differences
		var x_distance = abs(_target_node.global_position.x - target_pos.x)
		
		if x_distance <= stop_distance:
			arrived = true
			_target_node.velocity = Vector2.ZERO
			if _animation_player: _animation_player.play("idle")
			#print_rich("[color=green]AidaMove: Arrived at X coordinate.[/color]")
		else:
			# 3. MOVE HORIZONTALLY ONLY
			# Determine direction: 1.0 (Right) or -1.0 (Left)
			var direction_x = sign(target_pos.x - _target_node.global_position.x)
			
			# Apply velocity only to X. Leave Y alone (or apply gravity if needed)
			_target_node.velocity.x = direction_x * movement_speed
			_target_node.velocity.y = 0 # Or apply gravity here if she needs it
			
			_target_node.move_and_slide()
			
			if _animation_player and _animation_player.current_animation != "walk":
				_animation_player.play("walk")
			
			if _sprite_2d:
				if _target_node.velocity.x > 0.1: _sprite_2d.flip_h = false
				elif _target_node.velocity.x < -0.1: _sprite_2d.flip_h = true
		
		await get_tree().physics_frame
		
func set_target_waypoint_index(index: int):
	if waypoints.is_empty(): return
	
	# Clamp ensures we don't crash if you give a bad number
	_current_waypoint_index = clamp(index, 0, waypoints.size() - 1)
	#print("WaypointMovement: Manually reset target to waypoint index %s" % _current_waypoint_index)
</file>

<file path="conversation/AdvancedConversationOverlay.gd">
extends CanvasLayer
class_name AdvancedConversationOverlay

signal conversation_finished(dialogue_resource: DialogueResource)

@export var dialogue_resource: DialogueResource
@export var start_dialogue_id: String = "start"
@export var character_roster: Array[CharacterProfile] = []
@export var background_aliases: Dictionary = {} # String -> String (res:// path)
@export var cg_aliases: Dictionary = {} # String -> String (res:// path)
@export var mental_image_shader: ShaderMaterial

var active_actors: Dictionary = {}

var _scan_active: bool = false
var _scan_time: float = 0.0
var _scan_tween: Tween

var current_balloon: Node = null

# --- SHAKE VARIABLES ---
var _is_shaking: bool = false
var _shake_timer: float = 0.0
var _shake_strength: float = 10.0
var _shake_rng := RandomNumberGenerator.new()
var _is_persistent_shake: bool = false
var _ignore_next_got_dialogue_signal: bool = false
var is_intro_sequence: bool = false
var _camera_offset: Vector2 = Vector2.ZERO

@onready var actor_stage: Control = $ActorStage
@onready var darken_backdrop: ColorRect = $DarkenBackdrop
@onready var background_layer: TextureRect = $BackgroundLayer
@onready var cg_layer: TextureRect = $CGLayer
@onready var mental_image_layer: TextureRect = $MentalImageLayer
@onready var solid_background: ColorRect = $SolidBackground
@onready var cinematic_container: Control = $CinematicContainer
@onready var cinematic_bg: ColorRect = $CinematicBackground
@onready var cinematic_sprite: AnimatedSprite2D = $CinematicSprite
@onready var continue_button: Button = $CinematicContinueButton
@onready var fade_overlay: ColorRect = $FadeOverlay
var _intro_silhouette: TextureRect = null
var _spawned_entities: Dictionary = {}
var is_cinematic_lock_active: bool = false
var patreon_btn: Button = null
var _mental_image_tween: Tween

# --- PREDICTIVE PRELOADER ---
var _texture_cache: Dictionary = {}   # String path -> Texture2D
var _loading_paths: Dictionary = {}   # String path -> bool (request sent)
const LOOKAHEAD_DEPTH: int = 20


func _ready():
	if is_instance_valid(darken_backdrop):
		darken_backdrop.mouse_filter = Control.MOUSE_FILTER_STOP
	if is_instance_valid(solid_background):
		solid_background.mouse_filter = Control.MOUSE_FILTER_STOP

	current_balloon = DialogueManager.show_dialogue_balloon_scene(
		preload("res://conversationballoon.tscn"),
		dialogue_resource,
		start_dialogue_id,
		[self]
	)
	DialogueManager.dialogue_ended.connect(_on_dialogue_ended)
	DialogueManager.got_dialogue.connect(_on_got_dialogue)
	_shake_rng.randomize()

	if continue_button:
		continue_button.hide()
		if not continue_button.pressed.is_connected(_on_cinematic_continue_pressed):
			continue_button.pressed.connect(_on_cinematic_continue_pressed)

		var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
		continue_button.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
		continue_button.add_theme_font_override("font", custom_font)
		continue_button.add_theme_font_size_override("font_size", 46 if OS.has_feature("mobile") else 32)

		var btn_normal = StyleBoxFlat.new()
		btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
		btn_normal.corner_radius_top_left = 6
		btn_normal.corner_radius_top_right = 6
		btn_normal.corner_radius_bottom_left = 6
		btn_normal.corner_radius_bottom_right = 6
		btn_normal.content_margin_left = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_right = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_top = 25 if OS.has_feature("mobile") else 15
		btn_normal.content_margin_bottom = 25 if OS.has_feature("mobile") else 15

		continue_button.reset_size()

		# Anchor to Top-Left so it never covers the center scene or overflows
		continue_button.anchor_left = 0.0
		continue_button.anchor_right = 0.0
		continue_button.anchor_top = 0.0
		continue_button.anchor_bottom = 0.0
		continue_button.grow_horizontal = Control.GROW_DIRECTION_END
		continue_button.grow_vertical = Control.GROW_DIRECTION_END

		# Add safe padding from the absolute edges
		continue_button.offset_left = 50 if OS.has_feature("mobile") else 30
		continue_button.offset_top = 50 if OS.has_feature("mobile") else 30
		btn_normal.border_width_left = 2
		btn_normal.border_width_top = 2
		btn_normal.border_width_right = 2
		btn_normal.border_width_bottom = 2
		btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

		var btn_hover = btn_normal.duplicate()
		btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
		btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

		continue_button.add_theme_stylebox_override("normal", btn_normal)
		continue_button.add_theme_stylebox_override("hover", btn_hover)
		continue_button.add_theme_stylebox_override("focus", btn_hover)
		continue_button.add_theme_stylebox_override("pressed", btn_hover)

		continue_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
		continue_button.add_theme_color_override("font_hover_color", Color.WHITE)
		continue_button.add_theme_color_override("font_pressed_color", Color.WHITE)


func _process(delta: float):
	if _scan_active:
		_scan_time += delta
		# Creates a smooth, undulating figure-8 "wavy" movement instead of a violent shake
		var offset_x = sin(_scan_time * 4.0) * 4.0
		var offset_y = cos(_scan_time * 3.0) * 4.0
		cg_layer.position = Vector2(offset_x, offset_y)

	# --- Shake Logic ---
	if _is_shaking:
		if not _is_persistent_shake:
			_shake_timer -= delta
			if _shake_timer <= 0:
				_is_shaking = false
				offset = _camera_offset # Reset CanvasLayer offset

		if _is_shaking:
			var offset_x = _shake_rng.randf_range(-_shake_strength, _shake_strength)
			var offset_y = _shake_rng.randf_range(-_shake_strength, _shake_strength)
			offset = _camera_offset + Vector2(offset_x, offset_y) # Shake the entire CanvasLayer


func _on_dialogue_ended(resource: DialogueResource):
	# Guard: another balloon's dialogue ending must not tear down this overlay.
	if resource != dialogue_resource:
		return
	DialogueManager.dialogue_ended.disconnect(_on_dialogue_ended)
	if DialogueManager.got_dialogue.is_connected(_on_got_dialogue):
		DialogueManager.got_dialogue.disconnect(_on_got_dialogue)

	conversation_finished.emit(dialogue_resource)

	var tween = create_tween().set_parallel(true)
	tween.tween_property(darken_backdrop, "color:a", 0.0, 0.4).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	if is_instance_valid(background_layer):
		tween.tween_property(background_layer, "modulate:a", 0.0, 0.4).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	if is_instance_valid(actor_stage):
		tween.tween_property(actor_stage, "modulate:a", 0.0, 0.4).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	if is_instance_valid(cg_layer):
		tween.tween_property(cg_layer, "modulate:a", 0.0, 0.4).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	if is_instance_valid(mental_image_layer):
		tween.tween_property(mental_image_layer, "modulate:a", 0.0, 0.4).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)

	# Ensure the node deletes itself and clears texture cache
	tween.chain().tween_callback(self._destroy_and_clear_cache)


func get_profile(id: String) -> CharacterProfile:
	for profile in character_roster:
		if profile.actor_id == id:
			return profile
	return null


func actor_enter(actor_id: String, emotion: String, slot_name: String):
	var profile = get_profile(actor_id)
	if not profile:
		push_warning("Actor profile not found: " + actor_id)
		return

	var tex_path: String = profile.expressions.get(emotion, "")
	if tex_path.is_empty():
		push_warning("Emotion '" + emotion + "' not found for actor: " + actor_id)
		return

	var tex: Texture2D = await _get_texture_async(tex_path)
	if not tex:
		push_warning("Failed to load texture: " + tex_path)
		return

	var rect = TextureRect.new()
	rect.texture = tex

	# 1. Calculate the Dynamic Scale safely using floats
	var screen_height = float(get_viewport().get_visible_rect().size.y)
	var tex_size = tex.get_size()

	# Base scale makes the image exactly as tall as the screen
	var base_scale = screen_height / float(tex_size.y)
	var final_scale = base_scale * profile.default_scale

	rect.scale = Vector2(final_scale, final_scale)

	# 2. Set the pivot to the bottom-center of the UNSCALED image
	rect.pivot_offset = Vector2(tex_size.x / 2.0, tex_size.y)

	actor_stage.add_child(rect)
	active_actors[actor_id] = rect

	# 3. Find the target marker
	var marker_name = "Slot" + slot_name.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker:
		push_warning("Could not find slot marker: " + marker_name)
		marker = actor_stage.get_node("SlotCenter")

	# 4. Position the Rect so its pivot sits exactly on the marker
	# Since position is unscaled in Godot, we just subtract the raw pivot_offset
	var target_pos = marker.position - rect.pivot_offset

	# 5. Animation setup (Start 200 pixels lower and transparent)
	rect.position = Vector2(target_pos.x, target_pos.y + 200.0)
	rect.modulate.a = 0.0

	var tween = create_tween().set_parallel(true)
	tween.tween_property(rect, "position:y", target_pos.y, 0.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	tween.tween_property(rect, "modulate:a", 1.0, 0.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)


func actor_leave(actor_id: String) -> void:
	if not active_actors.has(actor_id):
		push_warning("AdvancedConversationOverlay: Actor '%s' not found." % actor_id)
		return

	var rect: TextureRect = active_actors[actor_id]
	active_actors.erase(actor_id)

	var tween = create_tween().set_parallel(true)
	tween.tween_property(rect, "position:y", rect.position.y + 200.0, 0.5)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	tween.tween_property(rect, "modulate:a", 0.0, 0.5)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	tween.chain().tween_callback(rect.queue_free)


func actor_change(actor_id: String, emotion: String) -> void:
	if not active_actors.has(actor_id):
		return

	var profile = get_profile(actor_id)
	if not profile:
		return

	var tex_path: String = profile.expressions.get(emotion, "")
	if not tex_path.is_empty():
		var tex: Texture2D = await _get_texture_async(tex_path)
		if tex:
			var rect: TextureRect = active_actors[actor_id]
			rect.texture = tex


func actor_effect(actor_id: String, effect_name: String) -> void:
	if not active_actors.has(actor_id):
		push_warning("AdvancedConversationOverlay: Actor '%s' not found." % actor_id)
		return

	var rect: TextureRect = active_actors[actor_id]

	if effect_name == "shock":
		var original_x = rect.position.x
		var base_s = rect.scale
		var peak_s = base_s * 1.05 # Scale up by 5% relatively

		var tween = create_tween()
		tween.tween_property(rect, "scale", peak_s, 0.05)
		tween.tween_property(rect, "position:x", original_x + 15.0, 0.025)
		tween.tween_property(rect, "position:x", original_x - 15.0, 0.05)
		tween.tween_property(rect, "position:x", original_x + 10.0, 0.025)
		tween.tween_property(rect, "position:x", original_x - 10.0, 0.025)
		tween.tween_property(rect, "position:x", original_x, 0.025)
		tween.tween_property(rect, "scale", base_s, 0.05)


func actor_walk_in(actor_id: String, emotion: String, slot_name: String, scale_modifier: float = 1.0):
	var profile = get_profile(actor_id)
	if not profile:
		push_warning("Actor profile not found: " + actor_id)
		return

	var tex_path: String = profile.expressions.get(emotion, "")
	if tex_path.is_empty():
		push_warning("Emotion '" + emotion + "' not found for actor: " + actor_id)
		return

	var tex: Texture2D = await _get_texture_async(tex_path)
	if not tex:
		push_warning("Failed to load texture: " + tex_path)
		return

	var rect = TextureRect.new()
	rect.texture = tex

	var screen_height = float(get_viewport().get_visible_rect().size.y)
	var tex_size = tex.get_size()
	var base_scale = screen_height / float(tex_size.y)
	var final_scale = base_scale * profile.default_scale * scale_modifier

	rect.scale = Vector2(final_scale, final_scale)
	rect.pivot_offset = Vector2(tex_size.x / 2.0, tex_size.y)

	actor_stage.add_child(rect)
	active_actors[actor_id] = rect

	var marker_name = "Slot" + slot_name.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker:
		push_warning("Could not find slot marker: " + marker_name)
		marker = actor_stage.get_node("SlotCenter")

	var target_pos = marker.position - rect.pivot_offset

	# Determine starting X position based on slot
	var start_x = target_pos.x
	if slot_name.to_lower() == "left":
		start_x = target_pos.x - 1500.0 # Start far off-screen left
	elif slot_name.to_lower() == "right":
		start_x = target_pos.x + 1500.0 # Start far off-screen right
	else:
		start_x = target_pos.x - 1500.0 # Default slide from left

	rect.position = Vector2(start_x, target_pos.y)
	rect.modulate.a = 1.0 # Fully visible, just off-screen

	var tween = create_tween()
	tween.tween_property(rect, "position:x", target_pos.x, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)


func actor_walk_out(actor_id: String, direction: String = "left"):
	if not active_actors.has(actor_id):
		return

	var rect: TextureRect = active_actors[actor_id]
	active_actors.erase(actor_id)

	var target_x = rect.position.x
	if direction.to_lower() == "right":
		target_x += 1500.0
	else:
		target_x -= 1500.0

	var tween = create_tween()
	tween.tween_property(rect, "position:x", target_x, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN)
	tween.chain().tween_callback(rect.queue_free)


func actor_move(actor_id: String, target_slot_name: String, duration: float = 0.5):
	if not active_actors.has(actor_id):
		push_warning("Cannot move actor, not found on stage: " + actor_id)
		return

	var rect: TextureRect = active_actors[actor_id]
	var target_base_pos: Vector2

	# Dynamically handle center-offsets so we don't need hardcoded markers in the scene
	if target_slot_name.to_lower() == "center_right":
		target_base_pos = Vector2(1200, 1080)
	elif target_slot_name.to_lower() == "center_left":
		target_base_pos = Vector2(720, 1080)
	else:
		# Fallback to standard markers for left, right, and center
		var marker_name = "Slot" + target_slot_name.capitalize()
		var marker = actor_stage.get_node_or_null(marker_name)
		if not marker:
			push_warning("Could not find slot marker: " + marker_name)
			marker = actor_stage.get_node("SlotCenter")
		target_base_pos = marker.position

	# Calculate the exact target position
	var target_pos = target_base_pos - rect.pivot_offset

	# Create a smooth tween to slide them over
	var tween = create_tween()
	tween.tween_property(rect, "position", target_pos, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)


func actor_flip(actor_id: String, is_flipped: bool = true):
	if not active_actors.has(actor_id):
		push_warning("Cannot flip actor, not found on stage: " + actor_id)
		return

	var rect: TextureRect = active_actors[actor_id]
	var current_scale = rect.scale

	# To flip a TextureRect, we make the X scale negative.
	# We use abs() to ensure we don't accidentally flip it twice if called repeatedly.
	if is_flipped:
		rect.scale = Vector2(-abs(current_scale.x), current_scale.y)
	else:
		rect.scale = Vector2(abs(current_scale.x), current_scale.y)


func actor_dash_in(actor_id: String, emotion: String, slot_name: String, scale_modifier: float = 1.0):
	var profile = get_profile(actor_id)
	if not profile:
		push_warning("Actor profile not found: " + actor_id)
		return

	var tex_path: String = profile.expressions.get(emotion, "")
	if tex_path.is_empty():
		push_warning("Emotion '" + emotion + "' not found for actor: " + actor_id)
		return

	var tex: Texture2D = await _get_texture_async(tex_path)
	if not tex:
		push_warning("Failed to load texture: " + tex_path)
		return

	var rect = TextureRect.new()
	rect.texture = tex

	var screen_height = float(get_viewport().get_visible_rect().size.y)
	var tex_size = tex.get_size()
	var base_scale = screen_height / float(tex_size.y)
	var final_scale = base_scale * profile.default_scale * scale_modifier

	rect.scale = Vector2(final_scale, final_scale)
	rect.pivot_offset = Vector2(tex_size.x / 2.0, tex_size.y)

	actor_stage.add_child(rect)
	active_actors[actor_id] = rect

	var marker_name = "Slot" + slot_name.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker:
		marker = actor_stage.get_node("SlotCenter")

	var target_pos = marker.position - rect.pivot_offset

	var start_x = target_pos.x
	if slot_name.to_lower() == "left":
		start_x = target_pos.x - 1500.0
	elif slot_name.to_lower() == "right":
		start_x = target_pos.x + 1500.0
	else:
		start_x = target_pos.x - 1500.0

	rect.position = Vector2(start_x, target_pos.y)
	rect.modulate.a = 1.0

	var tween = create_tween()
	# TRANS_BACK creates the "overshoot and settle" spring effect.
	# A shorter duration (0.4s) makes it feel fast and energetic.
	tween.tween_property(rect, "position:x", target_pos.x, 0.4).set_trans(Tween.TRANS_BACK).set_ease(Tween.EASE_OUT)

func actor_peek_in(actor_id: String, emotion: String, side: String = "right"):
	var profile = get_profile(actor_id)
	if not profile: return
	var tex_path: String = profile.expressions.get(emotion, "")
	if tex_path.is_empty(): return
	var tex: Texture2D = await _get_texture_async(tex_path)
	if not tex: return

	var rect = TextureRect.new()
	rect.texture = tex
	var screen_height = float(get_viewport().get_visible_rect().size.y)
	var tex_size = tex.get_size()
	var base_scale = screen_height / float(tex_size.y)
	var final_scale = base_scale * profile.default_scale

	rect.scale = Vector2(final_scale, final_scale)
	rect.pivot_offset = Vector2(tex_size.x / 2.0, tex_size.y)
	actor_stage.add_child(rect)
	active_actors[actor_id] = rect

	var marker_name = "Slot" + side.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker: marker = actor_stage.get_node("SlotCenter")

	var target_pos = marker.position - rect.pivot_offset

	if side.to_lower() == "right":
		rect.rotation_degrees = -12.0
		# Shift down by 250 pixels on the Y axis to hide the bottom cutoff
		rect.position = Vector2(target_pos.x + 800.0, target_pos.y + 250.0)
		rect.modulate.a = 1.0
		var tween = create_tween()
		tween.tween_property(rect, "position:x", target_pos.x + 350.0, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	else:
		rect.rotation_degrees = 12.0
		# Shift down by 250 pixels on the Y axis to hide the bottom cutoff
		rect.position = Vector2(target_pos.x - 800.0, target_pos.y + 250.0)
		rect.modulate.a = 1.0
		var tween = create_tween()
		tween.tween_property(rect, "position:x", target_pos.x - 350.0, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

func actor_enter_from_peek(actor_id: String, target_slot_name: String, duration: float = 0.5):
	if not active_actors.has(actor_id): return
	var rect: TextureRect = active_actors[actor_id]
	var marker_name = "Slot" + target_slot_name.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker: return

	var target_pos = marker.position - rect.pivot_offset
	var tween = create_tween().set_parallel(true)
	tween.tween_property(rect, "position", target_pos, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	tween.tween_property(rect, "rotation_degrees", 0.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

func hide_dialogue_ui():
	if current_balloon and current_balloon.has_method("set_ui_hidden"):
		current_balloon.set_ui_hidden(true)

func show_dialogue_ui():
	if current_balloon and current_balloon.has_method("set_ui_hidden"):
		current_balloon.set_ui_hidden(false)

var _original_actor_transforms = {}

func actor_focus(actor_id: String, zoom_modifier: float = 2.0, duration: float = 0.01, face_x_ratio: float = 0.5):
	if not active_actors.has(actor_id): return

	if _original_actor_transforms.is_empty():
		for id in active_actors:
			var r = active_actors[id]
			_original_actor_transforms[id] = {"pos": r.position, "scale": r.scale}

	var rect: TextureRect = active_actors[actor_id]
	actor_stage.move_child(rect, -1)

	var profile = get_profile(actor_id)
	var tex_size = rect.texture.get_size()
	var screen_size = get_viewport().get_visible_rect().size

	var base_scale = screen_size.y / float(tex_size.y)
	var target_scale_mag = base_scale * profile.default_scale * zoom_modifier
	var target_scale = Vector2(target_scale_mag, target_scale_mag)
	if rect.scale.x < 0: target_scale.x = -target_scale.x

	var local_face = Vector2(tex_size.x * face_x_ratio, tex_size.y * 0.2)
	var screen_focus_point = Vector2(screen_size.x * 0.5, screen_size.y * 0.35)
	var target_pos = screen_focus_point - rect.pivot_offset - (local_face - rect.pivot_offset) * target_scale

	var tween = create_tween().set_parallel(true)
	var is_smash_cut = duration <= 0.05

	if is_smash_cut:
		rect.scale = target_scale
		rect.position = target_pos
		rect.modulate = Color.WHITE
	else:
		tween.tween_property(rect, "scale", target_scale, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
		tween.tween_property(rect, "position", target_pos, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
		tween.tween_property(rect, "modulate", Color.WHITE, duration)

	if is_instance_valid(background_layer):
		tween.tween_property(background_layer, "modulate", Color(0.2, 0.2, 0.2, 1.0), duration)

	for id in active_actors:
		if id != actor_id:
			var other_rect = active_actors[id]
			if _original_actor_transforms.has(id):
				var orig = _original_actor_transforms[id]
				var target_other_scale = orig["scale"]

				if sign(other_rect.scale.x) != sign(target_other_scale.x):
					target_other_scale.x = -target_other_scale.x

				if is_smash_cut:
					other_rect.scale = target_other_scale
					other_rect.position = orig["pos"]
					other_rect.modulate = Color(1.0, 1.0, 1.0, 0.0)
				else:
					tween.tween_property(other_rect, "scale", target_other_scale, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
					tween.tween_property(other_rect, "position", orig["pos"], duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
					tween.tween_property(other_rect, "modulate", Color(1.0, 1.0, 1.0, 0.0), duration)

func actor_unfocus_all(duration: float = 0.2):
	var tween = create_tween().set_parallel(true)
	var is_smash_cut = duration <= 0.05

	if is_instance_valid(background_layer):
		tween.tween_property(background_layer, "modulate", Color.WHITE, duration)

	for id in active_actors:
		var rect: TextureRect = active_actors[id]
		if _original_actor_transforms.has(id):
			var orig = _original_actor_transforms[id]
			var target_scale = orig["scale"]
			if sign(rect.scale.x) != sign(target_scale.x):
				target_scale.x = -target_scale.x

			if is_smash_cut:
				rect.scale = target_scale
				rect.position = orig["pos"]
				rect.modulate = Color.WHITE
			else:
				tween.tween_property(rect, "scale", target_scale, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
				tween.tween_property(rect, "position", orig["pos"], duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
				tween.tween_property(rect, "modulate", Color.WHITE, duration)
		elif is_smash_cut:
			rect.modulate = Color.WHITE

	if is_smash_cut:
		_original_actor_transforms.clear()
	else:
		tween.chain().tween_callback(func(): _original_actor_transforms.clear())

func set_backdrop_darkness(alpha: float = 0.6, duration: float = 0.5):
	if not is_instance_valid(darken_backdrop): return
	var tween = create_tween()
	tween.tween_property(darken_backdrop, "color:a", alpha, duration)


func change_background(bg_name: String, transition: String = "fade", duration: float = 0.5):
	if bg_name.is_empty():
		background_layer.texture = null
		return

	var resolved_path: String = ""
	if background_aliases.has(bg_name):
		resolved_path = background_aliases[bg_name]
	elif ResourceLoader.exists(bg_name):
		resolved_path = bg_name

	var new_texture: Texture2D = null
	if not resolved_path.is_empty():
		new_texture = await _get_texture_async(resolved_path)

	if not new_texture:
		push_warning("Could not find background alias or path: " + bg_name)
		return

	if transition in ["slide_left", "slide_right"] and background_layer.texture != null:
		if SoundManager and SoundManager.has_method("play_sfx"):
			SoundManager.play_sfx("swish", 1.0, -5.0)

		var ghost = TextureRect.new()
		ghost.texture = background_layer.texture
		ghost.expand_mode = background_layer.expand_mode
		ghost.stretch_mode = background_layer.stretch_mode
		# Temporarily use Top-Left so the layout engine doesn't fight the position tween
		ghost.set_anchors_and_offsets_preset(Control.PRESET_TOP_LEFT)
		ghost.size = background_layer.size
		ghost.position = background_layer.position
		ghost.mouse_filter = Control.MOUSE_FILTER_IGNORE

		background_layer.get_parent().add_child(ghost)
		background_layer.get_parent().move_child(ghost, background_layer.get_index())

		background_layer.texture = new_texture
		background_layer.modulate.a = 1.0

		background_layer.set_anchors_and_offsets_preset(Control.PRESET_TOP_LEFT)
		background_layer.size = get_viewport().get_visible_rect().size

		var screen_width = background_layer.size.x
		var new_start_x = 0.0
		var old_end_x = 0.0

		if transition == "slide_left":
			new_start_x = -screen_width
			old_end_x = screen_width
		elif transition == "slide_right":
			new_start_x = screen_width
			old_end_x = -screen_width

		background_layer.position.x = new_start_x

		var tween = create_tween().set_parallel(true)
		tween.tween_property(background_layer, "position:x", 0.0, duration).set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)
		tween.tween_property(ghost, "position:x", old_end_x, duration).set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)

		tween.chain().tween_callback(ghost.queue_free)
		tween.tween_callback(func():
			if is_instance_valid(background_layer):
				background_layer.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
		)

	elif transition == "fade" and background_layer.texture != null and background_layer.modulate.a > 0.0:
		var ghost = TextureRect.new()
		ghost.texture = background_layer.texture
		ghost.expand_mode = background_layer.expand_mode
		ghost.stretch_mode = background_layer.stretch_mode
		ghost.set_anchors_preset(Control.PRESET_FULL_RECT)
		ghost.mouse_filter = Control.MOUSE_FILTER_IGNORE

		background_layer.get_parent().add_child(ghost)
		background_layer.get_parent().move_child(ghost, background_layer.get_index() + 1)

		background_layer.texture = new_texture
		background_layer.modulate.a = 1.0

		var tween = create_tween()
		tween.tween_property(ghost, "modulate:a", 0.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
		tween.tween_callback(ghost.queue_free)
	else:
		background_layer.texture = new_texture
		background_layer.position = Vector2.ZERO
		var tween = create_tween()
		tween.tween_property(background_layer, "modulate:a", 1.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)


func actor_show(actor_id: String, emotion: String, slot_name: String):
	var profile = get_profile(actor_id)
	if not profile:
		push_warning("Actor profile not found: " + actor_id)
		return

	var tex_path: String = profile.expressions.get(emotion, "")
	if tex_path.is_empty():
		push_warning("Emotion '" + emotion + "' not found for actor: " + actor_id)
		return

	var tex: Texture2D = await _get_texture_async(tex_path)
	if not tex:
		push_warning("Failed to load texture: " + tex_path)
		return

	var rect = TextureRect.new()
	rect.texture = tex

	var screen_height = float(get_viewport().get_visible_rect().size.y)
	var tex_size = tex.get_size()
	var base_scale = screen_height / float(tex_size.y)
	var final_scale = base_scale * profile.default_scale

	rect.scale = Vector2(final_scale, final_scale)
	rect.pivot_offset = Vector2(tex_size.x / 2.0, tex_size.y)

	actor_stage.add_child(rect)
	active_actors[actor_id] = rect

	var marker_name = "Slot" + slot_name.capitalize()
	var marker = actor_stage.get_node_or_null(marker_name)
	if not marker:
		marker = actor_stage.get_node("SlotCenter")

	rect.position = marker.position - rect.pivot_offset
	rect.modulate.a = 1.0


func actor_hide(actor_id: String) -> void:
	if not active_actors.has(actor_id):
		return
	var rect: TextureRect = active_actors[actor_id]
	active_actors.erase(actor_id)
	rect.queue_free()


func show_cg(cg_name: String, transition: String = "fade", duration: float = 0.5):
	var resolved_path: String = ""
	if cg_aliases.has(cg_name):
		resolved_path = cg_aliases[cg_name]
	elif ResourceLoader.exists(cg_name):
		resolved_path = cg_name

	var new_texture: Texture2D = null
	if not resolved_path.is_empty():
		new_texture = await _get_texture_async(resolved_path)

	if not new_texture:
		push_warning("Could not find CG alias or path: " + cg_name)
		return

	# --- SLIDE TRANSITIONS ---
	if transition in ["slide_left", "slide_right"]:
		if SoundManager and SoundManager.has_method("play_sfx"):
			SoundManager.play_sfx("swish", 1.0, -5.0)

		var slide_tween = create_tween().set_parallel(true)
		var screen_width = get_viewport().get_visible_rect().size.x

		var new_start_x = 0.0
		var old_end_x = 0.0

		if transition == "slide_left":
			new_start_x = -screen_width
			old_end_x = screen_width
		elif transition == "slide_right":
			new_start_x = screen_width
			old_end_x = -screen_width

		# Only create a ghost if there is an existing texture to slide out
		if cg_layer.texture != null:
			var ghost = TextureRect.new()
			ghost.texture = cg_layer.texture
			ghost.expand_mode = cg_layer.expand_mode
			ghost.stretch_mode = cg_layer.stretch_mode
			ghost.set_anchors_and_offsets_preset(Control.PRESET_TOP_LEFT)
			ghost.size = get_viewport().get_visible_rect().size
			ghost.position = Vector2.ZERO
			ghost.mouse_filter = Control.MOUSE_FILTER_IGNORE

			cg_layer.get_parent().add_child(ghost)
			cg_layer.get_parent().move_child(ghost, cg_layer.get_index())

			slide_tween.tween_property(ghost, "position:x", old_end_x, duration).set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)
			slide_tween.chain().tween_callback(ghost.queue_free)

		# Setup New Sprite
		cg_layer.texture = new_texture
		cg_layer.modulate.a = 1.0
		cg_layer.set_anchors_and_offsets_preset(Control.PRESET_TOP_LEFT)
		cg_layer.size = get_viewport().get_visible_rect().size
		cg_layer.position.x = new_start_x

		slide_tween.tween_property(cg_layer, "position:x", 0.0, duration).set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)
		slide_tween.chain().tween_callback(func():
			if is_instance_valid(cg_layer):
				cg_layer.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
		)

	# --- FADE TRANSITION ---
	elif transition == "fade":
		if cg_layer.texture != null and cg_layer.modulate.a > 0.0:
			var ghost = TextureRect.new()
			ghost.texture = cg_layer.texture
			ghost.expand_mode = cg_layer.expand_mode
			ghost.stretch_mode = cg_layer.stretch_mode
			ghost.set_anchors_preset(Control.PRESET_FULL_RECT)
			ghost.mouse_filter = Control.MOUSE_FILTER_IGNORE

			cg_layer.get_parent().add_child(ghost)
			cg_layer.get_parent().move_child(ghost, cg_layer.get_index() + 1)

			cg_layer.texture = new_texture
			cg_layer.modulate.a = 1.0

			var tween = create_tween()
			tween.tween_property(ghost, "modulate:a", 0.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
			tween.tween_callback(ghost.queue_free)
		else:
			cg_layer.texture = new_texture
			cg_layer.position = Vector2.ZERO
			var tween = create_tween()
			tween.tween_property(cg_layer, "modulate:a", 1.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

	# --- INSTANT TRANSITION ---
	else:
		cg_layer.texture = new_texture
		cg_layer.position = Vector2.ZERO
		cg_layer.modulate.a = 1.0

func hide_cg(transition: String = "fade", duration: float = 0.5):
	if transition in ["slide_left", "slide_right"]:
		var screen_width = get_viewport().get_visible_rect().size.x
		var target_x = 0.0

		if transition == "slide_left":
			target_x = -screen_width
		elif transition == "slide_right":
			target_x = screen_width

		var tween = create_tween()
		tween.tween_property(cg_layer, "position:x", target_x, duration).set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)
		tween.tween_callback(func():
			cg_layer.texture = null
			cg_layer.position = Vector2.ZERO
			cg_layer.modulate.a = 0.0
		)
	elif transition == "fade":
		var tween = create_tween()
		tween.tween_property(cg_layer, "modulate:a", 0.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
		tween.tween_callback(func(): cg_layer.texture = null)
	else:
		cg_layer.modulate.a = 0.0
		cg_layer.texture = null


func start_tech_scan():
	_scan_active = true
	_scan_time = 0.0

	# Center the pivot so it scales from the middle, then scale up by 5%
	cg_layer.pivot_offset = cg_layer.size / 2.0
	cg_layer.scale = Vector2(1.05, 1.05)

	if _scan_tween:
		_scan_tween.kill()

	_scan_tween = create_tween().set_loops()
	_scan_tween.tween_property(cg_layer, "modulate", Color(0.6, 0.8, 0.9, 1.0), 1.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	_scan_tween.tween_property(cg_layer, "modulate", Color(0.4, 0.5, 0.6, 1.0), 1.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)


func stop_tech_scan():
	_scan_active = false
	if _scan_tween:
		_scan_tween.kill()

	# Smoothly return the image to its original position, color, and scale
	var tween = create_tween().set_parallel(true)
	tween.tween_property(cg_layer, "position", Vector2.ZERO, 0.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	tween.tween_property(cg_layer, "scale", Vector2.ONE, 0.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	tween.tween_property(cg_layer, "modulate", Color.WHITE, 0.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)


func play_cg_sequence(cg_names: Array, hold_duration: float = 2.0, fade_duration: float = 1.0):
	for i in range(cg_names.size()):
		var cg_name = cg_names[i]
		var resolved_path: String = ""
		if cg_aliases.has(cg_name):
			resolved_path = cg_aliases[cg_name]
		elif ResourceLoader.exists(cg_name):
			resolved_path = cg_name

		var new_texture: Texture2D = null
		if not resolved_path.is_empty():
			new_texture = await _get_texture_async(resolved_path)

		if not new_texture:
			push_warning("play_cg_sequence: Could not find CG alias or path: " + cg_name)
			continue

		if i == 0 and cg_layer.modulate.a == 0.0:
			# First image, fading in from nothing
			cg_layer.texture = new_texture
			var tween = create_tween()
			tween.tween_property(cg_layer, "modulate:a", 1.0, fade_duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
			await tween.finished
		else:
			# Crossfading from an existing CG
			var ghost = TextureRect.new()
			ghost.texture = cg_layer.texture
			ghost.expand_mode = cg_layer.expand_mode
			ghost.stretch_mode = cg_layer.stretch_mode
			ghost.set_anchors_preset(Control.PRESET_FULL_RECT)
			ghost.mouse_filter = Control.MOUSE_FILTER_IGNORE

			cg_layer.get_parent().add_child(ghost)
			cg_layer.get_parent().move_child(ghost, cg_layer.get_index() + 1)

			cg_layer.texture = new_texture
			cg_layer.modulate.a = 1.0

			var tween = create_tween()
			tween.tween_property(ghost, "modulate:a", 0.0, fade_duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
			await tween.finished
			ghost.queue_free()

		if i < cg_names.size() - 1:
			await get_tree().create_timer(hold_duration).timeout


func start_mental_image(image_name: String, fade_duration: float = 0.5, tint: Color = Color.WHITE, final_opacity: float = 0.6, start_scale: float = 1.0):
	var resolved_path: String = ""
	if cg_aliases.has(image_name):
		resolved_path = cg_aliases[image_name]
	elif background_aliases.has(image_name):
		resolved_path = background_aliases[image_name]
	elif ResourceLoader.exists(image_name):
		resolved_path = image_name

	var new_texture: Texture2D = null
	if not resolved_path.is_empty():
		new_texture = await _get_texture_async(resolved_path)

	if not new_texture:
		push_warning("start_mental_image: Could not find alias or path: " + image_name)
		return

	var screen_size = get_viewport().get_visible_rect().size

	# 1. Use Godot's built-in "Cover" mode so we don't have to do custom math
	mental_image_layer.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	mental_image_layer.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_COVERED

	# 2. Lock it to the exact bounds of the screen
	mental_image_layer.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	mental_image_layer.texture = new_texture

	# 3. Set pivot to the exact center of the screen
	mental_image_layer.pivot_offset = screen_size / 2.0

	# 4. FORCE scale to 1.0 (Ignoring start_scale which was coming in as 0.5 from DialogueManager)
	mental_image_layer.scale = Vector2.ONE
	mental_image_layer.position = Vector2.ZERO

	var start_modulate = tint
	start_modulate.a = 0.0
	mental_image_layer.modulate = start_modulate
	mental_image_layer.visible = true

	var mat = null
	if mental_image_shader:
		mat = mental_image_shader.duplicate()
		mat.set_shader_parameter("strength", 0.0)
		background_layer.material = mat
		actor_stage.material = mat
		cg_layer.material = mat

	if _mental_image_tween:
		_mental_image_tween.kill()

	_mental_image_tween = create_tween().set_parallel(true)

	# Fade in opacity and color
	_mental_image_tween.tween_property(mental_image_layer, "modulate:a", final_opacity, fade_duration).set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_OUT)

	# Fade the world to grayscale
	if mat:
		_mental_image_tween.tween_property(mat, "shader_parameter/strength", 1.0, fade_duration).set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_OUT)

	# C: The "Infinite" Slow Zoom
	# Since we forced the scale to 1.0, we just tween up to 1.5
	var target_scale_vec = Vector2(1.5, 1.5)

	_mental_image_tween.tween_property(mental_image_layer, "scale", target_scale_vec, 100.0)\
		.set_trans(Tween.TRANS_LINEAR)


func stop_mental_image(fade_duration: float = 0.5):
	if _mental_image_tween:
		_mental_image_tween.kill()

	_mental_image_tween = create_tween().set_parallel(true)

	# Fade out the Ghost Sprite
	_mental_image_tween.tween_property(mental_image_layer, "modulate:a", 0.0, fade_duration).set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_IN)

	# Fade out the Grayscale Shader
	if background_layer.material:
		_mental_image_tween.tween_property(background_layer.material, "shader_parameter/strength", 0.0, fade_duration).set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_IN)

	await _mental_image_tween.finished

	# Cleanup
	mental_image_layer.visible = false
	mental_image_layer.texture = null
	mental_image_layer.scale = Vector2.ONE
	background_layer.material = null
	actor_stage.material = null
	cg_layer.material = null


func shake(duration: float = 0.4, strength: float = 10.0):
	_shake_strength = strength
	_is_shaking = true
	if duration < 0:
		_is_persistent_shake = true
		_ignore_next_got_dialogue_signal = true
	else:
		_is_persistent_shake = false
		_ignore_next_got_dialogue_signal = false
		_shake_timer = duration


func _on_got_dialogue(line: DialogueLine):
	if _ignore_next_got_dialogue_signal:
		_ignore_next_got_dialogue_signal = false
		return
	if _is_persistent_shake:
		_is_shaking = false
		_is_persistent_shake = false
		offset = Vector2.ZERO

	_update_predictive_cache(line)


# --- PREDICTIVE PRELOADER SUBSYSTEM ---

func _update_predictive_cache(line: DialogueLine):
	var needed_paths: Dictionary = {}
	_collect_upcoming_paths(line.next_id, LOOKAHEAD_DEPTH, needed_paths, {})

	# GC: drop cached textures that are no longer in the upcoming window
	var to_remove: Array = []
	for path in _texture_cache:
		if not needed_paths.has(path):
			to_remove.append(path)
	for path in to_remove:
		_texture_cache.erase(path)
		_loading_paths.erase(path)

	# Request background-thread loading for new paths
	for path in needed_paths:
		if not _texture_cache.has(path) and not _loading_paths.has(path):
			if ResourceLoader.exists(path):
				_loading_paths[path] = true
				ResourceLoader.load_threaded_request(path)


func _collect_upcoming_paths(line_id: String, depth: int, out_paths: Dictionary, visited: Dictionary):
	if depth <= 0:
		return
	if line_id in ["end", "end!", "", null]:
		return
	if visited.has(line_id):
		return
	visited[line_id] = true

	# Handle stacked IDs (pipe-separated return addresses)
	var base_id: String = line_id.split("|")[0]
	if base_id.is_empty() or not dialogue_resource.lines.has(base_id):
		return

	var data: Dictionary = dialogue_resource.lines[base_id]

	# Extract texture paths from mutation lines
	if data.type == &"mutation" and data.has("mutation"):
		_extract_paths_from_mutation(data.mutation, out_paths)

	# Follow response branches
	if data.has("responses"):
		for resp_id in data.responses:
			if dialogue_resource.lines.has(resp_id):
				var resp_data: Dictionary = dialogue_resource.lines[resp_id]
				if resp_data.has("next_id"):
					_collect_upcoming_paths(resp_data.next_id, depth - 1, out_paths, visited)

	# Follow condition branches
	if data.has("next_sibling_id") and not data.next_sibling_id.is_empty():
		_collect_upcoming_paths(data.next_sibling_id, depth - 1, out_paths, visited)
	if data.has("next_id_after") and not data.next_id_after.is_empty():
		_collect_upcoming_paths(data.next_id_after, depth - 1, out_paths, visited)

	# Follow the main next_id
	if data.has("next_id"):
		var next: String = data.next_id.split("|")[0]
		_collect_upcoming_paths(next, depth - 1, out_paths, visited)


func _extract_paths_from_mutation(mutation: Dictionary, out_paths: Dictionary):
	if not mutation.has("expression"):
		return
	var expression: Array = mutation.expression
	if expression.is_empty():
		return

	var token: Dictionary = expression[0]
	if token.type != &"function":
		return

	var func_name: String = token.function

	match func_name:
		"actor_enter", "actor_change", "actor_walk_in", "actor_dash_in", "actor_show", "actor_peek_in":
			var actor_id: String = _extract_string_arg(token, 0)
			var emotion: String = _extract_string_arg(token, 1)
			if not actor_id.is_empty() and not emotion.is_empty():
				var profile = get_profile(actor_id)
				if profile:
					var tex_path: String = profile.expressions.get(emotion, "")
					if not tex_path.is_empty():
						out_paths[tex_path] = true

		"change_background":
			var bg_name: String = _extract_string_arg(token, 0)
			if not bg_name.is_empty():
				if background_aliases.has(bg_name):
					out_paths[background_aliases[bg_name]] = true
				elif ResourceLoader.exists(bg_name):
					out_paths[bg_name] = true

		"show_cg", "reveal_shock_from_black":
			var cg_name: String = _extract_string_arg(token, 0)
			if not cg_name.is_empty():
				if cg_aliases.has(cg_name):
					out_paths[cg_aliases[cg_name]] = true
				elif ResourceLoader.exists(cg_name):
					out_paths[cg_name] = true

		"start_mental_image":
			var image_name: String = _extract_string_arg(token, 0)
			if not image_name.is_empty():
				if cg_aliases.has(image_name):
					out_paths[cg_aliases[image_name]] = true
				elif background_aliases.has(image_name):
					out_paths[background_aliases[image_name]] = true
				elif ResourceLoader.exists(image_name):
					out_paths[image_name] = true


func _extract_string_arg(token: Dictionary, index: int) -> String:
	if not token.has("value"):
		return ""
	var args: Array = token.value
	if index >= args.size():
		return ""
	var arg_tokens: Array = args[index]
	if arg_tokens.is_empty():
		return ""
	# Skip closing-bracket/paren tokens that _resolve_each also skips
	if arg_tokens[0].type in [&"parens_close", &"bracket_close", &"brace_close"]:
		return ""
	if arg_tokens[0].type == &"string":
		return arg_tokens[0].value
	return ""


func _get_texture_async(path: String) -> Texture2D:
	if path.is_empty():
		return null

	# Already in cache
	if _texture_cache.has(path):
		return _texture_cache[path]

	# A threaded load was already requested by the predictive crawler
	if _loading_paths.has(path):
		while ResourceLoader.load_threaded_get_status(path) == ResourceLoader.THREAD_LOAD_IN_PROGRESS:
			await get_tree().process_frame
		var tex: Texture2D = ResourceLoader.load_threaded_get(path)
		_texture_cache[path] = tex
		_loading_paths.erase(path)
		return tex

	# Not cached and not loading — start a threaded request now
	if ResourceLoader.exists(path):
		ResourceLoader.load_threaded_request(path)
		while ResourceLoader.load_threaded_get_status(path) == ResourceLoader.THREAD_LOAD_IN_PROGRESS:
			await get_tree().process_frame
		var tex: Texture2D = ResourceLoader.load_threaded_get(path)
		_texture_cache[path] = tex
		return tex

	# Fallback to synchronous load
	var tex: Texture2D = load(path)
	if tex:
		_texture_cache[path] = tex
	return tex


func _destroy_and_clear_cache():
	# 1. Sever the nodes' ties to the textures so the reference count drops to 0
	if is_instance_valid(background_layer): background_layer.texture = null
	if is_instance_valid(cg_layer): cg_layer.texture = null
	if is_instance_valid(mental_image_layer): mental_image_layer.texture = null

	# 2. Clear the predictive texture cache
	_texture_cache.clear()
	_loading_paths.clear()

	# 3. Delay destruction if this is the Intro sequence (smooth transition to main world)
	if is_intro_sequence:
		await get_tree().create_timer(2.0).timeout

	if is_instance_valid(current_balloon):
		current_balloon.queue_free()

	# 4. Delete the node (Godot will automatically flush the VRAM now)
	queue_free()

# --- ENGINE-DRIVEN CINEMATIC FUNCTIONS ---
func set_solid_background(hex_color: String, duration: float = 1.0):
	if is_instance_valid(darken_backdrop):
		darken_backdrop.hide()

	var target_color = Color(hex_color)
	if duration <= 0.0:
		solid_background.color = target_color
	else:
		var tween = create_tween()
		tween.tween_property(solid_background, "color", target_color, duration)

func show_intro_silhouette(texture_path: String):
	if _intro_silhouette: return

	var tex = load(texture_path)
	if not tex:
		print_rich("[color=red]Cinematic Error: Could not load texture at path: %s[/color]" % texture_path)
		return

	_intro_silhouette = TextureRect.new()
	_intro_silhouette.texture = tex

	# Get sizes to manually center the image
	var screen_size = Vector2(1920, 1080) # Fallback baseline
	if is_inside_tree():
		screen_size = get_viewport().get_visible_rect().size

	var tex_size = _intro_silhouette.texture.get_size()
	var centered_pos = (screen_size - tex_size) / 2.0

	# Set pivot to the center of the image so scaling shrinks it towards its middle
	_intro_silhouette.pivot_offset = tex_size / 2.0

	# --- SCALE CONTROL ---
	# Adjust this Vector2 to make the silhouette bigger or smaller!
	_intro_silhouette.scale = Vector2(0.45, 0.45)

	# Start invisible and 50 pixels lower than center
	_intro_silhouette.modulate.a = 0.0
	_intro_silhouette.position = centered_pos + Vector2(0, 50)

	cinematic_container.add_child(_intro_silhouette)

	var tween = create_tween().set_parallel(true)
	tween.tween_property(_intro_silhouette, "modulate:a", 1.0, 3.0)
	# Tween up to the true center
	tween.tween_property(_intro_silhouette, "position:y", centered_pos.y, 3.0).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

	tween.chain().tween_callback(self._start_silhouette_float.bind(centered_pos.y))

func _start_silhouette_float(center_y: float):
	var float_tween = create_tween().set_loops()
	float_tween.tween_property(_intro_silhouette, "position:y", center_y - 15.0, 2.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	float_tween.tween_property(_intro_silhouette, "position:y", center_y + 15.0, 2.5).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

func spawn_cinematic_entity(entity_name: String, tscn_path: String, screen_x: float, screen_y: float):
	var scene = load(tscn_path)
	if not scene: return
	var instance = scene.instantiate()
	instance.position = Vector2(screen_x, screen_y)
	instance.modulate.a = 0.0
	cinematic_container.add_child(instance)
	_spawned_entities[entity_name] = instance
	create_tween().tween_property(instance, "modulate:a", 1.0, 2.0)

func remove_cinematic_entity(entity_name: String):
	if _spawned_entities.has(entity_name):
		var entity = _spawned_entities[entity_name]
		_spawned_entities.erase(entity_name)
		var tween = create_tween()
		tween.tween_property(entity, "modulate:a", 0.0, 1.5)
		tween.tween_callback(entity.queue_free)

func create_glow_texture(center_color: Color, edge_color: Color, size: int = 256) -> GradientTexture2D:
	var grad = Gradient.new()
	grad.offsets = PackedFloat32Array([0.0, 1.0])
	grad.colors = PackedColorArray([center_color, edge_color])
	var tex = GradientTexture2D.new()
	tex.gradient = grad
	tex.fill = GradientTexture2D.FILL_RADIAL
	tex.fill_from = Vector2(0.5, 0.5)
	tex.fill_to = Vector2(0.5, 0.0)
	tex.width = size
	tex.height = size
	return tex

func activate_hope_surround():
	var aura = TextureRect.new()
	aura.texture = create_glow_texture(Color(1.0, 0.6, 0.7, 1.0), Color(1.0, 0.4, 0.6, 0.0), 2000)
	aura.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	aura.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	aura.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	aura.material = CanvasItemMaterial.new()
	aura.material.blend_mode = CanvasItemMaterial.BLEND_MODE_ADD
	aura.modulate = Color(1.0, 1.0, 1.0, 0.0)

	cinematic_container.add_child(aura)
	cinematic_container.move_child(aura, _intro_silhouette.get_index())

	var tween = create_tween()
	tween.tween_property(aura, "modulate:a", 0.6, 2.0)
	var pulse = create_tween().set_loops()
	pulse.tween_property(aura, "scale", Vector2(1.05, 1.05), 2.0).set_trans(Tween.TRANS_SINE)
	pulse.tween_property(aura, "scale", Vector2(0.95, 0.95), 2.0).set_trans(Tween.TRANS_SINE)

# --- CINEMATIC & FADE FUNCTIONS ---

func play_cinematic(animation_name: String = "default", hide_balloon: bool = true, force_one_loop: bool = false, transition_effect: String = "none"):
	if transition_effect == "dissolve" or transition_effect == "fade":
		if cinematic_bg:
			cinematic_bg.modulate.a = 0.0
			cinematic_bg.show()
		if cinematic_sprite:
			cinematic_sprite.modulate.a = 0.0
			cinematic_sprite.show()
			cinematic_sprite.play(animation_name)

		var tween = create_tween().set_parallel(true)
		if cinematic_bg: tween.tween_property(cinematic_bg, "modulate:a", 1.0, 0.5).set_trans(Tween.TRANS_SINE)
		if cinematic_sprite: tween.tween_property(cinematic_sprite, "modulate:a", 1.0, 0.5).set_trans(Tween.TRANS_SINE)
	else:
		if cinematic_bg:
			cinematic_bg.modulate.a = 1.0
			cinematic_bg.show()
		if cinematic_sprite:
			cinematic_sprite.modulate.a = 1.0
			cinematic_sprite.show()
			cinematic_sprite.play(animation_name)

	is_cinematic_lock_active = true
	if hide_balloon and current_balloon: current_balloon.hide()
	if continue_button: continue_button.show()

func _on_cinematic_continue_pressed():
	is_cinematic_lock_active = false
	if continue_button: continue_button.hide()

	if current_balloon:
		current_balloon.show()
		if current_balloon.has_method("next") and current_balloon.dialogue_line:
			current_balloon.next(current_balloon.dialogue_line.next_id)
		else:
			stop_cinematic()

func stop_cinematic(transition_effect: String = "none"):
	is_cinematic_lock_active = false
	if continue_button: continue_button.hide()

	if transition_effect == "dissolve" or transition_effect == "fade":
		var tween = create_tween().set_parallel(true)
		if cinematic_bg: tween.tween_property(cinematic_bg, "modulate:a", 0.0, 0.5)
		if cinematic_sprite: tween.tween_property(cinematic_sprite, "modulate:a", 0.0, 0.5)
		await tween.finished

		if cinematic_bg: cinematic_bg.hide()
		if cinematic_sprite:
			cinematic_sprite.stop()
			cinematic_sprite.hide()
	else:
		if cinematic_bg: cinematic_bg.hide()
		if cinematic_sprite:
			cinematic_sprite.stop()
			cinematic_sprite.hide()

	if current_balloon: current_balloon.show()

func fade_to_black(duration: float = 1.0):
	if not fade_overlay: return
	fade_overlay.color = Color.BLACK
	fade_overlay.show()
	var tween = create_tween()
	tween.tween_property(fade_overlay, "modulate:a", 1.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	await tween.finished

func fade_from_black(duration: float = 1.0):
	if not fade_overlay: return
	var tween = create_tween()
	tween.tween_property(fade_overlay, "modulate:a", 0.0, duration).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	await tween.finished
	fade_overlay.hide()

func reveal_shock_from_black(cg_alias_name: String, shake_power: float = 25.0):
	# Route through show_cg to automatically check the cg_aliases dictionary
	await show_cg(cg_alias_name, "none", 0.0)
	shake(0.5, shake_power)
	if fade_overlay:
		fade_overlay.color = Color.WHITE
		fade_overlay.modulate.a = 1.0
		fade_overlay.show()
		var tween = create_tween()
		tween.tween_property(fade_overlay, "modulate:a", 0.0, 0.2).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
		tween.tween_callback(fade_overlay.hide)

func actor_tackle(attacker_id: String, target_id: String):
	if not active_actors.has(attacker_id) or not active_actors.has(target_id): return

	var attacker: TextureRect = active_actors[attacker_id]
	var target: TextureRect = active_actors[target_id]
	var orig_attacker_pos = attacker.position

	var dir = sign(target.position.x - attacker.position.x)
	if dir == 0: dir = 1.0

	actor_stage.move_child(attacker, -1) # Bring attacker to front

	var tween = create_tween()

	# 1. ANTICIPATION: Pull back smoothly to wind up the punch
	tween.tween_property(attacker, "position:x", orig_attacker_pos.x - (dir * 60.0), 0.25)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

	# 2. THE STRIKE: Fast lunge, but stop slightly before the target's center to simulate physical contact!
	var hit_pos = target.position.x - (dir * 80.0)
	tween.tween_property(attacker, "position:x", hit_pos, 0.1)\
		.set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN)

	# 3. THE IMPACT
	tween.tween_callback(func():
		shake(0.2, 25.0) # Snappier, heavier screen shake
		active_actors.erase(target_id)

		var target_fly = create_tween().set_parallel(true)

		# Knockback: Starts explosive, then slows down (EASE_OUT)
		target_fly.tween_property(target, "position:x", target.position.x + (dir * 800.0), 0.5)\
			.set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)

		# Fall down: Target drops out of frame
		target_fly.tween_property(target, "position:y", target.position.y + 300.0, 0.5)\
			.set_trans(Tween.TRANS_QUAD).set_ease(Tween.EASE_IN)

		# Tilt backward 90 degrees like getting knocked off their feet (instead of spinning)
		target_fly.tween_property(target, "rotation_degrees", dir * 90.0, 0.5)\
			.set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)

		target_fly.chain().tween_callback(target.queue_free)
	)

	# 4. FOLLOW-THROUGH & RECOVERY
	tween.tween_interval(0.3) # Hold the punch pose for a moment

	# Calmly step back to the original spot
	tween.tween_property(attacker, "position:x", orig_attacker_pos.x, 0.4)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

func show_patreon_button():
	if is_instance_valid(patreon_btn): return

	var tex = load("res://Icons/patreon_logo.png")
	if not tex:
		push_warning("Could not load Patreon logo at res://Icons/patreon_logo.png")
		return

	# Resize the massive 2048x2048 image down to a clean 96x96 UI icon
	var img = tex.get_image()
	if img:
		img.resize(96, 96, Image.INTERPOLATE_BILINEAR)
		tex = ImageTexture.create_from_image(img)

	var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")

	patreon_btn = Button.new()
	patreon_btn.text = " Support on Patreon"
	patreon_btn.icon = tex
	patreon_btn.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	patreon_btn.add_theme_constant_override("h_separation", 18) # Space between icon and text

	# Style the text
	patreon_btn.add_theme_font_override("font", custom_font)
	patreon_btn.add_theme_font_size_override("font_size", 44 if OS.has_feature("mobile") else 36)
	patreon_btn.add_theme_color_override("font_color", Color(0.9, 0.9, 0.9, 1.0))
	patreon_btn.add_theme_color_override("font_hover_color", Color.WHITE)
	patreon_btn.add_theme_color_override("font_pressed_color", Color.WHITE)

	# Build the Theme Boxes
	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.9)
	btn_normal.corner_radius_top_left = 12
	btn_normal.corner_radius_top_right = 12
	btn_normal.corner_radius_bottom_left = 12
	btn_normal.corner_radius_bottom_right = 12
	btn_normal.content_margin_left = 25
	btn_normal.content_margin_right = 35
	btn_normal.content_margin_top = 15
	btn_normal.content_margin_bottom = 15
	btn_normal.border_width_left = 3
	btn_normal.border_width_top = 3
	btn_normal.border_width_right = 3
	btn_normal.border_width_bottom = 3
	btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.95)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.9)

	patreon_btn.add_theme_stylebox_override("normal", btn_normal)
	patreon_btn.add_theme_stylebox_override("hover", btn_hover)
	patreon_btn.add_theme_stylebox_override("focus", btn_hover)
	patreon_btn.add_theme_stylebox_override("pressed", btn_hover)

	# Force the button to calculate its size based on the text/margins
	patreon_btn.reset_size()

	# Hover juice
	patreon_btn.mouse_entered.connect(func():
		var t = create_tween()
		t.tween_property(patreon_btn, "scale", Vector2(1.05, 1.05), 0.15).set_trans(Tween.TRANS_SINE)
	)
	patreon_btn.mouse_exited.connect(func():
		var t = create_tween()
		t.tween_property(patreon_btn, "scale", Vector2(1.0, 1.0), 0.15).set_trans(Tween.TRANS_SINE)
	)

	# --- CLICK ACTION ---
	patreon_btn.pressed.connect(func():
		if SoundManager and SoundManager.has_method("play_sfx"):
			SoundManager.play_sfx("ui_click")
		OS.shell_open(PatreonWorldButton.PATREON_URL)
	)

	# --- CRITICAL FIX: Add to the Dialogue Balloon so it sits on Layer 100! ---
	if is_instance_valid(current_balloon):
		current_balloon.add_child(patreon_btn)
	else:
		$RootContainer.add_child(patreon_btn)

	# Wait 1 frame so Godot can calculate the final UI size before we grab pivot/position
	await get_tree().process_frame

	# Center the pivot for clean scaling
	patreon_btn.pivot_offset = patreon_btn.size / 2.0

	# Drop animation logic
	var screen_size = get_viewport().get_visible_rect().size
	var drop_y_factor: float = 0.58 if OS.has_feature("mobile") else 0.5
	var target_pos = Vector2(screen_size.x * 0.28 - (patreon_btn.size.x / 2.0), screen_size.y * drop_y_factor - (patreon_btn.size.y / 2.0))
	var start_pos = target_pos - Vector2(0, 1000)

	patreon_btn.position = start_pos

	var tween = create_tween()
	tween.tween_property(patreon_btn, "position", target_pos, 0.8).set_trans(Tween.TRANS_BOUNCE).set_ease(Tween.EASE_OUT)

func hide_patreon_button():
	if is_instance_valid(patreon_btn):
		var tween = create_tween()
		tween.tween_property(patreon_btn, "position:y", patreon_btn.position.y + 1000, 0.5).set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN)
		tween.tween_callback(patreon_btn.queue_free)

func center_patreon_button_and_keep_alive():
	if not is_instance_valid(patreon_btn): return

	var screen_size = get_viewport().get_visible_rect().size
	# Center horizontally, and push it 65% down the screen
	var target_pos = Vector2(screen_size.x * 0.5 - (patreon_btn.size.x / 2.0), screen_size.y * 0.65 - (patreon_btn.size.y / 2.0))

	var tween = create_tween()
	tween.tween_property(patreon_btn, "position", target_pos, 0.6).set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN_OUT)

	# Rename it so the Memory Box can find it easily later
	patreon_btn.name = "PersistentPatreonBtn"

	# Reparent the button to the MemoryBoxOverlay so it survives this scene's destruction
	var memory_box = get_parent()
	if is_instance_valid(memory_box):
		patreon_btn.reparent(memory_box, true)
		# Null our local reference so we don't accidentally manipulate it during cleanup
		patreon_btn = null
</file>

<file path="conversation/balloon.gd">
extends CanvasLayer
## A basic dialogue balloon for use with Dialogue Manager.

## The action to use for advancing the dialogue
@export var next_action: StringName = &"ui_accept"

## The action to use to skip typing the dialogue
@export var skip_action: StringName = &"ui_cancel"

## The dialogue resource
var resource: DialogueResource

## Temporary game states
var temporary_game_states: Array = []

## See if we are waiting for the player
var is_waiting_for_input: bool = false

## See if we are running a long mutation and should hide the balloon
var will_hide_balloon: bool = false

## A dictionary to store any ephemeral variables
var locals: Dictionary = {}

var _locale: String = TranslationServer.get_locale()

## The current line
var dialogue_line: DialogueLine:
	set(value):
		if value:
			dialogue_line = value
			apply_dialogue_line()
		else:
			# The dialogue has finished so close the balloon
			queue_free()
	get:
		return dialogue_line

## A cooldown timer for delaying the balloon hide when encountering a mutation.
var mutation_cooldown: Timer = Timer.new()

## The base balloon anchor
@onready var balloon: Control = %Balloon

## The label showing the name of the currently speaking character
@onready var character_label: RichTextLabel = %CharacterLabel

## The label showing the currently spoken dialogue
@onready var dialogue_label: DialogueLabel = %DialogueLabel

## The menu of responses
@onready var responses_menu: DialogueResponsesMenu = %ResponsesMenu


func _ready() -> void:
	balloon.hide()
	Engine.get_singleton("DialogueManager").mutated.connect(_on_mutated)

	# If the responses menu doesn't have a next action set, use this one
	if responses_menu.next_action.is_empty():
		responses_menu.next_action = next_action

	mutation_cooldown.timeout.connect(_on_mutation_cooldown_timeout)
	add_child(mutation_cooldown)



func _unhandled_input(_event: InputEvent) -> void:
	# Only the balloon is allowed to handle input while it's showing
	get_viewport().set_input_as_handled()


func _notification(what: int) -> void:
	## Detect a change of locale and update the current dialogue line to show the new language
	if what == NOTIFICATION_TRANSLATION_CHANGED and _locale != TranslationServer.get_locale() and is_instance_valid(dialogue_label):
		_locale = TranslationServer.get_locale()
		var visible_ratio = dialogue_label.visible_ratio
		self.dialogue_line = await resource.get_next_dialogue_line(dialogue_line.id)
		if visible_ratio < 1:
			dialogue_label.skip_typing()


## Start some dialogue
func start(dialogue_resource: DialogueResource, title: String, extra_game_states: Array = []) -> void:
	temporary_game_states = [self] + extra_game_states
	is_waiting_for_input = false
	resource = dialogue_resource
	self.dialogue_line = await resource.get_next_dialogue_line(title, temporary_game_states)


## Apply any changes to the balloon given a new [DialogueLine].
func apply_dialogue_line() -> void:
	mutation_cooldown.stop()

	is_waiting_for_input = false
	balloon.focus_mode = Control.FOCUS_ALL
	balloon.grab_focus()

	character_label.visible = not dialogue_line.character.is_empty()
	character_label.text = tr(dialogue_line.character, "dialogue")

	dialogue_label.hide()
	dialogue_label.dialogue_line = dialogue_line

	responses_menu.hide()
	responses_menu.responses = dialogue_line.responses

	# Show our balloon
	balloon.show()
	will_hide_balloon = false

	dialogue_label.show()
	if not dialogue_line.text.is_empty():
		dialogue_label.type_out()
		await dialogue_label.finished_typing

	# Wait for input
	if dialogue_line.responses.size() > 0:
		balloon.focus_mode = Control.FOCUS_NONE
		responses_menu.show()
	elif dialogue_line.time != "":
		var time = dialogue_line.text.length() * 0.02 if dialogue_line.time == "auto" else dialogue_line.time.to_float()
		await get_tree().create_timer(time).timeout
		next(dialogue_line.next_id)
	else:
		is_waiting_for_input = true
		balloon.focus_mode = Control.FOCUS_ALL
		balloon.grab_focus()


## Go to the next line
func next(next_id: String) -> void:
	self.dialogue_line = await resource.get_next_dialogue_line(next_id, temporary_game_states)


#region Signals


func _on_mutation_cooldown_timeout() -> void:
	if will_hide_balloon:
		will_hide_balloon = false
		balloon.hide()


func _on_mutated(_mutation: Dictionary) -> void:
	is_waiting_for_input = false
	will_hide_balloon = true
	mutation_cooldown.start(0.1)


func _on_balloon_gui_input(event: InputEvent) -> void:
	# See if we need to skip typing of the dialogue
	if dialogue_label.is_typing:
		var mouse_was_clicked: bool = event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed()
		var skip_button_was_pressed: bool = event.is_action_pressed(skip_action)
		if mouse_was_clicked or skip_button_was_pressed:
			get_viewport().set_input_as_handled()
			dialogue_label.skip_typing()
			return

	if not is_waiting_for_input: return
	if dialogue_line.responses.size() > 0: return

	# When there are no response options the balloon itself is the clickable thing
	get_viewport().set_input_as_handled()

	# --- MODIFIED SECTION BELOW ---
	if event is InputEventMouseButton and event.is_pressed() and event.button_index == MOUSE_BUTTON_LEFT:
		# Play the sound when clicking to go next
		SoundManager.play_sfx("ui_click")
		next(dialogue_line.next_id)

	elif event.is_action_pressed(next_action) and get_viewport().gui_get_focus_owner() == balloon:
		# Play the sound when pressing Space/Enter to go next
		SoundManager.play_sfx("ui_click")
		next(dialogue_line.next_id)


func _on_responses_menu_response_selected(response: DialogueResponse) -> void:
	# Play the sound when clicking a choice button
	SoundManager.play_sfx("ui_click")
	next(response.next_id)

# --- ADD THIS FUNCTION ---
func _on_dialogue_label_spoke(letter: String, letter_index: int, speed: float) -> void:
	# Don't play sound for spaces
	if letter == " ":
		return

	SoundManager.play_dialogue_blip()

#endregion
</file>

<file path="conversation/character_conversation_overlay.gd">
extends CanvasLayer
class_name CharacterConversationOverlay

signal conversation_finished(dialogue_resource: DialogueResource)

# --- EXPORT VARIABLES ---
@export var conversation_dialogue_file: DialogueResource
@export var background_animations: SpriteFrames
@export var initial_animation_name: String = "float_loop"
@export var scene_character_sprite_texture: Texture2D
# Assign your grayscale_material.tres here in Inspector
@export var mental_image_shader: ShaderMaterial

# --- NEW AUDIO EXPORT ---
@export_group("Audio")
## Assign a name (Key) and an AudioStream (Value) to play in dialogue.
@export var music_tracks: Dictionary[String, AudioStream]

# --- NODE REFERENCES ---
@onready var root_container: Control = $RootContainer
@onready var animated_background: TextureRect = $RootContainer/AnimatedBackground
@onready var character_main_sprite: Sprite2D = $RootContainer/CharacterMainSprite
@onready var background_sprite: Sprite2D = $RootContainer/BackgroundSprite
@onready var mental_image_sprite: Sprite2D = $RootContainer/MentalImageSprite

# --- CINEMATIC NODES ---
# Ensure these exist in your scene inside RootContainer
@onready var cinematic_bg: ColorRect = $RootContainer/CinematicBackground
@onready var cinematic_sprite: AnimatedSprite2D = $RootContainer/CinematicSprite
# [NEW] The button for manual cinematic advancement
@onready var continue_button: Button = $RootContainer/CinematicContinueButton 
@onready var fade_overlay: ColorRect = $RootContainer/FadeOverlay

# --- TWEEN VARIABLES ---
var _background_tween: Tween
var _mental_image_tween: Tween

# --- NEW AUDIO VARIABLE ---
var _music_player: AudioStreamPlayer

# --- SHAKE VARIABLES ---
var _is_shaking: bool = false
var _shake_timer: float = 0.0
var _shake_strength: float = 10.0
var _shake_rng := RandomNumberGenerator.new()
var _is_persistent_shake: bool = false
var _ignore_next_got_dialogue_signal: bool = false

# --- ANIMATION VARIABLES ---
var _current_anim_name: String = ""
var _current_frame_index: int = 0
var _time_since_last_frame: float = 0.0
var _is_playing: bool = false

# --- CINEMATIC STATE ---
var is_intro_sequence: bool = false
# Accessed by the Balloon script to block input
var is_cinematic_lock_active: bool = false
# [NEW] Holds the reference to the active dialogue balloon
var current_balloon: Node = null


func _ready():
	if is_instance_valid(root_container):
		root_container.mouse_filter = Control.MOUSE_FILTER_STOP

	# 1. Setup Character Sprite
	if character_main_sprite:
		if scene_character_sprite_texture:
			character_main_sprite.texture = scene_character_sprite_texture
			character_main_sprite.visible = true
		else:
			character_main_sprite.visible = false
	else:
		print_rich("[color=orange]CharacterConversationOverlay: 'CharacterMainSprite' node not found.[/color]")

	# 2. Setup Cinematic Nodes (Hide by default)
	if cinematic_bg: cinematic_bg.hide()
	if cinematic_sprite: cinematic_sprite.hide()
	
	# [NEW] Setup Continue Button
	if continue_button:
		continue_button.hide()
		if not continue_button.pressed.is_connected(_on_cinematic_continue_pressed):
			continue_button.pressed.connect(_on_cinematic_continue_pressed)

		var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
		continue_button.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
		continue_button.add_theme_font_override("font", custom_font)
		continue_button.add_theme_font_size_override("font_size", 46 if OS.has_feature("mobile") else 32)

		var btn_normal = StyleBoxFlat.new()
		btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
		btn_normal.corner_radius_top_left = 6
		btn_normal.corner_radius_top_right = 6
		btn_normal.corner_radius_bottom_left = 6
		btn_normal.corner_radius_bottom_right = 6
		btn_normal.content_margin_left = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_right = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_top = 25 if OS.has_feature("mobile") else 15
		btn_normal.content_margin_bottom = 25 if OS.has_feature("mobile") else 15

		continue_button.reset_size()

		# Anchor to Top-Left so it never covers the center scene or overflows
		continue_button.anchor_left = 0.0
		continue_button.anchor_right = 0.0
		continue_button.anchor_top = 0.0
		continue_button.anchor_bottom = 0.0
		continue_button.grow_horizontal = Control.GROW_DIRECTION_END
		continue_button.grow_vertical = Control.GROW_DIRECTION_END

		# Add safe padding from the absolute edges
		continue_button.offset_left = 50 if OS.has_feature("mobile") else 30
		continue_button.offset_top = 50 if OS.has_feature("mobile") else 30
		btn_normal.border_width_left = 2
		btn_normal.border_width_top = 2
		btn_normal.border_width_right = 2
		btn_normal.border_width_bottom = 2
		btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

		var btn_hover = btn_normal.duplicate()
		btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
		btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

		continue_button.add_theme_stylebox_override("normal", btn_normal)
		continue_button.add_theme_stylebox_override("hover", btn_hover)
		continue_button.add_theme_stylebox_override("focus", btn_hover)
		continue_button.add_theme_stylebox_override("pressed", btn_hover)

		continue_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
		continue_button.add_theme_color_override("font_hover_color", Color.WHITE)
		continue_button.add_theme_color_override("font_pressed_color", Color.WHITE)
	else:
		print_rich("[color=orange]Warning: 'CinematicContinueButton' not found in RootContainer.[/color]")



		
	# 3. Connect Dialogue Manager Signals
	DialogueManager.dialogue_ended.connect(_on_dialogue_ended_from_manager)
	DialogueManager.got_dialogue.connect(_on_got_dialogue)
	_shake_rng.randomize()

	# 4. Start Background Animation
	if background_animations:
		play_animation(initial_animation_name)
	else:
		print_rich("[color=red]No 'background_animations' (SpriteFrames) assigned![/color]")

# Ensure mental image sprite is hidden at start
	if mental_image_sprite:
		mental_image_sprite.visible = false

	# 5. Launch Dialogue Balloon & CAPTURE IT
	if conversation_dialogue_file:
		# [UPDATED] We capture the return value into 'current_balloon'
		current_balloon = DialogueManager.show_dialogue_balloon_scene(
			"res://conversationballoon.tscn",
			conversation_dialogue_file,
			"start",
			[self] # Pass 'self' as extra_game_state so balloon can find us
		)
	else:
		print_rich("[color=red]No 'conversation_dialogue_file' assigned![/color]")
		conversation_finished.emit(null)
		_cleanup_and_queue_free()

	if background_sprite:
		# Force the background to align to the top-left corner on startup
		background_sprite.centered = false 
		background_sprite.position = Vector2.ZERO
	if fade_overlay:
		fade_overlay.color = Color.BLACK
		fade_overlay.modulate.a = 0.0 # Start transparent
		fade_overlay.show() # Keep it "visible" but transparent so we can tween it

func _process(delta: float):
	# --- Shake Logic ---
	if _is_shaking:
		if not _is_persistent_shake:
			_shake_timer -= delta
			if _shake_timer <= 0:
				_is_shaking = false
				root_container.position = Vector2.ZERO

		if _is_shaking:
			var offset_x = _shake_rng.randf_range(-_shake_strength, _shake_strength)
			var offset_y = _shake_rng.randf_range(-_shake_strength, _shake_strength)
			root_container.position = Vector2(offset_x, offset_y)

	# --- Animation Logic ---
	if not _is_playing or not background_animations:
		return

	var anim_speed = background_animations.get_animation_speed(_current_anim_name)
	if anim_speed == 0: return

	var frame_count = background_animations.get_frame_count(_current_anim_name)
	var does_loop = background_animations.get_animation_loop(_current_anim_name)
	var time_per_frame = 1.0 / anim_speed
	_time_since_last_frame += delta
	if _time_since_last_frame >= time_per_frame:
		_time_since_last_frame -= time_per_frame
		_current_frame_index += 1
		if _current_frame_index >= frame_count:
			if does_loop: _current_frame_index = 0
			else:
				_current_frame_index = frame_count - 1
				_is_playing = false
		update_frame()


# ---------------------------------------------------------
# NEW: CINEMATIC FUNCTIONS (With Transitions)
# ---------------------------------------------------------

# Arguments:
# 1. animation_name: Name of animation in SpriteFrames
# 2. hide_balloon: If true, hides the text box
# 3. force_one_loop: If true, locks input until loop finishes (not used if using Continue Button)
# 4. transition_effect: "none", "dissolve", or "fade"
func play_cinematic(animation_name: String = "default", hide_balloon: bool = true, force_one_loop: bool = false, transition_effect: String = "none"):
	
	# 1. Setup Initial Visibility based on Effect
	if transition_effect == "dissolve" or transition_effect == "fade":
		# Start invisible so we can fade in
		if cinematic_bg: 
			cinematic_bg.modulate.a = 0.0
			cinematic_bg.show()
		if cinematic_sprite: 
			cinematic_sprite.modulate.a = 0.0
			cinematic_sprite.show()
			cinematic_sprite.play(animation_name)
		
		# Create the Fade-In Tween
		var tween = create_tween().set_parallel(true)
		if cinematic_bg:
			tween.tween_property(cinematic_bg, "modulate:a", 1.0, 0.5).set_trans(Tween.TRANS_SINE)
		if cinematic_sprite:
			tween.tween_property(cinematic_sprite, "modulate:a", 1.0, 0.5).set_trans(Tween.TRANS_SINE)
			
	else:
		# INSTANT (Default)
		if cinematic_bg: 
			cinematic_bg.modulate.a = 1.0 # Ensure fully visible
			cinematic_bg.show()
		if cinematic_sprite: 
			cinematic_sprite.modulate.a = 1.0 # Ensure fully visible
			cinematic_sprite.show()
			cinematic_sprite.play(animation_name)
	
	# 2. Lock Input (Prevent Spacebar skipping)
	is_cinematic_lock_active = true
	
	# 3. Handle UI Visibility
	if hide_balloon and current_balloon:
		current_balloon.hide() 
	
	if continue_button:
		# If fading in, maybe delay the button slightly? 
		# For now, let's just show it.
		continue_button.show()

# Called when the on-screen "Continue" button is clicked
func _on_cinematic_continue_pressed():
	# 1. Release Lock
	is_cinematic_lock_active = false
	
	# 2. Hide The Button
	if continue_button:
		continue_button.hide()
	
	# 3. SHOW THE BALLOON (This is the key!)
	if current_balloon:
		current_balloon.show() 
		
		# 4. Advance Dialogue Manually
		# This jumps over the "..." line in your dialogue file
		if current_balloon.has_method("next") and current_balloon.dialogue_line:
			current_balloon.next(current_balloon.dialogue_line.next_id)
		else:
			# Fallback
			stop_cinematic()

# Called via dialogue: do ConversationOverlay.stop_cinematic()
# Called via dialogue: do stop_cinematic("dissolve")
func stop_cinematic(transition_effect: String = "none"):
	
	is_cinematic_lock_active = false
	if continue_button: continue_button.hide()
	
	# 1. Handle the Visual Exit
	if transition_effect == "dissolve" or transition_effect == "fade":
		var tween = create_tween().set_parallel(true)
		
		# Fade out
		if cinematic_bg:
			tween.tween_property(cinematic_bg, "modulate:a", 0.0, 0.5)
		if cinematic_sprite:
			tween.tween_property(cinematic_sprite, "modulate:a", 0.0, 0.5)
		
		# Wait for tween to finish before actually hiding logic
		await tween.finished
		
		if cinematic_bg: cinematic_bg.hide()
		if cinematic_sprite: 
			cinematic_sprite.stop()
			cinematic_sprite.hide()
			
	else:
		# INSTANT
		if cinematic_bg: cinematic_bg.hide()
		if cinematic_sprite: 
			cinematic_sprite.stop()
			cinematic_sprite.hide()
	
	# 2. Restore Balloon Visibility
	if current_balloon:
		current_balloon.show()
	
	# 3. Restore Standard Visuals
	if background_sprite: background_sprite.show()
	if character_main_sprite: character_main_sprite.show()
	if animated_background: animated_background.show()
# ---------------------------------------------------------


# --- MENTAL IMAGE FUNCTIONS ---
# Added 'start_scale' parameter at the end (defaults to 1.0)
func start_mental_image(texture_path: String, fade_duration: float = 0.5, tint: Color = Color.WHITE, final_opacity: float = 0.6, start_scale: float = 1.0):
	# 1. Setup Grayscale Shader
	if mental_image_shader:
		root_container.material = mental_image_shader
		root_container.material.set_shader_parameter("strength", 0.0)

	# 2. Setup Ghost Sprite
	var new_texture = load(texture_path)
	if new_texture is Texture2D:
		mental_image_sprite.texture = new_texture

		# Reset Visuals
		var start_modulate = tint
		start_modulate.a = 0.0
		mental_image_sprite.modulate = start_modulate

		# Set Initial Scale
		var initial_vec = Vector2(start_scale, start_scale)
		mental_image_sprite.scale = initial_vec
		mental_image_sprite.visible = true

		# 3. Handle Tweening
		if _mental_image_tween:
			_mental_image_tween.kill()

		_mental_image_tween = create_tween().set_parallel(true)

		# A: Fade in Opacity
		_mental_image_tween.tween_property(mental_image_sprite, "modulate:a", final_opacity, fade_duration)\
			.set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_OUT)

		# B: Fade in Shader
		if mental_image_shader:
			_mental_image_tween.tween_property(root_container.material, "shader_parameter/strength", 1.0, fade_duration)\
				.set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_OUT)

		# C: The "Infinite" Slow Zoom
		var zoom_amount = 0.5
		var target_scale_vec = Vector2(start_scale + zoom_amount, start_scale + zoom_amount)

		_mental_image_tween.tween_property(mental_image_sprite, "scale", target_scale_vec, 100.0)\
			.set_trans(Tween.TRANS_LINEAR)

		print_rich("[color=lightblue]Starting mental image (Infinite Zoom).[/color]")
	else:
		print_rich("[color=red]Mental Image Error: Failed to load texture at path: " + texture_path + "[/color]")

func stop_mental_image(fade_duration: float = 0.5):
	# 1. Kill any running fade-ins
	if _mental_image_tween:
		_mental_image_tween.kill()

	# 2. Create fade-out tween
	_mental_image_tween = create_tween().set_parallel(true)

	# A: Fade out the Ghost Sprite
	_mental_image_tween.tween_property(mental_image_sprite, "modulate:a", 0.0, fade_duration)\
		.set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_IN)

	# B: Fade out the Grayscale Shader
	if root_container.material:
		_mental_image_tween.tween_property(root_container.material, "shader_parameter/strength", 0.0, fade_duration)\
			.set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_IN)

	# 3. Wait for animation to finish
	await _mental_image_tween.finished

	# 4. Cleanup
	mental_image_sprite.visible = false
	root_container.material = null # Remove the shader entirely
	print_rich("[color=lightblue]Mental image fade-out complete.[/color]")


# --- BACKGROUND ZOOM FUNCTIONS ---

# Call this to start the slow zoom on the background.
func start_background_zoom(zoom_amount: float = 0.2, duration: float = 40.0):
	if not background_sprite: return

	# 1. Reset scale to 1.0 (Normal) so it starts fresh
	background_sprite.scale = Vector2.ONE

	# 2. Kill any existing background animation
	if _background_tween:
		_background_tween.kill()

	# 3. Create the tween
	_background_tween = create_tween()

	# 4. Calculate target (Current 1.0 + Amount)
	var target_scale = Vector2(1.0 + zoom_amount, 1.0 + zoom_amount)

	# 5. Animate linearly (steady speed)
	_background_tween.tween_property(background_sprite, "scale", target_scale, duration)\
		.set_trans(Tween.TRANS_LINEAR)

	print_rich("[color=lightblue]Started background zoom.[/color]")


# Call this to smoothly return the background to normal size.
func stop_background_zoom(reset_duration: float = 0.5):
	if not background_sprite: return

	if _background_tween:
		_background_tween.kill()

	_background_tween = create_tween()

	# Tween back to Vector2.ONE (1.0, 1.0)
	_background_tween.tween_property(background_sprite, "scale", Vector2.ONE, reset_duration)\
		.set_trans(Tween.TRANS_QUINT).set_ease(Tween.EASE_OUT)

	print_rich("[color=lightblue]Resetting background zoom.[/color]")


# --- GENERAL HELPER FUNCTIONS ---

func _on_got_dialogue(_line: DialogueLine):
	if _ignore_next_got_dialogue_signal:
		_ignore_next_got_dialogue_signal = false
		return
	if _is_persistent_shake:
		_is_shaking = false
		_is_persistent_shake = false
		root_container.position = Vector2.ZERO

func shake(duration: float = 0.4, strength: float = 10.0):
	_shake_strength = strength
	_is_shaking = true
	if duration < 0:
		_is_persistent_shake = true
		_ignore_next_got_dialogue_signal = true
	else:
		_is_persistent_shake = false
		_ignore_next_got_dialogue_signal = false
		_shake_timer = duration

func play_animation(anim_name: String):
	if not background_animations.has_animation(anim_name):
		return
	_current_anim_name = anim_name
	_current_frame_index = 0
	_time_since_last_frame = 0.0
	_is_playing = true
	update_frame()

func update_frame():
	if animated_background and background_animations.has_animation(_current_anim_name):
		var texture = background_animations.get_frame_texture(_current_anim_name, _current_frame_index)
		animated_background.texture = texture

func change_background_animation(animation_name: String):
	play_animation(animation_name)


# --- BACKGROUND SWITCHING LOGIC ---
func change_background_sprite(texture_path: String, effect: String = ""):
	if not background_sprite: return

	# Handle empty path (hide background)
	if texture_path.is_empty():
		background_sprite.visible = false
		return

	var new_texture = load(texture_path)
	if not (new_texture is Texture2D):
		print_rich("[color=red]Error: Could not load texture at: %s[/color]" % texture_path)
		return

	# --- CALCULATE EXACT SCALE TO FILL THE SCREEN ---
	var screen_size = get_viewport().get_visible_rect().size
	var tex_size = new_texture.get_size()
	# This keeps the image's aspect ratio but ensures the whole screen is covered
	var scale_factor = max(screen_size.x / tex_size.x, screen_size.y / tex_size.y)
	var new_scale = Vector2(scale_factor, scale_factor)

	# --- CASE 1: NO EFFECT (Standard Switch) ---
	if effect == "" or effect == "none":
		background_sprite.texture = new_texture
		background_sprite.scale = new_scale # Apply dynamic scale
		background_sprite.visible = true
		background_sprite.position = Vector2.ZERO
		return

	# --- CASE 2: DISSOLVE TRANSITION (Cross-fade) ---
	if effect == "dissolve":
		# 1. Create a "Ghost" of the OLD image
		var temp_old_sprite = background_sprite.duplicate()
		root_container.add_child(temp_old_sprite)
		root_container.move_child(temp_old_sprite, background_sprite.get_index() + 1)

		# 2. Ensure Ghost has the old texture and properties
		temp_old_sprite.texture = background_sprite.texture
		temp_old_sprite.position = background_sprite.position
		temp_old_sprite.scale = background_sprite.scale

		# 3. Set the MAIN sprite to the NEW image immediately
		background_sprite.texture = new_texture
		background_sprite.scale = new_scale # Apply dynamic scale
		background_sprite.visible = true

		# 4. Tween the Ghost's Opacity to 0
		var fade_duration = 0.5 
		var dissolve_tween = create_tween()
		dissolve_tween.tween_property(temp_old_sprite, "modulate:a", 0.0, fade_duration)\
			.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

		# 5. Cleanup the ghost
		dissolve_tween.tween_callback(temp_old_sprite.queue_free)
		return

	# --- CASE 3: SLIDE TRANSITIONS (Whip Pan) ---

	# 1. Play Sound
	if SoundManager.has_method("play_sfx"):
		SoundManager.play_sfx("swish", 1.0, -5.0)

	# 2. Create Dummy Sprite for the old image
	var temp_old_sprite = background_sprite.duplicate()
	root_container.add_child(temp_old_sprite)
	root_container.move_child(temp_old_sprite, background_sprite.get_index()) # Put BEHIND/Same layer

	temp_old_sprite.texture = background_sprite.texture
	temp_old_sprite.position = Vector2.ZERO
	temp_old_sprite.scale = background_sprite.scale

	# 3. Setup New Sprite
	background_sprite.texture = new_texture
	background_sprite.scale = new_scale # Apply dynamic scale
	background_sprite.visible = true

	# 4. Calculate Positions
	var screen_width = get_viewport().get_visible_rect().size.x
	var duration = 0.35

	var new_sprite_start_pos = Vector2.ZERO
	var old_sprite_end_pos = Vector2.ZERO

	if effect == "slide_left":
		new_sprite_start_pos = Vector2(-screen_width, 0)
		old_sprite_end_pos = Vector2(screen_width, 0)

	elif effect == "slide_right":
		new_sprite_start_pos = Vector2(screen_width, 0)
		old_sprite_end_pos = Vector2(-screen_width, 0)

	# 5. Apply positions
	background_sprite.position = new_sprite_start_pos

	# 6. Animate
	var slide_tween = create_tween().set_parallel(true)

	slide_tween.tween_property(background_sprite, "position", Vector2.ZERO, duration)\
		.set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)

	slide_tween.tween_property(temp_old_sprite, "position", old_sprite_end_pos, duration)\
		.set_trans(Tween.TRANS_EXPO).set_ease(Tween.EASE_OUT)

	# 7. Cleanup
	slide_tween.chain().tween_callback(temp_old_sprite.queue_free)

func _on_dialogue_ended_from_manager(resource: DialogueResource):
	if resource == conversation_dialogue_file:
		conversation_finished.emit(resource)
		_cleanup_and_queue_free()

func _cleanup_and_queue_free():
	if DialogueManager.is_connected("dialogue_ended", _on_dialogue_ended_from_manager):
		DialogueManager.disconnect("dialogue_ended", _on_dialogue_ended_from_manager)
	if DialogueManager.is_connected("got_dialogue", _on_got_dialogue):
		DialogueManager.disconnect("got_dialogue", _on_got_dialogue)

	# Release large textures so VRAM is freed immediately, not when GC runs
	if is_instance_valid(background_sprite): background_sprite.texture = null
	if is_instance_valid(animated_background): animated_background.texture = null
	if is_instance_valid(mental_image_sprite): mental_image_sprite.texture = null
	if is_instance_valid(character_main_sprite): character_main_sprite.texture = null
	background_animations = null

	# Only delay the destruction if this is the Intro sequence!
	if is_intro_sequence:
		await get_tree().create_timer(2.0).timeout

	queue_free()

# NEW FUNCTION: Play a sequence of images with a dissolve effect
func play_dissolve_sequence(image_paths: Array, hold_duration: float = 2.0, fade_duration: float = 1.0):
	if not background_sprite: return

	# Loop through every image path provided in the list
	for i in range(image_paths.size()):
		var texture_path = image_paths[i]
		var new_texture = load(texture_path)

		if not (new_texture is Texture2D):
			print_rich("[color=red]Error loading: %s[/color]" % texture_path)
			continue # Skip this bad image and try the next

		# --- CALCULATE EXACT SCALE TO FILL THE SCREEN ---
		var screen_size = get_viewport().get_visible_rect().size
		var tex_size = new_texture.get_size()
		var scale_factor = max(screen_size.x / tex_size.x, screen_size.y / tex_size.y)
		var new_scale = Vector2(scale_factor, scale_factor)

		# --- STEP 1: If this is the FIRST image and screen is empty, just show it ---
		if i == 0 and not background_sprite.visible:
			background_sprite.texture = new_texture
			background_sprite.scale = new_scale # Apply dynamic scale
			background_sprite.visible = true
			background_sprite.modulate.a = 0.0 # Start invisible

			# Simple fade in for the first image
			var tween = create_tween()
			tween.tween_property(background_sprite, "modulate:a", 1.0, fade_duration)
			await tween.finished

		# --- STEP 2: If we are transitioning from an existing image ---
		else:
			# 1. Create a "Ghost" of the current image
			var temp_old_sprite = background_sprite.duplicate()
			root_container.add_child(temp_old_sprite)
			root_container.move_child(temp_old_sprite, background_sprite.get_index() + 1) # Put ON TOP

			# 2. Setup the "Ghost" properties
			temp_old_sprite.texture = background_sprite.texture
			temp_old_sprite.position = background_sprite.position
			temp_old_sprite.centered = background_sprite.centered
			temp_old_sprite.scale = background_sprite.scale

			# 3. Set the REAL sprite to the NEW image underneath
			background_sprite.texture = new_texture
			background_sprite.scale = new_scale # Apply dynamic scale
			background_sprite.visible = true

			# 4. Tween the GHOST to transparent (revealing the new one)
			var tween = create_tween()
			tween.tween_property(temp_old_sprite, "modulate:a", 0.0, fade_duration)\
				.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

			# 5. Wait for fade to finish
			await tween.finished
			temp_old_sprite.queue_free() # Delete the ghost

		# --- STEP 3: Wait for the "Hold" time before showing the next one ---
		if i < image_paths.size() - 1:
			await get_tree().create_timer(hold_duration).timeout
# ---------------------------------------------------------
# NEW: FADE FUNCTIONS
# ---------------------------------------------------------

# Called via dialogue: do fade_to_black(1.0)
func fade_to_black(duration: float = 1.0):
	print("--- DEBUG: fade_to_black called! Duration: ", duration, " ---")
	
	if not fade_overlay:
		print("--- ERROR: fade_overlay node is MISSING! Check your Scene Tree. ---")
		return
	
	# 1. Force it to be visible
	fade_overlay.visible = true
	
	# 2. Force it to be on top of EVERYTHING (The Nuclear Option)
	fade_overlay.z_index = 100 
	# If using CanvasLayer, make sure this node is last, or use z_index
	
	# 3. Ensure color is black
	fade_overlay.color = Color.BLACK
	
	# 4. Run the Tween
	print("--- DEBUG: Starting Tween... ---")
	var tween = create_tween()
	tween.tween_property(fade_overlay, "modulate:a", 1.0, duration)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	
	await tween.finished
	print("--- DEBUG: Fade complete. ---")
	
# Called via dialogue: do fade_from_black(1.0)
func fade_from_black(duration: float = 1.0):
	if not fade_overlay: return
	
	# Create a tween to fade alpha from Current -> 0.0 (Transparent)
	var tween = create_tween()
	tween.tween_property(fade_overlay, "modulate:a", 0.0, duration)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	
	await tween.finished

# Instant reset if needed
func clear_fade():
	if fade_overlay:
		fade_overlay.modulate.a = 0.0
		
# ---------------------------------------------------------
# SHOCK SWITCH FUNCTION
# ---------------------------------------------------------

# Called via dialogue: do shock_switch_background("res://face_shock.png", 20.0, Color.WHITE)
# 1. texture_path: The image to switch TO (The realization/shock face)
# 2. shake_power: How hard to shake (20-30 is good for shocks)
# 3. flash_color: Color.WHITE (flashbang) or Color.RED (pain/danger) or Color.TRANSPARENT (just shake)
func shock_switch_background(texture_path: String, shake_power: float = 25.0, flash_color: Color = Color.WHITE):
	# 1. Start the Violence (Shake)
	# We shake for 0.5 seconds so it settles after the switch
	shake(0.5, shake_power)
	
	# 2. The Flash (Masks the transition)
	if fade_overlay:
		# A. Instant Flash ON
		fade_overlay.color = flash_color
		fade_overlay.modulate.a = 0.8 # Not fully opaque, so we see the shake underneath
		fade_overlay.show()
		
		# B. Swap the Sprite BEHIND the flash
		# We use a tiny delay so the swap happens exactly when the flash is brightest
		var timer = get_tree().create_timer(0.05)
		await timer.timeout
		change_background_sprite(texture_path, "none")
		
		# C. Fast Fade Out (The Reveal)
		var tween = create_tween()
		tween.tween_property(fade_overlay, "modulate:a", 0.0, 0.2)\
			.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	else:
		# Fallback if no overlay exists
		change_background_sprite(texture_path, "none")

# ---------------------------------------------------------

# ---------------------------------------------------------
# REVEAL SHOCK FUNCTION
# Use this when the screen is ALREADY black and you want to 
# suddenly reveal an image with a flash/shake.
# ---------------------------------------------------------
func reveal_shock_from_black(texture_path: String, shake_power: float = 25.0):
	# 1. Change the sprite BEHIND the darkness
	change_background_sprite(texture_path, "none")
	
	# 2. Start the violence (Shake)
	shake(0.5, shake_power)
	
	if fade_overlay:
		# 3. THE FLASHBANG EFFECT
		# Instantly turn the Black screen into a White Flash
		fade_overlay.color = Color.WHITE
		fade_overlay.modulate.a = 1.0 
		
		# 4. Fade the White Flash away quickly (0.2s) to reveal the image
		var tween = create_tween()
		tween.tween_property(fade_overlay, "modulate:a", 0.0, 0.2)\
			.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
	else:
		# Fallback if no overlay
		pass
func _exit_tree():
	_cleanup_and_queue_free()
</file>

<file path="conversation/conversationballoon.gd">
extends CanvasLayer
## A basic dialogue balloon for use with Dialogue Manager.

## The action to use for advancing the dialogue
@export var next_action: StringName = &"ui_accept"

## The action to use to skip typing the dialogue
@export var skip_action: StringName = &"ui_cancel"

# --- ICON EXPORTS ---
@export_group("Response Icons")
@export var proceed_icon: Texture2D
@export var back_icon: Texture2D
@export var leave_icon: Texture2D

## The dialogue resource
var resource: DialogueResource

## Temporary game states
var temporary_game_states: Array = []

## See if we are waiting for the player
var is_waiting_for_input: bool = false

## See if we are running a long mutation and should hide the balloon
var will_hide_balloon: bool = false

## A dictionary to store any ephemeral variables
var locals: Dictionary = {}

var _locale: String = TranslationServer.get_locale()
var _is_responses_clickable: bool = false

# --- CACHED OBJECTS (avoid per-line allocations) ---
var _bbcode_regex: RegEx
var _blank_spacer_icon: ImageTexture
var _cached_name_styles: Dictionary = {}  # lookup_key -> StyleBoxFlat

# --- Character Background Color Lookup Table (For the Nameplate) ---
var character_colors: Dictionary = {
	"AIda": Color("#20B2AA"),   # Light Sea Green
	"Sergey": Color("#DAA520"), # Goldenrod
	"McBucket": Color("#FF4500"), # Orange-Red
	"Old Man": Color("#FF4500"),
	"Old Man McBucket": Color("#FF4500"),
	"Layla": Color("#FF8299"),   # Soft Rose Pink
	"Lewgend": Color("#708090"), # Neutral Slate Grey
	"Nathan": Color("#FF69B4"),  # Hot Pink
	"Dread": Color("#4B0082"),   # Indigo
	"The... Toilet?": Color("#DC143C"),   # Crimson Red (Warning!)
	"Player": Color("#FFD65C"),            # Blonde Gold (HSV 43,44,100)
	"Alyssa": Color("#9370DB"),   # Medium Purple
}

# --- Character Portrait Lookup Table ---
var character_portraits: Dictionary = {
	"AIda": preload("res://Sprites/dialogue sprites/aida_dialogue_sprite.PNG"),
	"Sergey": preload("res://Sprites/dialogue sprites/sergey_dialogue_sprite.png"),
	"McBucket": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Old Man": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Old Man McBucket": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Layla": preload("res://Sprites/dialogue sprites/layla_dialogue_sprite.png"),
	"Lewgend": preload("res://Sprites/dialogue sprites/lewgend_dialogue_sprite.png"),
	"Nathan": preload("res://icon.svg"),
	"The... Toilet?": preload("res://Sprites/dialogue sprites/toilet_dialogue_sprite.png"),
	"Player": preload("res://Sprites/dialogue sprites/protag_dialogue_sprite.png")
}

# --- Character Shader Background Colors ---
var character_bg_colors: Dictionary = {
	"AIda": {
		"top": Color("#c22b64"),
		"bot": Color("#ffffff"),
		"dot": Color(1.0, 1.0, 1.0, 0.4)
	},
	"Layla": {
		"top": Color("#FF9EAF"),
		"bot": Color("#FFE0E6"),
		"dot": Color(1.0, 1.0, 1.0, 0.4)
	},
	"Lewgend": {
		"top": Color("#4F4F4F"),
		"bot": Color("#A9A9A9"),
		"dot": Color(1.0, 1.0, 1.0, 0.2)
	},
	"The... Toilet?": {
		"top": Color("#330000"),
		"bot": Color("#a30000"),
		"dot": Color(0.0, 0.0, 0.0, 0.3)
	},
	"Player": {
		"top": Color("#000000"),
		"bot": Color("#FFD65C"),
		"dot": Color(1.0, 0.84, 0.36, 0.4)
	},
	"Sergey": {
		"top": Color("#B8860B"),
		"bot": Color("#FFFFFF"),
		"dot": Color(1.0, 1.0, 1.0, 0.4)
	},
	"McBucket": {
		"top": Color("#9400D3"),
		"bot": Color("#FFD700"),
		"dot": Color(1.0, 0.0, 0.2, 0.4)
	},
	"Old Man": {
		"top": Color("#9400D3"),
		"bot": Color("#FFD700"),
		"dot": Color(1.0, 0.0, 0.2, 0.4)
	},
	"Old Man McBucket": {
		"top": Color("#9400D3"),
		"bot": Color("#FFD700"),
		"dot": Color(1.0, 0.0, 0.2, 0.4)
	},
	"Alyssa": {
		"top": Color("#4B0082"), # Indigo
		"bot": Color("#E6E6FA"), # Lavender
		"dot": Color(1.0, 1.0, 1.0, 0.4)
	},
}
## The current line
var dialogue_line: DialogueLine:
	set(value):
		if value:
			dialogue_line = value
			apply_dialogue_line()
		else:
			queue_free()
	get:
		return dialogue_line

## A cooldown timer for delaying the balloon hide when encountering a mutation.
var mutation_cooldown: Timer = Timer.new()

## Timer for auto-advancing text
var auto_advance_timer: Timer = Timer.new()

## The base balloon anchor
@onready var balloon: Control = %Balloon

## The label showing the name of the currently speaking character
@onready var character_label: RichTextLabel = %CharacterLabel

## The label showing the currently spoken dialogue
@onready var dialogue_label: DialogueLabel = %DialogueLabel

## The menu of responses
@onready var responses_menu: DialogueResponsesMenu = %ResponsesMenu

## Reference to the Portrait TextureRect (The Image itself)
@onready var portrait_rect: TextureRect = %PortraitRect

## Reference to the Container wrapping the portrait (The Box + Background)
@onready var portrait_container: PanelContainer = %PortraitContainer

## Reference to the Name Panel Container
@onready var name_panel: PanelContainer = $Balloon/NamePanel

## Reference to the Dialogue Container
@onready var dialogue_container: MarginContainer = $Balloon/Dialogue

## Is the UI currently hidden by the player?
var is_ui_hidden: bool = false

var _is_log_open: bool = false

## Quick Menu references
@onready var quick_menu: HBoxContainer = %QuickMenu
@onready var log_button: Button = %LogButton
@onready var hide_button: Button = %HideButton
@onready var menu_button: Button = %MenuButton
@onready var auto_button: Button = %AutoButton


func _ready() -> void:
	if log_button:
		log_button.pressed.connect(_on_log_button_pressed)
	if menu_button:
		menu_button.pressed.connect(_on_menu_button_pressed)
	if auto_button:
		auto_button.pressed.connect(_on_auto_button_pressed)
	if hide_button:
		hide_button.pressed.connect(_on_hide_button_pressed)
	balloon.hide()
	Engine.get_singleton("DialogueManager").mutated.connect(_on_mutated)

	if responses_menu.next_action.is_empty():
		responses_menu.next_action = next_action

	# --- Disable auto focus outline on the first item ---
	responses_menu.auto_focus_first_item = false

	mutation_cooldown.timeout.connect(_on_mutation_cooldown_timeout)
	add_child(mutation_cooldown)

	auto_advance_timer.one_shot = true
	auto_advance_timer.timeout.connect(_on_auto_advance_timeout)
	add_child(auto_advance_timer)

	if GameManager:
		_update_auto_button_visuals()
		if not Settings.auto_forward_toggled.is_connected(_on_global_auto_toggled):
			Settings.auto_forward_toggled.connect(_on_global_auto_toggled)

	# --- Style the responses menu and template button ---
	# 1. Increase vertical spacing between response buttons
	responses_menu.add_theme_constant_override("separation", 8)

	# 2. Setup Rounded Corners, Alignment, and Padding for the template button
	var template_btn = responses_menu.response_template as Button
	if template_btn:
		template_btn.alignment = HORIZONTAL_ALIGNMENT_LEFT
		template_btn.autowrap_mode = TextServer.AUTOWRAP_OFF
		template_btn.clip_contents = false
		template_btn.size_flags_horizontal = Control.SIZE_SHRINK_BEGIN
		template_btn.custom_minimum_size = Vector2(400, 0)

		var normal_style = StyleBoxFlat.new()
		normal_style.bg_color = Color(0.15, 0.15, 0.15, 0.85) # Dark gray, slightly transparent
		normal_style.corner_radius_top_left = 6
		normal_style.corner_radius_top_right = 6
		normal_style.corner_radius_bottom_left = 6
		normal_style.corner_radius_bottom_right = 6
		# Add internal padding
		normal_style.content_margin_left = 15
		normal_style.content_margin_top = 8
		normal_style.content_margin_bottom = 8
		normal_style.content_margin_right = 25
		# Add an invisible border to the normal state so the button doesn't change size on hover
		normal_style.border_width_left = 2
		normal_style.border_width_top = 2
		normal_style.border_width_right = 2
		normal_style.border_width_bottom = 2
		normal_style.border_color = Color(1.0, 1.0, 1.0, 0.0)
		normal_style.anti_aliasing = false

		var hover_style = normal_style.duplicate()
		hover_style.bg_color = Color(0.1, 0.25, 0.3, 0.9) # Slightly cyan background
		hover_style.border_color = Color(0.2, 0.85, 1.0, 0.8) # Crisp cyan border

		template_btn.add_theme_stylebox_override("normal", normal_style)
		template_btn.add_theme_stylebox_override("hover", hover_style)
		template_btn.add_theme_stylebox_override("focus", StyleBoxEmpty.new())
		template_btn.add_theme_stylebox_override("pressed", hover_style)
	# ---------------------------------------------------------

	# --- MOBILE UI SCALING ---
	if OS.has_feature("mobile"):
		quick_menu.add_theme_constant_override("separation", 50)
		menu_button.add_theme_font_size_override("font_size", 44)
		log_button.add_theme_font_size_override("font_size", 44)
		auto_button.add_theme_font_size_override("font_size", 44)
		hide_button.add_theme_font_size_override("font_size", 44)

		for qbtn in [menu_button, log_button, hide_button]:
			qbtn.add_theme_color_override("font_hover_color", Color(0.6, 0.6, 0.6, 1.0))

		responses_menu.add_theme_constant_override("separation", 25)
		if template_btn:
			template_btn.custom_minimum_size = Vector2(600, 0)
			var mobile_style = template_btn.get_theme_stylebox("normal").duplicate()
			mobile_style.content_margin_top = 20
			mobile_style.content_margin_bottom = 20
			mobile_style.content_margin_right = 35
			template_btn.add_theme_stylebox_override("normal", mobile_style)
			template_btn.add_theme_stylebox_override("hover", mobile_style)
			template_btn.add_theme_stylebox_override("focus", StyleBoxEmpty.new())
			template_btn.add_theme_stylebox_override("pressed", mobile_style)

		dialogue_container.add_theme_constant_override("margin_top", 35)
		dialogue_container.add_theme_constant_override("margin_bottom", 30)

		# Bring the top anchors up by 5% to make the box slightly taller on mobile
		var panel = $Balloon/Panel
		var dialogue_node = $Balloon/Dialogue

		panel.anchor_top -= 0.05
		dialogue_node.anchor_top -= 0.05

		# Scale up and shift up the portrait box to match the taller mobile panel.
		# The TextureRect inside will automatically scale the image to fit this new 190x190 box.
		if is_instance_valid(portrait_container):
			portrait_container.offset_left = 40
			portrait_container.offset_top = -260
			portrait_container.offset_right = 230
			portrait_container.offset_bottom = -70

		# Move the NamePanel up by the same 5% so it stays perfectly aligned above the box
		if is_instance_valid(name_panel):
			name_panel.anchor_top -= 0.05
			name_panel.anchor_bottom -= 0.05

	# --- FIX RESPONSE EXPANSION DIRECTION (Universal) ---
	var responses_container = $Balloon/Responses
	var responses_menu_vbox = %ResponsesMenu
	if is_instance_valid(responses_container) and is_instance_valid(responses_menu_vbox):
		responses_container.anchor_right = 0.95
		responses_menu_vbox.size_flags_horizontal = Control.SIZE_SHRINK_BEGIN

	# --- Pre-compile regex and create reusable spacer icon ---
	_bbcode_regex = RegEx.new()
	_bbcode_regex.compile("\\[.*?\\]")

	var blank_img = Image.create_empty(32, 32, false, Image.FORMAT_RGBA8)
	_blank_spacer_icon = ImageTexture.create_from_image(blank_img)

	# Hard-resize the exported icons to exactly 32x32 so the layout engine doesn't crush them
	proceed_icon = _resize_texture(proceed_icon, 32)
	back_icon = _resize_texture(back_icon, 32)
	leave_icon = _resize_texture(leave_icon, 32)

	# --- DYNAMIC TEXT SCALING (PC, Mobile & Marketing) ---
	var scale_mult: float = Settings.dialogue_text_scale if GameManager else 1.0

	var base_diag_size = 44 if OS.has_feature("mobile") else 28
	var base_name_size = 44 if OS.has_feature("mobile") else 30
	var base_resp_size = 38 if OS.has_feature("mobile") else 28

	var diag_size = int(base_diag_size * scale_mult)
	var name_size = int(base_name_size * scale_mult)
	var resp_size = int(base_resp_size * scale_mult)

	dialogue_label.add_theme_font_size_override("normal_font_size", diag_size)
	dialogue_label.add_theme_font_size_override("bold_font_size", diag_size)
	dialogue_label.add_theme_font_size_override("italics_font_size", diag_size)
	dialogue_label.add_theme_font_size_override("bold_italics_font_size", diag_size)

	character_label.add_theme_font_size_override("normal_font_size", name_size)
	character_label.add_theme_font_size_override("bold_font_size", name_size)

	var template_btn_ref = responses_menu.response_template as Button
	if template_btn_ref:
		template_btn_ref.add_theme_font_size_override("font_size", resp_size)


func _unhandled_input(_event: InputEvent) -> void:
	get_viewport().set_input_as_handled()


func _notification(what: int) -> void:
	if what == NOTIFICATION_TRANSLATION_CHANGED and _locale != TranslationServer.get_locale() and is_instance_valid(dialogue_label):
		_locale = TranslationServer.get_locale()
		var visible_ratio = dialogue_label.visible_ratio
		self.dialogue_line = await resource.get_next_dialogue_line(dialogue_line.id)
		if visible_ratio < 1:
			dialogue_label.skip_typing()


## Start some dialogue
func start(dialogue_resource: DialogueResource, title: String, extra_game_states: Array = []) -> void:
	temporary_game_states = [self] + extra_game_states
	is_waiting_for_input = false
	resource = dialogue_resource
	self.dialogue_line = await resource.get_next_dialogue_line(title, temporary_game_states)


## Apply any changes to the balloon given a new [DialogueLine].
func apply_dialogue_line() -> void:
	mutation_cooldown.stop()
	auto_advance_timer.stop()

	is_waiting_for_input = false
	balloon.focus_mode = Control.FOCUS_ALL
	balloon.grab_focus()

	# --- FIX: Move the reset up here to run BEFORE evaluating the new line! ---
	balloon.show()
	will_hide_balloon = false
	_restore_dialogue_visibility()
	# --------------------------------------------------------------------------

	# 1. Get the Raw name (includes BBCode like [color]...)
	var raw_name_with_tags = dialogue_line.character
	
	# 2. Create a "Clean" name (No Tags) for Dictionary lookups
	var lookup_key = _bbcode_regex.sub(raw_name_with_tags, "", true).strip_edges() # "Dread"

	# 2b. Create a display name that swaps "Player" for the actual name or "???"
	var display_name = raw_name_with_tags
	if lookup_key == "Player":
		if GameManager and Flags.get_game_flag("first_name_correct"):
			display_name = display_name.replace("Player", "Fiona")
		else:
			display_name = display_name.replace("Player", "???")

	# 3. Handle Name Panel Visibility and Color
	if display_name.is_empty():
		name_panel.visible = false
	else:
		name_panel.visible = true
		
		# --- SMART SPACE INSERTER LOGIC ---
		var spaced_name = ""
		var inside_tag = false

		for i in range(display_name.length()):
			var char = display_name[i]

			if char == "[":
				inside_tag = true

			spaced_name += char

			if char == "]":
				inside_tag = false

			# If we are NOT inside a tag, and it's not a bracket, add a space
			if not inside_tag and char != "[" and char != "]":
				# Only add space if the NEXT char isn't a tag opener
				if i < display_name.length() - 1 and display_name[i+1] != "[":
					spaced_name += " "
		# ----------------------------------

		character_label.text = "[center][b]" + spaced_name + "[/b][/center]"
		character_label.add_theme_color_override("default_color", Color.WHITE)
		
		# Update Background Panel Color using the CLEAN lookup key
		if not _cached_name_styles.has(lookup_key):
			var new_style = name_panel.get_theme_stylebox("panel").duplicate()
			if character_colors.has(lookup_key):
				new_style.bg_color = character_colors[lookup_key]
			else:
				new_style.bg_color = Color(0.2, 0.2, 0.2, 0.9)
			_cached_name_styles[lookup_key] = new_style
		name_panel.add_theme_stylebox_override("panel", _cached_name_styles[lookup_key])

	# 4. Update Portrait using CLEAN lookup key
	if character_portraits.has(lookup_key):
		portrait_rect.texture = character_portraits[lookup_key]
		
		# SHOW THE WHOLE CONTAINER (Box + Pattern + Image)
		portrait_container.visible = true 
		
		dialogue_container.add_theme_constant_override("margin_left", 260 if OS.has_feature("mobile") else 230)
		# --- NEW: SWAP SHADER COLORS ---
		var bg_pattern = portrait_container.get_node("BackgroundPattern")
		if bg_pattern and bg_pattern.material is ShaderMaterial:
			if character_bg_colors.has(lookup_key):
				var colors = character_bg_colors[lookup_key]
				bg_pattern.material.set_shader_parameter("color_top", colors["top"])
				bg_pattern.material.set_shader_parameter("color_bottom", colors["bot"])
				bg_pattern.material.set_shader_parameter("dot_color", colors["dot"])
			else:
				# Default generic colors if we forgot to add them to the dictionary
				bg_pattern.material.set_shader_parameter("color_top", Color("#1f2938"))
				bg_pattern.material.set_shader_parameter("color_bottom", Color("#0a0d14"))
				bg_pattern.material.set_shader_parameter("dot_color", Color(0, 0, 0, 0.3))
	else:
		# HIDE THE WHOLE CONTAINER
		portrait_container.visible = false 
		
		dialogue_container.add_theme_constant_override("margin_left", 50 if OS.has_feature("mobile") else 30)

	dialogue_label.hide()
	dialogue_label.dialogue_line = dialogue_line
	if GameManager:
		if Settings.instant_text:
			dialogue_label.seconds_per_step = 0.0
		else:
			dialogue_label.seconds_per_step = Settings.text_speed

	responses_menu.hide()
	responses_menu.modulate.a = 1.0 # --- Reset alpha for safety
	responses_menu.responses = dialogue_line.responses

	# --- FORMATTING LOGIC FOR RESPONSES ---
	for i in range(responses_menu.get_child_count()):
		var button = responses_menu.get_child(i)

		# Ignore the template and any non-button children
		if button == responses_menu.response_template or not button is Button:
			continue

		# Retrieve the DialogueResponse object attached to the button
		if not button.has_meta("response"):
			continue

		var response_obj = button.get_meta("response")
		var original_text = response_obj.text
		var display_text = original_text

		# Safely check if this line has been visited using a truly unique ID
		var unique_choice_id = resource.resource_path + "::" + response_obj.id
		var is_visited = false
		if GameManager and "visited_dialogue_responses" in GameManager:
			is_visited = DialogueHistory.visited_responses.has(unique_choice_id)

		# Default colors and icon
		var resting_color = Color.WHITE # Reverted back to pure white
		var hover_color = Color.WHITE
		var lower_text = display_text.to_lower()
		var assigned_icon: Texture2D = null

		# Parse custom tags and apply styling/icons
		if "[proceed]" in lower_text:
			display_text = display_text.replacen("[proceed]", "").strip_edges()
			assigned_icon = proceed_icon
			resting_color = Color(0.2, 0.85, 1.0, 1.0) # Cyan
			hover_color = Color.WHITE
		elif "[back]" in lower_text:
			display_text = display_text.replacen("[back]", "").strip_edges()
			assigned_icon = back_icon
			resting_color = Color(0.6, 0.6, 0.6, 1.0) # Gray
			hover_color = Color(0.8, 0.8, 0.8, 1.0) # Light Gray
		elif "[leave]" in lower_text:
			display_text = display_text.replacen("[leave]", "").strip_edges()
			assigned_icon = leave_icon
			resting_color = Color(0.6, 0.6, 0.6, 1.0) # Gray
			hover_color = Color(0.8, 0.8, 0.8, 1.0) # Light Gray
		elif is_visited:
			resting_color = Color(0.6, 0.6, 0.6, 1.0) # Gray
			hover_color = Color(0.8, 0.8, 0.8, 1.0) # Light Gray

		# --- ICON AND ALIGNMENT REFACTOR ---
		button.size_flags_horizontal = Control.SIZE_SHRINK_BEGIN

		if assigned_icon == null:
			assigned_icon = _blank_spacer_icon

		button.text = display_text
		button.icon = assigned_icon
		button.expand_icon = false
		button.icon_alignment = HORIZONTAL_ALIGNMENT_LEFT
		button.add_theme_constant_override("h_separation", 12)
		# --------------------------------------------------------------------------------------

		# Text colors
		button.add_theme_color_override("font_color", resting_color)
		button.add_theme_color_override("font_focus_color", resting_color)
		button.add_theme_color_override("font_hover_color", hover_color)
		button.add_theme_color_override("font_pressed_color", hover_color)

		# Icon colors
		button.add_theme_color_override("icon_normal_color", resting_color)
		button.add_theme_color_override("icon_focus_color", resting_color)
		button.add_theme_color_override("icon_hover_color", hover_color)
		button.add_theme_color_override("icon_pressed_color", hover_color)

		button.add_theme_stylebox_override("focus", StyleBoxEmpty.new())
		if OS.has_feature("mobile"):
			button.add_theme_color_override("font_hover_color", resting_color)
			button.add_theme_color_override("icon_hover_color", resting_color)

		# --- Fix sticky hover state by dropping focus on mouse exit ---
		if not button.mouse_exited.is_connected(button.release_focus):
			button.mouse_exited.connect(button.release_focus)
	# ------------------------------------------

	# --- RECORD HISTORY ---
	if GameManager and not dialogue_line.text.is_empty():
		var history_display_name = lookup_key
		if lookup_key == "Player":
			if Flags.get_game_flag("first_name_correct"):
				history_display_name = "Fiona"
			else:
				history_display_name = "???"

		DialogueHistory.add_line(lookup_key, history_display_name, dialogue_line.text)

	dialogue_label.show()
	if not dialogue_line.text.is_empty():
		dialogue_label.type_out()
		await dialogue_label.finished_typing

	if dialogue_line.responses.size() > 0:
		balloon.focus_mode = Control.FOCUS_NONE

		# --- Fade-In Cooldown to prevent spam-clicking ---
		_is_responses_clickable = false
		responses_menu.modulate.a = 0.0
		responses_menu.show()

		var fade_tween = create_tween()
		fade_tween.tween_property(responses_menu, "modulate:a", 1.0, 0.4).set_trans(Tween.TRANS_SINE)
		fade_tween.tween_callback(func(): _is_responses_clickable = true)
		# ------------------------------------------------------

		# --- AUTO-FORWARD SINGLE CHOICE ---
		if GameManager and Settings.is_auto_playing and dialogue_line.responses.size() == 1:
			_start_auto_advance_timer()

	elif dialogue_line.time != "":
		var time = dialogue_line.text.length() * 0.02 if dialogue_line.time == "auto" else dialogue_line.time.to_float()
		await get_tree().create_timer(time).timeout
		next(dialogue_line.next_id)
	else:
		is_waiting_for_input = true
		balloon.focus_mode = Control.FOCUS_ALL
		balloon.grab_focus()

		if GameManager and Settings.is_auto_playing:
			_start_auto_advance_timer()

## Go to the next line
func next(next_id: String) -> void:
	self.dialogue_line = await resource.get_next_dialogue_line(next_id, temporary_game_states)


#region Signals

func _on_mutation_cooldown_timeout() -> void:
	if will_hide_balloon:
		will_hide_balloon = false
		balloon.hide()

func _on_mutated(_mutation: Dictionary) -> void:
	is_waiting_for_input = false

	# --- COMMENT THESE OUT TO FIX THE BLINK ---
	# will_hide_balloon = true
	# mutation_cooldown.start(0.1)

func _on_balloon_gui_input(event: InputEvent) -> void:
	var is_right_click = event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_RIGHT and event.is_pressed()
	var is_h_key = event is InputEventKey and event.keycode == KEY_H and event.is_pressed() and not event.is_echo()

	# --- NEW: UNHIDE ON CLICK OR 'H' ---
	if is_ui_hidden:
		if event is InputEventMouseButton and event.is_pressed() and event.button_index in [MOUSE_BUTTON_LEFT, MOUSE_BUTTON_RIGHT]:
			get_viewport().set_input_as_handled()
			set_ui_hidden(false)
			return
		elif event.is_action_pressed(next_action) or event.is_action_pressed(skip_action) or is_h_key:
			get_viewport().set_input_as_handled()
			set_ui_hidden(false)
			return

	# --- NEW: HIDE ON RIGHT-CLICK OR 'H' ---
	if is_right_click or is_h_key:
		get_viewport().set_input_as_handled()
		set_ui_hidden(true)
		return

	# --- NEW: SCROLL UP TO OPEN LOG ---
	if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_WHEEL_UP and event.is_pressed():
		get_viewport().set_input_as_handled()
		_on_log_button_pressed()
		return

	# See if we need to skip typing of the dialogue
	if dialogue_label.is_typing:
		var mouse_was_clicked: bool = event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed()
		var skip_button_was_pressed: bool = event.is_action_pressed(skip_action)
		if mouse_was_clicked or skip_button_was_pressed:
			get_viewport().set_input_as_handled()
			dialogue_label.skip_typing()
			return

	if not is_waiting_for_input: return
	if dialogue_line.responses.size() > 0: return

	get_viewport().set_input_as_handled()

	if event is InputEventMouseButton and event.is_pressed() and event.button_index == MOUSE_BUTTON_LEFT:
		if SoundManager: SoundManager.play_sfx("ui_click")
		next(dialogue_line.next_id)

	elif event.is_action_pressed(next_action) and get_viewport().gui_get_focus_owner() == balloon:
		if SoundManager: SoundManager.play_sfx("ui_click")
		next(dialogue_line.next_id)

func _on_responses_menu_response_selected(response: DialogueResponse) -> void:
	# --- Ignore clicks if the menu is still fading in ---
	if not _is_responses_clickable:
		return
	# ---------------------------------------------------------

	# Record visited response using a truly unique ID
	var unique_choice_id = resource.resource_path + "::" + response.id
	if GameManager and "visited_dialogue_responses" in GameManager:
		DialogueHistory.visited_responses[unique_choice_id] = true

	# --- RECORD CHOICE HISTORY ---
	if GameManager:
		var all_options: Array[String] = []
		var chosen_idx = 0
		for i in range(dialogue_line.responses.size()):
			var resp = dialogue_line.responses[i]
			all_options.append(resp.text)
			if resp.id == response.id:
				chosen_idx = i

		var player_name = "Player"
		if Flags.get_game_flag("first_name_correct"):
			player_name = "Fiona"
		else:
			player_name = "???"

		DialogueHistory.add_choice("Player", player_name, all_options, chosen_idx)

	if SoundManager: SoundManager.play_sfx("ui_click")
	next(response.next_id)

func _on_dialogue_label_spoke(letter: String, letter_index: int, speed: float) -> void:
	if letter == " ": return
	# if SoundManager: SoundManager.play_dialogue_blip()

#endregion


#region Quick Menu (VN Style)

func _on_hide_button_pressed() -> void:
	if SoundManager: SoundManager.play_sfx("ui_click")
	set_ui_hidden(true)

func set_ui_hidden(hide: bool) -> void:
	is_ui_hidden = hide
	var is_visible = not hide

	if hide:
		if not auto_advance_timer.is_stopped():
			auto_advance_timer.paused = true
	else:
		if GameManager and Settings.is_auto_playing:
			if is_instance_valid(auto_advance_timer) and auto_advance_timer.paused:
				auto_advance_timer.paused = false
			elif not dialogue_label.is_typing:
				if is_waiting_for_input or (dialogue_line and dialogue_line.responses.size() == 1):
					_start_auto_advance_timer()

	var panel = $Balloon/Panel
	panel.visible = is_visible
	dialogue_container.visible = is_visible
	$Balloon/Responses.visible = is_visible
	quick_menu.visible = is_visible

	# Only restore character elements if we are toggling ON, and the current line requires them
	if is_visible and dialogue_line != null:
		var raw_name = dialogue_line.character
		name_panel.visible = not raw_name.is_empty()

		var lookup_key = _bbcode_regex.sub(raw_name, "", true).strip_edges()
		portrait_container.visible = character_portraits.has(lookup_key)
	else:
		name_panel.visible = false
		portrait_container.visible = false

func _restore_dialogue_visibility() -> void:
	set_ui_hidden(false)

func _on_log_button_pressed() -> void:
	if SoundManager: SoundManager.play_sfx("ui_click")
	var log_scene = load("res://dialogue_history_ui.tscn")
	if log_scene:
		var log_instance = log_scene.instantiate()
		get_tree().root.add_child(log_instance)

		# Suspend Auto-Forward while the log is open
		_is_log_open = true
		if not auto_advance_timer.is_stopped():
			auto_advance_timer.paused = true

		log_instance.tree_exited.connect(func():
			if not is_instance_valid(self):
				return

			_is_log_open = false
			if GameManager and Settings.is_auto_playing:
				# If timer was paused, resume it
				if is_instance_valid(auto_advance_timer) and auto_advance_timer.paused:
					auto_advance_timer.paused = false
				# If text finished typing while the log was open, start the timer now
				elif not dialogue_label.is_typing:
					if is_waiting_for_input or (dialogue_line and dialogue_line.responses.size() == 1):
						_start_auto_advance_timer()
		)

func _on_auto_button_pressed() -> void:
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager:
		Settings.is_auto_playing = not Settings.is_auto_playing

func _on_global_auto_toggled(is_on: bool):
	_update_auto_button_visuals()
	if is_on and not dialogue_label.is_typing:
		if is_waiting_for_input:
			_start_auto_advance_timer()
		elif dialogue_line.responses.size() == 1:
			_start_auto_advance_timer()
	elif not is_on:
		auto_advance_timer.stop()

func _start_auto_advance_timer():
	if _is_log_open or is_ui_hidden:
		return

	# Base wait from settings + 0.025 seconds per character
	var wait_time = Settings.auto_time_delay + (dialogue_line.text.length() * 0.025)
	auto_advance_timer.start(wait_time)

func _on_auto_advance_timeout():
	if is_waiting_for_input:
		next(dialogue_line.next_id)
	elif dialogue_line.responses.size() == 1:
		_simulate_single_response_click(dialogue_line.responses[0])

func _simulate_single_response_click(response: DialogueResponse):
	var target_btn: Button = null
	for i in range(responses_menu.get_child_count()):
		var btn = responses_menu.get_child(i)
		if btn is Button and btn.has_meta("response") and btn.get_meta("response") == response:
			target_btn = btn
			break

	if target_btn:
		target_btn.grab_focus()
		target_btn.pivot_offset = target_btn.size / 2.0

		var tween = create_tween()
		# Visually compress and tint the button to simulate a physical click
		tween.tween_property(target_btn, "scale", Vector2(0.95, 0.95), 0.1).set_trans(Tween.TRANS_SINE)
		tween.parallel().tween_property(target_btn, "modulate", Color(0.6, 0.85, 1.0, 1.0), 0.1)

		# Bounce back
		tween.tween_property(target_btn, "scale", Vector2(1.0, 1.0), 0.1).set_trans(Tween.TRANS_SINE)
		tween.parallel().tween_property(target_btn, "modulate", Color.WHITE, 0.1)

		# Trigger the actual dialogue progression right when the button reaches maximum compression
		tween.parallel().tween_callback(func():
			_on_responses_menu_response_selected(response)
		).set_delay(0.1)
	else:
		_on_responses_menu_response_selected(response)

func _cancel_auto_mode():
	if GameManager and Settings.is_auto_playing:
		Settings.is_auto_playing = false

func _update_auto_button_visuals():
	if not auto_button: return
	if GameManager and Settings.is_auto_playing:
		auto_button.add_theme_color_override("font_color", Color(0.2, 0.85, 1.0, 1.0))
		auto_button.add_theme_color_override("font_hover_color", Color(0.2, 0.85, 1.0, 1.0) if OS.has_feature("mobile") else Color(0.4, 0.95, 1.0, 1.0))
	else:
		auto_button.add_theme_color_override("font_color", Color(0.6, 0.6, 0.6, 1.0))
		auto_button.add_theme_color_override("font_hover_color", Color(0.6, 0.6, 0.6, 1.0) if OS.has_feature("mobile") else Color(0.2, 0.85, 1.0, 1.0))

func _on_menu_button_pressed() -> void:
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager and is_instance_valid(GameManager.pause_menu_ui):
		GameManager.pause_menu_ui.toggle_pause()

#endregion

func _resize_texture(tex: Texture2D, size: int) -> Texture2D:
	if not is_instance_valid(tex): return null
	var img = tex.get_image()
	if img:
		if img.get_width() != size or img.get_height() != size:
			img.resize(size, size, Image.INTERPOLATE_BILINEAR)
		return ImageTexture.create_from_image(img)
	return tex
</file>

<file path="conversation/MedicineCabinetZoom.gd">
extends ObjectZoomOverlay

@export var caught_dialogue_resource: DialogueResource
@export var caught_dialogue_start_id: String = "aida_caught_snooping"

func _ready():
	super._ready()
	
	print_rich("[color=orange]MedicineCabinet: Zoom opened. Forcing Game Unpause to allow Aida to move.[/color]")
	get_tree().paused = false
	
	if GameManager and Flags.current_level_state_manager:
		if not Flags.current_level_state_manager.level_flag_changed.is_connected(_on_level_flag_changed):
			Flags.current_level_state_manager.level_flag_changed.connect(_on_level_flag_changed)

func _on_level_flag_changed(flag_name: String, new_value: bool):
	if flag_name == "aida_in_main_room" and new_value == true:
		print_rich("[color=red]MedicineCabinet: AIDA RETURNED! Triggering caught sequence.[/color]")
		_trigger_forced_exit()

func _trigger_forced_exit():
	# Trigger the dialogue FIRST, before we delete this node
	if caught_dialogue_resource:
		_start_caught_dialogue()

	_cleanup_and_queue_free()

func _cleanup_and_queue_free():
	# Safely disconnect the signal to prevent crashes when Aida moves around later
	if GameManager and Flags.current_level_state_manager:
		if Flags.current_level_state_manager.level_flag_changed.is_connected(_on_level_flag_changed):
			Flags.current_level_state_manager.level_flag_changed.disconnect(_on_level_flag_changed)

	# Inform the GameManager that we are returning to the main level.
	if GameManager:
		GameManager.persisting_verb_id = ""
		GameManager.cancel_current_action(false)
		if GameManager.has_method("exit_to_world_state"):
			GameManager.exit_to_world_state()

	# Emit our own signal before we disappear.
	zoom_view_closed.emit()

	# Remove the overlay from the game.
	queue_free()

func _start_caught_dialogue():
	print_rich("[color=red]MedicineCabinet: Playing Caught Dialogue.[/color]")

	if GameManager and GameManager.has_method("clear_active_dialogue_balloons"):
		GameManager.clear_active_dialogue_balloons()

	if GameManager:
		if not DialogueManager.dialogue_ended.is_connected(GameManager.restore_world_after_object_dialogue):
			DialogueManager.dialogue_ended.connect(
				GameManager.restore_world_after_object_dialogue,
				CONNECT_ONE_SHOT
			)

	DialogueManager.show_dialogue_balloon_scene(
		"res://conversationballoon.tscn",
		caught_dialogue_resource,
		caught_dialogue_start_id
	)
</file>

<file path="conversation/MemoryBoxOverlay.gd">
# MemoryBoxOverlay.gd
extends CanvasLayer

@export var all_memory_data: Array[MemoryGroupData] = []
@export var dev_cta_dialogue: DialogueResource
@onready var location_list_container: VBoxContainer = $Panel/ScrollContainer/LocationListContainer
@onready var back_button: Button = $Panel/BackButton
@onready var panel: Panel = $Panel

const LocationRowScene = preload("res://LocationRow.tscn")
const ADVANCED_OVERLAY_SCENE = preload("res://AdvancedConversationOverlay.tscn")

func _ready():
	back_button.pressed.connect(_on_back_button_pressed)

	# --- MOBILE SCALING FOR MEMORY BOX MAIN UI ---
	if OS.has_feature("mobile"):
		# Perfectly center the Title
		var title = $Panel/TitleLabel
		title.add_theme_font_size_override("font_size", 64)
		title.anchor_left = 0.0
		title.anchor_right = 1.0
		title.offset_left = 0
		title.offset_right = 0

		# Make the Back button much larger
		back_button.add_theme_font_size_override("font_size", 72)
		back_button.custom_minimum_size = Vector2(100, 100)

		# Push the chapter list down to create a gap below the main title
		var scroll = $Panel/ScrollContainer
		scroll.anchor_top = 0.20

	_populate_list()

	# Hide the panel instantly before the screen even becomes visible
	panel.modulate.a = 0.0

	call_deferred("_restore_patreon_button")

func _populate_list():
	# First, clear any location rows that are already there.
	for child in location_list_container.get_children():
		child.queue_free()

	# Show every memory group (category filtering removed with the Story/Spicy toggle).
	for memory_group in all_memory_data:
		var new_row = LocationRowScene.instantiate()
		location_list_container.add_child(new_row)
		new_row.populate(memory_group)
		new_row.chapter_selected.connect(_on_chapter_selected)

# --- Signal Handlers ---

func _on_back_button_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager:
		GameManager.exit_to_world_state()

	print("Back button pressed, closing overlay.")
	queue_free()


func _on_chapter_selected(data: MemoryChapterData):
	if data.triggers_dev_cta:
		if not dev_cta_dialogue:
			push_warning("MemoryBoxOverlay: triggers_dev_cta is set but dev_cta_dialogue is not assigned.")
			return
		# Clear the persistent Patreon button if they replay the scene
		var old_btn = get_node_or_null("PersistentPatreonBtn")
		if is_instance_valid(old_btn):
			old_btn.queue_free()

		var instance = ADVANCED_OVERLAY_SCENE.instantiate()
		instance.dialogue_resource = dev_cta_dialogue
		add_child(instance)
	else:
		print("Loading scene: ", data.scene_path_to_load)

# --- RELAXED RETRO BOOT SEQUENCE ---
func play_boot_sequence():
	# 1. Setup Initial State: Transparent, slightly smaller, slightly shifted down
	panel.pivot_offset = panel.size / 2.0
	panel.scale = Vector2(0.95, 0.95)
	panel.position.y += 20.0
	panel.modulate.a = 0.0
	
	# (Future Audio Spot: A deep, ambient synth hum goes here!)
	# if SoundManager: SoundManager.play_sfx("ps2_ambient_hum")
	
	# 2. Smooth, chill tweens
	var tween = create_tween().set_parallel(true)
	
	# Phase A: Fade in slowly over 1.5 seconds
	tween.tween_property(panel, "modulate:a", 1.0, 1.5)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	
	# Phase B: Gently float up and expand to full size over 2.0 seconds
	tween.tween_property(panel, "scale", Vector2(1.0, 1.0), 2.0)\
		.set_trans(Tween.TRANS_QUART).set_ease(Tween.EASE_OUT)
		
	tween.tween_property(panel, "position:y", panel.position.y - 20.0, 2.0)\
		.set_trans(Tween.TRANS_QUART).set_ease(Tween.EASE_OUT)

func _restore_patreon_button():
	if not GameManager:
		return
	if not Flags.get_level_flag("dev_cta_completed"):
		return
	var existing = get_node_or_null("PersistentPatreonBtn")
	if is_instance_valid(existing):
		return

	var tex = load("res://Icons/patreon_logo.png")
	if not tex:
		return

	var img = tex.get_image()
	if img:
		img.resize(96, 96, Image.INTERPOLATE_BILINEAR)
		tex = ImageTexture.create_from_image(img)

	var custom_font = load("res://Fonts/VarelaRound-Regular.ttf")

	var btn = Button.new()
	btn.name = "PersistentPatreonBtn"
	btn.text = " Support on Patreon"
	btn.icon = tex
	btn.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	btn.add_theme_constant_override("h_separation", 18)

	btn.add_theme_font_override("font", custom_font)
	btn.add_theme_font_size_override("font_size", 44 if OS.has_feature("mobile") else 36)
	btn.add_theme_color_override("font_color", Color(0.9, 0.9, 0.9, 1.0))
	btn.add_theme_color_override("font_hover_color", Color.WHITE)
	btn.add_theme_color_override("font_pressed_color", Color.WHITE)

	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.9)
	btn_normal.corner_radius_top_left = 12
	btn_normal.corner_radius_top_right = 12
	btn_normal.corner_radius_bottom_left = 12
	btn_normal.corner_radius_bottom_right = 12
	btn_normal.content_margin_left = 25
	btn_normal.content_margin_right = 35
	btn_normal.content_margin_top = 15
	btn_normal.content_margin_bottom = 15
	btn_normal.border_width_left = 3
	btn_normal.border_width_top = 3
	btn_normal.border_width_right = 3
	btn_normal.border_width_bottom = 3
	btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.95)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.9)

	btn.add_theme_stylebox_override("normal", btn_normal)
	btn.add_theme_stylebox_override("hover", btn_hover)
	btn.add_theme_stylebox_override("focus", btn_hover)
	btn.add_theme_stylebox_override("pressed", btn_hover)

	btn.reset_size()

	btn.mouse_entered.connect(func():
		var t = create_tween()
		t.tween_property(btn, "scale", Vector2(1.05, 1.05), 0.15).set_trans(Tween.TRANS_SINE)
	)
	btn.mouse_exited.connect(func():
		var t = create_tween()
		t.tween_property(btn, "scale", Vector2(1.0, 1.0), 0.15).set_trans(Tween.TRANS_SINE)
	)

	btn.pressed.connect(func():
		if SoundManager and SoundManager.has_method("play_sfx"):
			SoundManager.play_sfx("ui_click")
		OS.shell_open(PatreonWorldButton.PATREON_URL)
	)

	add_child(btn)

	await get_tree().process_frame
	btn.pivot_offset = btn.size / 2.0
	var screen_size = get_viewport().get_visible_rect().size
	btn.position = Vector2(screen_size.x * 0.5 - (btn.size.x / 2.0), screen_size.y * 0.65 - (btn.size.y / 2.0))
</file>

<file path="conversation/object_zoom_overlay.gd">
extends CanvasLayer
class_name ObjectZoomOverlay

signal zoom_view_closed

@export var background_texture: Texture2D
@export var default_verb_id: String = ""

@onready var close_button: Button = $RootContainer/CloseButton
@onready var zoom_background: TextureRect = $RootContainer/ZoomBackground

func _ready():
	# --- Set the Background Texture from the Inspector ---
	if zoom_background:
		if background_texture:
			zoom_background.texture = background_texture
		else:
			print_rich("[color=orange]ObjectZoomOverlay: No 'background_texture' has been assigned in the Inspector.[/color]")
	else:
		print_rich("[color=red]ObjectZoomOverlay: The 'ZoomBackground' node was not found under RootContainer![/color]")

	# --- Connect the Close Button ---
	if close_button:
		close_button.pressed.connect(_on_close_button_pressed)

		# --- APPLY POLISHED STYLING ---
		var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
		close_button.text = "Close"
		close_button.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
		close_button.add_theme_font_override("font", custom_font)
		close_button.add_theme_font_size_override("font_size", 46 if OS.has_feature("mobile") else 32)

		var btn_normal = StyleBoxFlat.new()
		btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
		btn_normal.corner_radius_top_left = 6
		btn_normal.corner_radius_top_right = 6
		btn_normal.corner_radius_bottom_left = 6
		btn_normal.corner_radius_bottom_right = 6
		btn_normal.content_margin_left = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_right = 50 if OS.has_feature("mobile") else 35
		btn_normal.content_margin_top = 25 if OS.has_feature("mobile") else 15
		btn_normal.content_margin_bottom = 25 if OS.has_feature("mobile") else 15

		close_button.reset_size()
		if OS.has_feature("mobile"):
			close_button.position = Vector2(40, 40)
		btn_normal.border_width_left = 2
		btn_normal.border_width_top = 2
		btn_normal.border_width_right = 2
		btn_normal.border_width_bottom = 2
		btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

		var btn_hover = btn_normal.duplicate()
		btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
		btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

		close_button.add_theme_stylebox_override("normal", btn_normal)
		close_button.add_theme_stylebox_override("hover", btn_hover)
		close_button.add_theme_stylebox_override("focus", btn_hover)
		close_button.add_theme_stylebox_override("pressed", btn_hover)

		close_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
		close_button.add_theme_color_override("font_hover_color", Color.WHITE)
		close_button.add_theme_color_override("font_pressed_color", Color.WHITE)
	else:
		print_rich("[color=red]ObjectZoomOverlay: The 'CloseButton' node was not found! The player may get stuck.[/color]")

	# --- Inform the GameManager about the state change ---
	if GameManager and GameManager.has_method("enter_zoom_view_state"):
		GameManager.enter_zoom_view_state()

		# 1. Clear whatever verb/item got us into this view silently
		GameManager.cancel_current_action(false)

		# 2. If a default verb is assigned in the inspector, select it automatically
		if not default_verb_id.is_empty():
			GameManager.select_verb(default_verb_id)
			# Make it sticky so it doesn't unselect after one use
			GameManager.persisting_verb_id = default_verb_id
	else:
		print_rich("[color=orange]ObjectZoomOverlay: GameManager or enter_zoom_view_state() not found.[/color]")


# --- THIS IS THE MISSING FUNCTION ---
func _on_close_button_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	# When the button is pressed, we start the cleanup process.
	_cleanup_and_queue_free()


# --- Cleanup Functions ---
func _cleanup_and_queue_free():
	# Clear the sticky verb and the current action silently before returning to the world
	if GameManager:
		GameManager.persisting_verb_id = ""
		GameManager.cancel_current_action(false)

	# Inform the GameManager that we are returning to the main level.
	if GameManager and GameManager.has_method("exit_to_world_state"):
		GameManager.exit_to_world_state()

	# Disconnect the signal to be tidy.
	if close_button and close_button.pressed.is_connected(_on_close_button_pressed):
		close_button.pressed.disconnect(_on_close_button_pressed)

	# Emit our own signal before we disappear.
	zoom_view_closed.emit()

	# Remove the overlay from the game.
	queue_free()
</file>

<file path="Cutscenes/Cutscene.gd">
class_name Cutscene
extends Node

# This signal allows the Trigger to know when we are done (useful for chaining)
signal cutscene_finished

# Call this to start the sequence
func start_cutscene():
	print_rich("[color=Orchid]CutsceneSystem: Starting cutscene '%s'[/color]" % name)
	
	# 1. Change GameState to CUTSCENE (This hides UI and blocks Input via GM)
	if GameManager:
		GameManager.change_game_state(GameManager.GameState.CUTSCENE)
	else:
		push_error("Cutscene: GameManager not found!")

	# 2. Run the specific steps for this cutscene
	await _execution_steps()
	
	# 3. Finish up
	_finish_cutscene()

# VIRTUAL FUNCTION: Override this in your specific cutscene script
func _execution_steps():
	# Default behavior: wait one frame so it's not instant
	await get_tree().process_frame

func _finish_cutscene():
	print_rich("[color=Orchid]CutsceneSystem: Finishing cutscene '%s'[/color]" % name)
	
	# 1. Restore GameState to PLAY (This restores UI and Input via GM)
	if GameManager:
		GameManager.change_game_state(GameManager.GameState.IN_GAME_PLAY)
	
	cutscene_finished.emit()
</file>

<file path="data/explanation_data.gd">
# explanation_data.gd
extends Resource

class_name ExplanationData

## The node we want the spotlight to focus on.
@export var target_node_path: NodePath

## The lines of text that will be displayed one by one.
@export var explanation_lines: PackedStringArray

# You can still add more properties here later, like a title!
# @export var title: String
@export var exceptions_to_hide: Array[NodePath]
</file>

<file path="data/InteractionResponse.gd">
# res://interactions/InteractionResponse.gd
class_name InteractionResponse
extends Resource

## The verb that the player must use (e.g., "use", "examine", "pickup").
@export var verb_id: String = ""

## If not empty, this is the item the player must have selected ("in hand").
@export var required_item_id: String = ""

## Optional: The name of a game flag that must be checked.
@export var required_flag_id: String = ""
## The value the flag must have for this interaction to be valid.
@export var required_flag_value: bool = true

# --- ADD THIS VARIABLE ---
## Checked: Player walks to object. Unchecked: Player acts from current position (remote).
@export var requires_walk: bool = true 
# -------------------------

## The sequence of actions to perform if the conditions are met.
@export var actions_to_perform: Array[Action]
</file>

<file path="data/ItemData.gd">
# ItemData.gd
extends Resource
class_name ItemData # Makes this usable as a type hint and for creating new resources easily

## The unique identifier for this item (e.g., "key_main_door", "burger_mystery_meat").
## Use snake_case, all lowercase, for consistency with verb_ids.
@export var item_id: String = ""

## The name of the item as it will be displayed to the player (e.g., in inventory, in dialogue).
@export var display_name: String = "New Item"

## The texture to use as an icon for this item in the inventory UI.
@export var icon: Texture2D = null

## The description of the item that the player sees/thinks when they "Examine" it
## (either directly in inventory or if they use "Examine" on the item in the world before picking it up).
@export_multiline var description: String = "It's an item."

## Can this item be stacked in the inventory? (e.g., arrows, coins)
## For most classic point-and-click adventure items, this will be false.
@export var is_stackable: bool = false

## If is_stackable is true, what's the maximum number that can be in one stack?
## (Not highly relevant if is_stackable is usually false).
@export var max_stack_size: int = 1

## (Optional) A list of verb_ids that this item can be *primarily* used with.
## This is more for filtering or providing hints, as the actual "Use Item X with Y"
## logic is usually defined on the target interactable (Y).
## Example: A key might primarily be for "use_item" (on a door). A food item might be for "give" or "use_item" (on self).
# @export var compatible_verb_ids: Array[String] = []

# You can add more game-specific properties here later, for example:
# @export var is_quest_item: bool = false
# @export var value: int = 0 # If you had currency
# @export var sfx_on_pickup: AudioStream = null
# @export var sfx_on_use: AudioStream = null

# No functions are strictly needed in this Resource script itself for basic data storage.
# Its purpose is to define a data structure that you can edit in the Inspector.

func _init(id: String = "", name: String = "", tex: Texture2D = null, desc: String = ""):
	# Optional constructor for creating instances from code, though mostly you'll use .tres files
	if id != "": item_id = id
	if name != "": display_name = name
	if tex != null: icon = tex
	if desc != "": description = desc
</file>

<file path="data/JournalPageData.gd">
extends Resource
class_name JournalPageData

## 0 = First Left, 1 = First Right, 2 = Second Left, etc.
@export var page_index: int = 0

## Drag your transparent PNG from CSP here!
@export var page_texture: Texture2D

## Leave blank to always show. Type a flag name to hide until unlocked.
@export var required_flag: String = ""
</file>

<file path="data/MemoryChapterData.gd">
# MemoryChapterData.gd
class_name MemoryChapterData
extends Resource

## The name of the chapter displayed on the button.
@export var chapter_name: String = "Chapter Title"

## The image displayed on the button.
@export var chapter_image: Texture2D

## The scene that will be loaded when this chapter is selected.
@export var scene_path_to_load: String = ""

## The GameManager flag that must be 'true' for this chapter to be unlocked.
## If empty, the chapter is unlocked by default.
@export var unlock_flag: String = ""

## If true, selecting this chapter plays the dev CTA dialogue
## instead of loading scene_path_to_load.
@export var triggers_dev_cta: bool = false
</file>

<file path="data/MemoryGroupData.gd">
# MemoryGroupData.gd
class_name MemoryGroupData
extends Resource

## Defines whether this group belongs in the "Story" or "Spicy" tab.
enum MemoryCategory { STORY, SPICY }

## The name of the group (e.g., "University", "Faye").
@export var group_name: String = "Location Name"

## The image for the location/group.
@export var group_image: Texture2D

## The category this group will be displayed under.
@export var category: MemoryCategory = MemoryCategory.STORY # Unused since Story/Spicy toggle removal — kept for .tres compatibility.

## An array that holds all the chapter data resources for this group.
@export var chapters: Array[MemoryChapterData] = []
</file>

<file path="data/VerbData.gd">
# VerbData.gd
extends Resource
class_name VerbData # Makes it usable as a type hint

## The unique internal identifier for this verb (e.g., "examine", "talk_to")
@export var verb_id: String = ""

## The text displayed on the UI button for this verb (e.g., "Examine", "Talk to")
@export var display_text: String = ""

## Optional: An icon for the verb button
# @export var icon: Texture2D = null

## Is this verb available by default when the game starts or a new general state begins?
@export var unlocked_by_default: bool = true

## Does this verb typically require the player to click on a target object after selecting the verb?
## (e.g., "Examine" might sometimes be used on the general scene, but mostly on objects)
@export var requires_target_object: bool = true

# --- ADD THIS NEW LINE ---
## The dialogue file to use as a fallback if no specific interaction is defined for this verb.
@export var fallback_dialogue_file: DialogueResource
# --- END OF ADDITION ---
# i can add new properties here later!
</file>

<file path="Dev/alyssa_exporter.gd">
extends Node

func _ready():
	# 1. Create a SubViewport with exact 1800x2400 dimensions
	var viewport = SubViewport.new()
	viewport.size = Vector2(1800, 2400)
	viewport.render_target_update_mode = SubViewport.UPDATE_ALWAYS
	viewport.transparent_bg = false
	add_child(viewport)

	# 2. Add the background image
	var tex_rect = TextureRect.new()
	tex_rect.texture = load("res://Backgrounds/layla_marketing_image.png")
	tex_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	tex_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_COVERED
	tex_rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	viewport.add_child(tex_rect)

	# 3. Increase font size dynamically for the 1800x2400 resolution
	if GameManager:
		Settings.dialogue_text_scale = 2.2 # Increased slightly

	# 4. Instantiate the balloon inside the viewport
	var dialogue_res = load("res://dialogue/alyssa_marketing.dialogue")
	var balloon = preload("res://conversationballoon.tscn").instantiate()
	viewport.add_child(balloon)
	balloon.start(dialogue_res, "start")

	# Wait for typing to initialize so we can safely override margins
	# (conversationballoon.gd modifies dialogue margins dynamically when it starts)
	await get_tree().create_timer(0.5).timeout

	# 5. Customize the Balloon layout for this exact aspect ratio!
	var panel = balloon.get_node("%Balloon/Panel")
	var diag = balloon.get_node("%Balloon/Dialogue")
	var name_panel = balloon.get_node("%Balloon/NamePanel")
	var quick_menu = balloon.get_node_or_null("%QuickMenu")
	var portrait_container = balloon.get_node_or_null("%PortraitContainer")

	# Hide the bottom quick menu (Menu, Log, Auto, Hide)
	if quick_menu:
		quick_menu.hide()

	# Adjust the main box to have nice padding and be taller
	if panel:
		panel.anchor_left = 0.0
		panel.anchor_right = 1.0
		panel.anchor_top = 1.0
		panel.anchor_bottom = 1.0
		panel.offset_left = 80
		panel.offset_right = -80
		panel.offset_top = -450 # Increased height
		panel.offset_bottom = -40

	if diag:
		diag.anchor_left = 0.0
		diag.anchor_right = 1.0
		diag.anchor_top = 1.0
		diag.anchor_bottom = 1.0
		diag.offset_left = 80
		diag.offset_right = -80
		diag.offset_top = -450
		diag.offset_bottom = -40
		# Ensure internal text padding is nice and clears the portrait
		diag.add_theme_constant_override("margin_left", 380)
		diag.add_theme_constant_override("margin_right", 60)
		diag.add_theme_constant_override("margin_top", 40)

	# Align the name plate perfectly on top of the new box bounds
	if name_panel:
		name_panel.anchor_left = 0.0
		name_panel.anchor_right = 0.0
		name_panel.anchor_top = 1.0
		name_panel.anchor_bottom = 1.0
		name_panel.offset_left = 80
		name_panel.offset_right = 550 # Widened to fit scaled text
		name_panel.offset_top = -450 - 90
		name_panel.offset_bottom = -450 + 5

	# Fix portrait container scaling and layout
	if portrait_container:
		portrait_container.anchor_left = 0.0
		portrait_container.anchor_right = 0.0
		portrait_container.anchor_top = 1.0
		portrait_container.anchor_bottom = 1.0
		# Make it a 300x300 box, centered in the 410px high panel
		portrait_container.offset_left = 120
		portrait_container.offset_top = -395
		portrait_container.offset_right = 420
		portrait_container.offset_bottom = -95

	# 6. Skip typing
	if is_instance_valid(balloon.dialogue_label):
		balloon.dialogue_label.skip_typing()

	# 7. Wait another moment to ensure text is fully rendered on screen
	await get_tree().create_timer(0.5).timeout

	# 8. Capture and Save
	var img = viewport.get_texture().get_image()
	var export_path = "res://alyssa_marketing_post_final.png"
	img.save_png(export_path)

	print("\n===========================================")
	print("SUCCESS! Image automatically exported to:")
	print(ProjectSettings.globalize_path(export_path))
	print("===========================================\n")

	if GameManager:
		Settings.dialogue_text_scale = 1.0

	get_tree().quit()
</file>

<file path="Dev/MarketingMockup.gd">
extends Control

@onready var level_state_manager = $LevelStateManager

func _ready():
	Input.set_mouse_mode(Input.MOUSE_MODE_HIDDEN)

	if GameManager:
		Flags.register_level_state_manager(level_state_manager)

	await get_tree().process_frame
	await get_tree().process_frame

	if GameManager:
		var all_verbs: Array[String] = ["examine", "talk_to", "pickup", "use", "give", "flash", "think"]
		for v in all_verbs:
			GameManager.unlock_verb(v)
		GameManager.set_active_scene_verbs(all_verbs)

		Inventory.add_item("hospital_toilet_paper")

		level_state_manager.set_level_flag("insurance_button_unlocked", true)
		level_state_manager.set_level_flag("dev_cta_completed", false)

		if is_instance_valid(GameManager.insurance_form_button_ui):
			GameManager.insurance_form_button_ui.show()

		if is_instance_valid(GameManager.journal_button_ui):
			GameManager.journal_button_ui.show()

		if is_instance_valid(GameManager.verb_ui):
			GameManager.verb_ui.show()

		if is_instance_valid(GameManager.inventory_ui):
			GameManager.inventory_ui.show()

		if is_instance_valid(GameManager.patreon_world_ui):
			GameManager.patreon_world_ui.hide()

		if is_instance_valid(GameManager.pause_menu_ui):
			GameManager.pause_menu_ui.menu_panel.show()
			GameManager.pause_menu_ui.set_cancel_mode(false)
</file>

<file path="Interactables/Game World/Hospital Room/medicine_dispenser.gd">
extends Node2D

enum State { IDLE, IN_USE }
var current_state: State = State.IDLE

# --- Texture Exports ---
@export_group("Drug Textures")
@export var invigirol_texture: Texture2D
@export var cannathink_texture: Texture2D
@export var zanopram_texture: Texture2D

# --- References ---
@onready var anim_player_glow: AnimationPlayer = $AnimationPlayerGlow
@onready var anim_player_sprite: AnimationPlayer = $AnimationPlayerSprite
@onready var pulse_overlay: ColorRect = $PulseOverlay
@onready var vitals_label: Label = $VitalsLabel
@onready var drug_display: Sprite2D = $DrugDisplay

var _screen_tween: Tween
# Variable to store the size you set in the Inspector
var _target_drug_scale: Vector2 

func _ready():
	_update_screen_visuals("normal")
	
	if drug_display:
		# 1. Capture the scale you set in the Inspector BEFORE hiding it
		_target_drug_scale = drug_display.scale
		drug_display.visible = false

func on_drug_used(_item_id: String):
	print("Dispenser: Request to use '%s'." % _item_id)

	if current_state == State.IN_USE:
		print("Dispenser is busy processing!")
		return
	
	current_state = State.IN_USE
	
	if anim_player_glow: anim_player_glow.play("in_use")
	if anim_player_sprite: anim_player_sprite.play("in_use")
	
	_update_screen_visuals(_item_id)
	_update_drug_sprite(_item_id)

	await get_tree().create_timer(1.5).timeout
	
	current_state = State.IDLE 
	print("Dispenser: Processing complete.")

func _update_drug_sprite(drug_type: String):
	if not drug_display: return
	
	var new_texture: Texture2D = null
	
	match drug_type:
		"invigirol": new_texture = invigirol_texture
		"cannathink": new_texture = cannathink_texture
		"zanopram": new_texture = zanopram_texture
	
	if new_texture:
		drug_display.texture = new_texture
		drug_display.visible = true
		
		# --- Pop Animation Fixed ---
		# Start at 10% of your desired size
		drug_display.scale = _target_drug_scale * 0.1 
		
		var pop_tween = create_tween()
		
		# Animate to the captured Inspector scale (_target_drug_scale) instead of (1,1)
		pop_tween.tween_property(drug_display, "scale", _target_drug_scale, 0.4)\
			.set_trans(Tween.TRANS_BACK).set_ease(Tween.EASE_OUT)
			
	else:
		drug_display.visible = false

func _update_screen_visuals(drug_type: String):
	var mat: ShaderMaterial = pulse_overlay.material
	
	if _screen_tween and _screen_tween.is_valid():
		_screen_tween.kill()
	
	_screen_tween = create_tween().set_parallel(true)
	
	var target_color: Color
	var target_speed: float
	var target_height: float
	var target_text: String
	
	match drug_type:
		"normal":
			target_color = Color.CYAN
			target_speed = 0.6
			target_height = 0.18
			target_text = "72\n98\n124/78"
		"invigirol":
			target_color = Color(1.0, 0.2, 0.2, 1.0)
			target_speed = 1.3
			target_height = 0.35
			target_text = "115\n99\n145/92"
		"cannathink":
			target_color = Color.CYAN
			target_speed = 0.45
			target_height = 0.15
			target_text = "60\n97\n110/70"
		"zanopram":
			target_color = Color.CYAN
			target_speed = 0.3
			target_height = 0.10
			target_text = "42\n94\n90/60"

	vitals_label.text = target_text
	_screen_tween.tween_property(mat, "shader_parameter/line_color", target_color, 2.0)
	_screen_tween.tween_property(mat, "shader_parameter/speed", target_speed, 2.0)
	_screen_tween.tween_property(mat, "shader_parameter/height", target_height, 2.0)
</file>

<file path="Interactables/Game World/Hospital Room/tv_controller.gd">
extends Sprite2D

# We need a reference to the shader material to change its parameters.
@onready var tv_material: ShaderMaterial = material

func _ready():
	# Ensure the TV starts in the "On" state.
	# We will use another shader (like the aurora one) for the "On" state.
	# For now, this just sets the shutdown progress to 0.
	tv_material.set_shader_parameter("progress", 0.0)


func set_tv_state(is_off: bool):
	var tween = create_tween()
	var target_val = 1.0 if is_off else 0.0
	tween.tween_property(tv_material, "shader_parameter/progress", target_val, 0.6).set_ease(Tween.EASE_OUT).set_trans(Tween.TRANS_SINE)
</file>

<file path="interactions/action.gd">
# res://interactions/Action.gd
class_name Action
extends Resource

# CHANGE: Removed "-> bool" and replaced with "-> Variant"
# This allows the function to handle both instant returns AND async "await" returns.
func execute(interactable_node: Interactable) -> Variant:
	push_warning("An Action resource did not override the execute() method!")
	# Return true by default.
	return true
</file>

<file path="interactions/actions/PlayerFlashAction.gd">
class_name PlayerFlashAction
extends Action

@export var sound_effect: String = ""

func execute(interactable_node: Interactable) -> Variant:
	var player = GameManager.player_node

	if is_instance_valid(player):
		if player.has_method("face_target"):
			player.face_target(interactable_node.global_position)

		if player.has_method("set_animation_state"):
			player.set_animation_state("flash")

		if sound_effect != "" and SoundManager:
			SoundManager.play_sfx(sound_effect)

		var anim_duration: float = 1.0
		var anim_player = player.get_node_or_null("AnimationPlayer")
		if anim_player and anim_player.has_animation("flash"):
			anim_duration = anim_player.get_animation("flash").length

		await interactable_node.get_tree().create_timer(anim_duration).timeout

		if player.has_method("set_animation_state"):
			player.set_animation_state("idle")

	return true
</file>

<file path="interactions/actions/PlaySoundAction.gd">
# res://interactions/actions/PlaySoundAction.gd
extends Action
class_name PlaySoundAction

## The key name of the sound to play (must exist in SoundManager.sfx_library).
@export var sound_name: String = ""

## If true, the game will wait for the sound to finish before starting the next action.
## Useful for "Sound -> Then Dialogue" sequences.
@export var wait_for_completion: bool = false

@export_range(0.1, 4.0) var pitch: float = 1.0
@export_range(-80.0, 24.0) var volume_db: float = 0.0

func execute(_interactable_node: Interactable) -> Variant:
	if sound_name == "":
		push_warning("PlaySoundAction: No sound_name specified.")
		return true

	# Call the manager and get the player instance back
	var audio_player = SoundManager.play_sfx(sound_name, pitch, volume_db)

	if wait_for_completion and is_instance_valid(audio_player):
		# This pauses the interaction sequence until the sound finishes
		await audio_player.finished
	
	return true
</file>

<file path="interactions/actions/TeleportAction.gd">
class_name TeleportAction
extends Action

@export var target_marker_name: String = ""

func execute(interactable_node: Interactable) -> bool:
	var player = GameManager.player_node
	var transition_layer = GameManager.transition_layer # Grab the layer
	
	if not is_instance_valid(player):
		return false
	
	var target_node = GameManager.main_game_scene_instance.find_child(target_marker_name, true, false)
	if not target_node:
		push_error("TeleportAction: Marker '%s' not found!" % target_marker_name)
		return false

	# 1. Lock Player Movement (Important!)
	# We don't want them walking away while the door closes
	player.set_can_move(false) # Or whatever function locks your player input
	
	# 2. Check if we have a transition layer to use
	if is_instance_valid(transition_layer):
		# Start the animation sequence
		transition_layer.play_transition_sequence()
		
		# WAIT here until the doors are fully closed (black screen)
		await transition_layer.transition_halfway
		
	# 3. Teleport Logic (Happens while screen is black)
	if GameManager.has_method("player_has_finished_walk_command"):
		GameManager.player_has_finished_walk_command()
	if player.has_method("stop_movement"): 
		player.stop_movement() 
	
	player.global_position = target_node.global_position
	
	var camera = interactable_node.get_viewport().get_camera_2d()
	if camera and camera.has_method("snap_to_target"):
		camera.snap_to_target()
		
	# 4. Cleanup
	# If we used the transition, we wait for it to finish opening
	if is_instance_valid(transition_layer):
		await transition_layer.transition_finished
	
	# Unlock player
	player.set_can_move(true)
	
	return true
</file>

<file path="interactions/interactable.gd">
extends Area2D
class_name Interactable

signal display_dialogue(text: String)
signal self_destruct_requested
signal interaction_processed

signal interaction_pending
signal interaction_started

enum ObjectCategory { OBJECT, CHARACTER }
# --- NEW ENUM TO DEFINE WHERE THE INTERACTABLE EXISTS ---
enum InteractionLocation { WORLD, UI_OVERLAY }

enum ApproachSide { ANY, LEFT_SIDE, RIGHT_SIDE }

@export var object_display_name: String = "Object"
@export var object_id: String = ""
@export var state_flag_id: String = ""
@export var category: ObjectCategory = ObjectCategory.OBJECT
# --- NEW EXPORT VARIABLE FOR THE LOCATION CONTEXT ---
@export var interaction_location: InteractionLocation = InteractionLocation.WORLD
@export var approach_side: ApproachSide = ApproachSide.ANY


# --- THE NEW SYSTEM IS NOW THE ONLY SYSTEM ---
@export var interactions: Array[InteractionResponse] = [preload("res://interactions/actions/DefaultExamineResponse.tres")]

@onready var object_sprite: Sprite2D = _find_object_sprite()
@onready var walk_to_point: Marker2D = $WalkToPoint if has_node("WalkToPoint") else null
var _is_mouse_over: bool = false
var _is_force_highlighted: bool = false
var _outline_material: Material = null

@export var character_conversation_overlay_scene: PackedScene
@export var character_conversation_scene_path: String = ""
@export var object_zoom_overlay_scene: PackedScene


func _ready():
	if not state_flag_id.is_empty():
		if Flags.get_level_flag(state_flag_id):
			get_parent().queue_free()
			return

	if object_id == "":
		object_id = name + "_" + str(get_instance_id())
		print_rich("[color=yellow]Interactable: '%s' no object_id. Auto-gen: %s[/color]" % [name, object_id])
	if category == ObjectCategory.CHARACTER and character_conversation_overlay_scene == null and character_conversation_scene_path.is_empty():
		print_rich("[color=orange]Interactable '%s' (ID: %s): Category CHARACTER but no 'character_conversation_overlay_scene'![/color]" % [object_display_name, object_id])
	add_to_group("interactables")
	mouse_entered.connect(_on_mouse_entered)
	mouse_exited.connect(_on_mouse_exited)
	input_event.connect(_on_input_event)
	self_destruct_requested.connect(queue_free)

	# Save the material and remove it to save GPU power
	var sprite = _find_object_sprite()
	if sprite and sprite.material:
		_outline_material = sprite.material
		sprite.material = null
	# --- AUTO-INJECT UNIVERSAL FLASH VERB ---
	var has_flash_assigned = false
	for response in interactions:
		if response and response.verb_id == "flash":
			has_flash_assigned = true
			break
			
	if not has_flash_assigned:
		var flash_response = InteractionResponse.new()
		flash_response.verb_id = "flash"
		flash_response.requires_walk = true
		
		var universal_flash = UniversalFlashAction.new()
		flash_response.actions_to_perform.append(universal_flash)
		
		interactions.append(flash_response)
	# ----------------------------------------

func _on_mouse_entered():
	if GameManager:
		if interaction_location == InteractionLocation.WORLD and GameManager.current_interaction_state != GameManager.InteractionState.WORLD:
			return
		if interaction_location == InteractionLocation.UI_OVERLAY and GameManager.current_interaction_state != GameManager.InteractionState.ZOOM_VIEW:
			return

	_is_mouse_over = true
	if GameManager: GameManager.set_hovered_object(self)

	if not OS.has_feature("mobile"):
		if has_node("HoverGlow"):
			get_node("HoverGlow").visible = true

		var sprite = _find_object_sprite()
		if sprite and _outline_material:
			sprite.material = _outline_material
			sprite.material.set_shader_parameter("enable_outline", true)

func _on_mouse_exited():
	_is_mouse_over = false
	if GameManager: GameManager.clear_hovered_object(self)
	if not OS.has_feature("mobile"):
		if has_node("HoverGlow"): get_node("HoverGlow").visible = false
		var sprite = _find_object_sprite()
		if sprite and not _is_force_highlighted:
			sprite.material = null

func force_highlight(active: bool):
	_is_force_highlighted = active
	var sprite = _find_object_sprite()

	if active:
		if has_node("HoverGlow"): get_node("HoverGlow").visible = true
		if sprite and _outline_material:
			sprite.material = _outline_material
			sprite.material.set_shader_parameter("enable_outline", true)
	else:
		if not _is_mouse_over or OS.has_feature("mobile"):
			if has_node("HoverGlow"): get_node("HoverGlow").visible = false
			if sprite: sprite.material = null

		if not _is_mouse_over:
			if GameManager: GameManager.clear_hovered_object(self)

func _on_input_event(_v: Viewport, event: InputEvent, _sidx: int):
	if GameManager:
		if interaction_location == InteractionLocation.WORLD and GameManager.current_interaction_state != GameManager.InteractionState.WORLD:
			return
		if interaction_location == InteractionLocation.UI_OVERLAY and GameManager.current_interaction_state != GameManager.InteractionState.ZOOM_VIEW:
			return

	if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed():
		if GameManager: GameManager.process_interaction_click(self)

# --- FULLY REFACTORED CORE LOGIC ---
func attempt_interaction(verb_id: String, item_id_used_with: String = ""):
	interaction_started.emit()
	print_rich("[color=Orchid]--- Interactable '%s': attempt_interaction --- Verb: '%s', ItemID: '%s'[/color]" % [object_display_name, verb_id, item_id_used_with])

	for i in range(interactions.size()):
		var response = interactions[i]

		if not response or response.verb_id.is_empty():
			continue

		var verb_matches: bool = (response.verb_id == verb_id)
		var item_matches: bool = (response.required_item_id == item_id_used_with)

		var flag_matches: bool = true
		if not response.required_flag_id.is_empty():
			if GameManager:
				flag_matches = (Flags.get_level_flag(response.required_flag_id) == response.required_flag_value)
			else:
				flag_matches = false

		if verb_matches and item_matches and flag_matches:
			# --- NEW CHECK: Ensure the match actually has actions ---
			var has_valid_actions = false
			for action in response.actions_to_perform:
				if action != null:
					has_valid_actions = true
					break

			if not has_valid_actions:
				print_rich("[color=yellow]Match found for '%s', but actions_to_perform is empty. Skipping to fallback.[/color]" % verb_id)
				continue
			# --- END NEW CHECK ---

			print_rich("[color=LimeGreen]Match found at index %s for Verb '%s' and Item '%s'.[/color]" % [i, verb_id, item_id_used_with])

			for action in response.actions_to_perform:
				if action:
					var should_continue = await action.execute(self)

					# If the action explicitly returns false, HALT the interaction chain.
					# This prevents GameManager from resetting the UI prematurely.
					if typeof(should_continue) == TYPE_BOOL and should_continue == false:
						return

			interaction_processed.emit()
			return

	# --- FALLBACK LOGIC (If no match found above) ---
	
	if GameManager:
		# Use the original verb_id for fallback lookup (or effective, depending on your fallback setup)
		var verb_data = GameManager.get_verb_data_by_id(verb_id) 
		if is_instance_valid(verb_data) and is_instance_valid(verb_data.fallback_dialogue_file):
			print_rich("[color=Goldenrod]No match found. Calling FallbackManager.[/color]")
			if FallbackManager:
				FallbackManager.trigger_fallback(verb_data, self.object_id, item_id_used_with)
			return

	# Final safety net if no fallback is configured
	print_rich("[color=Red]No interaction response and no fallback file found for verb '%s'.[/color]" % verb_id)
	display_dialogue.emit("I can't seem to do that.")
	interaction_processed.emit()

func get_walk_to_position() -> Vector2:
	if walk_to_point: return walk_to_point.global_position
	return get_parent().global_position

# --- CORRECTED VERSION OF THIS FUNCTION ---
# This function provides simple, reliable logic for walking.
func does_verb_require_walk(verb_id_to_check: String, item_data_used: ItemData = null) -> bool:
	# 1. "Walk to" always requires walking.
	if verb_id_to_check == "walk_to":
		return true

	# 2. Determine the Item ID we are checking against
	var item_id_checking = ""
	if item_data_used:
		item_id_checking = item_data_used.item_id

	# 3. Search through the interactions array to find the SPECIFIC response that will trigger.
	# We use the exact same matching logic as attempt_interaction() to find the right one.
	for response in interactions:
		if not response: continue

		# Check basic matches
		var verb_matches: bool = (response.verb_id == verb_id_to_check)
		var item_matches: bool = (response.required_item_id == item_id_checking)

		# Check flag matches (Logic matching attempt_interaction)
		var flag_matches: bool = true
		if not response.required_flag_id.is_empty():
			# If a flag ID is specified, we must check it.
			flag_matches = (Flags.get_level_flag(response.required_flag_id) == response.required_flag_value)

		# If we found the valid interaction response that is going to fire:
		if verb_matches and item_matches and flag_matches:
			# Return the custom setting from the Inspector
			return response.requires_walk

	# 4. Fallback defaults if no specific interaction was found in the list.
	# If the specific interaction isn't defined, we fall back to generic rules.
	
	if verb_id_to_check == "examine":
		return false
		
	if verb_id_to_check == "talk_to" and category == ObjectCategory.CHARACTER:
		return true

	# Default safety net: If we don't know what to do, walk to it.
	return true


func notify_interaction_pending():
	interaction_pending.emit()

func _find_object_sprite() -> Sprite2D:
	# Check children of the Area2D
	if has_node("ObjectSprite"):
		return $ObjectSprite as Sprite2D
	if has_node("Sprite"):
		return $Sprite as Sprite2D
		
	# Check siblings (children of the parent)
	var p = get_parent()
	if p:
		if p.has_node("ObjectSprite"):
			return p.get_node("ObjectSprite") as Sprite2D
		if p.has_node("Sprite"):
			return p.get_node("Sprite") as Sprite2D
			
	return null
</file>

<file path="Non Interactables/camera_2d.gd">
extends Camera2D

@export var target_path: NodePath
var target_node: Node2D

@export_group("Marketing & Debug")
## Increase this value (e.g., 1.5 or 2.0) to zoom the camera in closer for GIFs.
@export var custom_zoom_level: float = 1.0

func _ready():
	# Apply the custom zoom level immediately
	zoom = Vector2(custom_zoom_level, custom_zoom_level)

	if target_path:
		target_node = get_node_or_null(target_path)

	position_smoothing_enabled = false

	set_process(false)
	set_physics_process(true)

func _physics_process(_delta):
	if is_instance_valid(target_node):
		# 3. Snap perfectly to the player in the physics step.
		# Because Physics Interpolation is ON globally, Godot will now automatically 
		# smooth the visual rendering of BOTH the player and the camera together!
		global_position = target_node.global_position

func snap_to_target():
	if not target_node: return
	global_position = target_node.global_position
	reset_physics_interpolation()

func set_camera_limits(left: int, right: int, top: int, bottom: int):
	limit_enabled = true
	limit_smoothed = false

	limit_left = left
	limit_right = right
	limit_top = top
	limit_bottom = bottom

	reset_physics_interpolation()
</file>

<file path="Non Interactables/camera_region.gd">
extends Area2D
class_name CameraRegion

@export var is_active_on_start: bool = false

@onready var collision_shape: CollisionShape2D = $CollisionShape2D

func _ready():
	collision_layer = 0
	collision_mask = 1

	body_entered.connect(_on_body_entered)

	if is_active_on_start:
		await get_tree().process_frame
		_apply_limits()

func _on_body_entered(body: Node2D):
	if body.is_in_group("player"):
		_apply_limits()

func _apply_limits():
	if not collision_shape or not collision_shape.shape is RectangleShape2D:
		push_warning("CameraRegion requires a CollisionShape2D with a RectangleShape2D.")
		return

	var shape = collision_shape.shape as RectangleShape2D

	var global_pos = collision_shape.global_position
	var extents = shape.size / 2.0

	var left = int(global_pos.x - extents.x)
	var right = int(global_pos.x + extents.x)
	var top = int(global_pos.y - extents.y)
	var bottom = int(global_pos.y + extents.y)

	var camera = get_viewport().get_camera_2d()
	if camera and camera.has_method("set_camera_limits"):
		camera.set_camera_limits(left, right, top, bottom)
</file>

<file path="testvideoplayer.gd">
extends VideoStreamPlayer

func _ready():
	# When the video finishes, force it to play again immediately
	finished.connect(play)
</file>

<file path="ui_interactable.gd">
# UI_Interactable.gd
# Attach this script to the root TextureButton node.

extends TextureButton

# Get a reference to the child node that has all the real game logic.
# The '$' syntax is a shorthand way to get a direct child node.
@onready var interactable_logic: Interactable = $Interactable


func _ready():
	# When this UI button is pressed, call our custom function.
	pressed.connect(_on_button_pressed)

	# Connect the hover signals to keep your outline effect!
	# This forwards the UI hover event to the Interactable's existing functions.
	mouse_entered.connect(interactable_logic._on_mouse_entered)
	mouse_exited.connect(interactable_logic._on_mouse_exited)


func _on_button_pressed():
	# When clicked, tell the GameManager to process an interaction,
	# but pass in our child node, which has all the data and logic.
	if GameManager:
		GameManager.process_interaction_click(interactable_logic)
</file>

<file path="ui/black_screen_overlay.gd">
# black_screen_overlay.gd
extends CanvasLayer
class_name BlackScreenOverlay

# This script is intentionally minimal for a simple overlay.
# You might add methods here later if the overlay needs to animate in/out.extends CanvasLayer
</file>

<file path="ui/ChapterButton.gd">
# ChapterButton.gd
extends Button

# This signal is emitted when the button is pressed, sending its own data along.
signal chapter_selected(data: MemoryChapterData)

# --- NODE REFERENCES ---
@onready var chapter_name_label: Label = $MarginContainer/VBoxContainer/Label
@onready var chapter_image_rect: TextureRect = $MarginContainer/VBoxContainer/TextureRect

# --- DATA ---
var chapter_data: MemoryChapterData
func _ready():
	# Connect our own "pressed" signal to a handler function.
	self.pressed.connect(_on_pressed)

	# --- TOUCH SCROLL FIX ---
	# Buttons default to STOP, which blocks the ScrollContainer from seeing swipes.
	# Changing this to PASS allows silky smooth native touch scrolling!
	self.mouse_filter = Control.MOUSE_FILTER_PASS

	# --- MOBILE SCALING FOR CHAPTER BUTTON ---
	if OS.has_feature("mobile"):
		self.custom_minimum_size = Vector2(240, 175)
		chapter_name_label.add_theme_font_size_override("font_size", 24)
		# Provide 56 pixels of vertical space so the text easily fits without clipping
		chapter_name_label.custom_minimum_size.y = 56

func _on_pressed():
	# When this button is pressed, emit our custom signal,
	# passing our own chapter_data along with it.
	emit_signal("chapter_selected", chapter_data)

## This function takes MemoryChapterData and configures the button's UI.
func populate(data: MemoryChapterData):
	self.chapter_data = data

	# --- 1. SET THE VISUALS ---
	chapter_name_label.text = chapter_data.chapter_name
	chapter_image_rect.texture = chapter_data.chapter_image

	# --- 2. HANDLE LOCKED/UNLOCKED STATE ---
	# Check if the chapter has a flag that needs to be checked.
	if not chapter_data.unlock_flag.is_empty():
		# Ask the GameManager if the flag is true or false.
		var is_unlocked = Flags.get_game_flag(chapter_data.unlock_flag)

		if not is_unlocked:
			# If the chapter is locked, make it look disabled.
			self.disabled = true
			# Modulate makes the button and its children look grayed out.
			self.modulate = Color(0.5, 0.5, 0.5, 1.0)

	# If there's no unlock_flag, the button is unlocked by default,
	# so we don't need to do anything.
</file>

<file path="ui/controls_menu.gd">
extends CanvasLayer

func _ready():
	process_mode = Node.PROCESS_MODE_ALWAYS
	layer = 120
	$ColorRect/VBoxContainer/CloseButton.pressed.connect(func():
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
	)

	if OS.has_feature("mobile"):
		$ColorRect.gui_input.connect(func(event):
			if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed():
				if SoundManager: SoundManager.play_sfx("ui_click")
				queue_free()
		)

func _input(event):
	if event is InputEventKey and event.keycode == KEY_ESCAPE and event.is_pressed() and not event.is_echo():
		get_viewport().set_input_as_handled()
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
	elif event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_RIGHT and event.is_pressed():
		get_viewport().set_input_as_handled()
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
</file>

<file path="ui/credits_menu.gd">
extends CanvasLayer

func _ready():
	process_mode = Node.PROCESS_MODE_ALWAYS
	layer = 120
	$ColorRect/VBoxContainer/CloseButton.pressed.connect(func():
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
	)

	if OS.has_feature("mobile"):
		$ColorRect.gui_input.connect(func(event):
			if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed():
				if SoundManager: SoundManager.play_sfx("ui_click")
				queue_free()
		)

func _input(event):
	if event is InputEventKey and event.keycode == KEY_ESCAPE and event.is_pressed() and not event.is_echo():
		get_viewport().set_input_as_handled()
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
	elif event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_RIGHT and event.is_pressed():
		get_viewport().set_input_as_handled()
		if SoundManager: SoundManager.play_sfx("ui_click")
		queue_free()
</file>

<file path="ui/custom_cursor_control.gd">
extends Control

@onready var parent = get_parent()

func _draw():
	var fill_color = Color(0.2, 0.85, 1.0, 1.0) if parent.is_hovering else Color.WHITE
	var outline_color = Color(0.0, 0.0, 0.0, 0.6) # Semi-transparent black outline
	var radius = parent._current_radius

	var dot_radius = 3.0
	var line_thickness = 3.0
	var outline_thickness = 2.0

	# Center dot outline
	draw_circle(Vector2.ZERO, dot_radius + outline_thickness, outline_color)
	# Center dot fill
	draw_circle(Vector2.ZERO, dot_radius, fill_color)

	# Draw crosshair lines
	var length = radius * 0.7
	for i in range(4):
		var angle = parent._rotation_angle + (i * PI / 2.0)
		var dir = Vector2(cos(angle), sin(angle))
		var start_pos = dir * (radius * 0.5)
		var end_pos = dir * (radius + length)

		# Line outline (drawn thicker and slightly longer/shorter to encase the fill)
		draw_line(start_pos - (dir * outline_thickness), end_pos + (dir * outline_thickness), outline_color, line_thickness + (outline_thickness * 2.0), true)
		# Line fill
		draw_line(start_pos, end_pos, fill_color, line_thickness, true)
</file>

<file path="ui/custom_cursor.gd">
extends CanvasLayer

var is_hovering: bool = false
var _rotation_angle: float = 0.0
var _current_radius: float = 8.0

func _ready():
	layer = 128 # Keep it above everything
	process_mode = Node.PROCESS_MODE_ALWAYS

func _process(delta):
	var mouse_pos = get_viewport().get_mouse_position()
	var needs_redraw = false

	# Only update if position changed
	if $Control.global_position != mouse_pos:
		$Control.global_position = mouse_pos
		needs_redraw = true

	# Smoothly interpolate radius
	var target_radius = 24.0 if is_hovering else 12.0
	if abs(_current_radius - target_radius) > 0.1:
		_current_radius = lerp(_current_radius, target_radius, delta * 15.0)
		needs_redraw = true

	# Rotate only if hovering, otherwise settle back to 0
	if is_hovering:
		_rotation_angle += delta * PI
		needs_redraw = true
	else:
		if abs(_rotation_angle) > 0.01:
			_rotation_angle = lerp_angle(_rotation_angle, 0.0, delta * 10.0)
			needs_redraw = true

	# Only force the GPU to redraw if something visually changed!
	if needs_redraw:
		$Control.queue_redraw()

func set_hover_state(hovering: bool):
	is_hovering = hovering
</file>

<file path="ui/DateInputWidget.gd">
# This script MUST extend Control.
extends Control

# --- THIS IS THE CRUCIAL FIX ---
# This code uses direct paths to the nodes shown in your screenshot.
# It is guaranteed to work with your scene structure.
@onready var d1: Control = $HBoxContainer/Day1
@onready var d2: Control = $HBoxContainer/Day2
@onready var m1: Control = $HBoxContainer/Month1
@onready var m2: Control = $HBoxContainer/Month2
@onready var y1: Control = $HBoxContainer/Year1
@onready var y2: Control = $HBoxContainer/Year2
@onready var y3: Control = $HBoxContainer/Year3
@onready var y4: Control = $HBoxContainer/Year4
# ---------------------------------

@onready var hbox_container: HBoxContainer = $HBoxContainer

func _ready():
	# This line fixes the layout/overlap issue.
	self.custom_minimum_size = hbox_container.get_combined_minimum_size()

func get_date_string() -> String:
	# Because the paths above are now correct, this function will work
	# without the "null instance" error.
	var day_str = str(d1.current_digit) + str(d2.current_digit)
	var month_str = str(m1.current_digit) + str(m2.current_digit)
	var year_str = str(y1.current_digit) + str(y2.current_digit) + str(y3.current_digit) + str(y4.current_digit)

	return "%s/%s/%s" % [day_str, month_str, year_str]
</file>

<file path="ui/dialogue_history_ui.gd">
extends CanvasLayer

@onready var scroll_container: ScrollContainer = $ColorRect/MarginContainer/VBoxContainer/ScrollContainer
@onready var history_list: VBoxContainer = $ColorRect/MarginContainer/VBoxContainer/ScrollContainer/HistoryList
@onready var close_button: Button = $ColorRect/MarginContainer/VBoxContainer/Header/CloseButton
@onready var title_label: Label = $ColorRect/MarginContainer/VBoxContainer/Header/Title

const PLAYER_NAMES = ["Player"]
var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")

var character_colors: Dictionary = {
	"AIda": Color("#20B2AA"),
	"Sergey": Color("#DAA520"),
	"McBucket": Color("#FF4500"),
	"Old Man": Color("#FF4500"),
	"Old Man McBucket": Color("#FF4500"),
	"Layla": Color("#FF8299"),
	"Lewgend": Color("#708090"),
	"Nathan": Color("#FF69B4"),
	"Dread": Color("#FF00FF"),
	"Hope": Color("#FFC8E3"),
	"The... Toilet?": Color("#DC143C"),
	"Player": Color("#FFD65C")
}

var character_portraits: Dictionary = {
	"AIda": preload("res://Sprites/dialogue sprites/aida_dialogue_sprite.PNG"),
	"Sergey": preload("res://Sprites/dialogue sprites/sergey_dialogue_sprite.png"),
	"McBucket": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Old Man": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Old Man McBucket": preload("res://Sprites/dialogue sprites/mcbucket_dialogue_sprite.PNG"),
	"Layla": preload("res://Sprites/dialogue sprites/layla_dialogue_sprite.png"),
	"Lewgend": preload("res://Sprites/dialogue sprites/lewgend_dialogue_sprite.png"),
	"Nathan": preload("res://icon.svg"),
	"The... Toilet?": preload("res://Sprites/dialogue sprites/toilet_dialogue_sprite.png"),
	"Player": preload("res://Sprites/dialogue sprites/protag_dialogue_sprite.png")
}

func _ready():
	process_mode = Node.PROCESS_MODE_ALWAYS
	layer = 115 # Above the dialogue balloon and Pause Menu (110)
	close_button.pressed.connect(_close_log)
	_populate_log()

	# --- UI POLISH: Title Label ---
	title_label.add_theme_font_override("font", custom_font)
	title_label.add_theme_font_size_override("font_size", 54 if OS.has_feature("mobile") else 36)
	title_label.add_theme_color_override("font_color", Color(0.2, 0.85, 1.0, 1.0)) # Cyan accent

	# --- UI POLISH: Close Button ---
	close_button.text = "Close"
	close_button.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	close_button.add_theme_font_override("font", custom_font)
	close_button.add_theme_font_size_override("font_size", 40 if OS.has_feature("mobile") else 24)

	# Remove the "flat" property if it was set in the inspector so our styleboxes work
	close_button.flat = false

	var normal_style = StyleBoxFlat.new()
	normal_style.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	normal_style.corner_radius_top_left = 6
	normal_style.corner_radius_top_right = 6
	normal_style.corner_radius_bottom_left = 6
	normal_style.corner_radius_bottom_right = 6
	normal_style.content_margin_left = 40 if OS.has_feature("mobile") else 25
	normal_style.content_margin_right = 40 if OS.has_feature("mobile") else 25
	normal_style.content_margin_top = 25 if OS.has_feature("mobile") else 10
	normal_style.content_margin_bottom = 25 if OS.has_feature("mobile") else 10
	normal_style.border_width_left = 2
	normal_style.border_width_top = 2
	normal_style.border_width_right = 2
	normal_style.border_width_bottom = 2
	normal_style.border_color = Color(1.0, 1.0, 1.0, 0.0) # Invisible border to prevent jitter
	normal_style.anti_aliasing = false

	var hover_style = normal_style.duplicate()
	hover_style.bg_color = Color(0.1, 0.25, 0.3, 0.9) # Subtle cyan tint
	hover_style.border_color = Color(0.2, 0.85, 1.0, 0.8) # Crisp cyan border

	close_button.add_theme_stylebox_override("normal", normal_style)
	close_button.add_theme_stylebox_override("hover", hover_style)
	close_button.add_theme_stylebox_override("focus", hover_style)
	close_button.add_theme_stylebox_override("pressed", hover_style)

	close_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
	close_button.add_theme_color_override("font_hover_color", Color.WHITE)
	close_button.add_theme_color_override("font_pressed_color", Color.WHITE)
	# ------------------------------

	if OS.has_feature("mobile"):
		scroll_container.scroll_deadzone = 24

	# Wait one frame for the VBoxContainer to calculate its size, then scroll to bottom
	await get_tree().process_frame
	scroll_container.scroll_vertical = int(scroll_container.get_v_scroll_bar().max_value)

func _input(event):
	# Close on Right Click
	if event is InputEventMouseButton and event.is_pressed():
		if event.button_index == MOUSE_BUTTON_RIGHT:
			get_viewport().set_input_as_handled()
			_close_log()
	# Close on Escape or H
	elif event is InputEventKey and event.is_pressed() and not event.is_echo():
		if event.keycode == KEY_ESCAPE or event.keycode == KEY_H:
			get_viewport().set_input_as_handled()
			_close_log()

func _populate_log():
	if not GameManager: return

	var previous_character = ""
	var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")

	for entry in DialogueHistory.entries:
		var align_right = false
		var lookup_name = entry.get("lookup_name", "")
		var display_name = entry.get("display_name", "")

		# Determine if this is a continuation of the same speaker
		var is_continuation = (lookup_name == previous_character and lookup_name != "")
		previous_character = lookup_name

		# --- ACTION LOGGING LOGIC (FIXED) ---
		if entry["type"] == "action":
			var action_label = RichTextLabel.new()
			action_label.bbcode_enabled = true
			action_label.fit_content = true
			action_label.mouse_filter = Control.MOUSE_FILTER_PASS
			action_label.size_flags_horizontal = Control.SIZE_EXPAND_FILL
			action_label.add_theme_font_override("normal_font", custom_font)
			action_label.add_theme_font_size_override("normal_font_size", 32 if OS.has_feature("mobile") else 20)

			var v = entry["verb"]
			var o = entry["object"]
			var i = entry["item"]

			var text = ""
			if i != "":
				text = "[center][color=#888888]— Used [color=#33d9ff]'%s'[/color] on [color=#33d9ff]'%s'[/color] —[/color][/center]" % [i, o]
			else:
				text = "[center][color=#888888]— Performed [color=#33d9ff]'%s'[/color] on [color=#33d9ff]'%s'[/color] —[/color][/center]" % [v, o]

			action_label.text = text

			var action_margin = MarginContainer.new()
			action_margin.add_theme_constant_override("margin_top", 10)
			action_margin.add_theme_constant_override("margin_bottom", 15)
			action_margin.add_child(action_label)

			history_list.add_child(action_margin)

			# Reset previous character so the next dialogue line forces a nameplate/portrait
			previous_character = ""
			continue
		# -----------------------------

		if lookup_name in PLAYER_NAMES:
			align_right = true

		var row_box = HBoxContainer.new()
		row_box.size_flags_horizontal = Control.SIZE_EXPAND_FILL
		row_box.add_theme_constant_override("separation", 20)

		var portrait = TextureRect.new()
		portrait.custom_minimum_size = Vector2(130, 130) if OS.has_feature("mobile") else Vector2(80, 80)
		portrait.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
		portrait.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
		portrait.mouse_filter = Control.MOUSE_FILTER_IGNORE

		if not is_continuation and character_portraits.has(lookup_name):
			portrait.texture = character_portraits[lookup_name]

		var text_vbox = VBoxContainer.new()
		text_vbox.size_flags_horizontal = Control.SIZE_EXPAND_FILL

		if not is_continuation and display_name != "":
			var name_label = Label.new()
			name_label.text = display_name
			name_label.add_theme_font_override("font", custom_font)
			name_label.add_theme_font_size_override("font_size", 38 if OS.has_feature("mobile") else 24)

			if character_colors.has(lookup_name):
				name_label.add_theme_color_override("font_color", character_colors[lookup_name])
			else:
				name_label.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8))

			if align_right:
				name_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_RIGHT
			text_vbox.add_child(name_label)

		if entry["type"] == "line":
			var text = entry["text"]
			var text_label = RichTextLabel.new()
			text_label.bbcode_enabled = true
			text_label.fit_content = true
			text_label.mouse_filter = Control.MOUSE_FILTER_PASS
			text_label.size_flags_horizontal = Control.SIZE_EXPAND_FILL
			text_label.add_theme_font_override("normal_font", custom_font)
			text_label.add_theme_font_size_override("normal_font_size", 42 if OS.has_feature("mobile") else 28)
			text_label.add_theme_color_override("default_color", Color(0.9, 0.9, 0.9))

			if align_right:
				text_label.text = "[right]" + text + "[/right]"
			else:
				text_label.text = text

			text_vbox.add_child(text_label)

		elif entry["type"] == "choice":
			var options = entry["options"]
			var selected_index = entry["selected_index"]

			for i in range(options.size()):
				var clean_text = options[i].replacen("[proceed]", "").replacen("[back]", "").replacen("[leave]", "").strip_edges()
				var choice_label = RichTextLabel.new()
				choice_label.bbcode_enabled = true
				choice_label.fit_content = true
				choice_label.mouse_filter = Control.MOUSE_FILTER_PASS
				choice_label.size_flags_horizontal = Control.SIZE_EXPAND_FILL
				choice_label.add_theme_font_override("normal_font", custom_font)
				choice_label.add_theme_font_size_override("normal_font_size", 38 if OS.has_feature("mobile") else 26)

				if i == selected_index:
					choice_label.text = "[right][color=#33d9ff]> " + clean_text + "[/color][/right]"
				else:
					choice_label.text = "[right][color=#888888][s]" + clean_text + "[/s][/color][/right]"

				text_vbox.add_child(choice_label)

		# Assemble the row
		if align_right:
			row_box.add_child(text_vbox)
			row_box.add_child(portrait)
		else:
			row_box.add_child(portrait)
			row_box.add_child(text_vbox)

		var margin = MarginContainer.new()
		margin.add_theme_constant_override("margin_bottom", 5 if is_continuation else 25)
		margin.add_child(row_box)
		history_list.add_child(margin)

func _close_log():
	if SoundManager: SoundManager.play_sfx("ui_click")
	queue_free()
</file>

<file path="ui/difficulty_select_screen.gd">
extends CanvasLayer

signal difficulty_chosen(is_assisted: bool)

@onready var adventure_button: Button = %AdventureButton
@onready var story_button: Button = %StoryButton

var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")

func _ready():
	_apply_button_styling(adventure_button)
	_apply_button_styling(story_button)

	adventure_button.pressed.connect(func(): _on_selection(false))
	story_button.pressed.connect(func(): _on_selection(true))

	# --- MOBILE TEXT SCALING ---
	if OS.has_feature("mobile"):
		var title = $CenterContainer/VBoxContainer/TitleLabel
		var subtitle = $CenterContainer/VBoxContainer/SubtitleLabel
		var std_desc = $CenterContainer/VBoxContainer/HBoxContainer/LeftVBox/AdventureDesc
		var std_rec = $CenterContainer/VBoxContainer/HBoxContainer/LeftVBox/AdventureRec
		var ast_desc = $CenterContainer/VBoxContainer/HBoxContainer/RightVBox/StoryDesc
		var ast_rec = $CenterContainer/VBoxContainer/HBoxContainer/RightVBox/StoryRec
		var disclaimer = $CenterContainer/VBoxContainer/DisclaimerLabel

		title.add_theme_font_size_override("font_size", 72)

		subtitle.add_theme_font_size_override("font_size", 36)
		subtitle.custom_minimum_size.x = 1200

		std_desc.add_theme_font_size_override("font_size", 32)
		std_desc.custom_minimum_size.x = 550

		std_rec.add_theme_font_size_override("font_size", 28)
		std_rec.custom_minimum_size.x = 550

		ast_desc.add_theme_font_size_override("font_size", 32)
		ast_desc.custom_minimum_size.x = 550

		ast_rec.add_theme_font_size_override("font_size", 28)
		ast_rec.custom_minimum_size.x = 550

		disclaimer.add_theme_font_size_override("font_size", 28)

func _apply_button_styling(btn: Button):
	btn.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	btn.add_theme_font_override("font", custom_font)
	btn.add_theme_font_size_override("font_size", 42 if OS.has_feature("mobile") else 28)

	var normal_style = StyleBoxFlat.new()
	normal_style.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	normal_style.corner_radius_top_left = 8
	normal_style.corner_radius_top_right = 8
	normal_style.corner_radius_bottom_left = 8
	normal_style.corner_radius_bottom_right = 8
	normal_style.content_margin_left = 50 if OS.has_feature("mobile") else 30
	normal_style.content_margin_right = 50 if OS.has_feature("mobile") else 30
	normal_style.content_margin_top = 35 if OS.has_feature("mobile") else 20
	normal_style.content_margin_bottom = 35 if OS.has_feature("mobile") else 20
	normal_style.border_width_left = 2
	normal_style.border_width_top = 2
	normal_style.border_width_right = 2
	normal_style.border_width_bottom = 2
	normal_style.border_color = Color(1.0, 1.0, 1.0, 0.0)
	normal_style.anti_aliasing = false

	var hover_style = normal_style.duplicate()
	hover_style.bg_color = Color(0.1, 0.25, 0.3, 0.9)
	hover_style.border_color = Color(0.2, 0.85, 1.0, 0.8)

	btn.add_theme_stylebox_override("normal", normal_style)
	btn.add_theme_stylebox_override("hover", normal_style if OS.has_feature("mobile") else hover_style)
	btn.add_theme_stylebox_override("pressed", hover_style)
	btn.add_theme_stylebox_override("disabled", normal_style)
	btn.focus_mode = Control.FOCUS_NONE
	btn.add_theme_stylebox_override("focus", StyleBoxEmpty.new())
	btn.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
	btn.add_theme_color_override("font_hover_color", Color(0.8, 0.8, 0.8, 1.0) if OS.has_feature("mobile") else Color.WHITE)
	btn.add_theme_color_override("font_disabled_color", Color(0.8, 0.8, 0.8, 1.0))

func _on_selection(is_assisted: bool):
	if SoundManager: SoundManager.play_sfx("start_game")

	adventure_button.disabled = true
	story_button.disabled = true

	difficulty_chosen.emit(is_assisted)
</file>

<file path="ui/DigitSpinner.gd">
# This script MUST extend "Control" because its root node is a Control node.
extends Control

# This signal is how the spinner communicates with the outside world.
# It will be used later if you want to validate the date as it's being entered.
signal digit_changed(new_digit: int)

# Get direct references to the nodes within this scene.
@onready var up_button: Button = $VBoxContainer/UpButton
@onready var down_button: Button = $VBoxContainer/DownButton
@onready var digit_label: Label = $VBoxContainer/DigitLabel

# This variable holds the "state" or current value of this spinner.
var current_digit: int = 0


# The _ready function runs once, when the node is first added to the scene.
# It's the perfect place for setup.
func _ready():
	# --- MOBILE SCALING ---
	if OS.has_feature("mobile"):
		custom_minimum_size = Vector2(80, 200)
		up_button.add_theme_font_size_override("font_size", 42)
		down_button.add_theme_font_size_override("font_size", 42)
		digit_label.add_theme_font_size_override("font_size", 46)

	# Set the label to its starting value.
	_update_label()
	# Connect the buttons' 'pressed' signals to our functions below.
	up_button.pressed.connect(_on_up_pressed)
	down_button.pressed.connect(_on_down_pressed)


# This function is called ONLY when the up_button is pressed.
func _on_up_pressed():
	# Increment the digit, wrapping from 9 back to 0 using the modulo operator.
	current_digit = (current_digit + 1) % 10
	# Update the visual text.
	_update_label()
	# Announce that the digit has changed and what its new value is.
	emit_signal("digit_changed", current_digit)


# This function is called ONLY when the down_button is pressed.
func _on_down_pressed():
	# Decrement the digit, wrapping from 0 back to 9.
	if current_digit == 0:
		current_digit = 9
	else:
		current_digit -= 1
	# Update the visual text.
	_update_label()
	# Announce that the digit has changed and what its new value is.
	emit_signal("digit_changed", current_digit)


# A helper function to avoid repeating the same line of code.
# It simply updates the Label's text to match our current_digit variable.
func _update_label():
	digit_label.text = str(current_digit)
</file>

<file path="ui/explanation_layer.gd">
extends CanvasLayer
signal explanation_finished

# --- Node References ---
@onready var color_rect: ColorRect = $ColorRect
@onready var explanation_label: Label = $PanelContainer/VBoxContainer/ExplanationLabel
@onready var next_button: Button = $PanelContainer/VBoxContainer/NextButton

# --- State Variables ---
var current_explanation_data: ExplanationData = null
var current_line_index: int = 0

func _ready():
	next_button.pressed.connect(_on_next_button_pressed)
	hide()

	if OS.has_feature("mobile"):
		var panel = $PanelContainer
		panel.custom_minimum_size = Vector2(1000, 450)
		panel.offset_left = -500
		panel.offset_top = -225
		panel.offset_right = 500
		panel.offset_bottom = 225

		explanation_label.add_theme_font_size_override("font_size", 44)
		next_button.add_theme_font_size_override("font_size", 40)

		var btn_style = next_button.get_theme_stylebox("normal").duplicate()
		btn_style.content_margin_top = 25
		btn_style.content_margin_bottom = 25
		btn_style.content_margin_left = 50
		btn_style.content_margin_right = 50
		next_button.add_theme_stylebox_override("normal", btn_style)
		next_button.add_theme_stylebox_override("hover", btn_style)
		next_button.add_theme_stylebox_override("pressed", btn_style)
		next_button.add_theme_stylebox_override("focus", btn_style)

func show_explanation(data: ExplanationData, root_node: Node):
	if not data or data.explanation_lines.is_empty():
		print("Error: Invalid or empty explanation data provided.")
		return

	await get_tree().process_frame

	var target_node: Node = root_node.get_node_or_null(data.target_node_path)

	if is_instance_valid(target_node) and target_node is CanvasItem:
		var node_size = target_node.get_rect().size
		var center_position = target_node.global_position + (node_size / 2.0)

		var target_screen_pos: Vector2

		if target_node is Control:
			target_screen_pos = center_position
		elif target_node is Node2D:
			target_screen_pos = get_viewport().get_canvas_transform().affine_inverse() * center_position
		else:
			print_rich("[color=orange]Explanation Warning: Target node is an unsupported visual type. Defaulting to center.[/color]")
			target_screen_pos = get_viewport().get_visible_rect().size / 2

		update_spotlight_position(target_screen_pos)
	else:
		print_rich("[color=orange]Explanation Warning: Target node not found. Defaulting to screen center.[/color]")
		update_spotlight_position(get_viewport().get_visible_rect().size / 2)

	current_explanation_data = data
	current_line_index = 0

	_update_display()
	show()

func hide_explanation():
	current_explanation_data = null
	hide()
	explanation_finished.emit()

func update_spotlight_position(pos: Vector2):
	if color_rect.material is ShaderMaterial:
		# Send the Viewport size to the shader to fix the scaling bug
		var screen_size = get_viewport().get_visible_rect().size
		color_rect.material.set_shader_parameter("screen_size", screen_size)
		color_rect.material.set_shader_parameter("hole_position", pos)

func _update_display():
	explanation_label.text = current_explanation_data.explanation_lines[current_line_index]

	if current_line_index == current_explanation_data.explanation_lines.size() - 1:
		next_button.text = "Finish"
	else:
		next_button.text = "Next"

func _on_next_button_pressed():
	# Play the exact same sound used in the dialogue balloon
	if SoundManager:
		SoundManager.play_sfx("ui_click")
	current_line_index += 1

	if current_line_index >= current_explanation_data.explanation_lines.size():
		hide_explanation()
	else:
		_update_display()
</file>

<file path="ui/game_over.gd">
extends CanvasLayer

@onready var animation_player: AnimationPlayer = $AnimationPlayer

func _ready():
	animation_player.play("fade_in")

func _on_main_menu_button_pressed():
	if SoundManager and SoundManager.has_method("play_sfx"):
		SoundManager.play_sfx("ui_click")

	# Swap back to Main Menu state
	GameManager.change_game_state(GameManager.GameState.MAIN_MENU)

	# Fade the black screen away smoothly so we can see the menu
	if is_instance_valid(GameManager.transition_layer) and GameManager.transition_layer.has_method("global_fade_from_black"):
		GameManager.transition_layer.global_fade_from_black(1.5)

	# Destroy this game over screen
	queue_free()
</file>

<file path="ui/input_blocker.gd">
# Attach this script to your InputBlocker ColorRect node temporarily
extends ColorRect

func _ready():
	print_rich("[color=cyan]InputBlocker ready: MouseFilter=%s, Size=%s[/color]" % [mouse_filter, size])

func _gui_input(event):
	if event is InputEventMouseButton:
		print_rich("[color=red]InputBlocker caught mouse click! This should block world input.[/color]")
		# Accept the event to consume it
		accept_event()
</file>

<file path="ui/insurance_form_button_ui.gd">
extends CanvasLayer

signal form_button_pressed

# We updated the node path to match the new panel structure
@onready var texture_button: TextureButton = $FormPanel/MarginContainer/TextureButton

func _ready():
	# --- UNIVERSAL LAYOUT (PC & Mobile) ---
	var form_panel = $FormPanel
	var tab_panel = $FormPanel/TabPanel
	var tab_label = $FormPanel/TabPanel/Label

	form_panel.anchor_left = 0.89
	form_panel.anchor_right = 0.98

	tab_panel.anchor_left = 0.5
	tab_panel.anchor_right = 0.5
	tab_panel.offset_left = -60
	tab_panel.offset_right = 60

	if OS.has_feature("mobile"):
		form_panel.anchor_top = 0.75
		tab_panel.offset_top = -50
		tab_label.add_theme_font_size_override("font_size", 28)
	# --------------------------------------

	texture_button.pressed.connect(_on_texture_button_pressed)
	
	# Add hover effects to make it feel responsive
	texture_button.mouse_entered.connect(_on_hover_enter)
	texture_button.mouse_exited.connect(_on_hover_exit)

func _on_hover_enter():
	# --- FIX: Prevent hover effects during explanations ---
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return
		
	# Tint it bright cyan when hovered
	texture_button.modulate = Color(0.2, 0.85, 1.0, 1.0)

func _on_hover_exit():
	# --- FIX: Prevent hover effects during explanations ---
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return
		
	# Return to normal colors
	texture_button.modulate = Color(1.0, 1.0, 1.0, 1.0)

func _on_texture_button_pressed():
	# --- FIX: Prevent clicking the button during explanations ---
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return
		
	# Play a UI click sound
	if SoundManager:
		SoundManager.play_sfx("ui_click")
		
	print("The insurance form was clicked on!")
	emit_signal("form_button_pressed")
</file>

<file path="ui/insurance_form.gd">
extends CanvasLayer

signal form_closed

const FORM_DIALOGUE = preload("res://form_related_dialogue.dialogue")
const CONVERSATION_BALLOON_SCENE = preload("res://conversationballoon.tscn")

# Inputs from LineEditContainer
@onready var first_name_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/FirstName_Edit
@onready var middle_name_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/MiddleName_Edit
@onready var last_name_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/LastName_Edit
@onready var dob_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/DOB_Edit
@onready var phone_number_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/PhoneNumber_Edit
@onready var account_number_edit: LineEdit = $TabletFrame/Padding/ContentCanvas/LineEditContainer/AccountNumber_Edit

# Buttons from ButtonContainer
@onready var first_name_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/FirstName_Button
@onready var middle_name_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/MiddleName_Button
@onready var last_name_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/LastName_Button
@onready var dob_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/DOB_Button
@onready var phone_number_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/PhoneNumber_Button
@onready var account_number_button: Button = $TabletFrame/Padding/ContentCanvas/ButtonContainer/AccountNumber_Button

# The Submit and Back buttons
@onready var submit_button: Button = $TabletFrame/Padding/ContentCanvas/Submit_Button
@onready var back_button: Button = $TabletFrame/Padding/ContentCanvas/Back_Button

func _ready():
	hide()
	# Connect every single button to its own function.
	first_name_button.pressed.connect(_on_first_name_submit)
	middle_name_button.pressed.connect(_on_middle_name_submit)
	last_name_button.pressed.connect(_on_last_name_submit)
	dob_button.pressed.connect(_on_dob_submit)
	phone_number_button.pressed.connect(_on_phone_number_submit)
	account_number_button.pressed.connect(_on_account_number_submit)

	submit_button.pressed.connect(_on_submit_application_pressed)
	back_button.pressed.connect(_on_submit_form) # Route the back button to close the form

	# --- BACK BUTTON OUTLINE (PC & Mobile) ---
	back_button.flat = false
	var back_normal = StyleBoxFlat.new()
	back_normal.bg_color = Color(0.1, 0.15, 0.2, 0.8)
	back_normal.corner_radius_top_left = 6
	back_normal.corner_radius_top_right = 6
	back_normal.corner_radius_bottom_left = 6
	back_normal.corner_radius_bottom_right = 6
	back_normal.border_width_left = 2
	back_normal.border_width_top = 2
	back_normal.border_width_right = 2
	back_normal.border_width_bottom = 2
	back_normal.border_color = Color(0.4, 0.5, 0.6, 0.8)
	back_normal.content_margin_left = 15
	back_normal.content_margin_right = 15
	back_normal.content_margin_top = 5
	back_normal.content_margin_bottom = 5

	var back_hover = back_normal.duplicate()
	back_hover.bg_color = Color(0.2, 0.3, 0.4, 0.9)
	back_hover.border_color = Color(0.2, 0.85, 1.0, 1.0)

	back_button.add_theme_stylebox_override("normal", back_normal)
	back_button.add_theme_stylebox_override("hover", back_hover)
	back_button.add_theme_stylebox_override("focus", back_hover)
	back_button.add_theme_stylebox_override("pressed", back_hover)

	# --- MOBILE SCALING FOR INSURANCE FORM ---
	if OS.has_feature("mobile"):
		$TabletFrame.anchor_left = 0.05
		$TabletFrame.anchor_right = 0.95
		$TabletFrame.anchor_top = 0.05
		$TabletFrame.anchor_bottom = 0.95

		var header_logo = $TabletFrame/Padding/ContentCanvas/HeaderContainer/HavemoreLabel
		var header_text = $TabletFrame/Padding/ContentCanvas/HeaderContainer/Label2
		var subtitle = $TabletFrame/Padding/ContentCanvas/Subtitle

		header_logo.add_theme_font_size_override("font_size", 60)
		header_text.add_theme_font_size_override("font_size", 44)
		subtitle.add_theme_font_size_override("font_size", 30)
		subtitle.offset_top = 130
		subtitle.offset_bottom = 170

		back_button.add_theme_font_size_override("font_size", 36)
		submit_button.add_theme_font_size_override("font_size", 42)

		var labels_container = $TabletFrame/Padding/ContentCanvas/LabelsContainer
		for child in labels_container.get_children():
			if child is Label:
				child.add_theme_font_size_override("font_size", 32)
				child.custom_minimum_size.y = 80

		var edits_container = $TabletFrame/Padding/ContentCanvas/LineEditContainer
		for child in edits_container.get_children():
			if child is LineEdit:
				child.add_theme_font_size_override("font_size", 30)
				child.custom_minimum_size.y = 80

		var btns_container = $TabletFrame/Padding/ContentCanvas/ButtonContainer
		for child in btns_container.get_children():
			if child is Button:
				child.add_theme_font_size_override("font_size", 30)
				child.custom_minimum_size.y = 80

	# --- DYNAMIC UI ADJUSTMENTS ---
	# Change all "Confirm" buttons to "Check"
	var check_buttons = [first_name_button, middle_name_button, last_name_button, dob_button, phone_number_button, account_number_button]
	for btn in check_buttons:
		btn.text = "Check"

	# Visually "disable" the submit button (but keep it clickable for dialogue)
	submit_button.modulate = Color(0.5, 0.5, 0.5, 1.0)

	# --- RESTORE PREVIOUSLY SOLVED FIELDS ---
	if GameManager and Flags.get_game_flag("first_name_correct"):
		lock_field("first_name", "FIONA")

	# Close form when clicking the dark background
	$ColorRect/BackgroundButton.pressed.connect(func():
		if SoundManager: SoundManager.play_sfx("ui_click")
		_on_submit_form()
	)

# --- HANDLER FUNCTIONS FOR EACH "OKAY" BUTTON ---

func _on_first_name_submit():
	_handle_field_submitted("first_name", first_name_edit.text)

func _on_middle_name_submit():
	_handle_field_submitted("middle_name", middle_name_edit.text)

func _on_last_name_submit():
	_handle_field_submitted("last_name", last_name_edit.text)

func _on_dob_submit():
	_handle_field_submitted("date_of_birth", dob_edit.text)

func _on_phone_number_submit():
	_handle_field_submitted("phone_number", phone_number_edit.text)

func _on_account_number_submit():
	_handle_field_submitted("account_number", account_number_edit.text)

# --- HANDLER FOR THE FINAL SUBMIT/CLOSE BUTTON ---

func _on_submit_application_pressed():
	_handle_submit_requested()

func _on_submit_form():
	# This function simply closes the form.
	emit_signal("form_closed")
	queue_free()

# --- UI LOCKING LOGIC ---

func lock_field(field_id: String, corrected_value: String):
	match field_id:
		"first_name":
			first_name_edit.text = corrected_value
			first_name_edit.editable = false
			first_name_edit.modulate = Color(0.5, 0.5, 0.5, 1.0) # Grey out text box
			
			first_name_button.disabled = true
			first_name_button.modulate = Color(0.5, 0.5, 0.5, 1.0) # Grey out button
			
		# You can add additional fields here as you expand the logic!
		# "last_name":
		#     last_name_edit.text = corrected_value
		#     last_name_edit.editable = false
		#     last_name_edit.modulate = Color(0.5, 0.5, 0.5, 1.0)
		#     last_name_button.disabled = true
		#     last_name_button.modulate = Color(0.5, 0.5, 0.5, 1.0)

# --- PUZZLE LOGIC (moved from GameManager) ---

func _handle_field_submitted(field_id: String, value):
	match field_id:
		"first_name":
			var regex = RegEx.new()
			regex.compile("(?i)^\\s*fiona\\s*$")

			if regex.search(value):
				if SoundManager: SoundManager.play_sfx("form_correct_input")
				lock_field("first_name", "FIONA")

				var balloon = DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, FORM_DIALOGUE, "first_name_correct")
				if is_instance_valid(balloon): balloon.process_mode = Node.PROCESS_MODE_ALWAYS

				Flags.set_game_flag("first_name_correct", true)

			else:
				if SoundManager: SoundManager.play_sfx("form_incorrect_input")

				var formatted_wrong_name = _format_wrong_name(value)
				var temp_state = {"wrong_name": formatted_wrong_name}
				var balloon = DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, FORM_DIALOGUE, "first_name_incorrect", [temp_state])
				if is_instance_valid(balloon): balloon.process_mode = Node.PROCESS_MODE_ALWAYS

		_:
			if SoundManager: SoundManager.play_sfx("form_incorrect_input")
			var balloon = DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, FORM_DIALOGUE, "field_not_ready")
			if is_instance_valid(balloon): balloon.process_mode = Node.PROCESS_MODE_ALWAYS

func _format_wrong_name(raw_name: String) -> String:
	var clean = raw_name.strip_edges().to_lower()
	if clean.length() == 0:
		return "..."
	elif clean.length() <= 2:
		return clean.substr(0, 1).to_upper() + clean.substr(1) + "..."
	else:
		var part1 = clean.substr(0, 1).to_upper() + clean.substr(1, 1)
		var part2 = clean.substr(2)
		return part1 + "..." + part2

func _handle_submit_requested():
	var f_name = Flags.get_game_flag("first_name_correct")
	var m_name = Flags.get_game_flag("middle_name_correct")
	var l_name = Flags.get_game_flag("last_name_correct")
	var dob = Flags.get_game_flag("dob_correct")
	var phone = Flags.get_game_flag("phone_number_correct")
	var account = Flags.get_game_flag("account_number_correct")

	if f_name and m_name and l_name and dob and phone and account:
		if SoundManager: SoundManager.play_sfx("form_correct_input")
		var balloon = DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, FORM_DIALOGUE, "complete_submit")
		if is_instance_valid(balloon): balloon.process_mode = Node.PROCESS_MODE_ALWAYS
	else:
		if SoundManager: SoundManager.play_sfx("form_incorrect_input")
		var balloon = DialogueManager.show_dialogue_balloon_scene(CONVERSATION_BALLOON_SCENE, FORM_DIALOGUE, "incomplete_submit")
		if is_instance_valid(balloon): balloon.process_mode = Node.PROCESS_MODE_ALWAYS
</file>

<file path="ui/InventoryUI.gd">
# InventoryUI.gd
extends CanvasLayer

# --- Node References ---
@onready var item_name_hover_label: Label = $ItemNameHoverLabel
@onready var inventory_grid_container: GridContainer = $InventoryGridPanel/InventoryGridContainer
@onready var up_button: Button = $InventoryGridPanel/UpButton
@onready var down_button: Button = $InventoryGridPanel/DownButton

# --- Configuration ---
const ITEMS_PER_PAGE: int = 6 # 3 columns * 2 rows
const EQUIPPED_INDICATOR_NODE_NAME: String = "EquippedIndicator"
const ITEM_ICON_NODE_NAME: String = "ItemIcon"

# --- State Variables ---
var all_inventory_slots: Array[Button] = [] # Will hold the 6 Button nodes for slots
var current_player_inventory_cache: Array[ItemData] = [] # Local cache of player's items
var current_page_index: int = 0
var total_pages: int = 0


func _ready():
	# Validate node paths
	if not item_name_hover_label: print_rich("[color=red]InventoryUI: ItemNameHoverLabel not found![/color]")
	if not inventory_grid_container: print_rich("[color=red]InventoryUI: InventoryGridContainer not found![/color]"); return
	if not up_button: print_rich("[color=red]InventoryUI: UpButton not found![/color]")
	if not down_button: print_rich("[color=red]InventoryUI: DownButton not found![/color]")

	if inventory_grid_container:
		inventory_grid_container.columns = 3 # Ensure 3 columns for 3x2 layout

	# Enforce horizontal anchors to prevent overlapping
	$InventoryGridPanel.anchor_left = 0.46
	$InventoryGridPanel.anchor_right = 0.76

	if OS.has_feature("mobile"):
		$InventoryGridPanel.anchor_top = 0.75

		$InventoryGridPanel/TabPanel.anchor_left = 1.0
		$InventoryGridPanel/TabPanel.anchor_right = 1.0
		$InventoryGridPanel/TabPanel.offset_left = -220
		$InventoryGridPanel/TabPanel.offset_right = -15
		$InventoryGridPanel/TabPanel.offset_top = -50

		$InventoryGridPanel/TabPanel/InventoryLabel.add_theme_font_size_override("font_size", 28)
		if up_button: up_button.add_theme_font_size_override("font_size", 24)
		if down_button: down_button.add_theme_font_size_override("font_size", 24)

	# --- Initialize Hover Label ---
	if item_name_hover_label:
		item_name_hover_label.visible = false

	# --- Initialize exactly ITEMS_PER_PAGE slot buttons ---
	# Clear any existing children from the editor (good practice)
	for child in inventory_grid_container.get_children():
		inventory_grid_container.remove_child(child)
		child.queue_free()
	all_inventory_slots.clear()

	for i in range(ITEMS_PER_PAGE):
		var slot_button = Button.new()
		slot_button.name = "InventorySlotButton_" + str(i)
		if OS.has_feature("mobile"):
			slot_button.custom_minimum_size = Vector2(0, 0)
			slot_button.add_theme_font_size_override("font_size", 26)
		else:
			slot_button.custom_minimum_size = Vector2(60, 60)
		slot_button.size_flags_horizontal = Control.SIZE_EXPAND_FILL
		slot_button.size_flags_vertical = Control.SIZE_EXPAND_FILL
		slot_button.disabled = true
		slot_button.focus_mode = Control.FOCUS_NONE
		slot_button.clip_text = true

		# Add a TextureRect inside the button for the icon
		var icon_rect = TextureRect.new()
		icon_rect.name = ITEM_ICON_NODE_NAME
		icon_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
		icon_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
		icon_rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT, Control.PRESET_MODE_MINSIZE, 2)
		icon_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
		slot_button.add_child(icon_rect)

		all_inventory_slots.append(slot_button)
		inventory_grid_container.add_child(slot_button)

	# --- Connect to GameManager Signals ---
	if GameManager:
		Inventory.inventory_updated.connect(_on_game_manager_inventory_updated)
		GameManager.selected_inventory_item_changed.connect(_on_game_manager_selected_item_changed)
		GameManager.interaction_complete.connect(_on_interaction_complete)
		GameManager.verb_changed.connect(_on_game_manager_verb_changed)
		GameManager.verb_lock_changed.connect(_on_game_manager_verb_lock_changed)

		if GameManager.has_method("get_player_inventory"):
			_on_game_manager_inventory_updated(Inventory.get_items())
		else:
			print_rich("[color=orange]InventoryUI: GameManager has no get_player_inventory method.[/color]")
	else:
		print_rich("[color=red]InventoryUI: GameManager not found![/color]")

	# --- Connect Pagination Buttons ---
	if up_button:
		up_button.pressed.connect(_on_up_button_pressed)
		up_button.text = ""
		_setup_button_icon(up_button, false)

	if down_button:
		down_button.pressed.connect(_on_down_button_pressed)
		down_button.text = ""
		_setup_button_icon(down_button, true)

	_update_pagination_buttons_state() # Initial state


func _process(_delta: float) -> void:
	if item_name_hover_label and item_name_hover_label.visible:
		item_name_hover_label.global_position = get_viewport().get_mouse_position() + Vector2(20, -30)


func _on_game_manager_inventory_updated(full_inventory_data: Array[ItemData]):
	current_player_inventory_cache = full_inventory_data.duplicate() # Store a copy

	total_pages = 1 if current_player_inventory_cache.is_empty() else ceil(float(current_player_inventory_cache.size()) / ITEMS_PER_PAGE)
	current_page_index = clamp(current_page_index, 0, max(0, total_pages - 1))

	_render_current_page() # This will also update selected state via its call to _update_slot_selected_visual_state
	_update_pagination_buttons_state()


func _render_current_page():
	if not is_instance_valid(inventory_grid_container):
		print_rich("[color=red]InventoryUI: _render_current_page - InventoryGridContainer is null or invalid![/color]")
		return
	if all_inventory_slots.is_empty():
		print_rich("[color=yellow]InventoryUI: _render_current_page - all_inventory_slots is empty.[/color]")
		return

	var start_index = current_page_index * ITEMS_PER_PAGE

	for i in range(all_inventory_slots.size()):
		var slot_button: Button = all_inventory_slots[i]
		if not is_instance_valid(slot_button): continue

		var icon_rect: TextureRect = slot_button.get_node_or_null(ITEM_ICON_NODE_NAME)

		# Safely disconnect any existing connections bound to this script
		for conn in slot_button.pressed.get_connections():
			if conn.callable.get_object() == self:
				slot_button.pressed.disconnect(conn.callable)

		for conn in slot_button.mouse_entered.get_connections():
			if conn.callable.get_object() == self:
				slot_button.mouse_entered.disconnect(conn.callable)

		for conn in slot_button.mouse_exited.get_connections():
			if conn.callable.get_object() == self:
				slot_button.mouse_exited.disconnect(conn.callable)

		var inventory_item_index = start_index + i
		if inventory_item_index < current_player_inventory_cache.size():
			var item_data: ItemData = current_player_inventory_cache[inventory_item_index]

			if not is_instance_valid(item_data):
				print_rich("[color=red]InventoryUI: _render_current_page - ItemData at inventory index %s is invalid![/color]" % inventory_item_index)
				slot_button.disabled = true
				slot_button.set_meta("item_data", null)
				if icon_rect: icon_rect.texture = null; icon_rect.visible = false
				var equipped_label: Label = slot_button.get_node_or_null(EQUIPPED_INDICATOR_NODE_NAME)
				if equipped_label: equipped_label.visible = false
				slot_button.text = "ERR"
				continue

			slot_button.disabled = false
			slot_button.set_meta("item_data", item_data)

			if icon_rect:
				icon_rect.texture = item_data.icon
				icon_rect.visible = (item_data.icon != null)

				if icon_rect.visible:
					slot_button.text = "" # Clear button text if icon is visible
				else:
					slot_button.text = item_data.display_name.substr(0, 3) if item_data.display_name else "???"
			else:
				print_rich("[color=yellow]InventoryUI: _render_current_page - ItemIcon TextureRect missing in slot %s.[/color]" % i)
				slot_button.text = item_data.display_name.substr(0,3) if item_data.display_name else "???"

			slot_button.pressed.connect(_on_inventory_slot_pressed.bind(item_data))
			slot_button.mouse_entered.connect(_on_slot_mouse_entered.bind(item_data, slot_button))
			slot_button.mouse_exited.connect(_on_slot_mouse_exited)
		else:
			# This slot is empty for the current page
			slot_button.disabled = true
			slot_button.set_meta("item_data", null)
			if icon_rect:
				icon_rect.texture = null
				icon_rect.visible = false
			var equipped_label: Label = slot_button.get_node_or_null(EQUIPPED_INDICATOR_NODE_NAME)
			if equipped_label: equipped_label.visible = false # Hide for empty slots
			slot_button.text = "-" # Placeholder for empty slot

	# After rendering all slots, update visual state for selected item
	if GameManager:
		_update_slot_selected_visual_state(GameManager.current_selected_item_data)
	else:
		_update_slot_selected_visual_state(null) 


func _on_inventory_slot_pressed(item_data_pressed: ItemData):
	SoundManager.play_sfx("ui_click")
	if GameManager and item_data_pressed: 
		GameManager.select_inventory_item(item_data_pressed)
	elif GameManager and not item_data_pressed: 
		GameManager.select_inventory_item(null) 


func _on_slot_mouse_entered(item_data_hovered: ItemData, _slot_button_node: Button):
	# Touchscreens do not have a persistent cursor, so hover labels get stuck when tapped.
	if OS.has_feature("mobile"):
		return

	if item_name_hover_label and item_data_hovered:
		item_name_hover_label.text = item_data_hovered.display_name
		item_name_hover_label.reset_size() # <--- Forces the box to shrink to the text size
		item_name_hover_label.visible = true

func _on_slot_mouse_exited():
	if item_name_hover_label:
		item_name_hover_label.visible = false


func _on_game_manager_selected_item_changed(selected_item_data: ItemData):
	_update_slot_selected_visual_state(selected_item_data)

	if item_name_hover_label and selected_item_data == null :
		item_name_hover_label.visible = false


func _on_interaction_complete():
	if item_name_hover_label:
		item_name_hover_label.visible = false
	if GameManager:
		_update_slot_selected_visual_state(GameManager.current_selected_item_data)


func _on_game_manager_verb_changed(_new_verb_id: String):
	if GameManager: _update_slot_selected_visual_state(GameManager.current_selected_item_data)

func _on_game_manager_verb_lock_changed(_is_active: bool):
	if GameManager: _update_slot_selected_visual_state(GameManager.current_selected_item_data)

func _update_slot_selected_visual_state(selected_item_data: ItemData):
	var is_item_verb_active = GameManager and (GameManager.current_verb_id == "use" or GameManager.current_verb_id == "give") and GameManager.is_verb_lock_active

	for slot_button in all_inventory_slots:
		if not is_instance_valid(slot_button): continue

		var slot_item_data = null
		if slot_button.has_meta("item_data"):
			slot_item_data = slot_button.get_meta("item_data")

		var is_this_slot_selected = false
		if slot_item_data and selected_item_data:
			if slot_item_data.item_id == selected_item_data.item_id:
				is_this_slot_selected = true

		slot_button.modulate = Color.WHITE

		var style = slot_button.get_theme_stylebox("normal", "Button").duplicate()
		if style is StyleBoxFlat:
			if is_this_slot_selected:
				style.border_color = Color(0.2, 0.85, 1.0, 1.0)
				style.border_width_left = 4
				style.border_width_top = 4
				style.border_width_right = 4
				style.border_width_bottom = 4
			else:
				style.border_color = Color(0.6, 0.6, 0.6, 0.8)
				style.border_width_left = 2
				style.border_width_top = 2
				style.border_width_right = 2
				style.border_width_bottom = 2

			if is_item_verb_active and slot_item_data != null and not slot_button.disabled:
				style.bg_color = Color(0.0, 0.35, 0.45, 0.8)
			else:
				style.bg_color = Color(0.15, 0.15, 0.15, 0.6)

		slot_button.add_theme_stylebox_override("normal", style)
		slot_button.add_theme_stylebox_override("hover", style)
		slot_button.add_theme_stylebox_override("focus", style)
		slot_button.add_theme_stylebox_override("pressed", style)

# --- Pagination Logic ---
func _on_up_button_pressed():
	SoundManager.play_sfx("ui_click")
	if current_page_index > 0:
		current_page_index -= 1
		_render_current_page()
		_update_pagination_buttons_state()

func _on_down_button_pressed():
	SoundManager.play_sfx("ui_click")
	if current_page_index < total_pages - 1:
		current_page_index += 1
		_render_current_page()
		_update_pagination_buttons_state()

func _update_pagination_buttons_state():
	if up_button:
		up_button.disabled = (current_page_index == 0)
		_update_icon_color(up_button)
	if down_button:
		down_button.disabled = (current_page_index >= total_pages - 1)
		_update_icon_color(down_button)

func _update_icon_color(btn: Button):
	if btn.get_child_count() > 0 and btn.get_child(-1) is TextureRect:
		var tex_rect = btn.get_child(-1)
		if tex_rect.get_child_count() > 0 and tex_rect.get_child(0) is ColorRect:
			var color_rect = tex_rect.get_child(0)
			color_rect.color = Color(0.5, 0.5, 0.5, 1.0) if btn.disabled else Color.WHITE


func _setup_button_icon(btn: Button, flip: bool):
	var tex_rect = TextureRect.new()
	tex_rect.texture = preload("res://Icons/up_arrow.png")
	tex_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	tex_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	tex_rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	tex_rect.offset_left = 10
	tex_rect.offset_top = 10
	tex_rect.offset_right = -10
	tex_rect.offset_bottom = -10
	tex_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
	tex_rect.flip_v = flip
	tex_rect.clip_children = CanvasItem.CLIP_CHILDREN_ONLY

	var color_rect = ColorRect.new()
	color_rect.color = Color.WHITE
	color_rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	color_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE

	tex_rect.add_child(color_rect)
	btn.add_child(tex_rect)
</file>

<file path="ui/journal_button_ui.gd">
extends CanvasLayer

signal journal_button_pressed

@onready var texture_button: TextureButton = $JournalPanel/MarginContainer/TextureButton
@onready var badge: PanelContainer = $JournalPanel/NotificationBadge
@onready var badge_label: Label = $JournalPanel/NotificationBadge/Label

var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
var pulse_tween: Tween

func _ready():
	# --- UNIVERSAL LAYOUT (PC & Mobile) ---
	var journal_panel = $JournalPanel
	var tab_panel = $JournalPanel/TabPanel
	var tab_label = $JournalPanel/TabPanel/Label

	journal_panel.anchor_left = 0.78
	journal_panel.anchor_right = 0.87

	tab_panel.anchor_left = 0.5
	tab_panel.anchor_right = 0.5
	tab_panel.offset_left = -80
	tab_panel.offset_right = 80

	if OS.has_feature("mobile"):
		journal_panel.anchor_top = 0.75
		tab_panel.offset_top = -50
		tab_label.add_theme_font_size_override("font_size", 28)
	# --------------------------------------

	texture_button.pressed.connect(_on_texture_button_pressed)
	texture_button.mouse_entered.connect(_on_hover_enter)
	texture_button.mouse_exited.connect(_on_hover_exit)

	# Style the Badge
	var badge_style = StyleBoxFlat.new()
	badge_style.bg_color = Color("#D4A017") # Dark Golden Yellow
	badge_style.corner_radius_top_left = 20
	badge_style.corner_radius_top_right = 20
	badge_style.corner_radius_bottom_left = 20
	badge_style.corner_radius_bottom_right = 20
	badge_style.border_width_left = 2
	badge_style.border_width_top = 2
	badge_style.border_width_right = 2
	badge_style.border_width_bottom = 2
	badge_style.border_color = Color.BLACK
	badge.add_theme_stylebox_override("panel", badge_style)

	badge_label.add_theme_font_override("font", custom_font)
	badge_label.add_theme_font_size_override("font_size", 24)
	badge_label.add_theme_color_override("font_color", Color.BLACK)

	# Check Flag
	if GameManager:
		badge.visible = not Flags.get_game_flag("journal_opened_once")

	# Start pulsing if visible
	if badge.visible:
		_start_pulse_animation()

func _start_pulse_animation():
	if pulse_tween:
		pulse_tween.kill()

	# Create an infinite looping tween
	pulse_tween = create_tween().set_loops()

	# Fade down to 30% opacity, then back to 100% opacity
	pulse_tween.tween_property(badge, "modulate:a", 0.3, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	pulse_tween.tween_property(badge, "modulate:a", 1.0, 0.6).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

func _on_hover_enter():
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return
	texture_button.modulate = Color(0.2, 0.85, 1.0, 1.0)

func _on_hover_exit():
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return
	texture_button.modulate = Color(1.0, 1.0, 1.0, 1.0)

func _on_texture_button_pressed():
	if GameManager and GameManager.current_game_state == GameManager.GameState.EXPLANATION:
		return

	if SoundManager:
		SoundManager.play_sfx("ui_click")

	# Hide badge, kill tween, and save flag
	if GameManager:
		Flags.set_game_flag("journal_opened_once", true)

	if pulse_tween:
		pulse_tween.kill()

	badge.visible = false
	badge.modulate.a = 1.0 # Reset alpha just in case

	emit_signal("journal_button_pressed")

func set_notification_enabled(is_enabled: bool):
	if not is_enabled:
		badge.visible = false
		if pulse_tween:
			pulse_tween.kill()
	else:
		if GameManager and not Flags.get_game_flag("journal_opened_once"):
			if not badge.visible:
				badge.visible = true
				_start_pulse_animation()
</file>

<file path="ui/journal_overlay.gd">
extends CanvasLayer

var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")

signal journal_closed

# --- NOW EXPORTED TO THE INSPECTOR ---
@export var journal_entries: Array[JournalPageData] = []

@onready var left_page_layers = %LeftPageLayers
@onready var right_page_layers = %RightPageLayers
@onready var page_indicator_label = %PageIndicatorLabel
@onready var prev_button = %PrevButton
@onready var next_button = %NextButton
@onready var close_button = %CloseButton

var current_spread: int = 0
var total_spreads: int = 1

func _ready():
	# Calculate how many total spreads (pairs of pages) we need
	var max_page = 0
	for entry in journal_entries:
		if entry and entry.page_index > max_page:
			max_page = entry.page_index
			
	total_spreads = ceil((max_page + 1) / 2.0)
	if total_spreads == 0: total_spreads = 1 
	
	prev_button.pressed.connect(_on_prev_pressed)
	next_button.pressed.connect(_on_next_pressed)
	close_button.pressed.connect(_on_close_pressed)

	# --- MOBILE SCALING FOR JOURNAL ---
	if OS.has_feature("mobile"):
		$CenterContainer/LayoutAnchor.custom_minimum_size = Vector2(1400, 800)
		page_indicator_label.add_theme_font_size_override("font_size", 46)

		close_button.position.y = -120
		var pagination = $CenterContainer/LayoutAnchor/PaginationContainer
		pagination.position.y = -120

		prev_button.custom_minimum_size = Vector2(100, 80)
		var prev_icon = prev_button.get_node("IconRect")
		prev_icon.offset_left = -24; prev_icon.offset_right = 24
		prev_icon.offset_top = -24; prev_icon.offset_bottom = 24
		prev_icon.pivot_offset = Vector2(24, 24)

		next_button.custom_minimum_size = Vector2(100, 80)
		var next_icon = next_button.get_node("IconRect")
		next_icon.offset_left = -24; next_icon.offset_right = 24
		next_icon.offset_top = -24; next_icon.offset_bottom = 24
		next_icon.pivot_offset = Vector2(24, 24)

	# --- CLOSE BUTTON POLISH ---
	close_button.text = "Close"
	close_button.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	close_button.add_theme_font_override("font", custom_font)
	close_button.add_theme_font_size_override("font_size", 24)

	close_button.flat = false
	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	btn_normal.corner_radius_top_left = 6
	btn_normal.corner_radius_top_right = 6
	btn_normal.corner_radius_bottom_left = 6
	btn_normal.corner_radius_bottom_right = 6
	btn_normal.content_margin_left = 40 if OS.has_feature("mobile") else 25
	btn_normal.content_margin_right = 40 if OS.has_feature("mobile") else 25
	btn_normal.content_margin_top = 25 if OS.has_feature("mobile") else 10
	btn_normal.content_margin_bottom = 25 if OS.has_feature("mobile") else 10
	close_button.add_theme_font_size_override("font_size", 40 if OS.has_feature("mobile") else 24)
	btn_normal.border_width_left = 2
	btn_normal.border_width_top = 2
	btn_normal.border_width_right = 2
	btn_normal.border_width_bottom = 2
	btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

	close_button.add_theme_stylebox_override("normal", btn_normal)
	close_button.add_theme_stylebox_override("hover", btn_hover)
	close_button.add_theme_stylebox_override("focus", btn_hover)
	close_button.add_theme_stylebox_override("pressed", btn_hover)

	close_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
	close_button.add_theme_color_override("font_hover_color", Color.WHITE)
	close_button.add_theme_color_override("font_pressed_color", Color.WHITE)

	# Close journal when clicking the dark background
	$DimBackground/BackgroundButton.pressed.connect(func():
		_on_close_pressed()
	)

	update_display()

func update_display():
	# 1. Clear old images from the pages
	for child in left_page_layers.get_children(): child.queue_free()
	for child in right_page_layers.get_children(): child.queue_free()
	
	# 2. Calculate which pages we are looking at
	var left_idx = current_spread * 2
	var right_idx = current_spread * 2 + 1
	
	# 3. Stack the transparent PNGs
	for entry in journal_entries:
		# Safety check if an array slot is empty or missing a texture
		if not entry or not entry.page_texture:
			continue
			
		# Check if this layer requires a flag to be visible
		if entry.required_flag != "":
			var flag_name = entry.required_flag
			var is_unlocked = false
			if Flags.current_level_state_manager and Flags.get_level_flag(flag_name):
				is_unlocked = true
			elif Flags.get_game_flag(flag_name):
				is_unlocked = true
				
			if not is_unlocked:
				continue # Skip adding this image layer!
		
		# Add the image to the correct side of the book
		if entry.page_index == left_idx:
			_add_image_layer(left_page_layers, entry.page_texture)
		elif entry.page_index == right_idx:
			_add_image_layer(right_page_layers, entry.page_texture)

	# 4. Update UI
	page_indicator_label.text = str(current_spread + 1) + " / " + str(total_spreads)
	prev_button.disabled = (current_spread == 0)
	next_button.disabled = (current_spread >= total_spreads - 1)

# Helper function to spawn a TextureRect dynamically using the assigned Texture
func _add_image_layer(parent: Control, tex: Texture2D):
	var rect = TextureRect.new()
	rect.texture = tex
	rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
	parent.add_child(rect)

func _on_prev_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	current_spread -= 1
	update_display()

func _on_next_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	current_spread += 1
	update_display()

func _on_close_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	journal_closed.emit()
</file>

<file path="ui/loading_screen.gd">
extends CanvasLayer

@onready var progress_bar: ProgressBar = $VBoxContainer/ProgressBar
@onready var info_label: Label = $VBoxContainer/LoadingInfoLabel

# The list of massive files we want to pre-load.
var load_queue = [
	{"path": "res://main_menu.tscn", "name": "System Interfaces"},
	{"path": "res://difficulty_select_screen.tscn", "name": "Game Configuration"},
	{"path": "res://conversation_backgrounds.tres", "name": "Visual Assets"},
	{"path": "res://AdvancedConversationOverlay.tscn", "name": "Narrative Engine"},
	{"path": "res://main.tscn", "name": "Environment Data"}
]

var current_load_index: int = 0
var progress_array: Array = []

func _ready():
	var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
	info_label.add_theme_font_override("font", custom_font)
	progress_bar.add_theme_font_override("font", custom_font)

	# --- MOBILE SCALING ---
	if OS.has_feature("mobile"):
		info_label.add_theme_font_size_override("font_size", 48)
		progress_bar.custom_minimum_size.y = 80
		progress_bar.add_theme_font_size_override("font_size", 36)

		var vbox = $VBoxContainer
		vbox.offset_top = -350
		vbox.offset_bottom = -100
		vbox.add_theme_constant_override("separation", 25)

	progress_bar.value = 0
	_start_next_load()

func _start_next_load():
	if current_load_index >= load_queue.size():
		_finish_loading()
		return
		
	var current_file = load_queue[current_load_index]
	info_label.text = "Loading: " + current_file["name"] + "..."
	
	# Start loading the file on a background CPU thread
	ResourceLoader.load_threaded_request(current_file["path"])
	set_process(true)

func _process(_delta):
	var current_file = load_queue[current_load_index]
	
	# Check the status of the background thread
	var load_status = ResourceLoader.load_threaded_get_status(current_file["path"], progress_array)
	
	if load_status == ResourceLoader.THREAD_LOAD_IN_PROGRESS:
		# Calculate overall progress across all files
		var base_progress = (float(current_load_index) / load_queue.size()) * 100.0
		var file_progress = (progress_array[0] * 100.0) / load_queue.size()
		progress_bar.value = base_progress + file_progress
		
	elif load_status == ResourceLoader.THREAD_LOAD_LOADED:
		set_process(false)
		
		# Grab the finished resource
		var loaded_resource = ResourceLoader.load_threaded_get(current_file["path"])
		
		# Save it to the GameManager based on what it is
		match current_file["path"]:
			"res://main_menu.tscn":
				GameManager.cached_main_menu_scene = loaded_resource
			"res://AdvancedConversationOverlay.tscn":
				GameManager.cached_intro_overlay_scene = loaded_resource
			"res://main.tscn":
				GameManager.cached_main_game_scene = loaded_resource
		
		# Move to the next file
		current_load_index += 1
		_start_next_load()
		
	elif load_status == ResourceLoader.THREAD_LOAD_FAILED or load_status == ResourceLoader.THREAD_LOAD_INVALID_RESOURCE:
		push_error("LoadingScreen: Failed to load %s (status %d) — skipping." % [current_file["path"], load_status])
		print_rich("[color=red]LoadingScreen Error: Failed to load %s — skipping.[/color]" % current_file["path"])
		set_process(false)
		current_load_index += 1
		_start_next_load()

func _finish_loading():
	progress_bar.value = 100
	info_label.text = "Loading Complete!"
	
	# Wait a tiny fraction of a second so the player sees 100%
	await get_tree().create_timer(0.5).timeout
	
	# Tell the GameManager to boot up the Main Menu!
	GameManager.change_game_state(GameManager.GameState.MAIN_MENU)
	
	queue_free()
</file>

<file path="ui/LocationRow.gd">
# LocationRow.gd
extends HBoxContainer
signal chapter_selected(data: MemoryChapterData)

@onready var location_name_label: Label = $LocationInfoPanel/VBoxContainer/Label
@onready var location_image_rect: TextureRect = $LocationInfoPanel/VBoxContainer/TextureRect
@onready var chapter_list_container: HBoxContainer = $ChaptersAreaPanel/HBoxContainer/ViewportPanel/ChapterListContainer
@onready var left_arrow_button: Button = $ChaptersAreaPanel/HBoxContainer/LeftArrowButton
@onready var right_arrow_button: Button = $ChaptersAreaPanel/HBoxContainer/RightArrowButton

const ChapterButtonScene = preload("res://ui/ChapterButton.tscn") 

@export var scroll_speed: float = 0.5
var memory_data: MemoryGroupData

func _ready():
	left_arrow_button.pressed.connect(_on_left_arrow_pressed)
	right_arrow_button.pressed.connect(_on_right_arrow_pressed)

	# Connect the native scrollbar so arrows update dynamically when the user swipes
	var viewport_panel = $ChaptersAreaPanel/HBoxContainer/ViewportPanel
	viewport_panel.get_h_scroll_bar().value_changed.connect(func(_val): _update_arrow_state())

	# --- TOUCH SCROLL FIX ---
	# Ensure the container itself doesn't block touch input
	viewport_panel.mouse_filter = Control.MOUSE_FILTER_PASS
	chapter_list_container.mouse_filter = Control.MOUSE_FILTER_PASS

	# --- MOBILE SCALING FOR LOCATION ROW ---
	if OS.has_feature("mobile"):
		# Scaled to ~70% for mobile so the row doesn't dominate the screen
		$LocationInfoPanel.custom_minimum_size = Vector2(205, 190)
		location_name_label.add_theme_font_size_override("font_size", 24)

		left_arrow_button.add_theme_font_size_override("font_size", 48)
		left_arrow_button.custom_minimum_size.x = 72

		right_arrow_button.add_theme_font_size_override("font_size", 48)
		right_arrow_button.custom_minimum_size.x = 72

		# Tighten the gap between chapter buttons (scene default is 50, desktop keeps that)
		chapter_list_container.add_theme_constant_override("separation", 34)

func populate(data: MemoryGroupData):
	self.memory_data = data

	location_name_label.text = memory_data.group_name
	location_image_rect.texture = memory_data.group_image

	for child in chapter_list_container.get_children():
		child.queue_free()

	for chapter_data in memory_data.chapters:
		var new_chapter_button = ChapterButtonScene.instantiate()
		chapter_list_container.add_child(new_chapter_button)
		new_chapter_button.populate(chapter_data)
		new_chapter_button.chapter_selected.connect(_on_chapter_button_selected)

	_update_arrow_state()

func _update_arrow_state():
	await get_tree().process_frame
	await get_tree().process_frame

	var viewport_panel = $ChaptersAreaPanel/HBoxContainer/ViewportPanel
	var viewport_width = viewport_panel.size.x
	var content_width = chapter_list_container.get_combined_minimum_size().x
	var max_scroll = max(0.0, content_width - viewport_width)

	left_arrow_button.disabled = (viewport_panel.scroll_horizontal <= 0)
	right_arrow_button.disabled = (viewport_panel.scroll_horizontal >= max_scroll)

func _on_left_arrow_pressed():
	if left_arrow_button.disabled: return
	var viewport_panel = $ChaptersAreaPanel/HBoxContainer/ViewportPanel
	var target = max(0.0, viewport_panel.scroll_horizontal - viewport_panel.size.x)
	create_tween().tween_property(viewport_panel, "scroll_horizontal", target, scroll_speed).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)

func _on_chapter_button_selected(data: MemoryChapterData):
	emit_signal("chapter_selected", data)

func _on_right_arrow_pressed():
	if right_arrow_button.disabled: return
	var viewport_panel = $ChaptersAreaPanel/HBoxContainer/ViewportPanel
	var content_width = chapter_list_container.get_combined_minimum_size().x
	var max_scroll = max(0.0, content_width - viewport_panel.size.x)
	var target = min(max_scroll, viewport_panel.scroll_horizontal + viewport_panel.size.x)
	create_tween().tween_property(viewport_panel, "scroll_horizontal", target, scroll_speed).set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_OUT)
</file>

<file path="ui/logo_cursor.gd">
extends Control

func _ready():
	var tex = load("res://Icons/logo_glass_icon.png")
	if not tex:
		push_warning("Logo Cursor: Could not load image at res://Icons/logo_glass_icon.png")
		return

	var tex_rect = TextureRect.new()
	tex_rect.texture = tex

	# Make the image perfectly fill the bounding box you set in the editor
	tex_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	tex_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	tex_rect.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)

	# Ensure it doesn't block any clicks (just in case)
	tex_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE

	add_child(tex_rect)
</file>

<file path="ui/LogoSplash.gd">
# LogoSplash.gd
extends Control

# Define a signal that this scene will emit when it's done.
signal splash_finished

# Get a reference to the AnimationPlayer that animates your logo.
# I'm assuming it's named "LogoAnimator" based on your screenshot.
@onready var logo_animator: AnimationPlayer = $LogoLabel/LogoAnimator

func _ready():
	# Connect to the AnimationPlayer's "animation_finished" signal.
	# When the animation is done, we'll call our own function.
	logo_animator.animation_finished.connect(_on_animation_finished)

	# Start playing the logo animation (assuming it's named "play_logo")
	logo_animator.play("splash_animation")

func _on_animation_finished(_anim_name: String):
	# The animation is done. Now we emit our custom signal.
	splash_finished.emit()

	# And finally, remove the splash screen from the game.
	queue_free()
</file>

<file path="ui/MainMenu.gd">
# MainMenu.gd
extends CanvasLayer
# The GameManager is listening for these specific signals
signal new_game_requested
signal quit_game_requested

@onready var button_block = $Content/ButtonBlock
@onready var start_btn = $Content/ButtonBlock/StartButton
@onready var settings_btn = $Content/ButtonBlock/SettingsButton
@onready var credits_btn = $Content/ButtonBlock/CreditsButton
@onready var quit_btn = $Content/ButtonBlock/QuitButton

func _ready():
	if OS.has_feature("mobile"):
		button_block.add_theme_constant_override("separation", 40)

		var mobile_font_size = 54
		start_btn.add_theme_font_size_override("font_size", mobile_font_size)
		settings_btn.add_theme_font_size_override("font_size", mobile_font_size)
		credits_btn.add_theme_font_size_override("font_size", mobile_font_size)
		quit_btn.add_theme_font_size_override("font_size", mobile_font_size)

	_add_version_label()

func _on_new_game_button_pressed():
	# Verify the click is working in the Output log
	print("MainMenu: New Game Button Pressed")
	if SoundManager: SoundManager.play_sfx("start_game")
	new_game_requested.emit()

func _on_quit_button_pressed():
	print("MainMenu: Quit Button Pressed")
	quit_game_requested.emit()

func _on_settings_button_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var settings_scene = load("res://settings_menu.tscn")
	if settings_scene:
		var instance = settings_scene.instantiate()
		get_tree().root.add_child(instance)

func _on_credits_button_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var credits_scene = load("res://credits_menu.tscn")
	if credits_scene:
		var instance = credits_scene.instantiate()
		get_tree().root.add_child(instance)

func _add_version_label():
	var version = str(ProjectSettings.get_setting("application/config/version", ""))
	if version.is_empty():
		return

	var version_label = Label.new()
	version_label.name = "VersionLabel"
	version_label.text = "v" + version
	version_label.mouse_filter = Control.MOUSE_FILTER_IGNORE
	version_label.modulate.a = 0.5

	version_label.add_theme_font_override("font", preload("res://Fonts/VarelaRound-Regular.ttf"))
	version_label.add_theme_font_size_override("font_size", 28 if OS.has_feature("mobile") else 16)

	add_child(version_label)
	version_label.set_anchors_and_offsets_preset(Control.PRESET_BOTTOM_RIGHT, Control.PRESET_MODE_MINSIZE, 12)
	version_label.grow_horizontal = Control.GROW_DIRECTION_BEGIN
	version_label.grow_vertical = Control.GROW_DIRECTION_BEGIN
</file>

<file path="ui/Notification.gd">
# Notification.gd
extends PanelContainer

const DURATION = 4.0 # 4 seconds feels better with the fade out

@onready var label = $Label
@onready var timer = $Timer

var _fade_tween: Tween

func _ready():
	# Set initial state for the "pop-in" animation
	modulate.a = 0.0
	scale = Vector2(0.8, 0.8)

	if OS.has_feature("mobile"):
		label.add_theme_font_size_override("font_size", 36)
		var mobile_style = get_theme_stylebox("panel").duplicate()
		mobile_style.content_margin_left = 40
		mobile_style.content_margin_right = 40
		mobile_style.content_margin_top = 20
		mobile_style.content_margin_bottom = 20
		add_theme_stylebox_override("panel", mobile_style)
		reset_size()

	# We want the scale to happen from the center of the capsule
	pivot_offset = size / 2.0

	# The Flashy Pop-in Tween
	var tween = create_tween().set_parallel(true)
	tween.tween_property(self, "modulate:a", 1.0, 0.25).set_trans(Tween.TRANS_SINE)
	tween.tween_property(self, "scale", Vector2(1.0, 1.0), 0.35).set_trans(Tween.TRANS_BACK).set_ease(Tween.EASE_OUT)

func show_message(text: String):
	label.text = text
	timer.wait_time = DURATION
	timer.start()

# --- NEW: Updates the notification in-place with an attention-grabbing effect ---
func update_message(new_text: String):
	label.text = new_text
	timer.start(DURATION) # Reset the hide timer

	# Stop fading out if it was in the middle of disappearing
	if _fade_tween and _fade_tween.is_valid():
		_fade_tween.kill()

	modulate.a = 1.0 # Ensure it's fully visible

	# Re-center pivot in case the new text size changed the panel's width
	pivot_offset = size / 2.0

	# Play a quick "bump" scale effect to catch the eye
	var bump_tween = create_tween()
	bump_tween.tween_property(self, "scale", Vector2(1.1, 1.1), 0.1).set_trans(Tween.TRANS_SINE)
	bump_tween.tween_property(self, "scale", Vector2(1.0, 1.0), 0.2).set_trans(Tween.TRANS_BACK).set_ease(Tween.EASE_OUT)

	# Briefly flash the text color cyan, then fade back to white
	label.modulate = Color(0.2, 0.85, 1.0, 1.0)
	var color_tween = create_tween()
	color_tween.tween_property(label, "modulate", Color.WHITE, 0.4)

func _on_timer_timeout():
	# The Smooth Fade-out Tween
	_fade_tween = create_tween()
	_fade_tween.tween_property(self, "modulate:a", 0.0, 0.5).set_trans(Tween.TRANS_SINE)
	# Destroy the node AFTER the fade out is complete
	_fade_tween.tween_callback(queue_free)
</file>

<file path="ui/patreon_world_button.gd">
# PatreonWorldButton.gd — self-building "Support on Patreon" world-UI button.
class_name PatreonWorldButton
extends CanvasLayer

const PATREON_URL := "https://www.patreon.com/cw/lewgend"


func _ready():
	layer = 1

	var btn = Button.new()
	add_child(btn)

	var tex = load("res://Icons/patreon_logo.png")
	if tex:
		var img = tex.get_image()
		if img:
			img.resize(48, 48, Image.INTERPOLATE_BILINEAR)
			tex = ImageTexture.create_from_image(img)
		btn.icon = tex

	btn.text = " Support on Patreon"
	btn.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND
	btn.add_theme_constant_override("h_separation", 10)
	btn.add_theme_font_override("font", preload("res://Fonts/VarelaRound-Regular.ttf"))
	btn.add_theme_font_size_override("font_size", 20)

	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.6)
	btn_normal.corner_radius_top_left = 10
	btn_normal.corner_radius_top_right = 10
	btn_normal.corner_radius_bottom_left = 10
	btn_normal.corner_radius_bottom_right = 10
	btn_normal.content_margin_left = 15
	btn_normal.content_margin_right = 20
	btn_normal.content_margin_top = 10
	btn_normal.content_margin_bottom = 10
	btn_normal.border_width_left = 2
	btn_normal.border_width_top = 2
	btn_normal.border_width_right = 2
	btn_normal.border_width_bottom = 2
	btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.8)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

	btn.add_theme_stylebox_override("normal", btn_normal)
	btn.add_theme_stylebox_override("hover", btn_hover)
	btn.add_theme_stylebox_override("focus", btn_hover)
	btn.add_theme_stylebox_override("pressed", btn_hover)

	btn.position = Vector2(20, 20)
	if OS.has_feature("mobile"):
		btn.position = Vector2(40, 40)
		btn.add_theme_font_size_override("font_size", 28)

	btn.pressed.connect(func():
		if SoundManager and SoundManager.has_method("play_sfx"): SoundManager.play_sfx("ui_click")
		OS.shell_open(PATREON_URL)
	)

	hide()
</file>

<file path="ui/pause_menu_ui.gd">
extends CanvasLayer

@onready var menu_panel = $MenuPanel
@onready var menu_button = $MenuPanel/MenuButton
@onready var hamburger_lines = [
	$MenuPanel/MarginContainer/VBoxContainer/Line1,
	$MenuPanel/MarginContainer/VBoxContainer/Line2,
	$MenuPanel/MarginContainer/VBoxContainer/Line3
]

@onready var overlay = $Overlay
@onready var bg_button = $Overlay/BackgroundButton
@onready var resume_btn = $Overlay/CenterContainer/VBoxContainer/ResumeButton
@onready var history_btn = $Overlay/CenterContainer/VBoxContainer/HistoryButton
@onready var settings_btn = $Overlay/CenterContainer/VBoxContainer/SettingsButton
@onready var controls_btn = $Overlay/CenterContainer/VBoxContainer/ControlsButton
@onready var credits_btn = $Overlay/CenterContainer/VBoxContainer/CreditsButton
@onready var quit_btn = $Overlay/CenterContainer/VBoxContainer/QuitButton

signal scan_cancel_requested

@onready var scan_cancel_panel = $ScanCancelPanel
@onready var scan_cancel_btn = $ScanCancelPanel/MarginContainer/ScanCancelButton

@onready var confirm_overlay = $ConfirmOverlay
@onready var confirm_panel = $ConfirmOverlay/CenterContainer/ConfirmPanel
@onready var confirm_label = $ConfirmOverlay/CenterContainer/ConfirmPanel/MarginContainer/VBoxContainer/Label
@onready var yes_btn = $ConfirmOverlay/CenterContainer/ConfirmPanel/MarginContainer/VBoxContainer/HBoxContainer/YesButton
@onready var no_btn = $ConfirmOverlay/CenterContainer/ConfirmPanel/MarginContainer/VBoxContainer/HBoxContainer/NoButton

var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
var _was_paused_before_menu: bool = false
var _pre_pause_game_state = null
var _was_menu_visible: bool = false
var _overlay_open_time_ms: int = 0

func _ready():
	process_mode = Node.PROCESS_MODE_ALWAYS
	layer = 110
	menu_panel.hide()
	scan_cancel_panel.hide()
	scan_cancel_btn.texture_normal = preload("res://Icons/magnifying-glass.png")
	overlay.hide()
	confirm_overlay.hide()

	_apply_style()

	menu_button.pressed.connect(toggle_pause)
	if OS.has_feature("mobile"):
		bg_button.pressed.connect(_on_bg_button_pressed)
	else:
		bg_button.mouse_default_cursor_shape = Control.CURSOR_ARROW
	$Overlay/CenterContainer/VBoxContainer.mouse_filter = Control.MOUSE_FILTER_STOP
	resume_btn.pressed.connect(toggle_pause)
	history_btn.pressed.connect(_on_history_pressed)
	settings_btn.pressed.connect(_on_settings_pressed)
	controls_btn.pressed.connect(_on_controls_pressed)
	credits_btn.pressed.connect(_on_credits_pressed)

	quit_btn.pressed.connect(_on_quit_pressed)
	yes_btn.pressed.connect(_on_confirm_yes)
	no_btn.pressed.connect(_on_confirm_no)

func _process(_delta):
	if is_instance_valid(menu_panel) and is_instance_valid(scan_cancel_panel):
		scan_cancel_panel.visible = menu_panel.visible

func _input(event):
	# 1. Handle Escape Key (Toggles Pause ON and OFF)
	if event.is_action_pressed("ui_cancel") or (event is InputEventKey and event.keycode == KEY_ESCAPE and event.is_pressed() and not event.is_echo()):
		if confirm_overlay.visible:
			get_viewport().set_input_as_handled()
			_on_confirm_no()
			return

		if GameManager and (GameManager.current_game_state == GameManager.GameState.IN_GAME_PLAY or GameManager.current_game_state == GameManager.GameState.PAUSED or GameManager.current_game_state == GameManager.GameState.INTRO_CONVERSATION):
			if get_tree().root.has_node("SettingsMenu") or get_tree().root.has_node("DialogueHistoryUI") or get_tree().root.has_node("CreditsMenu") or get_tree().root.has_node("ControlsMenu"):
				return

			get_viewport().set_input_as_handled()
			toggle_pause()

	# 2. Handle Right-Click (Acts as "Back" / "Unpause" ONLY when menu is open)
	elif event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_RIGHT and event.is_pressed():
		if confirm_overlay.visible:
			get_viewport().set_input_as_handled()
			_on_confirm_no()
			return

		if overlay.visible:
			if get_tree().root.has_node("SettingsMenu") or get_tree().root.has_node("DialogueHistoryUI") or get_tree().root.has_node("CreditsMenu") or get_tree().root.has_node("ControlsMenu"):
				return

			get_viewport().set_input_as_handled()
			toggle_pause()

func _on_bg_button_pressed():
	if Time.get_ticks_msec() - _overlay_open_time_ms < 300: return
	toggle_pause()

func toggle_pause():
	if SoundManager: SoundManager.play_sfx("ui_click")

	if overlay.visible:
		overlay.hide()
		if is_instance_valid(menu_panel):
			menu_panel.visible = _was_menu_visible
		get_tree().paused = _was_paused_before_menu
		if GameManager and _pre_pause_game_state != null:
			GameManager.change_game_state(_pre_pause_game_state)
			_pre_pause_game_state = null
	else:
		_was_paused_before_menu = get_tree().paused
		if GameManager:
			_pre_pause_game_state = GameManager.current_game_state
		_was_menu_visible = menu_panel.visible if is_instance_valid(menu_panel) else false
		if is_instance_valid(menu_panel):
			menu_panel.hide()
		overlay.show()
		_overlay_open_time_ms = Time.get_ticks_msec()
		get_tree().paused = true
		if GameManager: GameManager.change_game_state(GameManager.GameState.PAUSED)

func _on_history_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var log_scene = load("res://dialogue_history_ui.tscn")
	if log_scene: get_tree().root.add_child(log_scene.instantiate())

func _on_settings_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var settings_scene = load("res://settings_menu.tscn")
	if settings_scene: get_tree().root.add_child(settings_scene.instantiate())

func _on_controls_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var controls_scene = load("res://controls_menu.tscn")
	if controls_scene: get_tree().root.add_child(controls_scene.instantiate())

func _on_credits_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	var credits_scene = load("res://credits_menu.tscn")
	if credits_scene: get_tree().root.add_child(credits_scene.instantiate())

func _on_quit_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	confirm_overlay.show()

func _on_confirm_yes():
	if SoundManager: SoundManager.play_sfx("ui_click")
	get_tree().paused = false
	confirm_overlay.hide()
	overlay.hide()
	if GameManager:
		if GameManager.has_method("quit_to_main_menu_smooth"):
			GameManager.quit_to_main_menu_smooth()
		else:
			GameManager.change_game_state(GameManager.GameState.MAIN_MENU)

func _on_confirm_no():
	if SoundManager: SoundManager.play_sfx("ui_click")
	confirm_overlay.hide()

func _apply_style():
	# --- MOBILE: Scale up the Hamburger Menu Icon ---
	if OS.has_feature("mobile"):
		menu_panel.offset_left = -160
		menu_panel.offset_bottom = 130
		var margin = $MenuPanel/MarginContainer
		margin.add_theme_constant_override("margin_top", 25)
		margin.add_theme_constant_override("margin_bottom", 25)
		margin.add_theme_constant_override("margin_left", 25)
		margin.add_theme_constant_override("margin_right", 25)
		for line in hamburger_lines:
			line.custom_minimum_size.y = 8

	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	btn_normal.corner_radius_top_left = 6
	btn_normal.corner_radius_top_right = 6
	btn_normal.corner_radius_bottom_left = 6
	btn_normal.corner_radius_bottom_right = 6
	btn_normal.content_margin_top = 15
	btn_normal.content_margin_bottom = 15
	btn_normal.content_margin_left = 40
	btn_normal.content_margin_right = 40
	btn_normal.border_width_left = 3
	btn_normal.border_width_top = 3
	btn_normal.border_width_right = 3
	btn_normal.border_width_bottom = 3
	btn_normal.border_color = Color.WHITE

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 1.0)

	var mobile_font_size = 28
	if OS.has_feature("mobile"):
		mobile_font_size = 44
		btn_normal.content_margin_top = 25
		btn_normal.content_margin_bottom = 25
		btn_hover.content_margin_top = 25
		btn_hover.content_margin_bottom = 25

	var buttons = [resume_btn, history_btn, settings_btn, controls_btn, credits_btn, quit_btn, yes_btn, no_btn]
	for btn in buttons:
		btn.add_theme_font_override("font", custom_font)
		btn.add_theme_font_size_override("font_size", mobile_font_size)
		btn.add_theme_stylebox_override("normal", btn_normal)
		btn.add_theme_stylebox_override("hover", btn_hover)
		btn.add_theme_stylebox_override("focus", btn_hover)
		btn.add_theme_stylebox_override("pressed", btn_hover)
		btn.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
		btn.add_theme_color_override("font_hover_color", Color.WHITE)
		btn.add_theme_color_override("font_pressed_color", Color.WHITE)
		btn.mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND

	var panel_style = StyleBoxFlat.new()
	panel_style.bg_color = Color(0, 0, 0, 0.5)
	panel_style.border_width_left = 3
	panel_style.border_width_top = 3
	panel_style.border_width_right = 3
	panel_style.border_width_bottom = 3
	panel_style.border_color = Color.WHITE
	panel_style.corner_radius_top_left = 10
	panel_style.corner_radius_top_right = 10
	panel_style.corner_radius_bottom_left = 10
	panel_style.corner_radius_bottom_right = 10

	var scan_style = panel_style.duplicate()
	scan_cancel_panel.add_theme_stylebox_override("panel", scan_style)

	if OS.has_feature("mobile"):
		scan_cancel_panel.offset_left = -160
		scan_cancel_panel.offset_top = 170
		scan_cancel_panel.offset_right = -20
		scan_cancel_panel.offset_bottom = 300
		var sc_margin = $ScanCancelPanel/MarginContainer
		sc_margin.add_theme_constant_override("margin_top", 35)
		sc_margin.add_theme_constant_override("margin_bottom", 35)
		sc_margin.add_theme_constant_override("margin_left", 35)
		sc_margin.add_theme_constant_override("margin_right", 35)

	var menu_style = panel_style.duplicate()
	menu_panel.add_theme_stylebox_override("panel", menu_style)

	menu_button.mouse_entered.connect(func():
		menu_style.border_color = Color(0.2, 0.85, 1.0, 1.0)
		for line in hamburger_lines: line.color = Color(0.2, 0.85, 1.0, 1.0)
	)
	menu_button.mouse_exited.connect(func():
		menu_style.border_color = Color.WHITE
		for line in hamburger_lines: line.color = Color.WHITE
	)

	scan_cancel_panel.mouse_filter = Control.MOUSE_FILTER_STOP
	scan_cancel_btn.mouse_filter = Control.MOUSE_FILTER_IGNORE

	scan_cancel_panel.mouse_entered.connect(func():
		if scan_style.border_color != Color(1.0, 0.3, 0.3, 1.0):
			scan_style.border_color = Color(0.2, 0.85, 1.0, 1.0)
	)
	scan_cancel_panel.mouse_exited.connect(func():
		if scan_style.border_color != Color(1.0, 0.3, 0.3, 1.0):
			if GameManager and GameManager.is_scan_active():
				scan_style.border_color = Color(0.2, 0.85, 1.0, 1.0)
			else:
				scan_style.border_color = Color.WHITE
	)
	scan_cancel_panel.gui_input.connect(func(event):
		if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed():
			scan_cancel_requested.emit()
	)

	var confirm_bg = StyleBoxFlat.new()
	confirm_bg.bg_color = Color(0.1, 0.1, 0.1, 0.95)
	confirm_bg.border_width_left = 3
	confirm_bg.border_width_top = 3
	confirm_bg.border_width_right = 3
	confirm_bg.border_width_bottom = 3
	confirm_bg.border_color = Color(0.2, 0.85, 1.0, 1.0)
	confirm_bg.corner_radius_top_left = 10
	confirm_bg.corner_radius_top_right = 10
	confirm_bg.corner_radius_bottom_left = 10
	confirm_bg.corner_radius_bottom_right = 10
	confirm_panel.add_theme_stylebox_override("panel", confirm_bg)

	confirm_label.add_theme_font_override("font", custom_font)
	confirm_label.add_theme_font_size_override("font_size", 44 if OS.has_feature("mobile") else 28)
	confirm_label.add_theme_color_override("font_color", Color.WHITE)
	confirm_label.autowrap_mode = TextServer.AUTOWRAP_WORD

	if OS.has_feature("mobile"):
		confirm_label.custom_minimum_size = Vector2(600, 0)

func set_scan_highlight(is_active: bool):
	var style = scan_cancel_panel.get_theme_stylebox("panel")
	if is_active:
		style.border_color = Color(0.2, 0.85, 1.0, 1.0)
	else:
		style.border_color = Color.WHITE

func set_cancel_mode(is_cancel: bool):
	var style = scan_cancel_panel.get_theme_stylebox("panel")
	if is_cancel:
		scan_cancel_btn.texture_normal = preload("res://Icons/cancel.png")
		style.border_color = Color(1.0, 0.3, 0.3, 1.0)
	else:
		scan_cancel_btn.texture_normal = preload("res://Icons/magnifying-glass.png")
		style.border_color = Color.WHITE
</file>

<file path="ui/settings_menu.gd">
extends CanvasLayer

@onready var title = %Title

# Tabs
@onready var tab_audio_btn = %TabAudio
@onready var tab_dialogue_btn = %TabDialogue
@onready var tab_gameplay_btn = %TabGameplay
@onready var tab_audio_content = %TabAudioContent
@onready var tab_dialogue_content = %TabDialogueContent
@onready var tab_gameplay_content = %TabGameplayContent

# Audio Tab
@onready var master_slider = %MasterSlider
@onready var master_label = %MasterLabel
@onready var master_minus = %MasterMinus
@onready var master_plus = %MasterPlus

@onready var music_slider = %MusicSlider
@onready var music_label = %MusicLabel
@onready var music_minus = %MusicMinus
@onready var music_plus = %MusicPlus

@onready var sfx_slider = %SFXSlider
@onready var sfx_label = %SFXLabel
@onready var sfx_minus = %SFXMinus
@onready var sfx_plus = %SFXPlus

# Dialogue Tab
@onready var text_scale_slider = %TextScaleSlider
@onready var text_scale_label = %TextScaleLabel
@onready var text_scale_minus = %TextScaleMinus
@onready var text_scale_plus = %TextScalePlus

@onready var instant_text_toggle = %InstantTextToggle

@onready var text_speed_slider = %TextSpeedSlider
@onready var text_speed_label = %TextSpeedLabel
@onready var text_speed_minus = %TextSpeedMinus
@onready var text_speed_plus = %TextSpeedPlus

@onready var auto_forward_toggle = %AutoForwardToggle

@onready var auto_delay_slider = %AutoDelaySlider
@onready var auto_delay_label = %AutoDelayLabel
@onready var auto_delay_minus = %AutoDelayMinus
@onready var auto_delay_plus = %AutoDelayPlus

# Gameplay Tab
@onready var assisted_mode_toggle = %AssistedModeToggle
@onready var assisted_mode_subtext = %AssistedModeSubtext

@onready var close_button = %CloseButton

var custom_font = preload("res://Fonts/VarelaRound-Regular.ttf")
var _open_time_ms: int = 0

func _ready():
	_open_time_ms = Time.get_ticks_msec()
	process_mode = Node.PROCESS_MODE_ALWAYS
	_apply_ui_polish()

	# Load initial values
	if GameManager:
		master_slider.value = round(Settings.get_bus_volume("Master") * 7.0)
		music_slider.value = round(Settings.get_bus_volume("Music") * 7.0)
		sfx_slider.value = round(Settings.get_bus_volume("SFX") * 7.0)

		var speed_mapped = remap(Settings.text_speed, 0.05, 0.005, 0.0, 10.0)
		text_speed_slider.value = round(speed_mapped)

		var auto_mapped = remap(Settings.auto_time_delay, 0.125, 1.75, 1.0, 10.0)
		auto_delay_slider.value = round(auto_mapped)

		text_scale_slider.value = round((Settings.dialogue_text_scale - 0.5) * 10.0)

		_update_instant_text_visuals(Settings.instant_text)
		_update_auto_forward_visuals(Settings.is_auto_playing)
		_update_assisted_toggle_visuals(Settings.assisted_mode)

	_update_labels()

	# Connect tab buttons
	tab_audio_btn.pressed.connect(_switch_tab.bind(0))
	tab_dialogue_btn.pressed.connect(_switch_tab.bind(1))
	tab_gameplay_btn.pressed.connect(_switch_tab.bind(2))

	# Start on Audio tab
	_switch_tab(0, false)

	# Connect +/- buttons
	_connect_increment_buttons(master_minus, master_plus, master_slider)
	_connect_increment_buttons(music_minus, music_plus, music_slider)
	_connect_increment_buttons(sfx_minus, sfx_plus, sfx_slider)
	_connect_increment_buttons(text_scale_minus, text_scale_plus, text_scale_slider)
	_connect_increment_buttons(text_speed_minus, text_speed_plus, text_speed_slider)
	_connect_increment_buttons(auto_delay_minus, auto_delay_plus, auto_delay_slider)

	# Connect sliders & toggles
	master_slider.value_changed.connect(_on_master_changed)
	music_slider.value_changed.connect(_on_music_changed)
	sfx_slider.value_changed.connect(_on_sfx_changed)
	text_speed_slider.value_changed.connect(_on_text_speed_changed)
	auto_delay_slider.value_changed.connect(_on_auto_delay_changed)
	text_scale_slider.value_changed.connect(_on_text_scale_changed)
	instant_text_toggle.pressed.connect(_on_instant_text_pressed)
	auto_forward_toggle.pressed.connect(_on_auto_forward_pressed)
	assisted_mode_toggle.pressed.connect(_on_assisted_mode_pressed)
	close_button.pressed.connect(_on_close_pressed)

	# Close on background click (Mobile Only)
	if OS.has_feature("mobile"):
		$ColorRect.gui_input.connect(func(event):
			if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.is_pressed():
				if Time.get_ticks_msec() - _open_time_ms < 300: return
				_on_close_pressed()
		)

func _input(event):
	if event is InputEventKey and event.keycode == KEY_ESCAPE and event.is_pressed() and not event.is_echo():
		get_viewport().set_input_as_handled()
		_on_close_pressed()
	# Right-click to close (PC Only)
	elif not OS.has_feature("mobile") and event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_RIGHT and event.is_pressed():
		get_viewport().set_input_as_handled()
		_on_close_pressed()

func _connect_increment_buttons(btn_minus: Button, btn_plus: Button, slider: HSlider):
	btn_minus.pressed.connect(func():
		if SoundManager: SoundManager.play_sfx("ui_click")
		slider.value -= slider.step
	)
	btn_plus.pressed.connect(func():
		if SoundManager: SoundManager.play_sfx("ui_click")
		slider.value += slider.step
	)

func _switch_tab(tab_index: int, play_sound: bool = true):
	if play_sound and SoundManager: SoundManager.play_sfx("ui_click")

	tab_audio_content.visible = (tab_index == 0)
	tab_dialogue_content.visible = (tab_index == 1)
	tab_gameplay_content.visible = (tab_index == 2)

	%TabScroll.scroll_vertical = 0

	_style_tab_button(tab_audio_btn, tab_index == 0)
	_style_tab_button(tab_dialogue_btn, tab_index == 1)
	_style_tab_button(tab_gameplay_btn, tab_index == 2)

func _apply_button_styles(btn: Button, normal_sb: StyleBox, hover_sb: StyleBox, pressed_sb: StyleBox, disabled_sb: StyleBox) -> void:
	btn.add_theme_stylebox_override("normal", normal_sb)
	btn.add_theme_stylebox_override("hover", normal_sb if OS.has_feature("mobile") else hover_sb)
	btn.add_theme_stylebox_override("pressed", pressed_sb)
	btn.add_theme_stylebox_override("hover_pressed", pressed_sb)
	btn.add_theme_stylebox_override("disabled", disabled_sb)
	btn.focus_mode = Control.FOCUS_NONE
	btn.add_theme_stylebox_override("focus", StyleBoxEmpty.new())

func _style_tab_button(btn: Button, is_active: bool):
	var style = StyleBoxFlat.new()
	style.corner_radius_top_left = 6
	style.corner_radius_top_right = 6
	style.corner_radius_bottom_left = 6
	style.corner_radius_bottom_right = 6
	style.anti_aliasing = false
	style.content_margin_top = 15 if OS.has_feature("mobile") else 10
	style.content_margin_bottom = 15 if OS.has_feature("mobile") else 10

	if is_active:
		style.bg_color = Color(0.2, 0.85, 1.0, 1.0)
		btn.add_theme_color_override("font_color", Color(0.1, 0.1, 0.1, 1.0))
		btn.add_theme_color_override("font_hover_color", Color(0.1, 0.1, 0.1, 1.0) if OS.has_feature("mobile") else Color.BLACK)
	else:
		style.bg_color = Color(0.15, 0.15, 0.15, 0.85)
		btn.add_theme_color_override("font_color", Color(0.6, 0.6, 0.6, 1.0))
		btn.add_theme_color_override("font_hover_color", Color(0.6, 0.6, 0.6, 1.0) if OS.has_feature("mobile") else Color.WHITE)

	_apply_button_styles(btn, style, style, style, style)

func _update_labels():
	master_label.text = "Master Volume: " + str(int(master_slider.value))
	music_label.text = "Music Volume: " + str(int(music_slider.value))
	sfx_label.text = "SFX Volume: " + str(int(sfx_slider.value))
	text_speed_label.text = "Text Speed: " + str(int(text_speed_slider.value))
	auto_delay_label.text = "Auto Wait Time: " + str(int(auto_delay_slider.value))
	text_scale_label.text = "Text Size: " + str(int(text_scale_slider.value))

func _on_master_changed(value: float):
	_update_labels()
	if GameManager: Settings.set_bus_volume("Master", value / 7.0)

func _on_music_changed(value: float):
	_update_labels()
	if GameManager: Settings.set_bus_volume("Music", value / 7.0)

func _on_sfx_changed(value: float):
	_update_labels()
	if GameManager: Settings.set_bus_volume("SFX", value / 7.0)

func _on_instant_text_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager:
		Settings.instant_text = not Settings.instant_text
		_update_instant_text_visuals(Settings.instant_text)

func _update_instant_text_visuals(is_on: bool):
	_style_toggle_button(instant_text_toggle, is_on)

	text_speed_slider.editable = not is_on
	var alpha = 0.4 if is_on else 1.0
	text_speed_label.modulate.a = alpha
	text_speed_slider.modulate.a = alpha
	text_speed_minus.modulate.a = alpha
	text_speed_plus.modulate.a = alpha
	text_speed_minus.disabled = is_on
	text_speed_plus.disabled = is_on

func _on_text_speed_changed(value: float):
	_update_labels()
	if GameManager:
		var new_speed = remap(value, 0.0, 10.0, 0.05, 0.005)
		Settings.text_speed = new_speed

func _on_auto_forward_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager:
		Settings.is_auto_playing = not Settings.is_auto_playing
		_update_auto_forward_visuals(Settings.is_auto_playing)

func _update_auto_forward_visuals(is_on: bool):
	_style_toggle_button(auto_forward_toggle, is_on)

	auto_delay_slider.editable = is_on
	var alpha = 1.0 if is_on else 0.4
	auto_delay_label.modulate.a = alpha
	auto_delay_slider.modulate.a = alpha
	auto_delay_minus.modulate.a = alpha
	auto_delay_plus.modulate.a = alpha
	auto_delay_minus.disabled = not is_on
	auto_delay_plus.disabled = not is_on

func _on_auto_delay_changed(value: float):
	_update_labels()
	if GameManager:
		var new_delay = remap(value, 1.0, 10.0, 0.125, 1.75)
		Settings.auto_time_delay = new_delay

func _on_text_scale_changed(value: float):
	_update_labels()
	if GameManager:
		Settings.dialogue_text_scale = 0.5 + (value * 0.1)

func _on_assisted_mode_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	if GameManager:
		Settings.assisted_mode = not Settings.assisted_mode
		_update_assisted_toggle_visuals(Settings.assisted_mode)
		if GameManager.has_method("refresh_hint_system"):
			GameManager.refresh_hint_system()

func _update_assisted_toggle_visuals(is_on: bool):
	_style_toggle_button(assisted_mode_toggle, is_on)

func _style_toggle_button(btn: Button, is_on: bool):
	var style = StyleBoxFlat.new()
	style.corner_radius_top_left = 6
	style.corner_radius_top_right = 6
	style.corner_radius_bottom_left = 6
	style.corner_radius_bottom_right = 6
	style.anti_aliasing = false

	if is_on:
		btn.text = "ON"
		btn.add_theme_color_override("font_color", Color(0.1, 0.1, 0.1, 1.0))
		btn.add_theme_color_override("font_hover_color", Color(0.1, 0.1, 0.1, 1.0) if OS.has_feature("mobile") else Color.BLACK)
		style.bg_color = Color(0.2, 0.85, 1.0, 1.0)
	else:
		btn.text = "OFF"
		btn.add_theme_color_override("font_color", Color(0.6, 0.6, 0.6, 1.0))
		btn.add_theme_color_override("font_hover_color", Color(0.6, 0.6, 0.6, 1.0) if OS.has_feature("mobile") else Color.WHITE)
		style.bg_color = Color(0.15, 0.15, 0.15, 1.0)

	_apply_button_styles(btn, style, style, style, style)

func _on_close_pressed():
	if SoundManager: SoundManager.play_sfx("ui_click")
	queue_free()

func _exit_tree():
	if GameManager:
		Settings.save_settings()

func _apply_ui_polish():
	# Fonts and main text sizing
	title.add_theme_font_override("font", custom_font)
	title.add_theme_font_size_override("font_size", 72 if OS.has_feature("mobile") else 48)
	title.add_theme_color_override("font_color", Color(0.2, 0.85, 1.0, 1.0))

	var mobile_mult = 1.5 if OS.has_feature("mobile") else 1.0
	var label_size = int(28 * mobile_mult) # Increased for readability
	var toggle_size = int(24 * mobile_mult)
	var tab_size = int(30 * mobile_mult)
	var plus_minus_size = int(28 * mobile_mult)

	# Apply font to Tabs
	for btn in [tab_audio_btn, tab_dialogue_btn, tab_gameplay_btn]:
		btn.add_theme_font_override("font", custom_font)
		btn.add_theme_font_size_override("font_size", tab_size)

	# Build Square style for the +/- buttons
	var square_btn_style = StyleBoxFlat.new()
	square_btn_style.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	square_btn_style.corner_radius_top_left = 6
	square_btn_style.corner_radius_top_right = 6
	square_btn_style.corner_radius_bottom_left = 6
	square_btn_style.corner_radius_bottom_right = 6
	square_btn_style.border_width_left = 2
	square_btn_style.border_width_top = 2
	square_btn_style.border_width_right = 2
	square_btn_style.border_width_bottom = 2
	square_btn_style.border_color = Color(1.0, 1.0, 1.0, 0.0)
	square_btn_style.anti_aliasing = false

	var square_btn_hover = square_btn_style.duplicate()
	square_btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
	square_btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

	var square_btn_disabled = square_btn_style.duplicate()
	square_btn_disabled.bg_color = Color(0.12, 0.12, 0.12, 0.5)

	var inc_dec_buttons = [
		master_minus, master_plus, music_minus, music_plus, sfx_minus, sfx_plus,
		text_scale_minus, text_scale_plus, text_speed_minus, text_speed_plus,
		auto_delay_minus, auto_delay_plus
	]

	# Apply font to all Labels and Toggles
	var all_nodes = tab_audio_content.get_children() + tab_dialogue_content.get_children() + tab_gameplay_content.get_children()
	for child in all_nodes:
		if child is Label:
			child.add_theme_font_override("font", custom_font)
			child.add_theme_font_size_override("font_size", label_size)
			if child.name.ends_with("Label"):
				child.add_theme_color_override("font_color", Color(0.2, 0.85, 1.0, 1.0))
		elif child is HBoxContainer:
			for subchild in child.get_children():
				if subchild is Label:
					subchild.add_theme_font_override("font", custom_font)
					subchild.add_theme_font_size_override("font_size", label_size)
				elif subchild is Button:
					subchild.add_theme_font_override("font", custom_font)
					if subchild in inc_dec_buttons:
						subchild.add_theme_font_size_override("font_size", plus_minus_size)
						_apply_button_styles(subchild, square_btn_style, square_btn_hover, square_btn_hover, square_btn_disabled)
						subchild.add_theme_color_override("font_disabled_color", Color(0.4, 0.4, 0.4, 0.5))
					else:
						subchild.add_theme_font_size_override("font_size", toggle_size)

	assisted_mode_subtext.add_theme_font_size_override("font_size", int(18 * mobile_mult))

	# --- CUSTOM SLIDER STYLE (Thicker track, bigger bar) ---
	var slider_bg = StyleBoxFlat.new()
	slider_bg.bg_color = Color(0.1, 0.12, 0.15, 1.0) # Dark solid track
	slider_bg.corner_radius_top_left = 2
	slider_bg.corner_radius_top_right = 2
	slider_bg.corner_radius_bottom_left = 2
	slider_bg.corner_radius_bottom_right = 2
	# Thicker track limits
	slider_bg.content_margin_top = 26 if OS.has_feature("mobile") else 16
	slider_bg.content_margin_bottom = 26 if OS.has_feature("mobile") else 16

	# The area left of the grabber is completely transparent so the dark track shows through
	var slider_fill = StyleBoxEmpty.new()

	# Create a custom ImageTexture for the vertical bar grabber (Taller and slightly wider)
	var grabber_width = 12 if OS.has_feature("mobile") else 8
	var grabber_height = 50 if OS.has_feature("mobile") else 32
	var grabber_img = Image.create_empty(grabber_width, grabber_height, false, Image.FORMAT_RGBA8)
	grabber_img.fill(Color(0.2, 0.85, 1.0, 1.0)) # Bright Cyan line
	var grabber_tex = ImageTexture.create_from_image(grabber_img)

	var sliders = [master_slider, music_slider, sfx_slider, text_speed_slider, auto_delay_slider, text_scale_slider]
	for s in sliders:
		s.scrollable = false
		s.add_theme_stylebox_override("slider", slider_bg)
		s.add_theme_stylebox_override("grabber_area", slider_fill)
		s.add_theme_stylebox_override("grabber_area_highlight", slider_fill)
		s.add_theme_icon_override("grabber", grabber_tex)
		s.add_theme_icon_override("grabber_highlight", grabber_tex)

	# Close Button Styling
	close_button.add_theme_font_override("font", custom_font)
	close_button.add_theme_font_size_override("font_size", 46 if OS.has_feature("mobile") else 28)

	var btn_normal = StyleBoxFlat.new()
	btn_normal.bg_color = Color(0.15, 0.15, 0.15, 0.85)
	btn_normal.corner_radius_top_left = 6
	btn_normal.corner_radius_top_right = 6
	btn_normal.corner_radius_bottom_left = 6
	btn_normal.corner_radius_bottom_right = 6
	btn_normal.content_margin_top = 25 if OS.has_feature("mobile") else 10
	btn_normal.content_margin_bottom = 25 if OS.has_feature("mobile") else 10
	btn_normal.border_width_left = 2
	btn_normal.border_width_top = 2
	btn_normal.border_width_right = 2
	btn_normal.border_width_bottom = 2
	btn_normal.border_color = Color(1.0, 1.0, 1.0, 0.0)
	btn_normal.anti_aliasing = false

	var btn_hover = btn_normal.duplicate()
	btn_hover.bg_color = Color(0.1, 0.25, 0.3, 0.9)
	btn_hover.border_color = Color(0.2, 0.85, 1.0, 0.8)

	_apply_button_styles(close_button, btn_normal, btn_hover, btn_hover, btn_normal)
	close_button.add_theme_color_override("font_color", Color(0.8, 0.8, 0.8, 1.0))
	close_button.add_theme_color_override("font_hover_color", Color(0.8, 0.8, 0.8, 1.0) if OS.has_feature("mobile") else Color.WHITE)
	close_button.add_theme_color_override("font_pressed_color", Color.WHITE)

	# --- SCROLLBAR STYLE ---
	var tab_scroll: ScrollContainer = %TabScroll
	var vbar: VScrollBar = tab_scroll.get_v_scroll_bar()
	vbar.custom_minimum_size.x = 24 if OS.has_feature("mobile") else 12

	var track_style = StyleBoxFlat.new()
	track_style.bg_color = Color(0.1, 0.12, 0.15, 1.0)
	track_style.corner_radius_top_left = 4
	track_style.corner_radius_top_right = 4
	track_style.corner_radius_bottom_left = 4
	track_style.corner_radius_bottom_right = 4

	var grabber_style = StyleBoxFlat.new()
	grabber_style.bg_color = Color(0.2, 0.85, 1.0, 0.9)
	grabber_style.corner_radius_top_left = 4
	grabber_style.corner_radius_top_right = 4
	grabber_style.corner_radius_bottom_left = 4
	grabber_style.corner_radius_bottom_right = 4

	var grabber_hl = grabber_style.duplicate()
	grabber_hl.bg_color = Color(0.2, 0.85, 1.0, 1.0)

	vbar.add_theme_stylebox_override("scroll", track_style)
	vbar.add_theme_stylebox_override("scroll_focus", track_style)
	vbar.add_theme_stylebox_override("grabber", grabber_style)
	vbar.add_theme_stylebox_override("grabber_highlight", grabber_hl)
	vbar.add_theme_stylebox_override("grabber_pressed", grabber_hl)
	%ScrollMargin.add_theme_constant_override("margin_right", 40 if OS.has_feature("mobile") else 24)

	if OS.has_feature("mobile"):
		tab_scroll.scroll_deadzone = 24
		var margin_container = $ColorRect/MarginContainer
		margin_container.add_theme_constant_override("margin_left", 120)
		margin_container.add_theme_constant_override("margin_right", 120)
		margin_container.add_theme_constant_override("margin_top", 60)
		margin_container.add_theme_constant_override("margin_bottom", 60)
		for btn in inc_dec_buttons:
			btn.custom_minimum_size = Vector2(90, 90)
		for toggle in [instant_text_toggle, auto_forward_toggle, assisted_mode_toggle]:
			toggle.custom_minimum_size = Vector2(180, 90)
		for s in sliders:
			s.custom_minimum_size.y = 90
		close_button.custom_minimum_size.y = 110
</file>

<file path="ui/TransitionLayer.gd">
# TransitionLayer.gd
extends CanvasLayer

signal transition_halfway 
signal transition_finished

@onready var left_shutter = get_node_or_null("LeftShutter")
@onready var right_shutter = get_node_or_null("RightShutter")
@onready var iris_rect = get_node_or_null("IrisColorRect")
@onready var global_fade_rect = get_node_or_null("GlobalFadeRect")

func _ready():
	open_instant()

func open_instant():
	# If Godot runs this before the screen is ready, safely abort to prevent crashes.
	if not left_shutter or not right_shutter: return
	
	var viewport_width = get_viewport().get_visible_rect().size.x
	left_shutter.position.x = -left_shutter.size.x
	right_shutter.position.x = viewport_width
	
	if iris_rect and iris_rect.material:
		iris_rect.material.set_shader_parameter("circle_size", 1.5)
		iris_rect.visible = false

# --- 1. SCI-FI DOOR TRANSITION (Used for Teleporting) ---
func play_transition_sequence():
	if not left_shutter or not right_shutter: return

	_set_gm_transitioning(true)

	var viewport_width = get_viewport().get_visible_rect().size.x
	var center_x = viewport_width / 2.0
	
	SoundManager.play_sfx("door_close")
	
	var tween = create_tween().set_parallel(true)
	tween.set_trans(Tween.TRANS_QUART).set_ease(Tween.EASE_OUT)
	tween.tween_property(left_shutter, "position:x", 0.0, 0.5)
	tween.tween_property(right_shutter, "position:x", center_x, 0.5)
	await tween.finished
	
	emit_signal("transition_halfway")
	
	# 1.2 second delay while screen is black (1 second longer)
	await get_tree().create_timer(1.2).timeout
	
	SoundManager.play_sfx("door_open")
	
	var open_tween = create_tween().set_parallel(true)
	open_tween.set_trans(Tween.TRANS_QUART).set_ease(Tween.EASE_IN)
	open_tween.tween_property(left_shutter, "position:x", -left_shutter.size.x, 0.5)
	open_tween.tween_property(right_shutter, "position:x", viewport_width, 0.5)
	
	await open_tween.finished

	_set_gm_transitioning(false)
	emit_signal("transition_finished")


# --- 2. IRIS "EYE" TRANSITIONS (Used for loading states) ---
func play_iris_close(duration: float = 1.0):
	_set_gm_transitioning(true)
	if not iris_rect or not iris_rect.material:
		await get_tree().create_timer(duration).timeout
		emit_signal("transition_halfway")
		return
	
	iris_rect.visible = true
	iris_rect.material.set_shader_parameter("circle_size", 1.5) # Start wide open
	
	var tween = create_tween()
	# Tween to -0.1 to completely swallow the soft edge!
	tween.tween_property(iris_rect.material, "shader_parameter/circle_size", -0.1, duration)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	
	await tween.finished
	emit_signal("transition_halfway")

func play_iris_open(duration: float = 1.0):
	if not iris_rect or not iris_rect.material:
		await get_tree().create_timer(duration).timeout
		_set_gm_transitioning(false)
		emit_signal("transition_finished")
		return

	iris_rect.visible = true
	iris_rect.material.set_shader_parameter("circle_size", -0.1) # Start completely closed

	var tween = create_tween()
	tween.tween_property(iris_rect.material, "shader_parameter/circle_size", 1.5, duration)\
		.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)

	await tween.finished
	iris_rect.visible = false
	_set_gm_transitioning(false)
	emit_signal("transition_finished")

# --- 3. TRADITIONAL FADES (For Game Over) ---
func global_fade_to_black(duration: float = 3.0):
	_set_gm_transitioning(true)
	if global_fade_rect:
		global_fade_rect.modulate.a = 0.0
		global_fade_rect.visible = true
		var tween = create_tween()
		tween.tween_property(global_fade_rect, "modulate:a", 1.0, duration).set_trans(Tween.TRANS_SINE)
		await tween.finished

func global_fade_from_black(duration: float = 1.0):
	if global_fade_rect:
		var tween = create_tween()
		tween.tween_property(global_fade_rect, "modulate:a", 0.0, duration).set_trans(Tween.TRANS_SINE)
		await tween.finished
		global_fade_rect.visible = false
	_set_gm_transitioning(false)


func _set_gm_transitioning(is_active: bool):
	if GameManager:
		GameManager.is_transitioning = is_active
		if GameManager.has_method("refresh_hovered_object"):
			GameManager.refresh_hovered_object()
		if GameManager.has_method("update_sentence_line_ui"):
			GameManager.update_sentence_line_ui()
</file>

<file path="ui/VerbUI.gd">
# VerbUI.gd
extends CanvasLayer

@onready var action_bubble_label: RichTextLabel = $ActionBubbleLabel
@onready var verb_grid_panel: Panel = $VerbGridPanel
@onready var dynamic_verb_vbox: VBoxContainer = %DynamicVerbVBox

var active_verb_buttons: Dictionary = {}
var all_button_slots: Array[Button] = []
var normal_bubble_style: StyleBoxFlat
var disabled_bubble_style: StyleBoxFlat
var think_pulse_tween: Tween

const PREFERRED_ORDER = ["examine", "use", "talk_to", "pickup"]
const ROW_RECIPES = {
	1: [1],
	2: [1, 1],
	3: [2, 1],
	4: [2, 2],
	5: [3, 2],
	6: [3, 3],
	7: [3, 2, 2],
	8: [3, 3, 2],
	9: [3, 3, 3],
}

func _ready():
	if OS.has_feature("mobile"):
		verb_grid_panel.anchor_top = 0.75
		var tab_panel = verb_grid_panel.get_node("TabPanel")
		tab_panel.offset_top = -50
		tab_panel.offset_left = 15
		tab_panel.offset_right = 220
		tab_panel.get_node("ActionsLabel").add_theme_font_size_override("font_size", 28)
		action_bubble_label.add_theme_font_size_override("normal_font_size", 28)

	if GameManager:
		GameManager.available_verbs_changed.connect(_on_available_verbs_changed)
		GameManager.verb_changed.connect(_on_game_manager_verb_changed)
		GameManager.sentence_line_updated.connect(_on_game_manager_sentence_line_updated)
		GameManager.interaction_complete.connect(_on_interaction_complete)
		GameManager.new_hint_available.connect(_on_new_hint_available)

		if GameManager.has_method("get_currently_displayable_verbs"):
			_on_available_verbs_changed(GameManager.get_currently_displayable_verbs())

	action_bubble_label.visible = false

	action_bubble_label.bbcode_enabled = true
	action_bubble_label.fit_content = true
	action_bubble_label.scroll_active = false
	action_bubble_label.autowrap_mode = TextServer.AUTOWRAP_OFF
	action_bubble_label.add_theme_font_override("normal_font", preload("res://Fonts/VarelaRound-Regular.ttf"))
	action_bubble_label.add_theme_font_size_override("normal_font_size", 24)
	action_bubble_label.add_theme_color_override("font_shadow_color", Color(0, 0, 0, 0.5))
	action_bubble_label.add_theme_constant_override("shadow_outline_size", 1)

	normal_bubble_style = action_bubble_label.get_theme_stylebox("normal").duplicate()
	disabled_bubble_style = normal_bubble_style.duplicate()
	disabled_bubble_style.bg_color = Color(0.15, 0.15, 0.15, 0.9)
	disabled_bubble_style.border_color = Color(0.4, 0.4, 0.4, 0.8)

func _process(_delta: float) -> void:
	if OS.has_feature("mobile"): return
	if action_bubble_label.visible:
		action_bubble_label.global_position = get_viewport().get_mouse_position() + Vector2(15, 15)

func _on_available_verbs_changed(available_verb_data_array: Array[VerbData]):
	active_verb_buttons.clear()
	all_button_slots.clear()

	for child in dynamic_verb_vbox.get_children():
		if child is HBoxContainer:
			child.queue_free()

	# Sort verbs: preferred order first, then the rest
	var sorted_verbs: Array[VerbData] = []
	var remaining: Array[VerbData] = available_verb_data_array.duplicate()
	for preferred_id in PREFERRED_ORDER:
		for verb in remaining:
			if verb.verb_id == preferred_id:
				sorted_verbs.append(verb)
				remaining.erase(verb)
				break
	sorted_verbs.append_array(remaining)

	var count = sorted_verbs.size()
	if count == 0:
		_update_button_selected_visual_state("")
		return

	var recipe = ROW_RECIPES.get(count, _fallback_recipe(count))

	var verb_index = 0
	for row_count in recipe:
		var hbox = HBoxContainer.new()
		hbox.size_flags_vertical = Control.SIZE_EXPAND_FILL
		hbox.add_theme_constant_override("separation", 8)
		dynamic_verb_vbox.add_child(hbox)

		for j in range(row_count):
			if verb_index >= sorted_verbs.size():
				break
			var verb_data: VerbData = sorted_verbs[verb_index]
			var btn = Button.new()
			btn.text = verb_data.display_text
			btn.size_flags_horizontal = Control.SIZE_EXPAND_FILL
			btn.size_flags_vertical = Control.SIZE_EXPAND_FILL
			btn.focus_mode = Control.FOCUS_NONE
			btn.set_meta("verb_id", verb_data.verb_id)
			btn.pressed.connect(_on_verb_button_pressed_dynamic.bind(verb_data.verb_id))

			_style_verb_button(btn)
			if not OS.has_feature("mobile"):
				btn.custom_minimum_size = Vector2(100, 30)

			hbox.add_child(btn)
			all_button_slots.append(btn)
			active_verb_buttons[verb_data.verb_id] = btn
			verb_index += 1

	_update_button_selected_visual_state(GameManager.current_verb_id if GameManager else "")

	if GameManager and GameManager.has_method("has_unread_hint"):
		_on_new_hint_available(GameManager.has_unread_hint())

func _fallback_recipe(count: int) -> Array:
	var recipe = []
	var left = count
	while left > 0:
		var row = mini(left, 3)
		recipe.append(row)
		left -= row
	return recipe

func _style_verb_button(btn: Button):
	if OS.has_feature("mobile"):
		btn.add_theme_font_size_override("font_size", 36)

func _on_verb_button_pressed_dynamic(verb_id_pressed: String):
	if SoundManager: SoundManager.play_sfx("ui_click")

	if GameManager and verb_id_pressed != "":
		GameManager.select_verb(verb_id_pressed)

func _on_game_manager_verb_changed(new_verb_id: String):
	if OS.has_feature("mobile"):
		action_bubble_label.visible = false
		_update_button_selected_visual_state(new_verb_id)
		return
	if new_verb_id == "":
		action_bubble_label.visible = false
	else:
		var verb_data = GameManager.get_verb_data_by_id(new_verb_id) if GameManager else null
		if verb_data:
			action_bubble_label.text = verb_data.display_text + ":"
		else:
			action_bubble_label.text = new_verb_id + ":"

		action_bubble_label.reset_size()
		action_bubble_label.visible = true

	_update_button_selected_visual_state(new_verb_id)

func _on_game_manager_sentence_line_updated(full_sentence: String):
	if OS.has_feature("mobile"):
		action_bubble_label.visible = false
		return
	if full_sentence == "":
		action_bubble_label.visible = false
	else:
		action_bubble_label.text = full_sentence
		action_bubble_label.reset_size()
		action_bubble_label.visible = true

		if GameManager and GameManager.current_verb_id == "give" and GameManager.current_selected_item_data == null and GameManager.hovered_interactable != null:
			action_bubble_label.add_theme_stylebox_override("normal", disabled_bubble_style)
			action_bubble_label.add_theme_color_override("default_color", Color(0.6, 0.6, 0.6, 1.0))
		else:
			action_bubble_label.add_theme_stylebox_override("normal", normal_bubble_style)
			action_bubble_label.remove_theme_color_override("default_color")

func _on_interaction_complete():
	action_bubble_label.visible = false
	_update_button_selected_visual_state("")

func _update_button_selected_visual_state(selected_verb_id: String):
	for button_node in all_button_slots:
		var button_verb_id = button_node.get_meta("verb_id", "")
		if is_instance_valid(button_node):
			if button_verb_id == "think" and think_pulse_tween and think_pulse_tween.is_valid():
				continue

			if button_verb_id != "" and button_verb_id == selected_verb_id:
				button_node.modulate = Color(0.2, 0.85, 1.0, 1.0)
			else:
				button_node.modulate = Color(1.0, 1.0, 1.0)

func _on_new_hint_available(is_available: bool):
	if not active_verb_buttons.has("think"): return
	var btn = active_verb_buttons["think"]

	if think_pulse_tween and think_pulse_tween.is_valid():
		think_pulse_tween.kill()

	if is_available and GameManager and Settings.assisted_mode:
		btn.modulate = Color.WHITE
		think_pulse_tween = create_tween().set_loops()
		think_pulse_tween.tween_property(btn, "modulate", Color(1.0, 1.0, 0.0, 1.0), 0.6).set_trans(Tween.TRANS_SINE)
		think_pulse_tween.tween_property(btn, "modulate", Color.WHITE, 0.6).set_trans(Tween.TRANS_SINE)
	else:
		if GameManager and GameManager.current_verb_id == "think":
			btn.modulate = Color(0.2, 0.85, 1.0, 1.0)
		else:
			btn.modulate = Color.WHITE
</file>

<file path="ui/walk_indicator.gd">
extends Node2D

var _pulse_time: float = 0.0

func _ready():
	z_index = 10 # Sit above floor, below player
	hide()

func _process(delta):
	if visible:
		_pulse_time += delta * 5.0
		queue_redraw()

func _draw():
	var color = Color(0.2, 0.85, 1.0, 0.6 + sin(_pulse_time) * 0.3)
	draw_arc(Vector2.ZERO, 15.0, 0, TAU, 32, color, 3.0, true)
	draw_circle(Vector2.ZERO, 4.0, color)
</file>

<file path="interactions/actions/CallMethodAction.gd">
# res://interactions/actions/CallMethodAction.gd
class_name CallMethodAction
extends Action

## A path to the node you want to call a function on.
@export var target_node_path: NodePath

## The name of the function to call on the target node.
@export var method_name: String = ""

## An array of arguments (optional) to pass to the function.
@export var arguments: Array


func execute(interactable_node: Interactable) -> bool:
	if target_node_path.is_empty() or method_name.is_empty():
		push_warning("CallMethodAction on '%s' is not configured correctly." % interactable_node.object_display_name)
		return true

	# Get the target node. The path is relative to the interactable that is
	# running this action. This is flexible and powerful.
	var target_node = interactable_node.get_node_or_null(target_node_path)

	if not is_instance_valid(target_node):
		push_warning("CallMethodAction on '%s' could not find the target node at path: %s" % [interactable_node.object_display_name, target_node_path])
		return true

	if not target_node.has_method(method_name):
		push_warning("CallMethodAction: Target node '%s' does not have method '%s'." % [target_node.name, method_name])
		return true

	# Call the method on the target node, passing the arguments.
	# callv is used to call a method with an array of arguments.
	target_node.callv(method_name, arguments)
	print_rich("[color=cyan]CallMethodAction: Called method '%s' on node '%s'[/color]" % [method_name, target_node.name])
	return true
</file>

<file path="interactions/actions/DestroyAction.gd">
# res://interactions/actions/DestroyAction.gd
class_name DestroyAction
extends Action

## An optional message to display just before the object is destroyed.
@export var message_on_destroy: String = ""


func execute(interactable_node: Interactable) -> bool:
	if not message_on_destroy.is_empty():
		interactable_node.display_dialogue.emit(message_on_destroy)

	# This logic is copied directly from your old _execute_action_details function.
	# It handles the special case where the interactable might be inside a UI
	# element (like a TextureButton) in a zoom view.
	var parent_node = interactable_node.get_parent()
	if parent_node is TextureButton:
		print_rich("[color=cyan]DestroyAction: Destroying parent TextureButton wrapper for '%s'.[/color]" % interactable_node.object_display_name)
		parent_node.queue_free()
		return true
	else:
		# If it's not in a UI wrapper, we just ask the Interactable to destroy itself.
		# We use the existing signal to ensure a clean, deferred removal.
		print_rich("[color=cyan]DestroyAction: Requesting self-destruction for '%s'.[/color]" % interactable_node.object_display_name)
		interactable_node.self_destruct_requested.emit()
		return true
</file>

<file path="interactions/actions/OpenZoomViewAction.gd">
# res://interactions/actions/OpenZoomViewAction.gd
class_name OpenZoomViewAction
extends Action

# This action is simple and needs no exported variables. It uses the
# PackedScene that is already configured on the Interactable itself.

func execute(interactable_node: Interactable) -> bool:
	# First, check if the Interactable has a zoom scene assigned.
	if interactable_node.object_zoom_overlay_scene:
		# Tell the GameManager to prepare for the zoom state.
		if GameManager:
			GameManager.enter_zoom_view_state()

		# Instantiate the scene and add it to the tree.
		var zoom_instance = interactable_node.object_zoom_overlay_scene.instantiate()
		interactable_node.get_tree().root.add_child(zoom_instance)

		print_rich("[color=cyan]OpenZoomViewAction: Opened zoom view for '%s'.[/color]" % interactable_node.object_display_name)

		# VERY IMPORTANT: Return false to stop the interaction cycle.
		# This prevents the UI from resetting while the zoom view is open.
		return false
	else:
		# If no scene is assigned, print an error and continue normally.
		push_warning("OpenZoomViewAction failed: No 'object_zoom_overlay_scene' assigned to '%s'." % interactable_node.object_display_name)
		interactable_node.display_dialogue.emit("It doesn't seem to open.")
		return true
</file>

<file path="interactions/actions/SayLineAction.gd">
# res://interactions/actions/SayLineAction.gd
class_name SayLineAction
extends Action

## The line of dialogue for the character to say.
@export var line_to_say: String = "..."


# Override the base execute method with specific logic for this action.
func execute(interactable_node: Interactable) -> bool:
	# We have access to the interactable that is running this action.
	# We can use it to emit its `display_dialogue` signal, which the
	# GameManager is already set up to listen for.
	interactable_node.display_dialogue.emit(line_to_say)
	return true
</file>

<file path="interactions/actions/TriggerCutsceneAction.gd">
class_name TriggerCutsceneAction
extends Action

# The name of the Node in the scene tree that holds the cutscene script.
# Example: "ToiletClogCutscene"
@export var cutscene_node_name: String = ""

func execute(interactable_node: Interactable) -> Variant:
	if cutscene_node_name == "":
		push_warning("TriggerCutsceneAction: No cutscene_node_name specified.")
		return true

	# --- THE FIX ---
	# Because we load the game dynamically now, get_tree().current_scene might point 
	# to the Boot scene. We must explicitly search the GameManager's main scene instance!
	var root_node = null
	if GameManager and is_instance_valid(GameManager.main_game_scene_instance):
		root_node = GameManager.main_game_scene_instance
	else:
		root_node = interactable_node.get_tree().current_scene
		
	if not root_node:
		push_error("TriggerCutsceneAction: Could not find a valid root scene to search.")
		return true

	# Search for the node by name (recursive = true)
	var cutscene_node = root_node.find_child(cutscene_node_name, true, false)
	
	if cutscene_node and cutscene_node.has_method("start_cutscene"):
		# We DO NOT await this so the interactable system can finish 
		# and hand full control over to the Cutscene state machine.
		cutscene_node.start_cutscene()
	else:
		push_error("TriggerCutsceneAction: Could not find Cutscene Node named '%s' (or it lacks start_cutscene method)." % cutscene_node_name)
		
	return true
</file>

<file path="interactions/actions/UniversalFlashAction.gd">
class_name UniversalFlashAction
extends Action

const FLASH_DIALOGUE_PATH = "res://dialogue/flash_dialogue.dialogue"

func execute(interactable_node: Interactable) -> Variant:
	var obj_id = interactable_node.object_id
	var player = GameManager.player_node

	var flash_dialogue = load(FLASH_DIALOGUE_PATH)
	if not flash_dialogue:
		push_error("UniversalFlashAction: Could not load flash_dialogue.dialogue!")
		return true

	# 1. Check Unflashables (Plays dialogue, skips animation, ends interaction)
	var unflashable_id = obj_id + "_unflashable"
	if flash_dialogue.titles.has(unflashable_id):
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", flash_dialogue, unflashable_id)
		await DialogueManager.dialogue_ended
		return true

	# Reset the abort flag before starting
	Flags.set_game_flag("abort_flash", false)

	# 2. Check Pre-Flash (Plays dialogue, then continues to animation)
	var pre_id = obj_id + "_pre"
	if flash_dialogue.titles.has(pre_id):
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", flash_dialogue, pre_id)
		await DialogueManager.dialogue_ended

	# Check if the dialogue requested the flash to be aborted
	if Flags.get_game_flag("abort_flash") == true:
		Flags.set_game_flag("abort_flash", false)
		return true

	# 3. Perform Flash Animation
	if is_instance_valid(player):
		if player.has_method("face_target"):
			player.face_target(interactable_node.global_position)

		if player.has_method("set_animation_state"):
			player.set_animation_state("flash")

		# if SoundManager: SoundManager.play_sfx("flashlight_click")

		var anim_duration: float = 1.0
		var anim_player = player.get_node_or_null("AnimationPlayer")
		if anim_player and anim_player.has_animation("flash"):
			anim_duration = anim_player.get_animation("flash").length

		await interactable_node.get_tree().create_timer(anim_duration).timeout

		if player.has_method("set_animation_state"):
			player.set_animation_state("idle")

	# 4. Check Post-Flash (Plays specific post-flash, or defaults to generic)
	var post_id = obj_id + "_post"
	if not flash_dialogue.titles.has(post_id):
		post_id = "default_post"

	if flash_dialogue.titles.has(post_id):
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", flash_dialogue, post_id)
		await DialogueManager.dialogue_ended

	return true
</file>

<file path="LevelHintManager.gd">
extends Node
class_name LevelHintManager

@export var hints_dialogue: DialogueResource
@export var hints_adventure_dialogue: DialogueResource

func evaluate_hint() -> String:
	if not GameManager: return ""

	var needs_meds = Flags.get_level_flag("mcbucket_interruption_happened")
	var in_bathroom = is_instance_valid(GameManager.player_node) and GameManager.player_node.global_position.y > 1000.0

	var has_any_drug = Inventory.has_item("zanopram") or Inventory.has_item("cannathink") or Inventory.has_item("invigirol")
	var used_any_drug = Flags.get_level_flag("mcbucket_zanopram_used") or Flags.get_level_flag("mcbucket_cannathink_used") or Flags.get_level_flag("mcbucket_invigirol_used")
	var cabinet_raided = has_any_drug or used_any_drug

	# ==========================================
	# THE ACTION CHAIN (Physical Puzzles)
	# ==========================================
	if Inventory.has_item("techpass"):
		return "hint_has_techpass"

	if Flags.get_level_flag("mcbucket_zanopram_used"):
		if needs_meds:
			return "hint_return_to_sergey"

	if Flags.get_level_flag("mcbucket_cannathink_used") or Flags.get_level_flag("mcbucket_invigirol_used"):
		if needs_meds: return "hint_wrong_drugs"
		else: return "hint_wrong_drugs_no_context"

	if Inventory.has_item("zanopram"):
		if needs_meds: return "hint_use_zanopram"
		else: return "hint_use_zanopram_no_context"

	if not Flags.get_level_flag("aida_in_main_room") and not cabinet_raided:
		if needs_meds: return "hint_aida_gone"
		else: return "hint_aida_gone_no_context"

	if Flags.get_level_flag("aida_in_main_room") and not cabinet_raided:
		if Flags.get_level_flag("toilet_clogged"):
			if needs_meds: return "hint_toilet_clogged"
			else: return "hint_toilet_clogged_no_context"

		if needs_meds and Flags.get_level_flag("aida_stopped_cabinet"):
			if in_bathroom: return "hint_clog_toilet_bathroom"
			else: return "hint_clog_toilet_main"

	# ==========================================
	# THE CONTEXT CHAIN (Story Objectives)
	# ==========================================
	if needs_meds:
		return "hint_need_distraction"

	if Flags.get_level_flag("has_tried_memory_box"):
		return "hint_ask_sergey"

	if Flags.get_level_flag("has_spoken_to_aida"):
		return "hint_use_memory_box"

	return "hint_start"
</file>

<file path="Cutscenes/ToiletClogCutscene.gd">
extends Cutscene

# --- REFERENCES ---
@export_group("Actors")
@export var aida_npc: CharacterBody2D
@export var player: CharacterBody2D

@export_group("Objects")
@export var toilet_interactable: Interactable 

@export_group("Locations")
@export var main_room_door_pos: Marker2D 
@export var player_spawn_pos: Marker2D 
@export var bathroom_entry_pos: Marker2D 
@export var toilet_fix_pos: Marker2D 
@export var main_room_return_pos: Marker2D

@export_group("Dialogue")
@export var aida_scold_dialogue: DialogueResource
@export var scold_dialogue_start_id: String = "AIda" # Updated to match your inspector
# --- NEW: Added the player's checkpoint ID here ---
@export var player_monologue_start_id: String = "Player" 

@export_group("Settings")
@export var fix_duration: float = 25.0 

func _execution_steps():
	print_rich("[color=magenta][Time: %s] Cutscene START.[/color]" % Time.get_ticks_msec())
	
	# --- STEP 0: PRE-SETUP ---
	Flags.set_level_flag("aida_fixing_toilet", true)
	
	var aida_mover = aida_npc.get_node_or_null("MovementController")
	if aida_mover: aida_mover.pause_movement()
	
	# Teleport Aida to waiting spot
	aida_npc.global_position = main_room_door_pos.global_position
	aida_npc.visible = true 
	
	# Face Aida
	var target_x = 0.0
	if player_spawn_pos: target_x = player_spawn_pos.global_position.x
	elif player: target_x = player.global_position.x
	if target_x < aida_npc.global_position.x:
		if aida_npc.has_node("Sprite"): aida_npc.get_node("Sprite").flip_h = true
	else:
		if aida_npc.has_node("Sprite"): aida_npc.get_node("Sprite").flip_h = false

	# Wait for fade-in
	await get_tree().create_timer(1.0).timeout

	# Face Player
	if player and player.has_method("face_target"):
		player.face_target(aida_npc.global_position)

	# --- STEP 1: AIDA DIALOGUE ---
	if aida_scold_dialogue:
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", aida_scold_dialogue, scold_dialogue_start_id)
		await DialogueManager.dialogue_ended
	
	# --- STEP 2: ENTER BATHROOM ---
	if SoundManager: 
		SoundManager.play_sfx("door_open")
	
	await get_tree().create_timer(0.2).timeout # Wait a split second to hear the door open
	aida_npc.visible = false 
	
	if SoundManager: 
		SoundManager.play_sfx("door_close")
		
	await get_tree().create_timer(0.3).timeout # Finish the original 0.5 second pause
	
	# --- NEW STEP 2.5: PLAYER MONOLOGUE ---
	# Right after Aida disappears and we wait half a second, the player talks.
	if aida_scold_dialogue and not player_monologue_start_id.is_empty():
		DialogueManager.show_dialogue_balloon_scene("res://conversationballoon.tscn", aida_scold_dialogue, player_monologue_start_id)
		await DialogueManager.dialogue_ended
	
	# Move Aida to her actual working position
	aida_npc.global_position = bathroom_entry_pos.global_position
	aida_npc.visible = true
	
	# EARLY RELEASE (Player regains control here, while Aida works in the background)
	print_rich("[color=magenta][Time: %s] Releasing Player Control.[/color]" % Time.get_ticks_msec())
	GameManager.change_game_state(GameManager.GameState.IN_GAME_PLAY)
	
	# --- BACKGROUND LOGIC ---
	
	# --- STEP 3: WALK TO TOILET ---
	if aida_mover:
		await aida_mover.move_to_position_async(toilet_fix_pos.global_position, 5.0, 10.0)
	
	# --- STEP 4: PLAY FIX ANIMATION ---
	var anim_player = aida_npc.get_node_or_null("AnimationPlayer")
	if anim_player and anim_player.has_animation("fix_toilet"):
		anim_player.play("fix_toilet")
		await get_tree().create_timer(fix_duration).timeout
		anim_player.play("idle")
	else:
		await get_tree().create_timer(5.0).timeout
	
	# --- STEP 5: UNCLOG TOILET ---
	if toilet_interactable:
		Flags.set_level_flag("toilet_clogged", false)
		Flags.set_level_flag("toilet_has_paper", false)
		
		var toilet_root = toilet_interactable.get_parent()
		if toilet_root and toilet_root.has_method("change_state"):
			toilet_root.change_state(0) 
	
	# --- STEP 6: LEAVE BATHROOM ---
	if aida_mover:
		await aida_mover.move_to_position_async(bathroom_entry_pos.global_position)
	
	aida_npc.visible = false
	await get_tree().create_timer(0.5).timeout
	
	# Teleport to Main Room Return Pos
	aida_npc.global_position = main_room_return_pos.global_position
	aida_npc.visible = true
	
	# ### MOVED FLAG RESET HERE ###
	Flags.set_level_flag("aida_fixing_toilet", false)
	# ---------------------------
	
	# --- STEP 7: RETURN TO PATROL ---
	print_rich("[color=magenta][Time: %s] Aida returning to desk...[/color]" % Time.get_ticks_msec())
	
	if aida_mover:
		if aida_mover.has_method("set_target_waypoint_index"):
			aida_mover.set_target_waypoint_index(0)
			
		aida_mover.resume_movement()
		
	# Reset Busy Flag
	Flags.set_level_flag("aida_fixing_toilet", false)
	
	print_rich("[color=magenta][Time: %s] Cutscene Script Complete.[/color]" % Time.get_ticks_msec())
</file>

<file path="Interactables/Game World/Hospital Bathroom/hospital_toilet.gd">
extends Node2D

enum ToiletState { NORMAL, HAS_PAPER, CLOGGED }

@onready var animation_player: AnimationPlayer = $AnimationPlayer
@onready var interactable_component: Interactable = $InteractionArea 

func _ready():
	await get_tree().process_frame
	if not GameManager: return
	_restore_state()

func _restore_state():
	if Flags.get_level_flag("toilet_clogged"):
		change_state(ToiletState.CLOGGED)
	elif Flags.get_level_flag("toilet_has_paper"):
		change_state(ToiletState.HAS_PAPER)
	else:
		change_state(ToiletState.NORMAL)

func change_state(new_state_int: int):
	var new_state = new_state_int as ToiletState
	
	if GameManager:
		# --- UPDATED LOGIC: Explicitly handle ALL flags ---
		match new_state:
			ToiletState.NORMAL:
				# Clear ALL flags when normal
				Flags.set_level_flag("toilet_has_paper", false)
				Flags.set_level_flag("toilet_clogged", false)
			ToiletState.HAS_PAPER:
				Flags.set_level_flag("toilet_has_paper", true)
				# Ensure clogged is false
				Flags.set_level_flag("toilet_clogged", false)
			ToiletState.CLOGGED:
				Flags.set_level_flag("toilet_clogged", true)
				# (Optional: keep has_paper true if you want, or clear it. Usually clogged implies paper is stuck)
				# Flags.set_level_flag("toilet_has_paper", true) 

	match new_state:
		ToiletState.NORMAL:
			if animation_player: 
				if animation_player.has_animation("idle"):
					animation_player.play("idle")
				else:
					animation_player.stop()
			print_rich("[color=cyan]Toilet State: NORMAL[/color]")
			
		ToiletState.HAS_PAPER:
			print_rich("[color=cyan]Toilet State: HAS_PAPER (Pending Flush)[/color]")
			
		ToiletState.CLOGGED:
			if animation_player: 
				if animation_player.has_animation("error"):
					animation_player.play("error")
				else:
					push_warning("HospitalToilet: 'error' animation not found.")
			print_rich("[color=red]Toilet State: CLOGGED[/color]")
</file>

<file path="interactions/actions/OpenMemoryBoxAction.gd">
# res://interactions/actions/OpenMemoryBoxAction.gd
class_name OpenMemoryBoxAction
extends Action

const MemoryBoxScene = preload("res://MemoryBoxOverlay.tscn")

func execute(interactable_node: Interactable) -> Variant:
	if not MemoryBoxScene:
		push_warning("OpenMemoryBoxAction failed: Scene could not be loaded.")
		return true

	# 1. ENTER UI STATE BEFORE FADE
	# This instantly hides the UI and locks player movement
	if GameManager:
		GameManager.cancel_current_action(false)
		GameManager.persisting_verb_id = ""
		GameManager.enter_conversation_state()

	# 2. IRIS CLOSE TO BLACK
	if GameManager and GameManager.transition_layer:
		await GameManager.transition_layer.play_iris_close(1.0)

	# 3. SPAWN THE UI (While screen is black)
	var instance = MemoryBoxScene.instantiate()
	interactable_node.get_tree().root.add_child(instance)

	await interactable_node.get_tree().process_frame

	# 4. TRIGGER RETRO BOOT SEQUENCE
	if instance.has_method("play_boot_sequence"):
		instance.play_boot_sequence()

	# 5. IRIS OPEN
	if GameManager and GameManager.transition_layer:
		await GameManager.transition_layer.play_iris_open(1.0)

	print_rich("[color=cyan]OpenMemoryBoxAction: Opened the Memory Box overlay.[/color]")

	return false
</file>

<file path="interactions/actions/RemoveItemAction.gd">
# res://interactions/actions/RemoveItemAction.gd
class_name RemoveItemAction
extends Action

## The unique ID of the item to remove from the player's inventory.
@export var item_id_to_remove: String = ""


func execute(interactable_node: Interactable) -> bool:
	if item_id_to_remove.is_empty():
		push_warning("RemoveItemAction executed on '%s' with an empty item_id." % interactable_node.object_display_name)
		return true

	# We use the interactable_node to emit the existing signal that the
	# GameManager is already set up to listen for.
	Inventory.remove_item(item_id_to_remove)
	print_rich("[color=cyan]RemoveItemAction: Requested to remove item '%s' from inventory.[/color]" % item_id_to_remove)
	return true
</file>

<file path="interactions/actions/SetLevelFlagAction.gd">
# res://interactions/actions/SetLevelFlagAction.gd
class_name SetLevelFlagAction
extends Action

@export var flag_name: String = ""
@export var flag_value: bool = true

func execute(_interactable_node: Interactable) -> Variant:
	if flag_name == "":
		push_warning("SetLevelFlagAction: No flag_name specified.")
		return true

	if GameManager:
		# This calls the function you already set up in GameManager
		# which routes it to the current LevelStateManager
		Flags.set_level_flag(flag_name, flag_value)
		print("Action: Set Level Flag '%s' to %s" % [flag_name, str(flag_value)])
	
	return true
</file>

<file path="interactions/actions/ShowCustomDialogueAction.gd">
# res://interactions/actions/ShowCustomDialogueAction.gd
class_name ShowCustomDialogueAction
extends Action

@export var dialogue_resource: DialogueResource
@export var dialogue_checkpoint: String = ""

# Define the specific balloon scene
const BALLOON_SCENE_PATH = "res://conversationballoon.tscn"

# Change return type to Variant to allow 'await' to work correctly
func execute(interactable_node: Interactable) -> Variant:
	if not dialogue_resource or dialogue_checkpoint.is_empty():
		push_warning("ShowCustomDialogueAction is not configured correctly.")
		return true

	# --- SAFETY CHECK ---
	# Ensure the checkpoint actually exists in the file to prevent hard crashes.
	var safe_checkpoint = dialogue_checkpoint
	if not dialogue_resource.titles.has(safe_checkpoint):
		push_warning("Dialogue checkpoint '%s' not found in resource! Falling back to 'start'." % safe_checkpoint)
		safe_checkpoint = "start"

	# 1. Start the dialogue using the CUSTOM SCENE
	DialogueManager.show_dialogue_balloon_scene(
		BALLOON_SCENE_PATH,
		dialogue_resource,
		safe_checkpoint
	)

	# 2. Wait for completion
	await DialogueManager.dialogue_ended

	# 3. Return true to signal the Action List to continue
	return true
</file>

<file path="interactions/actions/StartConversationAction.gd">
# res://interactions/actions/StartConversationAction.gd
class_name StartConversationAction
extends Action

# This action is very simple and requires no exported variables.
# It intelligently uses the "character_conversation_overlay_scene"
# that is already assigned on the Interactable itself.

func execute(interactable_node: Interactable) -> bool:
	# Determine the conversation scene: prefer the string path (soft ref), fall back to PackedScene (hard ref)
	var conversation_scene: PackedScene = null
	if not interactable_node.character_conversation_scene_path.is_empty():
		conversation_scene = load(interactable_node.character_conversation_scene_path)
	elif interactable_node.character_conversation_overlay_scene:
		conversation_scene = interactable_node.character_conversation_overlay_scene

	if conversation_scene:
		# Safety check for the GameManager.
		if not GameManager:
			push_warning("GameManager not found. Cannot enter conversation state.")
			return true # Fail gracefully, continue interaction cycle.

		# Tell the GameManager to hide the main UI and prepare for the conversation.
		GameManager.enter_conversation_state()

		# Instantiate the scene and add it to the root of the tree.
		var conversation_instance = conversation_scene.instantiate()
		interactable_node.get_tree().root.add_child(conversation_instance)
		GameManager.register_character_conversation(conversation_instance)

		print_rich("[color=cyan]StartConversationAction: Launched conversation for '%s'.[/color]" % interactable_node.object_display_name)

		# VERY IMPORTANT: Return false to stop the interaction cycle.
		# This leaves the game in the "conversation" state.
		return false
	else:
		# If no scene is assigned, the character can't talk.
		push_warning("StartConversationAction failed: No conversation scene assigned to '%s'." % interactable_node.object_display_name)
		# We can use the simple dialogue system for a fallback line.
		interactable_node.display_dialogue.emit("They don't seem to have much to say.")
		return true # Return true because the interaction is "over".
</file>

<file path="Boot.gd">
# Boot.gd
extends Node

const LOGO_SPLASH_SCENE_PATH = "res://logo_splash.tscn"

func _ready():
	if not GameManager:
		print_rich("[color=red]Boot Error: GameManager not found! Cannot proceed.[/color]")
		return

	start_logo_splash()


func start_logo_splash():
	print_rich("[color=yellow]Boot: Starting logo splash...[/color]")

	if GameManager:
		GameManager.change_game_state(GameManager.GameState.LOGO_SPLASH)

	var logo_splash_packed_scene = load(LOGO_SPLASH_SCENE_PATH)
	if not logo_splash_packed_scene:
		print_rich("[color=red]Boot Error: Failed to load Logo Splash Scene at path: %s[/color]" % LOGO_SPLASH_SCENE_PATH)
		# If the logo fails, go straight to the menu
		if GameManager:
			GameManager.change_game_state(GameManager.GameState.MAIN_MENU)
		return

	var logo_splash_instance = logo_splash_packed_scene.instantiate()

	if not logo_splash_instance.has_signal("splash_finished"):
		print_rich("[color=red]Boot Error: LogoSplash scene does not have a 'splash_finished' signal. Cannot proceed automatically.[/color]")
		add_child(logo_splash_instance)
		return

	logo_splash_instance.splash_finished.connect(_on_logo_splash_finished, CONNECT_ONE_SHOT)
	add_child(logo_splash_instance)


func _on_logo_splash_finished():
	print_rich("[color=yellow]Boot: Logo splash finished. Transitioning to loading screen...[/color]")
	
	# Load and instance the loading screen
	var loading_screen_scene = load("res://loading_screen.tscn")
	var loading_instance = loading_screen_scene.instantiate()
	add_child(loading_instance)
</file>

<file path="interactions/actions/AddItemAction.gd">
# res://interactions/actions/AddItemAction.gd
class_name AddItemAction
extends Action

@export var item_id_to_add: String = ""


# CHANGE THIS LINE: from "-> void" to "-> bool"
func execute(interactable_node: Interactable) -> bool:
	if item_id_to_add.is_empty():
		push_warning("AddItemAction executed on '%s' with an empty item_id." % interactable_node.object_display_name)
		return true # Also return true here for consistency

	Inventory.add_item(item_id_to_add)
	print_rich("[color=cyan]AddItemAction: Requested to add item '%s' to inventory.[/color]" % item_id_to_add)

	# --- NEW LOGIC: Automatically set the state flag so the item doesn't respawn ---
	if not interactable_node.state_flag_id.is_empty():
		if GameManager:
			Flags.set_level_flag(interactable_node.state_flag_id, true)
			print_rich("[color=green]AddItemAction: Auto-set level flag '%s' to true.[/color]" % interactable_node.state_flag_id)

	return true
</file>

<file path="interactions/actions/ShowDialogueActiont.gd">
# res://interactions/actions/ShowDialogueAction.gd
class_name ShowDialogueAction
extends Action

@export var dialogue_resource: DialogueResource = preload("res://examinables.dialogue")

# Define the specific balloon scene to ensure visual consistency
const BALLOON_SCENE_PATH = "res://conversationballoon.tscn"

func execute(interactable_node: Interactable) -> Variant:
	# --- Safety Checks ---
	if not dialogue_resource:
		push_warning("ShowDialogueAction on '%s' has no DialogueResource assigned." % interactable_node.object_display_name)
		return true

	if not DialogueManager:
		push_warning("DialogueManager autoload not found.")
		return true

	if not GameManager:
		push_warning("GameManager autoload not found.")
		return true

	if interactable_node.object_id.is_empty():
		push_warning("Interactable '%s' has an empty object_id." % interactable_node.object_display_name)

	# --- Core Logic ---
	var target_object_id: String = interactable_node.object_id

	# Safety check: if the dialogue file doesn't have this specific object's ID, fall back to "~ start"
	var checkpoint_to_use: String = target_object_id
	if dialogue_resource and not dialogue_resource.titles.has(checkpoint_to_use):
		checkpoint_to_use = "start"

	print_rich("[color=cyan]ShowDialogueAction: Showing custom balloon for '%s' (Resolved to: '%s')[/color]" % [target_object_id, checkpoint_to_use])

	# USE THE CUSTOM SCENE instead of the default project setting
	DialogueManager.show_dialogue_balloon_scene(
		BALLOON_SCENE_PATH,
		dialogue_resource,
		checkpoint_to_use
	)

	await DialogueManager.dialogue_ended

	return true
</file>

<file path="Main.gd">
# Main.gd (script attached to your 'Main' root node)
extends Control

# --- Level-Specific Event Resources ---
@export var aida_dialogue_resource: DialogueResource
@export var aida_explanation_data: ExplanationData

@export_group("UI Settings")
@export var enable_journal_notification: bool = true
@export var force_assisted_mode: bool = false

# --- Node References ---
@onready var level_state_manager: LevelStateManager = $LevelStateManager
@onready var sergey_interactable: Interactable = $Sergei_Path/Sergei/InteractionArea
@onready var mcbucket_interactable: Interactable = $McBucket_Path/McBucket/InteractionArea
@onready var memory_box_interactable: Interactable = $MemoryBox
@onready var level_hint_manager: LevelHintManager = $LevelHintManager


func _ready():
	_inject_progression_blockers()

	# We must wait for one frame. This is a crucial step.
	await get_tree().process_frame

	# --- Remove local PauseMenuUI (now spawned globally by GameManager) ---
	var local_pause = get_node_or_null("%PauseMenuUI")
	if is_instance_valid(local_pause):
		local_pause.queue_free()

	# --- DEBUG: Force Assisted Mode ---
	if GameManager and force_assisted_mode:
		Settings.assisted_mode = true
		GameManager.unlock_verb("think")
		GameManager.current_unread_hint = ""
		GameManager.last_read_hint = ""
	# ----------------------------------

	# --- Apply Journal Notification Setting ---
	if GameManager and is_instance_valid(GameManager.journal_button_ui):
		GameManager.journal_button_ui.set_notification_enabled(enable_journal_notification)

	# --- 1. Hide UI Button on Start ---
	if is_instance_valid(GameManager.insurance_form_button_ui):
		GameManager.insurance_form_button_ui.hide()
	else:
		print_rich("[color=orange]Main.gd: Could not hide insurance button on start, GameManager reference is invalid.[/color]")

	# --- 2. Setup Global Signals ---
	if GameManager:
		# Connect to signal so we know when Aida finishes talking
		GameManager.character_conversation_ended.connect(_on_character_conversation_ended)

		# Register this level's state manager
		if is_instance_valid(level_state_manager):
			Flags.register_level_state_manager(level_state_manager)
		if is_instance_valid(level_hint_manager):
			GameManager.current_hint_manager = level_hint_manager
		else:
			print_rich("[color=red]%s: LevelStateManager node not found...[/color]" % name)
	else:
		print_rich("[color=red]%s: GameManager not found.[/color]" % name)


func _exit_tree():
	if GameManager and is_instance_valid(level_state_manager):
		if Flags.current_level_state_manager == level_state_manager:
			Flags.register_level_state_manager(null)
			GameManager.current_hint_manager = null
			print_rich("[color=yellow]%s: Unregistered its LevelStateManager.[/color]" % name)


# --- SIGNAL HANDLERS ---

func _on_character_conversation_ended(resource: DialogueResource):
	if resource == aida_dialogue_resource:
		var just_spoke_to_aida = level_state_manager.get_level_flag("has_spoken_to_aida")
		var explanation_shown = level_state_manager.get_level_flag("aida_explanation_shown")

		if just_spoke_to_aida and not explanation_shown:
			level_state_manager.set_level_flag("aida_explanation_shown", true)
			level_state_manager.set_level_flag("insurance_button_unlocked", true)

			# Add a slight delay to let the game world "breathe" before the pop-up
			await get_tree().create_timer(0.5).timeout

			GameManager.start_explanation(aida_explanation_data, self)

func _inject_progression_blockers():
	var generic_dialogue = preload("res://generic_lines.dialogue")

	var act_talk_no_aida = ShowCustomDialogueAction.new()
	act_talk_no_aida.dialogue_resource = generic_dialogue
	act_talk_no_aida.dialogue_checkpoint = "block_talk_no_aida"

	var resp_talk_no_aida = InteractionResponse.new()
	resp_talk_no_aida.verb_id = "talk_to"
	resp_talk_no_aida.required_flag_id = "has_spoken_to_aida"
	resp_talk_no_aida.required_flag_value = false
	resp_talk_no_aida.actions_to_perform.append(act_talk_no_aida)

	var act_talk_no_box = ShowCustomDialogueAction.new()
	act_talk_no_box.dialogue_resource = generic_dialogue
	act_talk_no_box.dialogue_checkpoint = "block_talk_no_memory_box"

	var resp_talk_no_box = InteractionResponse.new()
	resp_talk_no_box.verb_id = "talk_to"
	resp_talk_no_box.required_flag_id = "has_tried_memory_box"
	resp_talk_no_box.required_flag_value = false
	resp_talk_no_box.actions_to_perform.append(act_talk_no_box)

	var act_use_box_no_aida = ShowCustomDialogueAction.new()
	act_use_box_no_aida.dialogue_resource = generic_dialogue
	act_use_box_no_aida.dialogue_checkpoint = "block_memory_box_no_aida"

	var resp_use_box_no_aida = InteractionResponse.new()
	resp_use_box_no_aida.verb_id = "use"
	resp_use_box_no_aida.required_flag_id = "has_spoken_to_aida"
	resp_use_box_no_aida.required_flag_value = false
	resp_use_box_no_aida.actions_to_perform.append(act_use_box_no_aida)

	if is_instance_valid(sergey_interactable):
		sergey_interactable.interactions.insert(0, resp_talk_no_box)
		sergey_interactable.interactions.insert(0, resp_talk_no_aida)

	if is_instance_valid(mcbucket_interactable):
		mcbucket_interactable.interactions.insert(0, resp_talk_no_box)
		mcbucket_interactable.interactions.insert(0, resp_talk_no_aida)

	if is_instance_valid(memory_box_interactable):
		memory_box_interactable.interactions.insert(0, resp_use_box_no_aida)
</file>

<file path="LevelStateManager.gd">
# LevelStateManager.gd
extends Node
class_name LevelStateManager

signal level_flag_changed(flag_name: String, new_value: bool)

# All level flags stored in a single dictionary. No need to edit this script to add new flags —
# just call set_level_flag("my_new_flag", true) from anywhere.
var _flags: Dictionary = {}

# Default flag values for this level. Only needed for flags that start as true.
# NOTE: Flags in _defaults without a debug toggle override:
#   - aida_in_main_room
var _defaults: Dictionary = {
	"aida_in_main_room": true
}

@export_group("Debug Flag Toggles")
@export var override_has_spoken_to_aida: bool = false
@export var override_aida_explanation_shown: bool = false
@export var override_insurance_button_unlocked: bool = false
@export var override_has_tried_memory_box: bool = false
@export var override_mcbucket_interruption_happened: bool = false
@export var override_mcbucket_has_screamed_at_player: bool = false
@export var override_mcbucket_zanopram_used: bool = false
@export var override_mcbucket_cannathink_used: bool = false
@export var override_mcbucket_invigirol_used: bool = false
@export var override_toilet_has_paper: bool = false
@export var override_toilet_clogged: bool = false
@export var override_tv_examined: bool = false
@export var override_tv_turned_off: bool = false
@export var override_learned_techpass_from_aida: bool = false
@export var override_learned_techpass_from_sergey: bool = false
@export var override_give_techpass: bool = false
@export var override_dev_cta_completed: bool = false


func _ready():
	for key in _defaults:
		_flags[key] = _defaults[key]

	# Apply individual debug toggles (only if true, so unchecked = no effect)
	var _toggles := {
		"has_spoken_to_aida": override_has_spoken_to_aida,
		"aida_explanation_shown": override_aida_explanation_shown,
		"insurance_button_unlocked": override_insurance_button_unlocked,
		"has_tried_memory_box": override_has_tried_memory_box,
		"mcbucket_interruption_happened": override_mcbucket_interruption_happened,
		"mcbucket_has_screamed_at_player": override_mcbucket_has_screamed_at_player,
		"mcbucket_zanopram_used": override_mcbucket_zanopram_used,
		"mcbucket_cannathink_used": override_mcbucket_cannathink_used,
		"mcbucket_invigirol_used": override_mcbucket_invigirol_used,
		"toilet_has_paper": override_toilet_has_paper,
		"toilet_clogged": override_toilet_clogged,
		"tv_examined": override_tv_examined,
		"tv_turned_off": override_tv_turned_off,
		"learned_techpass_from_aida": override_learned_techpass_from_aida,
		"learned_techpass_from_sergey": override_learned_techpass_from_sergey,
		"dev_cta_completed": override_dev_cta_completed,
	}
	for flag_name in _toggles:
		if _toggles[flag_name]:
			_flags[flag_name] = true

	# Debug: give techpass item directly
	if override_give_techpass and GameManager:
		Inventory.add_item("techpass")

	print_rich("[color=LawnGreen]LevelStateManager for '%s' is ready.[/color]" % (get_parent().name if get_parent() else "UnnamedLevel"))


func print_initial_flags():
	for flag_name in _flags:
		print_rich("  [color=gray]Initial Flag '%s': %s[/color]" % [flag_name, _flags[flag_name]])


func set_level_flag(flag_name: String, value: bool):
	if _flags.get(flag_name, false) == value:
		return

	_flags[flag_name] = value
	print_rich("[color=LawnGreen]LevelStateManager ('%s'): Level Flag Set -> %s = %s[/color]" % [get_parent().name if get_parent() else "", flag_name, value])
	level_flag_changed.emit(flag_name, value)


func get_level_flag(flag_name: String) -> bool:
	return _flags.get(flag_name, false)
</file>

<file path="project.godot">
; Engine configuration file.
; It's best edited using the editor UI and not directly,
; since the parameters that go here are not all obvious.
;
; Format:
;   [section] ; section goes between []
;   param=value ; assign values to parameters

config_version=5

[animation]

compatibility/default_parent_skeleton_in_mesh_instance_3d=true

[application]

config/name="If I Remember Correctly"
config/version="0.1.0-demo"
run/main_scene="uid://vmpt4xerpfew"
config/use_custom_user_dir=true
config/custom_user_dir_name="Lewgend/If I Remember Correctly"
config/features=PackedStringArray("4.6", "GL Compatibility")
run/max_fps=60
boot_splash/bg_color=Color(0.039215688, 0.039215688, 0.05490196, 1)
boot_splash/show_image=false
config/icon="res://icon.svg"

[audio]

buses/default_bus_layout="uid://cuj677vfholik"

[autoload]

DialogueManager="*res://addons/dialogue_manager/dialogue_manager.gd"
SoundManager="*res://autoload/SoundManager.gd"
Events="*res://autoload/events.gd"
Inventory="*res://autoload/inventory.gd"
DialogueHistory="*res://autoload/dialogue_history.gd"
Flags="*res://autoload/flags.gd"
Settings="*res://autoload/settings.gd"
GameManager="*res://autoload/GameManager.tscn"
ConversationEventManager="*res://autoload/ConversationEventManager.gd"
FallbackManager="*res://autoload/FallbackManager.gd"
NotificationManager="*res://autoload/NotificationManager.tscn"

[dialogue_manager]

runtime/balloon_path="uid://dmfq0fjxxjgay"

[display]

window/size/viewport_width=1920
window/size/viewport_height=1080
window/size/mode=2
window/stretch/mode="canvas_items"

[editor_plugins]

enabled=PackedStringArray("res://addons/dialogue_manager/plugin.cfg")

[input]

ui_left={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":0,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":4194319,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventJoypadButton,"resource_local_to_scene":false,"resource_name":"","device":-1,"button_index":13,"pressure":0.0,"pressed":false,"script":null)
, Object(InputEventJoypadMotion,"resource_local_to_scene":false,"resource_name":"","device":-1,"axis":0,"axis_value":-1.0,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":65,"key_label":0,"unicode":97,"location":0,"echo":false,"script":null)
]
}
ui_right={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":0,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":4194321,"physical_keycode":0,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventJoypadButton,"resource_local_to_scene":false,"resource_name":"","device":-1,"button_index":14,"pressure":0.0,"pressed":false,"script":null)
, Object(InputEventJoypadMotion,"resource_local_to_scene":false,"resource_name":"","device":-1,"axis":0,"axis_value":1.0,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":68,"key_label":0,"unicode":100,"location":0,"echo":false,"script":null)
]
}
move_left={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":65,"key_label":0,"unicode":97,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194319,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
move_right={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":68,"key_label":0,"unicode":100,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194321,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
talk_to_npc={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":84,"key_label":0,"unicode":116,"location":0,"echo":false,"script":null)
]
}

[internationalization]

locale/translations_pot_files=PackedStringArray("res://dialogue/talkto.dialogue", "res://dialogue/npcs/faye.dialogue", "res://dialogue/intro.dialogue", "res://dialogue/flash_dialogue.dialogue", "res://dialogue/alyssa_marketing.dialogue", "res://dialogue/AIda.dialogue", "res://dialogue/aida_dialogue_hub.dialogue", "res://dialogue/aida_toilet_assistance.dialogue", "res://dialogue/dev_cta.dialogue", "res://dialogue/examinables.dialogue", "res://dialogue/form_related_dialogue.dialogue", "res://dialogue/generic_lines.dialogue", "res://dialogue/giveables.dialogue", "res://dialogue/hints.dialogue", "res://dialogue/hints_adventure.dialogue", "res://dialogue/mcbucket_give_tp.dialogue", "res://dialogue/mcbucket_thinking_dialogue.dialogue", "res://dialogue/mcbucket_unified.dialogue", "res://dialogue/sergey_dialogue_1.dialogue", "res://dialogue/sergey_dialogue_2.dialogue", "res://dialogue/testdialogue.dialogue", "res://dialogue/unflashables.dialogue", "res://dialogue/unpickables.dialogue", "res://dialogue/untalkables.dialogue", "res://dialogue/unusables.dialogue", "res://dialogue/use_item_accompanying_dialogue.dialogue")

[physics]

common/physics_interpolation=true

[rendering]

renderer/rendering_method="gl_compatibility"
renderer/rendering_method.mobile="gl_compatibility"
textures/vram_compression/import_s3tc_bptc=true
textures/vram_compression/import_etc2_astc=true
environment/defaults/default_clear_color=Color(0.039215688, 0.039215688, 0.05490196, 1)
</file>

</files>
