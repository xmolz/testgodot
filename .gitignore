# Godot 4+ specific ignores
.godot/
# .nomedia # Generally not needed unless you have specific reasons for media scanning

# Godot general specific ignores (some from GitHub template, some standard)
# .import/  # For Godot 3. For Godot 4, .godot/ covers this. Keep if you might use Godot 3.
export_presets.cfg # Renamed from export.cfg in newer Godot versions, good to have both
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
# data_mono/ # Often better to explicitly add data folders if needed, than wildcard ignore
# *.pidb
# *.userprefs
# *.suo
mono_crash.*.json # From GitHub template

# Visual Studio Code (Common editor)
.vscode/

# Builds (Very important to ignore these)
build/
Build/
builds/
Builds/
*.apk
*.aab
*.xap
*.ipa
*.dmg
*.pkg
*.deb
*.rpm
*.exe
*.dll
*.pck
*.zip     # Be careful if you use zips for actual assets, but usually build artifacts
*.tar.gz
*.tar.bz2
*.7z

# User-specific files & sensitive data (NEVER commit these)
# Example:
# secrets.gd
# .env

# Optional: If you had specific files in your local .gitignore (like 'my_secret_file.txt' from my example)
# that are NOT covered above and you still want to ignore them, add them here.